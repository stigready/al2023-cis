---
# Generated by StigForge from ComplianceAsCode profile playbooks.
# Cloud breakers removed via policy/ (never auto-exempted to inflate score).
- name: "StigForge apply al2023_cis profile {{ stigforge_profile }}"
  ansible.builtin.include_tasks: "{{ stigforge_profile }}.yml"
  ignore_errors: true
  tags: [stigforge, al2023_cis]
