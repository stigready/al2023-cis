# Source: ComplianceAsCode/content (scap-security-guide) — BSD-3-Clause
# Imported by StigForge; cloud-breaker tasks stripped per policy/cloud-ami.yml
---
- block:
  - name: Gather the package facts
    ansible.builtin.package_facts:
      manager: auto
    tags:
    - always
  rescue:
  - name: 'StigForge: continue after error in ''Gather the package facts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Gather the package facts'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_always | default(true) | bool)
- block:
  - name: Ensure aide is installed
    ansible.builtin.package:
      name: aide
      state: present
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_aide_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure aide is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure aide is installed'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_aide_installed | default(true) | bool)
- block:
  - name: Build and Test AIDE Database - Ensure AIDE Is Installed
    ansible.builtin.package:
      name: '{{ item }}'
      state: present
    with_items:
    - aide
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Ensure AIDE Is Installed'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Ensure AIDE Is Installed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_aide_build_database | default(true) | bool)
- block:
  - name: Ensure aide is installed
    ansible.builtin.package:
      name: '{{ item }}'
      state: present
    with_items:
    - aide
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AU-9(3)
    - NIST-800-53-AU-9(3).1
    - aide_check_audit_tools
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure aide is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure aide is installed'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_aide_check_audit_tools | default(true) | bool)
- block:
  - name: Ensure AIDE is installed
    ansible.builtin.package:
      name: aide
      state: present
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_periodic_cron_checking
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure AIDE is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure AIDE is installed'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_aide_periodic_cron_checking | default(true) | bool)
- block:
  - name: Install cron
    ansible.builtin.package:
      name: cronie
      state: present
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_periodic_cron_checking
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Install cron'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Install cron'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_aide_periodic_cron_checking | default(true) | bool)
- block:
  - name: Ensure sudo is installed
    ansible.builtin.package:
      name: sudo
      state: present
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_sudo_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sudo is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sudo is installed'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_sudo_installed | default(true) | bool)
- block:
  - name: Ensure rsyslog is installed
    ansible.builtin.package:
      name: rsyslog
      state: present
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_rsyslog_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_rsyslog_installed | default(true) | bool)
- block:
  - name: Configure Firewalld to Restrict Loopback Traffic - Ensure firewalld Package is Installed
    ansible.builtin.package:
      name: '{{ item }}'
      state: present
    with_items:
    - firewalld
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - configure_strategy
    - firewalld_loopback_traffic_restricted
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Configure Firewalld to Restrict Loopback Traffic - Ensure firewalld Package
      is Installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Firewalld to Restrict Loopback Traffic - Ensure firewalld Package is Installed'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_firewalld_loopback_traffic_restricted | default(true)
    | bool)
- block:
  - name: Configure Firewalld to Trust Loopback Traffic - Ensure firewalld Package is Installed
    ansible.builtin.package:
      name: '{{ item }}'
      state: present
    with_items:
    - firewalld
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - configure_strategy
    - firewalld_loopback_traffic_trusted
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Configure Firewalld to Trust Loopback Traffic - Ensure firewalld Package is
      Installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Firewalld to Trust Loopback Traffic - Ensure firewalld Package is Installed'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_firewalld_loopback_traffic_trusted | default(true)
    | bool)
- block:
  - name: Ensure nftables is installed
    ansible.builtin.package:
      name: nftables
      state: present
    when: ( "kernel" in ansible_facts.packages )
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.1
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_nftables_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure nftables is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure nftables is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_nftables_installed | default(true) | bool)
- block:
  - name: Ensure libselinux is installed
    ansible.builtin.package:
      name: libselinux
      state: present
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.6
    - enable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_libselinux_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure libselinux is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure libselinux is installed'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_libselinux_installed | default(true) | bool)
- block:
  - name: 'Uninstall mcstrans Package: Ensure mcstrans is removed'
    ansible.builtin.package:
      name: mcstrans
      state: absent
    when: '"kernel" in ansible_facts.packages'
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_mcstrans_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall mcstrans Package: Ensure mcstrans is removed'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall mcstrans Package: Ensure mcstrans is removed'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_mcstrans_removed | default(true) | bool)
- block:
  - name: 'Uninstall setroubleshoot Package: Ensure setroubleshoot is removed'
    ansible.builtin.package:
      name: setroubleshoot
      state: absent
    when: '"kernel" in ansible_facts.packages'
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_setroubleshoot_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall setroubleshoot Package: Ensure setroubleshoot is removed'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall setroubleshoot Package: Ensure setroubleshoot is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_setroubleshoot_removed | default(true) | bool)
- block:
  - name: 'Uninstall avahi Server Package: Ensure avahi is removed'
    ansible.builtin.package:
      name: avahi
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_avahi_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall avahi Server Package: Ensure avahi is removed'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall avahi Server Package: Ensure avahi is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_avahi_removed | default(true) | bool)
- block:
  - name: 'Uninstall DHCP Server Package: Ensure dhcp is removed'
    ansible.builtin.package:
      name: dhcp
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_dhcp_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall DHCP Server Package: Ensure dhcp is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall DHCP Server Package: Ensure dhcp is removed'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_dhcp_removed | default(true) | bool)
- block:
  - name: 'Uninstall dnsmasq Package: Ensure dnsmasq is removed'
    ansible.builtin.package:
      name: dnsmasq
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_dnsmasq_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall dnsmasq Package: Ensure dnsmasq is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall dnsmasq Package: Ensure dnsmasq is removed'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_dnsmasq_removed | default(true) | bool)
- block:
  - name: 'Uninstall bind Package: Ensure bind is removed'
    ansible.builtin.package:
      name: bind
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_bind_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall bind Package: Ensure bind is removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall bind Package: Ensure bind is removed'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_bind_removed | default(true) | bool)
- block:
  - name: 'Remove ftp Package: Ensure ftp is removed'
    ansible.builtin.package:
      name: ftp
      state: absent
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_ftp_removed
  rescue:
  - name: 'StigForge: continue after error in ''Remove ftp Package: Ensure ftp is removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove ftp Package: Ensure ftp is removed'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_ftp_removed | default(true) | bool)
- block:
  - name: 'Uninstall vsftpd Package: Ensure vsftpd is removed'
    ansible.builtin.package:
      name: vsftpd
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-CM-7.1(ii)
    - NIST-800-53-IA-5(1)(c)
    - NIST-800-53-IA-5(1).1(v)
    - disable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_vsftpd_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall vsftpd Package: Ensure vsftpd is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall vsftpd Package: Ensure vsftpd is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_vsftpd_removed | default(true) | bool)
- block:
  - name: 'Uninstall httpd Package: Ensure httpd is removed'
    ansible.builtin.package:
      name: httpd
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_httpd_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall httpd Package: Ensure httpd is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall httpd Package: Ensure httpd is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_httpd_removed | default(true) | bool)
- block:
  - name: 'Uninstall nginx Package: Ensure nginx is removed'
    ansible.builtin.package:
      name: nginx
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_nginx_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall nginx Package: Ensure nginx is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall nginx Package: Ensure nginx is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_nginx_removed | default(true) | bool)
- block:
  - name: 'Uninstall cyrus-imapd Package: Ensure cyrus-imapd is removed'
    ansible.builtin.package:
      name: cyrus-imapd
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_cyrus-imapd_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall cyrus-imapd Package: Ensure cyrus-imapd is removed'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall cyrus-imapd Package: Ensure cyrus-imapd is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall dovecot Package: Ensure dovecot is removed'
    ansible.builtin.package:
      name: dovecot
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_dovecot_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall dovecot Package: Ensure dovecot is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall dovecot Package: Ensure dovecot is removed'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_dovecot_removed | default(true) | bool)
- block:
  - name: 'Ensure LDAP client is not installed: Ensure openldap-clients is removed'
    ansible.builtin.package:
      name: openldap-clients
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_openldap-clients_removed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure LDAP client is not installed: Ensure openldap-clients is removed''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure LDAP client is not installed: Ensure openldap-clients is removed'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: 'Uninstall rsync Package: Ensure rsync is removed'
    ansible.builtin.package:
      name: rsync
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_rsync_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall rsync Package: Ensure rsync is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall rsync Package: Ensure rsync is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_rsync_removed | default(true) | bool)
- block:
  - name: 'Uninstall telnet-server Package: Ensure telnet-server is removed'
    ansible.builtin.package:
      name: telnet-server
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.2
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_telnet-server_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall telnet-server Package: Ensure telnet-server is removed'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall telnet-server Package: Ensure telnet-server is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Remove telnet Clients: Ensure telnet is removed'
    ansible.builtin.package:
      name: telnet
      state: absent
    tags:
    - NIST-800-171-3.1.13
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_telnet_removed
  rescue:
  - name: 'StigForge: continue after error in ''Remove telnet Clients: Ensure telnet is removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove telnet Clients: Ensure telnet is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_telnet_removed | default(true) | bool)
- block:
  - name: 'Uninstall tftp-server Package: Ensure tftp-server is removed'
    ansible.builtin.package:
      name: tftp-server
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_tftp-server_removed
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall tftp-server Package: Ensure tftp-server is removed'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall tftp-server Package: Ensure tftp-server is removed'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Uninstall CUPS Package: Ensure cups is removed'
    ansible.builtin.package:
      name: cups
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_cups_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall CUPS Package: Ensure cups is removed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall CUPS Package: Ensure cups is removed'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_cups_removed | default(true) | bool)
- block:
  - name: 'Uninstall squid Package: Ensure squid is removed'
    ansible.builtin.package:
      name: squid
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_squid_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall squid Package: Ensure squid is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall squid Package: Ensure squid is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_squid_removed | default(true) | bool)
- block:
  - name: 'Uninstall Samba Package: Ensure samba is removed'
    ansible.builtin.package:
      name: samba
      state: absent
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_samba_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall Samba Package: Ensure samba is removed'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall Samba Package: Ensure samba is removed'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_samba_removed | default(true) | bool)
- block:
  - name: 'Uninstall net-snmp Package: Ensure net-snmp is removed'
    ansible.builtin.package:
      name: net-snmp
      state: absent
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_net-snmp_removed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Uninstall net-snmp Package: Ensure net-snmp is removed'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Uninstall net-snmp Package: Ensure net-snmp is removed'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Remove the X Windows Package Group: Ensure xorg-x11-server-common is removed'
    ansible.builtin.package:
      name: xorg-x11-server-common
      state: absent
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_xorg-x11-server-common_removed
  rescue:
  - name: 'StigForge: continue after error in ''Remove the X Windows Package Group: Ensure xorg-x11-server-common is removed''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove the X Windows Package Group: Ensure xorg-x11-server-common is removed'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
- block:
  - name: Ensure audit is installed
    ansible.builtin.package:
      name: audit
      state: present
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-7(a)
    - NIST-800-53-AU-12(2)
    - NIST-800-53-AU-14
    - NIST-800-53-AU-2(a)
    - NIST-800-53-AU-7(1)
    - NIST-800-53-AU-7(2)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.1
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_audit_installed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure audit is installed'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure audit is installed'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_package_audit_installed | default(true) | bool)
- block:
  - name: Gather the package facts
    ansible.builtin.package_facts:
      manager: auto
    tags:
    - always
  rescue:
  - name: 'StigForge: continue after error in ''Gather the package facts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Gather the package facts'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_always | default(true) | bool)
- block:
  - name: Enable systemd-journald Service - Enable service systemd-journald
    block:
    - name: Enable systemd-journald Service - Enable Service systemd-journald
      ansible.builtin.systemd:
        name: systemd-journald
        enabled: true
        state: started
        masked: false
      when:
      - '"systemd" in ansible_facts.packages'
    tags:
    - NIST-800-53-SC-24
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_systemd-journald_enabled
    - special_service_block
    when: '"kernel" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Enable systemd-journald Service - Enable service systemd-journald'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable systemd-journald Service - Enable service systemd-journald'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_special_service_block | default(true) | bool)
- block:
  - name: Verify nftables Service is Disabled - Disable service nftables
    block:
    - name: Verify nftables Service is Disabled - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Verify nftables Service is Disabled - Ensure nftables.service is Masked
      ansible.builtin.systemd:
        name: nftables.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("nftables.service", multiline=True)
    - name: Unit Socket Exists - nftables.socket
      ansible.builtin.command: systemctl -q list-unit-files nftables.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Verify nftables Service is Disabled - Disable Socket nftables
      ansible.builtin.systemd:
        name: nftables.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("nftables.socket", multiline=True)
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.1
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_nftables_disabled
    - special_service_block
    when: ( "firewalld" in ansible_facts.packages and "nftables" in ansible_facts.packages and "kernel" in ansible_facts.packages
      )
  rescue:
  - name: 'StigForge: continue after error in ''Verify nftables Service is Disabled - Disable service nftables'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Verify nftables Service is Disabled - Disable service nftables'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_service_nftables_disabled | default(true) | bool)
- block:
  - name: Enable cron Service - Enable service crond
    block:
    - name: Enable cron Service - Enable Service crond
      ansible.builtin.systemd:
        name: crond
        enabled: true
        state: started
        masked: false
      when:
      - '"cronie" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_crond_enabled
    - special_service_block
    when: '"kernel" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Enable cron Service - Enable service crond'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable cron Service - Enable service crond'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_service_crond_enabled | default(true) | bool)
- block:
  - name: Disable rpcbind Service - Disable service rpcbind
    block:
    - name: Disable rpcbind Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable rpcbind Service - Ensure rpcbind.service is Masked
      ansible.builtin.systemd:
        name: rpcbind.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("rpcbind.service", multiline=True)
    - name: Unit Socket Exists - rpcbind.socket
      ansible.builtin.command: systemctl -q list-unit-files rpcbind.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable rpcbind Service - Disable Socket rpcbind
      ansible.builtin.systemd:
        name: rpcbind.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("rpcbind.socket", multiline=True)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - service_rpcbind_disabled
    - special_service_block
    when: '"kernel" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable rpcbind Service - Disable service rpcbind'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable rpcbind Service - Disable service rpcbind'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_service_rpcbind_disabled | default(true) | bool)
- block:
  - name: Disable Network File System (nfs) - Disable service nfs-server
    block:
    - name: Disable Network File System (nfs) - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Network File System (nfs) - Ensure nfs-server.service is Masked
      ansible.builtin.systemd:
        name: nfs-server.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("nfs-server.service", multiline=True)
    - name: Unit Socket Exists - nfs-server.socket
      ansible.builtin.command: systemctl -q list-unit-files nfs-server.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable Network File System (nfs) - Disable Socket nfs-server
      ansible.builtin.systemd:
        name: nfs-server.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("nfs-server.socket", multiline=True)
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - service_nfs_disabled
    - special_service_block
    - unknown_severity
    when: '"kernel" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Disable Network File System (nfs) - Disable service nfs-server'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Network File System (nfs) - Disable service nfs-server'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_special_service_block | default(true) | bool)
- block:
  - name: Disable snmpd Service - Disable service snmpd
    block:
    - name: Disable snmpd Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable snmpd Service - Ensure snmpd.service is Masked
      ansible.builtin.systemd:
        name: snmpd.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("snmpd.service", multiline=True)
    - name: Unit Socket Exists - snmpd.socket
      ansible.builtin.command: systemctl -q list-unit-files snmpd.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable snmpd Service - Disable Socket snmpd
      ansible.builtin.systemd:
        name: snmpd.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("snmpd.socket", multiline=True)
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - service_snmpd_disabled
    - special_service_block
    when: ( "net-snmp" in ansible_facts.packages and "kernel" in ansible_facts.packages )
  rescue:
  - name: 'StigForge: continue after error in ''Disable snmpd Service - Disable service snmpd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable snmpd Service - Disable service snmpd'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_service_snmpd_disabled | default(true) | bool)
- block:
  - name: Enable auditd Service - Enable service auditd
    block:
    - name: Enable auditd Service - Enable Service auditd
      ansible.builtin.systemd:
        name: auditd
        enabled: true
        state: started
        masked: false
      when:
      - '"audit" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-171-3.3.2
    - NIST-800-171-3.3.6
    - NIST-800-53-AC-2(g)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-10
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-14(1)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-AU-3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-4(23)
    - PCI-DSS-Req-10.1
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_auditd_enabled
    - special_service_block
    when:
    - '"kernel" in ansible_facts.packages'
    - '"audit" in ansible_facts.packages'
  rescue:
  - name: 'StigForge: continue after error in ''Enable auditd Service - Enable service auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable auditd Service - Enable service auditd'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_service_auditd_enabled | default(true) | bool)
- block:
  - name: Gather the service facts
    ansible.builtin.service_facts: null
    tags:
    - always
  rescue:
  - name: 'StigForge: continue after error in ''Gather the service facts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Gather the service facts'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_always | default(true) | bool)
- block:
  - name: Build and Test AIDE Database - Build and Test AIDE Database
    ansible.builtin.command: /usr/sbin/aide --init
    changed_when: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Build and Test AIDE Database'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Build and Test AIDE Database'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_aide_build_database | default(true) | bool)
- block:
  - name: Build and Test AIDE Database - Check Whether the Stock AIDE Database Exists
    ansible.builtin.stat:
      path: /var/lib/aide/aide.db.new.gz
    register: aide_database_stat
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Check Whether the Stock AIDE Database Exists''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Check Whether the Stock AIDE Database Exists'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_aide_build_database | default(true) | bool)
- block:
  - name: Build and Test AIDE Database - Stage AIDE Database
    ansible.builtin.copy:
      src: /var/lib/aide/aide.db.new.gz
      dest: /var/lib/aide/aide.db.gz
      backup: true
      remote_src: true
    when:
    - '"kernel" in ansible_facts.packages'
    - ((aide_database_stat is not skipped and aide_database_stat.stat.exists) is defined and (aide_database_stat is not skipped
      and aide_database_stat.stat.exists))
    tags:
    - CJIS-5.10.1.3
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Build and Test AIDE Database - Stage AIDE Database'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Build and Test AIDE Database - Stage AIDE Database'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_aide_build_database | default(true) | bool)
- block:
  - name: Set audit_tools fact
    ansible.builtin.set_fact:
      audit_tools:
      - /usr/sbin/audispd
      - /usr/sbin/auditctl
      - /usr/sbin/auditd
      - /usr/sbin/augenrules
      - /usr/sbin/aureport
      - /usr/sbin/ausearch
      - /usr/sbin/autrace
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AU-9(3)
    - NIST-800-53-AU-9(3).1
    - aide_check_audit_tools
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set audit_tools fact'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set audit_tools fact'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_aide_check_audit_tools | default(true) | bool)
- block:
  - name: Ensure existing AIDE configuration for audit tools are correct
    ansible.builtin.lineinfile:
      path: /etc/aide.conf
      regexp: ^{{ item }}\s
      line: '{{ item }} p+i+n+u+g+s+b+acl+selinux+xattrs+sha512'
      create: true
    with_items: '{{ audit_tools }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - '"aide" in ansible_facts.packages'
    tags:
    - NIST-800-53-AU-9(3)
    - NIST-800-53-AU-9(3).1
    - aide_check_audit_tools
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure existing AIDE configuration for audit tools are correct'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure existing AIDE configuration for audit tools are correct'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_aide_check_audit_tools | default(true) | bool)
- block:
  - name: Configure AIDE to properly protect audit tools
    ansible.builtin.lineinfile:
      path: /etc/aide.conf
      line: '{{ item }} p+i+n+u+g+s+b+acl+selinux+xattrs+sha512'
      create: true
    with_items: '{{ audit_tools }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - '"aide" in ansible_facts.packages'
    tags:
    - NIST-800-53-AU-9(3)
    - NIST-800-53-AU-9(3).1
    - aide_check_audit_tools
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure AIDE to properly protect audit tools'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure AIDE to properly protect audit tools'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_aide_check_audit_tools | default(true) | bool)
- block:
  - name: Configure Periodic Execution of AIDE
    ansible.builtin.cron:
      name: run AIDE check
      minute: 5
      hour: 4
      weekday: 0
      user: root
      job: /usr/sbin/aide --check
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_periodic_cron_checking
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure Periodic Execution of AIDE'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Periodic Execution of AIDE'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_aide_periodic_cron_checking | default(true) | bool)
- block:
  - name: Configure System Cryptography Policy
    ansible.builtin.lineinfile:
      path: /etc/crypto-policies/config
      regexp: ^(?!#)(\S+)$
      line: '{{ var_system_crypto_policy }}'
      create: true
    tags:
    - NIST-800-53-AC-17(2)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-MA-4(6)
    - NIST-800-53-SC-12(2)
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SC-13
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.7
    - configure_crypto_policy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure System Cryptography Policy'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure System Cryptography Policy'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_configure_crypto_policy | default(true) | bool)
- block:
  - name: Verify that Crypto Policy is Set (runtime)
    ansible.builtin.command: /usr/bin/update-crypto-policies --set {{ var_system_crypto_policy }}
    tags:
    - NIST-800-53-AC-17(2)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-MA-4(6)
    - NIST-800-53-SC-12(2)
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SC-13
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.7
    - configure_crypto_policy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Verify that Crypto Policy is Set (runtime)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Verify that Crypto Policy is Set (runtime)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_configure_crypto_policy | default(true) | bool)
- block:
  - name: Configure SSH to use System Crypto Policy
    ansible.builtin.lineinfile:
      dest: /etc/sysconfig/sshd
      state: absent
      regexp: (?i)^\s*CRYPTO_POLICY.*$
    tags:
    - NIST-800-53-AC-17(2)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-MA-4(6)
    - NIST-800-53-SC-13
    - PCI-DSS-Req-2.2
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.7
    - configure_ssh_crypto_policy
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in ''Configure SSH to use System Crypto Policy'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure SSH to use System Crypto Policy'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_configure_ssh_crypto_policy | default(true) | bool)
- block:
  - name: Ensure use_pty is enabled in /etc/sudoers
    ansible.builtin.lineinfile:
      path: /etc/sudoers
      regexp: ^[\s]*Defaults.*\buse_pty\b.*$
      line: Defaults use_pty
      validate: /usr/sbin/visudo -cf %s
    when:
    - '"kernel" in ansible_facts.packages'
    - '"sudo" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sudo_add_use_pty
  rescue:
  - name: 'StigForge: continue after error in ''Ensure use_pty is enabled in /etc/sudoers'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure use_pty is enabled in /etc/sudoers'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sudo_add_use_pty | default(true) | bool)
- block:
  - name: Ensure logfile is enabled with the appropriate value in /etc/sudoers
    ansible.builtin.lineinfile:
      path: /etc/sudoers
      regexp: ^[\s]*Defaults\s(.*)\blogfile=[-]?.+\b(.*)$
      line: Defaults \1logfile={{ var_sudo_logfile }}\2
      validate: /usr/sbin/visudo -cf %s
      backrefs: true
    register: edit_sudoers_logfile_option
    when:
    - '"kernel" in ansible_facts.packages'
    - '"sudo" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
    - sudo_custom_logfile
  rescue:
  - name: 'StigForge: continue after error in ''Ensure logfile is enabled with the appropriate value in /etc/sudoers'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure logfile is enabled with the appropriate value in /etc/sudoers'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sudo_custom_logfile | default(true) | bool)
- block:
  - name: Enable logfile option with appropriate value in /etc/sudoers
    ansible.builtin.lineinfile:
      path: /etc/sudoers
      line: Defaults logfile={{ var_sudo_logfile }}
      validate: /usr/sbin/visudo -cf %s
    when:
    - '"kernel" in ansible_facts.packages'
    - '"sudo" in ansible_facts.packages'
    - edit_sudoers_logfile_option is defined and not edit_sudoers_logfile_option.changed
    tags:
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy
    - sudo_custom_logfile
  rescue:
  - name: 'StigForge: continue after error in ''Enable logfile option with appropriate value in /etc/sudoers'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable logfile option with appropriate value in /etc/sudoers'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sudo_custom_logfile | default(true) | bool)
- block:
  - name: Ensure GPG check is globally activated
    community.general.ini_file:
      dest: /etc/dnf/dnf.conf
      section: main
      option: gpgcheck
      value: 1
      no_extra_spaces: true
      create: false
    when: '"dnf" in ansible_facts.packages'
    tags:
    - CJIS-5.10.4.1
    - NIST-800-171-3.4.8
    - NIST-800-53-CM-11(a)
    - NIST-800-53-CM-11(b)
    - NIST-800-53-CM-5(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SA-12
    - NIST-800-53-SA-12(10)
    - NIST-800-53-SC-12
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SI-7
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-6.3
    - PCI-DSSv4-6.3.3
    - configure_strategy
    - ensure_gpgcheck_globally_activated
    - high_severity
    - low_complexity
    - medium_disruption
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure GPG check is globally activated'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure GPG check is globally activated'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_ensure_gpgcheck_globally_activated | default(true)
    | bool)
- block:
  - name: Modify the System Login Banner - Ensure Correct Banner
    ansible.builtin.copy:
      dest: /etc/issue
      content: '{{ login_banner_text | regex_replace("^\^(.*)\$$", "\1") | regex_replace("^\((.*\.)\|.*\)$", "\1") | regex_replace("\[\\s\\n\]\+","
        ") | regex_replace("\(\?:\[\\n\]\+\|\(\?:\\\\n\)\+\)", "\n") | regex_replace("\\", "") | wordwrap() }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.9
    - NIST-800-53-AC-8(a)
    - NIST-800-53-AC-8(c)
    - banner_etc_issue
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Modify the System Login Banner - Ensure Correct Banner'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Modify the System Login Banner - Ensure Correct Banner'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_banner_etc_issue | default(true) | bool)
- block:
  - name: Modify the System Login Banner for Remote Connections - ensure correct banner
    ansible.builtin.copy:
      dest: /etc/issue.net
      content: '{{ remote_login_banner_text | regex_replace("^\^(.*)\$$", "\1") | regex_replace("^\((.*\.)\|.*\)$", "\1")
        | regex_replace("\[\\s\\n\]\+"," ") | regex_replace("\(\?:\[\\n\]\+\|\(\?:\\\\n\)\+\)", "\n") | regex_replace("\\",
        "") | wordwrap() }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - banner_etc_issue_net
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Modify the System Login Banner for Remote Connections - ensure correct banner''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Modify the System Login Banner for Remote Connections - ensure correct banner'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_banner_etc_issue_net | default(true) | bool)
- block:
  - name: Modify the System Message of the Day Banner - ensure correct banner
    ansible.builtin.copy:
      dest: /etc/motd
      content: '{{ motd_banner_text | regex_replace("^\^(.*)\$$", "\1") | regex_replace("^\((.*\.)\|.*\)$", "\1") | regex_replace("\[\\s\\n\]\+","
        ") | regex_replace("\(\?:\[\\n\]\+\|\(\?:\\\\n\)\+\)", "\n") | regex_replace("\\", "") | wordwrap() }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - banner_etc_motd
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - unknown_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Modify the System Message of the Day Banner - ensure correct banner'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Modify the System Message of the Day Banner - ensure correct banner'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_banner_etc_motd | default(true) | bool)
- block:
  - name: Set the file_groupowner_etc_issue_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_issue_newgroup: '0'
    tags:
    - configure_strategy
    - file_groupowner_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_issue_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_issue_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_issue | default(true) | bool)
- block:
  - name: Test for existence /etc/issue
    ansible.builtin.stat:
      path: /etc/issue
    register: file_exists
    tags:
    - configure_strategy
    - file_groupowner_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/issue'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/issue'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_issue | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/issue
    ansible.builtin.file:
      path: /etc/issue
      follow: false
      group: '{{ file_groupowner_etc_issue_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupowner_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/issue'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/issue'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_issue | default(true) | bool)
- block:
  - name: Set the file_groupowner_etc_issue_net_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_issue_net_newgroup: '0'
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_groupowner_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_issue_net_newgroup variable if represented by
      gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_issue_net_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_issue_net | default(true) | bool)
- block:
  - name: Test for existence /etc/issue.net
    ansible.builtin.stat:
      path: /etc/issue.net
    register: file_exists
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_groupowner_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/issue.net'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/issue.net'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_issue_net | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/issue.net
    ansible.builtin.file:
      path: /etc/issue.net
      follow: false
      group: '{{ file_groupowner_etc_issue_net_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_groupowner_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/issue.net'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/issue.net'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_issue_net | default(true) | bool)
- block:
  - name: Set the file_groupowner_etc_motd_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_motd_newgroup: '0'
    tags:
    - configure_strategy
    - file_groupowner_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_motd_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_motd_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_motd | default(true) | bool)
- block:
  - name: Test for existence /etc/motd
    ansible.builtin.stat:
      path: /etc/motd
    register: file_exists
    tags:
    - configure_strategy
    - file_groupowner_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/motd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/motd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_motd | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/motd
    ansible.builtin.file:
      path: /etc/motd
      follow: false
      group: '{{ file_groupowner_etc_motd_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupowner_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/motd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/motd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_motd | default(true) | bool)
- block:
  - name: Set the file_owner_etc_issue_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_issue_newown: '0'
    tags:
    - configure_strategy
    - file_owner_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_issue_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_issue_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_issue | default(true) | bool)
- block:
  - name: Test for existence /etc/issue
    ansible.builtin.stat:
      path: /etc/issue
    register: file_exists
    tags:
    - configure_strategy
    - file_owner_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/issue'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/issue'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_issue | default(true) | bool)
- block:
  - name: Ensure owner on /etc/issue
    ansible.builtin.file:
      path: /etc/issue
      follow: false
      owner: '{{ file_owner_etc_issue_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_owner_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/issue'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/issue'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_issue | default(true) | bool)
- block:
  - name: Set the file_owner_etc_issue_net_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_issue_net_newown: '0'
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_owner_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_issue_net_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_issue_net_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_issue_net | default(true) | bool)
- block:
  - name: Test for existence /etc/issue.net
    ansible.builtin.stat:
      path: /etc/issue.net
    register: file_exists
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_owner_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/issue.net'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/issue.net'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_issue_net | default(true) | bool)
- block:
  - name: Ensure owner on /etc/issue.net
    ansible.builtin.file:
      path: /etc/issue.net
      follow: false
      owner: '{{ file_owner_etc_issue_net_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_owner_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/issue.net'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/issue.net'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_issue_net | default(true) | bool)
- block:
  - name: Set the file_owner_etc_motd_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_motd_newown: '0'
    tags:
    - configure_strategy
    - file_owner_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_motd_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_motd_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_motd | default(true) | bool)
- block:
  - name: Test for existence /etc/motd
    ansible.builtin.stat:
      path: /etc/motd
    register: file_exists
    tags:
    - configure_strategy
    - file_owner_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/motd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/motd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_motd | default(true) | bool)
- block:
  - name: Ensure owner on /etc/motd
    ansible.builtin.file:
      path: /etc/motd
      follow: false
      owner: '{{ file_owner_etc_motd_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_owner_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/motd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/motd'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_motd | default(true) | bool)
- block:
  - name: Test for existence /etc/issue
    ansible.builtin.stat:
      path: /etc/issue
    register: file_exists
    tags:
    - configure_strategy
    - file_permissions_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/issue'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/issue'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_etc_issue | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/issue
    ansible.builtin.file:
      path: /etc/issue
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_etc_issue
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/issue'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/issue'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_etc_issue | default(true) | bool)
- block:
  - name: Test for existence /etc/issue.net
    ansible.builtin.stat:
      path: /etc/issue.net
    register: file_exists
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_permissions_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/issue.net'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/issue.net'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_etc_issue_net | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/issue.net
    ansible.builtin.file:
      path: /etc/issue.net
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.8
    - configure_strategy
    - file_permissions_etc_issue_net
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/issue.net'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/issue.net'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_etc_issue_net | default(true) | bool)
- block:
  - name: Test for existence /etc/motd
    ansible.builtin.stat:
      path: /etc/motd
    register: file_exists
    tags:
    - configure_strategy
    - file_permissions_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/motd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/motd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_etc_motd | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/motd
    ansible.builtin.file:
      path: /etc/motd
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_etc_motd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/motd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/motd'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_etc_motd | default(true) | bool)
- block:
  - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/password-auth File. - Check if system relies on
      authselect tool
    ansible.builtin.stat:
      path: /usr/bin/authselect
    register: result_authselect_present
    tags:
    - NIST-800-53-AC-7 (a)
    - account_password_pam_faillock_password_auth
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Configure the Use of the pam_faillock.so Module in the /etc/pam.d/password-auth
      File. - Check if system relies on authselect tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure the Use of the pam_faillock.so Module in the /etc/pam.d/password-auth File. - Check if
        system relies on authselect tool'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_account_password_pam_faillock_password_auth | default(true)
    | bool)
- block:
  - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/password-auth File. - Remediation where authselect
      tool is present
    block:
    - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/password-auth File. - Check integrity of authselect
        current profile
      ansible.builtin.command:
        cmd: authselect check
      register: result_authselect_check_cmd
      changed_when: false
      check_mode: false
      failed_when: false
    - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/password-auth File. - Informative message based
        on the authselect integrity check result
      ansible.builtin.assert:
        that:
        - ansible_check_mode or result_authselect_check_cmd.rc == 0
        fail_msg:
        - authselect integrity check failed. Remediation aborted!
        - This remediation could not be applied because an authselect profile was not selected or the selected profile is
          not intact.
        - It is not recommended to manually edit the PAM files when authselect tool is available.
        - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile is recommended.
        success_msg:
        - authselect integrity check passed
    - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/password-auth File. - Get authselect current
        features
      ansible.builtin.shell:
        cmd: authselect current | tail -n+3 | awk '{ print $2 }'
      register: result_authselect_features
      changed_when: false
      check_mode: false
      when:
      - result_authselect_check_cmd is success
    - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/password-auth File. - Ensure "with-faillock"
        feature is enabled using authselect tool
      ansible.builtin.command:
        cmd: authselect enable-feature with-faillock
      register: result_authselect_enable_feature_cmd
      when:
      - result_authselect_check_cmd is success
      - result_authselect_features.stdout is not search("with-faillock")
    - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/password-auth File. - Ensure authselect changes
        are applied
      ansible.builtin.command:
        cmd: authselect apply-changes -b
      when:
      - result_authselect_enable_feature_cmd is not skipped
      - result_authselect_enable_feature_cmd is success
    when: (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    tags:
    - NIST-800-53-AC-7 (a)
    - account_password_pam_faillock_password_auth
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Configure the Use of the pam_faillock.so Module in the /etc/pam.d/password-auth
      File. - Remediation where authselect tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure the Use of the pam_faillock.so Module in the /etc/pam.d/password-auth File. - Remediation
        where authselect tool is present'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_account_password_pam_faillock_password_auth | default(true)
    | bool)
- block:
  - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/password-auth File. - Remediation where authselect
      tool is not present
    block:
    - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/password-auth File. - Check if pam_faillock.so
        is already enabled
      ansible.builtin.lineinfile:
        path: /etc/pam.d/system-auth
        regexp: .*auth.*pam_faillock\.so (preauth|authfail)
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_faillock_is_enabled
    - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/password-auth File. - Enable pam_faillock.so
        preauth editing PAM files
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        line: auth        required      pam_faillock.so preauth
        insertbefore: ^auth.*sufficient.*pam_unix\.so.*
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_is_enabled.found == 0
    - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/password-auth File. - Enable pam_faillock.so
        authfail editing PAM files
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        line: auth        required      pam_faillock.so authfail
        insertbefore: ^auth.*required.*pam_deny\.so.*
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_is_enabled.found == 0
    - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/password-auth File. - Enable pam_faillock.so
        account section editing PAM files
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        line: account     required      pam_faillock.so
        insertbefore: ^account.*required.*pam_unix\.so.*
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_is_enabled.found == 0
    when: not (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    tags:
    - NIST-800-53-AC-7 (a)
    - account_password_pam_faillock_password_auth
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Configure the Use of the pam_faillock.so Module in the /etc/pam.d/password-auth
      File. - Remediation where authselect tool is not present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure the Use of the pam_faillock.so Module in the /etc/pam.d/password-auth File. - Remediation
        where authselect tool is not present'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_account_password_pam_faillock_password_auth | default(true)
    | bool)
- block:
  - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/system-auth File. - Check if system relies on
      authselect tool
    ansible.builtin.stat:
      path: /usr/bin/authselect
    register: result_authselect_present
    tags:
    - NIST-800-53-AC-7 (a)
    - account_password_pam_faillock_system_auth
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Configure the Use of the pam_faillock.so Module in the /etc/pam.d/system-auth
      File. - Check if system relies on authselect tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure the Use of the pam_faillock.so Module in the /etc/pam.d/system-auth File. - Check if system
        relies on authselect tool'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_account_password_pam_faillock_system_auth | default(true)
    | bool)
- block:
  - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/system-auth File. - Remediation where authselect
      tool is present
    block:
    - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/system-auth File. - Check integrity of authselect
        current profile
      ansible.builtin.command:
        cmd: authselect check
      register: result_authselect_check_cmd
      changed_when: false
      check_mode: false
      failed_when: false
    - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/system-auth File. - Informative message based
        on the authselect integrity check result
      ansible.builtin.assert:
        that:
        - ansible_check_mode or result_authselect_check_cmd.rc == 0
        fail_msg:
        - authselect integrity check failed. Remediation aborted!
        - This remediation could not be applied because an authselect profile was not selected or the selected profile is
          not intact.
        - It is not recommended to manually edit the PAM files when authselect tool is available.
        - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile is recommended.
        success_msg:
        - authselect integrity check passed
    - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/system-auth File. - Get authselect current features
      ansible.builtin.shell:
        cmd: authselect current | tail -n+3 | awk '{ print $2 }'
      register: result_authselect_features
      changed_when: false
      check_mode: false
      when:
      - result_authselect_check_cmd is success
    - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/system-auth File. - Ensure "with-faillock" feature
        is enabled using authselect tool
      ansible.builtin.command:
        cmd: authselect enable-feature with-faillock
      register: result_authselect_enable_feature_cmd
      when:
      - result_authselect_check_cmd is success
      - result_authselect_features.stdout is not search("with-faillock")
    - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/system-auth File. - Ensure authselect changes
        are applied
      ansible.builtin.command:
        cmd: authselect apply-changes -b
      when:
      - result_authselect_enable_feature_cmd is not skipped
      - result_authselect_enable_feature_cmd is success
    when: (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    tags:
    - NIST-800-53-AC-7 (a)
    - account_password_pam_faillock_system_auth
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Configure the Use of the pam_faillock.so Module in the /etc/pam.d/system-auth
      File. - Remediation where authselect tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure the Use of the pam_faillock.so Module in the /etc/pam.d/system-auth File. - Remediation
        where authselect tool is present'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_account_password_pam_faillock_system_auth | default(true)
    | bool)
- block:
  - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/system-auth File. - Remediation where authselect
      tool is not present
    block:
    - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/system-auth File. - Check if pam_faillock.so
        is already enabled
      ansible.builtin.lineinfile:
        path: /etc/pam.d/system-auth
        regexp: .*auth.*pam_faillock\.so (preauth|authfail)
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_faillock_is_enabled
    - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/system-auth File. - Enable pam_faillock.so preauth
        editing PAM files
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        line: auth        required      pam_faillock.so preauth
        insertbefore: ^auth.*sufficient.*pam_unix\.so.*
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_is_enabled.found == 0
    - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/system-auth File. - Enable pam_faillock.so authfail
        editing PAM files
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        line: auth        required      pam_faillock.so authfail
        insertbefore: ^auth.*required.*pam_deny\.so.*
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_is_enabled.found == 0
    - name: Configure the Use of the pam_faillock.so Module in the /etc/pam.d/system-auth File. - Enable pam_faillock.so account
        section editing PAM files
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        line: account     required      pam_faillock.so
        insertbefore: ^account.*required.*pam_unix\.so.*
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_is_enabled.found == 0
    when: not (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    tags:
    - NIST-800-53-AC-7 (a)
    - account_password_pam_faillock_system_auth
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Configure the Use of the pam_faillock.so Module in the /etc/pam.d/system-auth
      File. - Remediation where authselect tool is not present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure the Use of the pam_faillock.so Module in the /etc/pam.d/system-auth File. - Remediation
        where authselect tool is not present'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_account_password_pam_faillock_system_auth | default(true)
    | bool)
- block:
  - name: 'Limit Password Reuse: password-auth - Check if system relies on authselect tool'
    ansible.builtin.stat:
      path: /usr/bin/authselect
    register: result_authselect_present
    when: '"pam" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.8
    - NIST-800-53-IA-5(1)(e)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.5
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.7
    - accounts_password_pam_pwhistory_remember_password_auth
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Limit Password Reuse: password-auth - Check if system relies on authselect
      tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit Password Reuse: password-auth - Check if system relies on authselect tool'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_pwhistory_remember_password_auth
    | default(true) | bool)
- block:
  - name: 'Limit Password Reuse: password-auth - Collect the available authselect features'
    ansible.builtin.command:
      cmd: authselect list-features sssd
    register: result_authselect_available_features
    changed_when: false
    check_mode: false
    when:
    - '"pam" in ansible_facts.packages'
    - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.8
    - NIST-800-53-IA-5(1)(e)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.5
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.7
    - accounts_password_pam_pwhistory_remember_password_auth
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Limit Password Reuse: password-auth - Collect the available authselect features''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit Password Reuse: password-auth - Collect the available authselect features'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_pwhistory_remember_password_auth
    | default(true) | bool)
- block:
  - name: 'Limit Password Reuse: password-auth - Enable pam_pwhistory.so using authselect feature'
    block:
    - name: 'Limit Password Reuse: password-auth - Check integrity of authselect current profile'
      ansible.builtin.command:
        cmd: authselect check
      register: result_authselect_check_cmd
      changed_when: false
      check_mode: false
      failed_when: false
    - name: 'Limit Password Reuse: password-auth - Informative message based on the authselect integrity check result'
      ansible.builtin.assert:
        that:
        - ansible_check_mode or result_authselect_check_cmd.rc == 0
        fail_msg:
        - authselect integrity check failed. Remediation aborted!
        - This remediation could not be applied because an authselect profile was not selected or the selected profile is
          not intact.
        - It is not recommended to manually edit the PAM files when authselect tool is available.
        - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile is recommended.
        success_msg:
        - authselect integrity check passed
    - name: 'Limit Password Reuse: password-auth - Get authselect current features'
      ansible.builtin.shell:
        cmd: authselect current | tail -n+3 | awk '{ print $2 }'
      register: result_authselect_features
      changed_when: false
      check_mode: false
      when:
      - result_authselect_check_cmd is success
    - name: 'Limit Password Reuse: password-auth - Ensure "with-pwhistory" feature is enabled using authselect tool'
      ansible.builtin.command:
        cmd: authselect enable-feature with-pwhistory
      register: result_authselect_enable_feature_cmd
      when:
      - result_authselect_check_cmd is success
      - result_authselect_features.stdout is not search("with-pwhistory")
    - name: 'Limit Password Reuse: password-auth - Ensure authselect changes are applied'
      ansible.builtin.command:
        cmd: authselect apply-changes -b
      when:
      - result_authselect_enable_feature_cmd is not skipped
      - result_authselect_enable_feature_cmd is success
    when:
    - '"pam" in ansible_facts.packages'
    - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    - result_authselect_available_features.stdout is search("with-pwhistory")
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.8
    - NIST-800-53-IA-5(1)(e)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.5
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.7
    - accounts_password_pam_pwhistory_remember_password_auth
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Limit Password Reuse: password-auth - Enable pam_pwhistory.so using authselect
      feature'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit Password Reuse: password-auth - Enable pam_pwhistory.so using authselect feature'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_pwhistory_remember_password_auth
    | default(true) | bool)
- block:
  - name: 'Limit Password Reuse: password-auth - Enable pam_pwhistory.so in appropriate PAM files'
    block:
    - name: 'Limit Password Reuse: password-auth - Define the PAM file to be edited as a local fact'
      ansible.builtin.set_fact:
        pam_file_path: /etc/pam.d/password-auth
    - name: 'Limit Password Reuse: password-auth - Check if system relies on authselect tool'
      ansible.builtin.stat:
        path: /usr/bin/authselect
      register: result_authselect_present
    - name: 'Limit Password Reuse: password-auth - Ensure authselect custom profile is used if authselect is present'
      block:
      - name: 'Limit Password Reuse: password-auth - Check integrity of authselect current profile'
        ansible.builtin.command:
          cmd: authselect check
        register: result_authselect_check_cmd
        changed_when: false
        check_mode: false
        failed_when: false
      - name: 'Limit Password Reuse: password-auth - Informative message based on the authselect integrity check result'
        ansible.builtin.assert:
          that:
          - ansible_check_mode or result_authselect_check_cmd.rc == 0
          fail_msg:
          - authselect integrity check failed. Remediation aborted!
          - This remediation could not be applied because an authselect profile was not selected or the selected profile is
            not intact.
          - It is not recommended to manually edit the PAM files when authselect tool is available.
          - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile is
            recommended.
          success_msg:
          - authselect integrity check passed
      - name: 'Limit Password Reuse: password-auth - Get authselect current profile'
        ansible.builtin.shell:
          cmd: authselect current -r | awk '{ print $1 }'
        register: result_authselect_profile
        changed_when: false
        when:
        - result_authselect_check_cmd is success
      - name: 'Limit Password Reuse: password-auth - Define the current authselect profile as a local fact'
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: '{{ result_authselect_profile.stdout }}'
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is match("custom/")
      - name: 'Limit Password Reuse: password-auth - Define the new authselect custom profile as a local fact'
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: custom/hardening
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is not match("custom/")
      - name: 'Limit Password Reuse: password-auth - Get authselect current features to also enable them in the custom profile'
        ansible.builtin.shell:
          cmd: authselect current | tail -n+3 | awk '{ print $2 }'
        register: result_authselect_features
        changed_when: false
        check_mode: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: 'Limit Password Reuse: password-auth - Check if any custom profile with the same name was already created'
        ansible.builtin.stat:
          path: /etc/authselect/{{ authselect_custom_profile }}
        register: result_authselect_custom_profile_present
        changed_when: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: 'Limit Password Reuse: password-auth - Create an authselect custom profile based on the current profile'
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b {{ authselect_current_profile }}
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is not match("^(custom/|local)")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: 'Limit Password Reuse: password-auth - Create an authselect custom profile based on sssd profile'
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b sssd
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is match("local")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: 'Limit Password Reuse: password-auth - Ensure authselect changes are applied'
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=before-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: 'Limit Password Reuse: password-auth - Ensure the authselect custom profile is selected'
        ansible.builtin.command:
          cmd: authselect select {{ authselect_custom_profile }}
        register: result_pam_authselect_select_profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: 'Limit Password Reuse: password-auth - Restore the authselect features in the custom profile'
        ansible.builtin.command:
          cmd: authselect enable-feature {{ item }}
        loop: '{{ result_authselect_features.stdout_lines }}'
        register: result_pam_authselect_restore_features
        when:
        - result_authselect_profile is not skipped
        - result_authselect_features is not skipped
        - result_pam_authselect_select_profile is not skipped
      - name: 'Limit Password Reuse: password-auth - Ensure authselect changes are applied'
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=after-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - result_pam_authselect_restore_features is not skipped
      - name: 'Limit Password Reuse: password-auth - Change the PAM file to be edited according to the custom authselect profile'
        ansible.builtin.set_fact:
          pam_file_path: /etc/authselect/{{ authselect_custom_profile }}/{{ pam_file_path | basename }}
        when:
        - authselect_custom_profile is defined
      when:
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    - name: 'Limit Password Reuse: password-auth - Define a fact for control already filtered in case filters are used'
      ansible.builtin.set_fact:
        pam_module_control: '{{ var_password_pam_remember_control_flag.split(",")[0] }}'
    - name: 'Limit Password Reuse: password-auth - Check if expected PAM module line is present in {{ pam_file_path }}'
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        regexp: ^\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_pwhistory.so\s*.*
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_line_present
    - name: 'Limit Password Reuse: password-auth - Include or update the PAM module line in {{ pam_file_path }}'
      block:
      - name: 'Limit Password Reuse: password-auth - Check if required PAM module line is present in {{ pam_file_path }} with
          different control'
        ansible.builtin.lineinfile:
          path: '{{ pam_file_path }}'
          regexp: ^\s*password\s+.*\s+pam_pwhistory.so\s*
          state: absent
        check_mode: true
        changed_when: false
        register: result_pam_line_other_control_present
      - name: 'Limit Password Reuse: password-auth - Ensure the correct control for the required PAM module line in {{ pam_file_path
          }}'
        ansible.builtin.replace:
          dest: '{{ pam_file_path }}'
          regexp: ^(\s*password\s+).*(\bpam_pwhistory.so.*)
          replace: \1{{ pam_module_control }} \2
        register: result_pam_module_edit
        when:
        - result_pam_line_other_control_present.found == 1
      - name: 'Limit Password Reuse: password-auth - Ensure the required PAM module line is included in {{ pam_file_path }}'
        ansible.builtin.lineinfile:
          dest: '{{ pam_file_path }}'
          insertafter: ^password.*requisite.*pam_pwquality\.so
          line: password    {{ pam_module_control }}    pam_pwhistory.so
        register: result_pam_module_add
        when:
        - result_pam_line_other_control_present.found == 0 or result_pam_line_other_control_present.found > 1
      - name: 'Limit Password Reuse: password-auth - Ensure authselect changes are applied'
        ansible.builtin.command:
          cmd: authselect apply-changes -b
        when:
        - result_authselect_present is defined
        - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
        - "(result_pam_module_add is defined and result_pam_module_add.changed)\n or (result_pam_module_edit is defined and\
          \ result_pam_module_edit.changed)"
      when:
      - result_pam_line_present.found is defined
      - result_pam_line_present.found == 0
    when:
    - '"pam" in ansible_facts.packages'
    - '(result_authselect_available_features.stdout is defined and result_authselect_available_features.stdout is not search("with-pwhistory"))
      or result_authselect_available_features is not defined

      '
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.8
    - NIST-800-53-IA-5(1)(e)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.5
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.7
    - accounts_password_pam_pwhistory_remember_password_auth
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Limit Password Reuse: password-auth - Enable pam_pwhistory.so in appropriate
      PAM files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit Password Reuse: password-auth - Enable pam_pwhistory.so in appropriate PAM files'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_pwhistory_remember_password_auth
    | default(true) | bool)
- block:
  - name: 'Limit Password Reuse: password-auth - Check the presence of /etc/security/pwhistory.conf file'
    ansible.builtin.stat:
      path: /etc/security/pwhistory.conf
    register: result_pwhistory_conf_check
    when: '"pam" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.8
    - NIST-800-53-IA-5(1)(e)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.5
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.7
    - accounts_password_pam_pwhistory_remember_password_auth
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Limit Password Reuse: password-auth - Check the presence of /etc/security/pwhistory.conf
      file'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit Password Reuse: password-auth - Check the presence of /etc/security/pwhistory.conf file'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_pwhistory_remember_password_auth
    | default(true) | bool)
- block:
  - name: 'Limit Password Reuse: password-auth - pam_pwhistory.so parameters are configured in /etc/security/pwhistory.conf
      file'
    block:
    - name: 'Limit Password Reuse: password-auth - Ensure the pam_pwhistory.so remember parameter in /etc/security/pwhistory.conf'
      ansible.builtin.lineinfile:
        path: /etc/security/pwhistory.conf
        regexp: ^\s*remember\s*=
        line: remember = {{ var_password_pam_remember }}
        state: present
    - name: 'Limit Password Reuse: password-auth - Ensure the pam_pwhistory.so remember parameter is removed from PAM files'
      block:
      - name: 'Limit Password Reuse: password-auth - Check if /etc/pam.d/password-auth file is present'
        ansible.builtin.stat:
          path: /etc/pam.d/password-auth
        register: result_pam_file_present
      - name: 'Limit Password Reuse: password-auth - Check the proper remediation for the system'
        block:
        - name: 'Limit Password Reuse: password-auth - Define the PAM file to be edited as a local fact'
          ansible.builtin.set_fact:
            pam_file_path: /etc/pam.d/password-auth
        - name: 'Limit Password Reuse: password-auth - Check if system relies on authselect tool'
          ansible.builtin.stat:
            path: /usr/bin/authselect
          register: result_authselect_present
        - name: 'Limit Password Reuse: password-auth - Ensure authselect custom profile is used if authselect is present'
          block:
          - name: 'Limit Password Reuse: password-auth - Check integrity of authselect current profile'
            ansible.builtin.command:
              cmd: authselect check
            register: result_authselect_check_cmd
            changed_when: false
            check_mode: false
            failed_when: false
          - name: 'Limit Password Reuse: password-auth - Informative message based on the authselect integrity check result'
            ansible.builtin.assert:
              that:
              - ansible_check_mode or result_authselect_check_cmd.rc == 0
              fail_msg:
              - authselect integrity check failed. Remediation aborted!
              - This remediation could not be applied because an authselect profile was not selected or the selected profile
                is not intact.
              - It is not recommended to manually edit the PAM files when authselect tool is available.
              - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile
                is recommended.
              success_msg:
              - authselect integrity check passed
          - name: 'Limit Password Reuse: password-auth - Get authselect current profile'
            ansible.builtin.shell:
              cmd: authselect current -r | awk '{ print $1 }'
            register: result_authselect_profile
            changed_when: false
            when:
            - result_authselect_check_cmd is success
          - name: 'Limit Password Reuse: password-auth - Define the current authselect profile as a local fact'
            ansible.builtin.set_fact:
              authselect_current_profile: '{{ result_authselect_profile.stdout }}'
              authselect_custom_profile: '{{ result_authselect_profile.stdout }}'
            when:
            - result_authselect_profile is not skipped
            - result_authselect_profile.stdout is match("custom/")
          - name: 'Limit Password Reuse: password-auth - Define the new authselect custom profile as a local fact'
            ansible.builtin.set_fact:
              authselect_current_profile: '{{ result_authselect_profile.stdout }}'
              authselect_custom_profile: custom/hardening
            when:
            - result_authselect_profile is not skipped
            - result_authselect_profile.stdout is not match("custom/")
          - name: 'Limit Password Reuse: password-auth - Get authselect current features to also enable them in the custom
              profile'
            ansible.builtin.shell:
              cmd: authselect current | tail -n+3 | awk '{ print $2 }'
            register: result_authselect_features
            changed_when: false
            check_mode: false
            when:
            - result_authselect_profile is not skipped
            - authselect_current_profile is not match("custom/")
          - name: 'Limit Password Reuse: password-auth - Check if any custom profile with the same name was already created'
            ansible.builtin.stat:
              path: /etc/authselect/{{ authselect_custom_profile }}
            register: result_authselect_custom_profile_present
            changed_when: false
            when:
            - result_authselect_profile is not skipped
            - authselect_current_profile is not match("custom/")
          - name: 'Limit Password Reuse: password-auth - Create an authselect custom profile based on the current profile'
            ansible.builtin.command:
              cmd: authselect create-profile hardening -b {{ authselect_current_profile }}
            when:
            - result_authselect_profile is not skipped
            - result_authselect_check_cmd is success
            - authselect_current_profile is not match("^(custom/|local)")
            - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
          - name: 'Limit Password Reuse: password-auth - Create an authselect custom profile based on sssd profile'
            ansible.builtin.command:
              cmd: authselect create-profile hardening -b sssd
            when:
            - result_authselect_profile is not skipped
            - result_authselect_check_cmd is success
            - authselect_current_profile is match("local")
            - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
          - name: 'Limit Password Reuse: password-auth - Ensure authselect changes are applied'
            ansible.builtin.command:
              cmd: authselect apply-changes -b --backup=before-hardening-custom-profile
            when:
            - result_authselect_check_cmd is success
            - result_authselect_profile is not skipped
            - authselect_current_profile is not match("custom/")
            - authselect_custom_profile is not match(authselect_current_profile)
          - name: 'Limit Password Reuse: password-auth - Ensure the authselect custom profile is selected'
            ansible.builtin.command:
              cmd: authselect select {{ authselect_custom_profile }}
            register: result_pam_authselect_select_profile
            when:
            - result_authselect_check_cmd is success
            - result_authselect_profile is not skipped
            - authselect_current_profile is not match("custom/")
            - authselect_custom_profile is not match(authselect_current_profile)
          - name: 'Limit Password Reuse: password-auth - Restore the authselect features in the custom profile'
            ansible.builtin.command:
              cmd: authselect enable-feature {{ item }}
            loop: '{{ result_authselect_features.stdout_lines }}'
            register: result_pam_authselect_restore_features
            when:
            - result_authselect_profile is not skipped
            - result_authselect_features is not skipped
            - result_pam_authselect_select_profile is not skipped
          - name: 'Limit Password Reuse: password-auth - Ensure authselect changes are applied'
            ansible.builtin.command:
              cmd: authselect apply-changes -b --backup=after-hardening-custom-profile
            when:
            - result_authselect_check_cmd is success
            - result_authselect_profile is not skipped
            - result_pam_authselect_restore_features is not skipped
          - name: 'Limit Password Reuse: password-auth - Change the PAM file to be edited according to the custom authselect
              profile'
            ansible.builtin.set_fact:
              pam_file_path: /etc/authselect/{{ authselect_custom_profile }}/{{ pam_file_path | basename }}
            when:
            - authselect_custom_profile is defined
          when:
          - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
        - name: 'Limit Password Reuse: password-auth - Define a fact for control already filtered in case filters are used'
          ansible.builtin.set_fact:
            pam_module_control: ''
        - name: 'Limit Password Reuse: password-auth - Check if {{ pam_file_path }} file is present'
          ansible.builtin.stat:
            path: '{{ pam_file_path }}'
          register: result_pam_file_present
        - name: 'Limit Password Reuse: password-auth - Ensure the "remember" option from "pam_pwhistory.so" is not present
            in {{ pam_file_path }}'
          ansible.builtin.replace:
            dest: '{{ pam_file_path }}'
            regexp: (.*password.*pam_pwhistory.so.*)\bremember\b=?[0-9a-zA-Z]*(.*)
            replace: \1\2
          register: result_pam_option_removal
          when: (result_pam_file_present is not skipped and result_pam_file_present.stat.exists)
        - name: 'Limit Password Reuse: password-auth - Ensure authselect changes are applied'
          ansible.builtin.command:
            cmd: authselect apply-changes -b
          when:
          - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
          - result_pam_option_removal is changed
        when:
        - (result_pam_file_present is not skipped and result_pam_file_present.stat.exists)
    when:
    - '"pam" in ansible_facts.packages'
    - (result_pwhistory_conf_check is not skipped and result_pwhistory_conf_check.stat.exists)
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.8
    - NIST-800-53-IA-5(1)(e)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.5
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.7
    - accounts_password_pam_pwhistory_remember_password_auth
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Limit Password Reuse: password-auth - pam_pwhistory.so parameters are configured
      in /etc/security/pwhistory.conf file'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit Password Reuse: password-auth - pam_pwhistory.so parameters are configured in /etc/security/pwhistory.conf
        file'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_pwhistory_remember_password_auth
    | default(true) | bool)
- block:
  - name: 'Limit Password Reuse: password-auth - pam_pwhistory.so parameters are configured in PAM files'
    block:
    - name: 'Limit Password Reuse: password-auth - Define the PAM file to be edited as a local fact'
      ansible.builtin.set_fact:
        pam_file_path: /etc/pam.d/password-auth
    - name: 'Limit Password Reuse: password-auth - Check if system relies on authselect tool'
      ansible.builtin.stat:
        path: /usr/bin/authselect
      register: result_authselect_present
    - name: 'Limit Password Reuse: password-auth - Ensure authselect custom profile is used if authselect is present'
      block:
      - name: 'Limit Password Reuse: password-auth - Check integrity of authselect current profile'
        ansible.builtin.command:
          cmd: authselect check
        register: result_authselect_check_cmd
        changed_when: false
        check_mode: false
        failed_when: false
      - name: 'Limit Password Reuse: password-auth - Informative message based on the authselect integrity check result'
        ansible.builtin.assert:
          that:
          - ansible_check_mode or result_authselect_check_cmd.rc == 0
          fail_msg:
          - authselect integrity check failed. Remediation aborted!
          - This remediation could not be applied because an authselect profile was not selected or the selected profile is
            not intact.
          - It is not recommended to manually edit the PAM files when authselect tool is available.
          - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile is
            recommended.
          success_msg:
          - authselect integrity check passed
      - name: 'Limit Password Reuse: password-auth - Get authselect current profile'
        ansible.builtin.shell:
          cmd: authselect current -r | awk '{ print $1 }'
        register: result_authselect_profile
        changed_when: false
        when:
        - result_authselect_check_cmd is success
      - name: 'Limit Password Reuse: password-auth - Define the current authselect profile as a local fact'
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: '{{ result_authselect_profile.stdout }}'
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is match("custom/")
      - name: 'Limit Password Reuse: password-auth - Define the new authselect custom profile as a local fact'
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: custom/hardening
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is not match("custom/")
      - name: 'Limit Password Reuse: password-auth - Get authselect current features to also enable them in the custom profile'
        ansible.builtin.shell:
          cmd: authselect current | tail -n+3 | awk '{ print $2 }'
        register: result_authselect_features
        changed_when: false
        check_mode: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: 'Limit Password Reuse: password-auth - Check if any custom profile with the same name was already created'
        ansible.builtin.stat:
          path: /etc/authselect/{{ authselect_custom_profile }}
        register: result_authselect_custom_profile_present
        changed_when: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: 'Limit Password Reuse: password-auth - Create an authselect custom profile based on the current profile'
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b {{ authselect_current_profile }}
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is not match("^(custom/|local)")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: 'Limit Password Reuse: password-auth - Create an authselect custom profile based on sssd profile'
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b sssd
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is match("local")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: 'Limit Password Reuse: password-auth - Ensure authselect changes are applied'
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=before-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: 'Limit Password Reuse: password-auth - Ensure the authselect custom profile is selected'
        ansible.builtin.command:
          cmd: authselect select {{ authselect_custom_profile }}
        register: result_pam_authselect_select_profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: 'Limit Password Reuse: password-auth - Restore the authselect features in the custom profile'
        ansible.builtin.command:
          cmd: authselect enable-feature {{ item }}
        loop: '{{ result_authselect_features.stdout_lines }}'
        register: result_pam_authselect_restore_features
        when:
        - result_authselect_profile is not skipped
        - result_authselect_features is not skipped
        - result_pam_authselect_select_profile is not skipped
      - name: 'Limit Password Reuse: password-auth - Ensure authselect changes are applied'
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=after-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - result_pam_authselect_restore_features is not skipped
      - name: 'Limit Password Reuse: password-auth - Change the PAM file to be edited according to the custom authselect profile'
        ansible.builtin.set_fact:
          pam_file_path: /etc/authselect/{{ authselect_custom_profile }}/{{ pam_file_path | basename }}
        when:
        - authselect_custom_profile is defined
      when:
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    - name: 'Limit Password Reuse: password-auth - Define a fact for control already filtered in case filters are used'
      ansible.builtin.set_fact:
        pam_module_control: requisite
    - name: 'Limit Password Reuse: password-auth - Check if expected PAM module line is present in {{ pam_file_path }}'
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        regexp: ^\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_pwhistory.so\s*.*
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_line_present
    - name: 'Limit Password Reuse: password-auth - Include or update the PAM module line in {{ pam_file_path }}'
      block:
      - name: 'Limit Password Reuse: password-auth - Check if required PAM module line is present in {{ pam_file_path }} with
          different control'
        ansible.builtin.lineinfile:
          path: '{{ pam_file_path }}'
          regexp: ^\s*password\s+.*\s+pam_pwhistory.so\s*
          state: absent
        check_mode: true
        changed_when: false
        register: result_pam_line_other_control_present
      - name: 'Limit Password Reuse: password-auth - Ensure the correct control for the required PAM module line in {{ pam_file_path
          }}'
        ansible.builtin.replace:
          dest: '{{ pam_file_path }}'
          regexp: ^(\s*password\s+).*(\bpam_pwhistory.so.*)
          replace: \1{{ pam_module_control }} \2
        register: result_pam_module_edit
        when:
        - result_pam_line_other_control_present.found == 1
      - name: 'Limit Password Reuse: password-auth - Ensure the required PAM module line is included in {{ pam_file_path }}'
        ansible.builtin.lineinfile:
          dest: '{{ pam_file_path }}'
          line: password    {{ pam_module_control }}    pam_pwhistory.so
        register: result_pam_module_add
        when:
        - result_pam_line_other_control_present.found == 0 or result_pam_line_other_control_present.found > 1
      - name: 'Limit Password Reuse: password-auth - Ensure authselect changes are applied'
        ansible.builtin.command:
          cmd: authselect apply-changes -b
        when:
        - result_authselect_present is defined
        - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
        - "(result_pam_module_add is defined and result_pam_module_add.changed)\n or (result_pam_module_edit is defined and\
          \ result_pam_module_edit.changed)"
      when:
      - result_pam_line_present.found is defined
      - result_pam_line_present.found == 0
    - name: 'Limit Password Reuse: password-auth - Define a fact for control already filtered in case filters are used'
      ansible.builtin.set_fact:
        pam_module_control: requisite
    - name: 'Limit Password Reuse: password-auth - Check if the required PAM module option is present in {{ pam_file_path
        }}'
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        regexp: ^\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_pwhistory.so\s*.*\sremember\b
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_module_accounts_password_pam_pwhistory_remember_password_auth_option_present
    - name: 'Limit Password Reuse: password-auth - Ensure the "remember" PAM option for "pam_pwhistory.so" is included in
        {{ pam_file_path }}'
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        backrefs: true
        regexp: ^(\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_pwhistory.so.*)
        line: \1 remember={{ var_password_pam_remember }}
        state: present
      register: result_pam_accounts_password_pam_pwhistory_remember_password_auth_add
      when:
      - result_pam_module_accounts_password_pam_pwhistory_remember_password_auth_option_present.found is defined
      - result_pam_module_accounts_password_pam_pwhistory_remember_password_auth_option_present.found == 0
    - name: 'Limit Password Reuse: password-auth - Ensure the required value for "remember" PAM option from "pam_pwhistory.so"
        in {{ pam_file_path }}'
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        backrefs: true
        regexp: ^(\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_pwhistory.so\s+.*)(remember)=[0-9a-zA-Z]*\s*(.*)
        line: \1\2={{ var_password_pam_remember }} \3
      register: result_pam_accounts_password_pam_pwhistory_remember_password_auth_edit
      when:
      - result_pam_module_accounts_password_pam_pwhistory_remember_password_auth_option_present.found > 0
    - name: 'Limit Password Reuse: password-auth - Ensure authselect changes are applied'
      ansible.builtin.command:
        cmd: authselect apply-changes -b
      when:
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
      - (result_pam_remember_add is defined and result_pam_remember_add.changed) or (result_pam_remember_edit is defined and
        result_pam_remember_edit.changed)
    when:
    - '"pam" in ansible_facts.packages'
    - not (result_pwhistory_conf_check is not skipped and result_pwhistory_conf_check.stat.exists)
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.8
    - NIST-800-53-IA-5(1)(e)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.5
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.7
    - accounts_password_pam_pwhistory_remember_password_auth
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Limit Password Reuse: password-auth - pam_pwhistory.so parameters are configured
      in PAM files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit Password Reuse: password-auth - pam_pwhistory.so parameters are configured in PAM files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_pwhistory_remember_password_auth
    | default(true) | bool)
- block:
  - name: 'Limit Password Reuse: system-auth - Check if system relies on authselect tool'
    ansible.builtin.stat:
      path: /usr/bin/authselect
    register: result_authselect_present
    when: '"pam" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.8
    - NIST-800-53-IA-5(1)(e)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.5
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.7
    - accounts_password_pam_pwhistory_remember_system_auth
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Limit Password Reuse: system-auth - Check if system relies on authselect tool''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit Password Reuse: system-auth - Check if system relies on authselect tool'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_pwhistory_remember_system_auth
    | default(true) | bool)
- block:
  - name: 'Limit Password Reuse: system-auth - Collect the available authselect features'
    ansible.builtin.command:
      cmd: authselect list-features sssd
    register: result_authselect_available_features
    changed_when: false
    check_mode: false
    when:
    - '"pam" in ansible_facts.packages'
    - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.8
    - NIST-800-53-IA-5(1)(e)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.5
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.7
    - accounts_password_pam_pwhistory_remember_system_auth
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Limit Password Reuse: system-auth - Collect the available authselect features''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit Password Reuse: system-auth - Collect the available authselect features'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_pwhistory_remember_system_auth
    | default(true) | bool)
- block:
  - name: 'Limit Password Reuse: system-auth - Enable pam_pwhistory.so using authselect feature'
    block:
    - name: 'Limit Password Reuse: system-auth - Check integrity of authselect current profile'
      ansible.builtin.command:
        cmd: authselect check
      register: result_authselect_check_cmd
      changed_when: false
      check_mode: false
      failed_when: false
    - name: 'Limit Password Reuse: system-auth - Informative message based on the authselect integrity check result'
      ansible.builtin.assert:
        that:
        - ansible_check_mode or result_authselect_check_cmd.rc == 0
        fail_msg:
        - authselect integrity check failed. Remediation aborted!
        - This remediation could not be applied because an authselect profile was not selected or the selected profile is
          not intact.
        - It is not recommended to manually edit the PAM files when authselect tool is available.
        - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile is recommended.
        success_msg:
        - authselect integrity check passed
    - name: 'Limit Password Reuse: system-auth - Get authselect current features'
      ansible.builtin.shell:
        cmd: authselect current | tail -n+3 | awk '{ print $2 }'
      register: result_authselect_features
      changed_when: false
      check_mode: false
      when:
      - result_authselect_check_cmd is success
    - name: 'Limit Password Reuse: system-auth - Ensure "with-pwhistory" feature is enabled using authselect tool'
      ansible.builtin.command:
        cmd: authselect enable-feature with-pwhistory
      register: result_authselect_enable_feature_cmd
      when:
      - result_authselect_check_cmd is success
      - result_authselect_features.stdout is not search("with-pwhistory")
    - name: 'Limit Password Reuse: system-auth - Ensure authselect changes are applied'
      ansible.builtin.command:
        cmd: authselect apply-changes -b
      when:
      - result_authselect_enable_feature_cmd is not skipped
      - result_authselect_enable_feature_cmd is success
    when:
    - '"pam" in ansible_facts.packages'
    - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    - result_authselect_available_features.stdout is search("with-pwhistory")
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.8
    - NIST-800-53-IA-5(1)(e)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.5
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.7
    - accounts_password_pam_pwhistory_remember_system_auth
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Limit Password Reuse: system-auth - Enable pam_pwhistory.so using authselect
      feature'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit Password Reuse: system-auth - Enable pam_pwhistory.so using authselect feature'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_pwhistory_remember_system_auth
    | default(true) | bool)
- block:
  - name: 'Limit Password Reuse: system-auth - Enable pam_pwhistory.so in appropriate PAM files'
    block:
    - name: 'Limit Password Reuse: system-auth - Define the PAM file to be edited as a local fact'
      ansible.builtin.set_fact:
        pam_file_path: /etc/pam.d/system-auth
    - name: 'Limit Password Reuse: system-auth - Check if system relies on authselect tool'
      ansible.builtin.stat:
        path: /usr/bin/authselect
      register: result_authselect_present
    - name: 'Limit Password Reuse: system-auth - Ensure authselect custom profile is used if authselect is present'
      block:
      - name: 'Limit Password Reuse: system-auth - Check integrity of authselect current profile'
        ansible.builtin.command:
          cmd: authselect check
        register: result_authselect_check_cmd
        changed_when: false
        check_mode: false
        failed_when: false
      - name: 'Limit Password Reuse: system-auth - Informative message based on the authselect integrity check result'
        ansible.builtin.assert:
          that:
          - ansible_check_mode or result_authselect_check_cmd.rc == 0
          fail_msg:
          - authselect integrity check failed. Remediation aborted!
          - This remediation could not be applied because an authselect profile was not selected or the selected profile is
            not intact.
          - It is not recommended to manually edit the PAM files when authselect tool is available.
          - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile is
            recommended.
          success_msg:
          - authselect integrity check passed
      - name: 'Limit Password Reuse: system-auth - Get authselect current profile'
        ansible.builtin.shell:
          cmd: authselect current -r | awk '{ print $1 }'
        register: result_authselect_profile
        changed_when: false
        when:
        - result_authselect_check_cmd is success
      - name: 'Limit Password Reuse: system-auth - Define the current authselect profile as a local fact'
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: '{{ result_authselect_profile.stdout }}'
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is match("custom/")
      - name: 'Limit Password Reuse: system-auth - Define the new authselect custom profile as a local fact'
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: custom/hardening
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is not match("custom/")
      - name: 'Limit Password Reuse: system-auth - Get authselect current features to also enable them in the custom profile'
        ansible.builtin.shell:
          cmd: authselect current | tail -n+3 | awk '{ print $2 }'
        register: result_authselect_features
        changed_when: false
        check_mode: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: 'Limit Password Reuse: system-auth - Check if any custom profile with the same name was already created'
        ansible.builtin.stat:
          path: /etc/authselect/{{ authselect_custom_profile }}
        register: result_authselect_custom_profile_present
        changed_when: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: 'Limit Password Reuse: system-auth - Create an authselect custom profile based on the current profile'
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b {{ authselect_current_profile }}
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is not match("^(custom/|local)")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: 'Limit Password Reuse: system-auth - Create an authselect custom profile based on sssd profile'
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b sssd
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is match("local")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: 'Limit Password Reuse: system-auth - Ensure authselect changes are applied'
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=before-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: 'Limit Password Reuse: system-auth - Ensure the authselect custom profile is selected'
        ansible.builtin.command:
          cmd: authselect select {{ authselect_custom_profile }}
        register: result_pam_authselect_select_profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: 'Limit Password Reuse: system-auth - Restore the authselect features in the custom profile'
        ansible.builtin.command:
          cmd: authselect enable-feature {{ item }}
        loop: '{{ result_authselect_features.stdout_lines }}'
        register: result_pam_authselect_restore_features
        when:
        - result_authselect_profile is not skipped
        - result_authselect_features is not skipped
        - result_pam_authselect_select_profile is not skipped
      - name: 'Limit Password Reuse: system-auth - Ensure authselect changes are applied'
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=after-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - result_pam_authselect_restore_features is not skipped
      - name: 'Limit Password Reuse: system-auth - Change the PAM file to be edited according to the custom authselect profile'
        ansible.builtin.set_fact:
          pam_file_path: /etc/authselect/{{ authselect_custom_profile }}/{{ pam_file_path | basename }}
        when:
        - authselect_custom_profile is defined
      when:
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    - name: 'Limit Password Reuse: system-auth - Define a fact for control already filtered in case filters are used'
      ansible.builtin.set_fact:
        pam_module_control: '{{ var_password_pam_remember_control_flag.split(",")[0] }}'
    - name: 'Limit Password Reuse: system-auth - Check if expected PAM module line is present in {{ pam_file_path }}'
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        regexp: ^\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_pwhistory.so\s*.*
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_line_present
    - name: 'Limit Password Reuse: system-auth - Include or update the PAM module line in {{ pam_file_path }}'
      block:
      - name: 'Limit Password Reuse: system-auth - Check if required PAM module line is present in {{ pam_file_path }} with
          different control'
        ansible.builtin.lineinfile:
          path: '{{ pam_file_path }}'
          regexp: ^\s*password\s+.*\s+pam_pwhistory.so\s*
          state: absent
        check_mode: true
        changed_when: false
        register: result_pam_line_other_control_present
      - name: 'Limit Password Reuse: system-auth - Ensure the correct control for the required PAM module line in {{ pam_file_path
          }}'
        ansible.builtin.replace:
          dest: '{{ pam_file_path }}'
          regexp: ^(\s*password\s+).*(\bpam_pwhistory.so.*)
          replace: \1{{ pam_module_control }} \2
        register: result_pam_module_edit
        when:
        - result_pam_line_other_control_present.found == 1
      - name: 'Limit Password Reuse: system-auth - Ensure the required PAM module line is included in {{ pam_file_path }}'
        ansible.builtin.lineinfile:
          dest: '{{ pam_file_path }}'
          insertafter: ^password.*requisite.*pam_pwquality\.so
          line: password    {{ pam_module_control }}    pam_pwhistory.so
        register: result_pam_module_add
        when:
        - result_pam_line_other_control_present.found == 0 or result_pam_line_other_control_present.found > 1
      - name: 'Limit Password Reuse: system-auth - Ensure authselect changes are applied'
        ansible.builtin.command:
          cmd: authselect apply-changes -b
        when:
        - result_authselect_present is defined
        - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
        - "(result_pam_module_add is defined and result_pam_module_add.changed)\n or (result_pam_module_edit is defined and\
          \ result_pam_module_edit.changed)"
      when:
      - result_pam_line_present.found is defined
      - result_pam_line_present.found == 0
    when:
    - '"pam" in ansible_facts.packages'
    - '(result_authselect_available_features.stdout is defined and result_authselect_available_features.stdout is not search("with-pwhistory"))
      or result_authselect_available_features is not defined

      '
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.8
    - NIST-800-53-IA-5(1)(e)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.5
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.7
    - accounts_password_pam_pwhistory_remember_system_auth
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Limit Password Reuse: system-auth - Enable pam_pwhistory.so in appropriate
      PAM files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit Password Reuse: system-auth - Enable pam_pwhistory.so in appropriate PAM files'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_pwhistory_remember_system_auth
    | default(true) | bool)
- block:
  - name: 'Limit Password Reuse: system-auth - Check the presence of /etc/security/pwhistory.conf file'
    ansible.builtin.stat:
      path: /etc/security/pwhistory.conf
    register: result_pwhistory_conf_check
    when: '"pam" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.8
    - NIST-800-53-IA-5(1)(e)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.5
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.7
    - accounts_password_pam_pwhistory_remember_system_auth
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Limit Password Reuse: system-auth - Check the presence of /etc/security/pwhistory.conf
      file'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit Password Reuse: system-auth - Check the presence of /etc/security/pwhistory.conf file'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_pwhistory_remember_system_auth
    | default(true) | bool)
- block:
  - name: 'Limit Password Reuse: system-auth - pam_pwhistory.so parameters are configured in /etc/security/pwhistory.conf
      file'
    block:
    - name: 'Limit Password Reuse: system-auth - Ensure the pam_pwhistory.so remember parameter in /etc/security/pwhistory.conf'
      ansible.builtin.lineinfile:
        path: /etc/security/pwhistory.conf
        regexp: ^\s*remember\s*=
        line: remember = {{ var_password_pam_remember }}
        state: present
    - name: 'Limit Password Reuse: system-auth - Ensure the pam_pwhistory.so remember parameter is removed from PAM files'
      block:
      - name: 'Limit Password Reuse: system-auth - Check if /etc/pam.d/system-auth file is present'
        ansible.builtin.stat:
          path: /etc/pam.d/system-auth
        register: result_pam_file_present
      - name: 'Limit Password Reuse: system-auth - Check the proper remediation for the system'
        block:
        - name: 'Limit Password Reuse: system-auth - Define the PAM file to be edited as a local fact'
          ansible.builtin.set_fact:
            pam_file_path: /etc/pam.d/system-auth
        - name: 'Limit Password Reuse: system-auth - Check if system relies on authselect tool'
          ansible.builtin.stat:
            path: /usr/bin/authselect
          register: result_authselect_present
        - name: 'Limit Password Reuse: system-auth - Ensure authselect custom profile is used if authselect is present'
          block:
          - name: 'Limit Password Reuse: system-auth - Check integrity of authselect current profile'
            ansible.builtin.command:
              cmd: authselect check
            register: result_authselect_check_cmd
            changed_when: false
            check_mode: false
            failed_when: false
          - name: 'Limit Password Reuse: system-auth - Informative message based on the authselect integrity check result'
            ansible.builtin.assert:
              that:
              - ansible_check_mode or result_authselect_check_cmd.rc == 0
              fail_msg:
              - authselect integrity check failed. Remediation aborted!
              - This remediation could not be applied because an authselect profile was not selected or the selected profile
                is not intact.
              - It is not recommended to manually edit the PAM files when authselect tool is available.
              - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile
                is recommended.
              success_msg:
              - authselect integrity check passed
          - name: 'Limit Password Reuse: system-auth - Get authselect current profile'
            ansible.builtin.shell:
              cmd: authselect current -r | awk '{ print $1 }'
            register: result_authselect_profile
            changed_when: false
            when:
            - result_authselect_check_cmd is success
          - name: 'Limit Password Reuse: system-auth - Define the current authselect profile as a local fact'
            ansible.builtin.set_fact:
              authselect_current_profile: '{{ result_authselect_profile.stdout }}'
              authselect_custom_profile: '{{ result_authselect_profile.stdout }}'
            when:
            - result_authselect_profile is not skipped
            - result_authselect_profile.stdout is match("custom/")
          - name: 'Limit Password Reuse: system-auth - Define the new authselect custom profile as a local fact'
            ansible.builtin.set_fact:
              authselect_current_profile: '{{ result_authselect_profile.stdout }}'
              authselect_custom_profile: custom/hardening
            when:
            - result_authselect_profile is not skipped
            - result_authselect_profile.stdout is not match("custom/")
          - name: 'Limit Password Reuse: system-auth - Get authselect current features to also enable them in the custom profile'
            ansible.builtin.shell:
              cmd: authselect current | tail -n+3 | awk '{ print $2 }'
            register: result_authselect_features
            changed_when: false
            check_mode: false
            when:
            - result_authselect_profile is not skipped
            - authselect_current_profile is not match("custom/")
          - name: 'Limit Password Reuse: system-auth - Check if any custom profile with the same name was already created'
            ansible.builtin.stat:
              path: /etc/authselect/{{ authselect_custom_profile }}
            register: result_authselect_custom_profile_present
            changed_when: false
            when:
            - result_authselect_profile is not skipped
            - authselect_current_profile is not match("custom/")
          - name: 'Limit Password Reuse: system-auth - Create an authselect custom profile based on the current profile'
            ansible.builtin.command:
              cmd: authselect create-profile hardening -b {{ authselect_current_profile }}
            when:
            - result_authselect_profile is not skipped
            - result_authselect_check_cmd is success
            - authselect_current_profile is not match("^(custom/|local)")
            - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
          - name: 'Limit Password Reuse: system-auth - Create an authselect custom profile based on sssd profile'
            ansible.builtin.command:
              cmd: authselect create-profile hardening -b sssd
            when:
            - result_authselect_profile is not skipped
            - result_authselect_check_cmd is success
            - authselect_current_profile is match("local")
            - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
          - name: 'Limit Password Reuse: system-auth - Ensure authselect changes are applied'
            ansible.builtin.command:
              cmd: authselect apply-changes -b --backup=before-hardening-custom-profile
            when:
            - result_authselect_check_cmd is success
            - result_authselect_profile is not skipped
            - authselect_current_profile is not match("custom/")
            - authselect_custom_profile is not match(authselect_current_profile)
          - name: 'Limit Password Reuse: system-auth - Ensure the authselect custom profile is selected'
            ansible.builtin.command:
              cmd: authselect select {{ authselect_custom_profile }}
            register: result_pam_authselect_select_profile
            when:
            - result_authselect_check_cmd is success
            - result_authselect_profile is not skipped
            - authselect_current_profile is not match("custom/")
            - authselect_custom_profile is not match(authselect_current_profile)
          - name: 'Limit Password Reuse: system-auth - Restore the authselect features in the custom profile'
            ansible.builtin.command:
              cmd: authselect enable-feature {{ item }}
            loop: '{{ result_authselect_features.stdout_lines }}'
            register: result_pam_authselect_restore_features
            when:
            - result_authselect_profile is not skipped
            - result_authselect_features is not skipped
            - result_pam_authselect_select_profile is not skipped
          - name: 'Limit Password Reuse: system-auth - Ensure authselect changes are applied'
            ansible.builtin.command:
              cmd: authselect apply-changes -b --backup=after-hardening-custom-profile
            when:
            - result_authselect_check_cmd is success
            - result_authselect_profile is not skipped
            - result_pam_authselect_restore_features is not skipped
          - name: 'Limit Password Reuse: system-auth - Change the PAM file to be edited according to the custom authselect
              profile'
            ansible.builtin.set_fact:
              pam_file_path: /etc/authselect/{{ authselect_custom_profile }}/{{ pam_file_path | basename }}
            when:
            - authselect_custom_profile is defined
          when:
          - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
        - name: 'Limit Password Reuse: system-auth - Define a fact for control already filtered in case filters are used'
          ansible.builtin.set_fact:
            pam_module_control: ''
        - name: 'Limit Password Reuse: system-auth - Check if {{ pam_file_path }} file is present'
          ansible.builtin.stat:
            path: '{{ pam_file_path }}'
          register: result_pam_file_present
        - name: 'Limit Password Reuse: system-auth - Ensure the "remember" option from "pam_pwhistory.so" is not present in
            {{ pam_file_path }}'
          ansible.builtin.replace:
            dest: '{{ pam_file_path }}'
            regexp: (.*password.*pam_pwhistory.so.*)\bremember\b=?[0-9a-zA-Z]*(.*)
            replace: \1\2
          register: result_pam_option_removal
          when: (result_pam_file_present is not skipped and result_pam_file_present.stat.exists)
        - name: 'Limit Password Reuse: system-auth - Ensure authselect changes are applied'
          ansible.builtin.command:
            cmd: authselect apply-changes -b
          when:
          - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
          - result_pam_option_removal is changed
        when:
        - (result_pam_file_present is not skipped and result_pam_file_present.stat.exists)
    when:
    - '"pam" in ansible_facts.packages'
    - (result_pwhistory_conf_check is not skipped and result_pwhistory_conf_check.stat.exists)
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.8
    - NIST-800-53-IA-5(1)(e)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.5
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.7
    - accounts_password_pam_pwhistory_remember_system_auth
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Limit Password Reuse: system-auth - pam_pwhistory.so parameters are configured
      in /etc/security/pwhistory.conf file'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit Password Reuse: system-auth - pam_pwhistory.so parameters are configured in /etc/security/pwhistory.conf
        file'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_pwhistory_remember_system_auth
    | default(true) | bool)
- block:
  - name: 'Limit Password Reuse: system-auth - pam_pwhistory.so parameters are configured in PAM files'
    block:
    - name: 'Limit Password Reuse: system-auth - Define the PAM file to be edited as a local fact'
      ansible.builtin.set_fact:
        pam_file_path: /etc/pam.d/system-auth
    - name: 'Limit Password Reuse: system-auth - Check if system relies on authselect tool'
      ansible.builtin.stat:
        path: /usr/bin/authselect
      register: result_authselect_present
    - name: 'Limit Password Reuse: system-auth - Ensure authselect custom profile is used if authselect is present'
      block:
      - name: 'Limit Password Reuse: system-auth - Check integrity of authselect current profile'
        ansible.builtin.command:
          cmd: authselect check
        register: result_authselect_check_cmd
        changed_when: false
        check_mode: false
        failed_when: false
      - name: 'Limit Password Reuse: system-auth - Informative message based on the authselect integrity check result'
        ansible.builtin.assert:
          that:
          - ansible_check_mode or result_authselect_check_cmd.rc == 0
          fail_msg:
          - authselect integrity check failed. Remediation aborted!
          - This remediation could not be applied because an authselect profile was not selected or the selected profile is
            not intact.
          - It is not recommended to manually edit the PAM files when authselect tool is available.
          - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile is
            recommended.
          success_msg:
          - authselect integrity check passed
      - name: 'Limit Password Reuse: system-auth - Get authselect current profile'
        ansible.builtin.shell:
          cmd: authselect current -r | awk '{ print $1 }'
        register: result_authselect_profile
        changed_when: false
        when:
        - result_authselect_check_cmd is success
      - name: 'Limit Password Reuse: system-auth - Define the current authselect profile as a local fact'
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: '{{ result_authselect_profile.stdout }}'
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is match("custom/")
      - name: 'Limit Password Reuse: system-auth - Define the new authselect custom profile as a local fact'
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: custom/hardening
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is not match("custom/")
      - name: 'Limit Password Reuse: system-auth - Get authselect current features to also enable them in the custom profile'
        ansible.builtin.shell:
          cmd: authselect current | tail -n+3 | awk '{ print $2 }'
        register: result_authselect_features
        changed_when: false
        check_mode: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: 'Limit Password Reuse: system-auth - Check if any custom profile with the same name was already created'
        ansible.builtin.stat:
          path: /etc/authselect/{{ authselect_custom_profile }}
        register: result_authselect_custom_profile_present
        changed_when: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: 'Limit Password Reuse: system-auth - Create an authselect custom profile based on the current profile'
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b {{ authselect_current_profile }}
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is not match("^(custom/|local)")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: 'Limit Password Reuse: system-auth - Create an authselect custom profile based on sssd profile'
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b sssd
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is match("local")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: 'Limit Password Reuse: system-auth - Ensure authselect changes are applied'
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=before-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: 'Limit Password Reuse: system-auth - Ensure the authselect custom profile is selected'
        ansible.builtin.command:
          cmd: authselect select {{ authselect_custom_profile }}
        register: result_pam_authselect_select_profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: 'Limit Password Reuse: system-auth - Restore the authselect features in the custom profile'
        ansible.builtin.command:
          cmd: authselect enable-feature {{ item }}
        loop: '{{ result_authselect_features.stdout_lines }}'
        register: result_pam_authselect_restore_features
        when:
        - result_authselect_profile is not skipped
        - result_authselect_features is not skipped
        - result_pam_authselect_select_profile is not skipped
      - name: 'Limit Password Reuse: system-auth - Ensure authselect changes are applied'
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=after-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - result_pam_authselect_restore_features is not skipped
      - name: 'Limit Password Reuse: system-auth - Change the PAM file to be edited according to the custom authselect profile'
        ansible.builtin.set_fact:
          pam_file_path: /etc/authselect/{{ authselect_custom_profile }}/{{ pam_file_path | basename }}
        when:
        - authselect_custom_profile is defined
      when:
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    - name: 'Limit Password Reuse: system-auth - Define a fact for control already filtered in case filters are used'
      ansible.builtin.set_fact:
        pam_module_control: requisite
    - name: 'Limit Password Reuse: system-auth - Check if expected PAM module line is present in {{ pam_file_path }}'
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        regexp: ^\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_pwhistory.so\s*.*
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_line_present
    - name: 'Limit Password Reuse: system-auth - Include or update the PAM module line in {{ pam_file_path }}'
      block:
      - name: 'Limit Password Reuse: system-auth - Check if required PAM module line is present in {{ pam_file_path }} with
          different control'
        ansible.builtin.lineinfile:
          path: '{{ pam_file_path }}'
          regexp: ^\s*password\s+.*\s+pam_pwhistory.so\s*
          state: absent
        check_mode: true
        changed_when: false
        register: result_pam_line_other_control_present
      - name: 'Limit Password Reuse: system-auth - Ensure the correct control for the required PAM module line in {{ pam_file_path
          }}'
        ansible.builtin.replace:
          dest: '{{ pam_file_path }}'
          regexp: ^(\s*password\s+).*(\bpam_pwhistory.so.*)
          replace: \1{{ pam_module_control }} \2
        register: result_pam_module_edit
        when:
        - result_pam_line_other_control_present.found == 1
      - name: 'Limit Password Reuse: system-auth - Ensure the required PAM module line is included in {{ pam_file_path }}'
        ansible.builtin.lineinfile:
          dest: '{{ pam_file_path }}'
          line: password    {{ pam_module_control }}    pam_pwhistory.so
        register: result_pam_module_add
        when:
        - result_pam_line_other_control_present.found == 0 or result_pam_line_other_control_present.found > 1
      - name: 'Limit Password Reuse: system-auth - Ensure authselect changes are applied'
        ansible.builtin.command:
          cmd: authselect apply-changes -b
        when:
        - result_authselect_present is defined
        - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
        - "(result_pam_module_add is defined and result_pam_module_add.changed)\n or (result_pam_module_edit is defined and\
          \ result_pam_module_edit.changed)"
      when:
      - result_pam_line_present.found is defined
      - result_pam_line_present.found == 0
    - name: 'Limit Password Reuse: system-auth - Define a fact for control already filtered in case filters are used'
      ansible.builtin.set_fact:
        pam_module_control: requisite
    - name: 'Limit Password Reuse: system-auth - Check if the required PAM module option is present in {{ pam_file_path }}'
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        regexp: ^\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_pwhistory.so\s*.*\sremember\b
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_module_accounts_password_pam_pwhistory_remember_system_auth_option_present
    - name: 'Limit Password Reuse: system-auth - Ensure the "remember" PAM option for "pam_pwhistory.so" is included in {{
        pam_file_path }}'
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        backrefs: true
        regexp: ^(\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_pwhistory.so.*)
        line: \1 remember={{ var_password_pam_remember }}
        state: present
      register: result_pam_accounts_password_pam_pwhistory_remember_system_auth_add
      when:
      - result_pam_module_accounts_password_pam_pwhistory_remember_system_auth_option_present.found is defined
      - result_pam_module_accounts_password_pam_pwhistory_remember_system_auth_option_present.found == 0
    - name: 'Limit Password Reuse: system-auth - Ensure the required value for "remember" PAM option from "pam_pwhistory.so"
        in {{ pam_file_path }}'
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        backrefs: true
        regexp: ^(\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_pwhistory.so\s+.*)(remember)=[0-9a-zA-Z]*\s*(.*)
        line: \1\2={{ var_password_pam_remember }} \3
      register: result_pam_accounts_password_pam_pwhistory_remember_system_auth_edit
      when:
      - result_pam_module_accounts_password_pam_pwhistory_remember_system_auth_option_present.found > 0
    - name: 'Limit Password Reuse: system-auth - Ensure authselect changes are applied'
      ansible.builtin.command:
        cmd: authselect apply-changes -b
      when:
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
      - (result_pam_remember_add is defined and result_pam_remember_add.changed) or (result_pam_remember_edit is defined and
        result_pam_remember_edit.changed)
    when:
    - '"pam" in ansible_facts.packages'
    - not (result_pwhistory_conf_check is not skipped and result_pwhistory_conf_check.stat.exists)
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.8
    - NIST-800-53-IA-5(1)(e)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.5
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.7
    - accounts_password_pam_pwhistory_remember_system_auth
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Limit Password Reuse: system-auth - pam_pwhistory.so parameters are configured
      in PAM files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Limit Password Reuse: system-auth - pam_pwhistory.so parameters are configured in PAM files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_pwhistory_remember_system_auth
    | default(true) | bool)
- block:
  - name: Lock Accounts After Failed Password Attempts - Check if system relies on authselect tool
    ansible.builtin.stat:
      path: /usr/bin/authselect
    register: result_authselect_present
    when: '"pam" in ansible_facts.packages'
    tags:
    - CJIS-5.5.3
    - NIST-800-171-3.1.8
    - NIST-800-53-AC-7(a)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.1.6
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.4
    - accounts_passwords_pam_faillock_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Lock Accounts After Failed Password Attempts - Check if system relies on authselect
      tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Lock Accounts After Failed Password Attempts - Check if system relies on authselect tool'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_passwords_pam_faillock_deny | default(true)
    | bool)
- block:
  - name: Lock Accounts After Failed Password Attempts - Remediation where authselect tool is present
    block:
    - name: Lock Accounts After Failed Password Attempts - Check integrity of authselect current profile
      ansible.builtin.command:
        cmd: authselect check
      register: result_authselect_check_cmd
      changed_when: false
      check_mode: false
      failed_when: false
    - name: Lock Accounts After Failed Password Attempts - Informative message based on the authselect integrity check result
      ansible.builtin.assert:
        that:
        - ansible_check_mode or result_authselect_check_cmd.rc == 0
        fail_msg:
        - authselect integrity check failed. Remediation aborted!
        - This remediation could not be applied because an authselect profile was not selected or the selected profile is
          not intact.
        - It is not recommended to manually edit the PAM files when authselect tool is available.
        - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile is recommended.
        success_msg:
        - authselect integrity check passed
    - name: Lock Accounts After Failed Password Attempts - Get authselect current features
      ansible.builtin.shell:
        cmd: authselect current | tail -n+3 | awk '{ print $2 }'
      register: result_authselect_features
      changed_when: false
      check_mode: false
      when:
      - result_authselect_check_cmd is success
    - name: Lock Accounts After Failed Password Attempts - Ensure "with-faillock" feature is enabled using authselect tool
      ansible.builtin.command:
        cmd: authselect enable-feature with-faillock
      register: result_authselect_enable_feature_cmd
      when:
      - result_authselect_check_cmd is success
      - result_authselect_features.stdout is not search("with-faillock")
    - name: Lock Accounts After Failed Password Attempts - Ensure authselect changes are applied
      ansible.builtin.command:
        cmd: authselect apply-changes -b
      when:
      - result_authselect_enable_feature_cmd is not skipped
      - result_authselect_enable_feature_cmd is success
    when:
    - '"pam" in ansible_facts.packages'
    - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    tags:
    - CJIS-5.5.3
    - NIST-800-171-3.1.8
    - NIST-800-53-AC-7(a)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.1.6
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.4
    - accounts_passwords_pam_faillock_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Lock Accounts After Failed Password Attempts - Remediation where authselect
      tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Lock Accounts After Failed Password Attempts - Remediation where authselect tool is present'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_passwords_pam_faillock_deny | default(true)
    | bool)
- block:
  - name: Lock Accounts After Failed Password Attempts - Remediation where authselect tool is not present
    block:
    - name: Lock Accounts After Failed Password Attempts - Check if pam_faillock.so is already enabled
      ansible.builtin.lineinfile:
        path: /etc/pam.d/system-auth
        regexp: .*auth.*pam_faillock\.so (preauth|authfail)
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_faillock_is_enabled
    - name: Lock Accounts After Failed Password Attempts - Enable pam_faillock.so preauth editing PAM files
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        line: auth        required      pam_faillock.so preauth
        insertbefore: ^auth.*sufficient.*pam_unix\.so.*
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_is_enabled.found == 0
    - name: Lock Accounts After Failed Password Attempts - Enable pam_faillock.so authfail editing PAM files
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        line: auth        required      pam_faillock.so authfail
        insertbefore: ^auth.*required.*pam_deny\.so.*
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_is_enabled.found == 0
    - name: Lock Accounts After Failed Password Attempts - Enable pam_faillock.so account section editing PAM files
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        line: account     required      pam_faillock.so
        insertbefore: ^account.*required.*pam_unix\.so.*
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_is_enabled.found == 0
    when:
    - '"pam" in ansible_facts.packages'
    - not (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    tags:
    - CJIS-5.5.3
    - NIST-800-171-3.1.8
    - NIST-800-53-AC-7(a)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.1.6
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.4
    - accounts_passwords_pam_faillock_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Lock Accounts After Failed Password Attempts - Remediation where authselect
      tool is not present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Lock Accounts After Failed Password Attempts - Remediation where authselect tool is not present''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_passwords_pam_faillock_deny | default(true)
    | bool)
- block:
  - name: Lock Accounts After Failed Password Attempts - Check the presence of /etc/security/faillock.conf file
    ansible.builtin.stat:
      path: /etc/security/faillock.conf
    register: result_faillock_conf_check
    when: '"pam" in ansible_facts.packages'
    tags:
    - CJIS-5.5.3
    - NIST-800-171-3.1.8
    - NIST-800-53-AC-7(a)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.1.6
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.4
    - accounts_passwords_pam_faillock_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Lock Accounts After Failed Password Attempts - Check the presence of /etc/security/faillock.conf
      file'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Lock Accounts After Failed Password Attempts - Check the presence of /etc/security/faillock.conf
        file'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_passwords_pam_faillock_deny | default(true)
    | bool)
- block:
  - name: Lock Accounts After Failed Password Attempts - Ensure the pam_faillock.so deny parameter in /etc/security/faillock.conf
    ansible.builtin.lineinfile:
      path: /etc/security/faillock.conf
      regexp: ^\s*deny\s*=
      line: deny = {{ var_accounts_passwords_pam_faillock_deny }}
      state: present
    when:
    - '"pam" in ansible_facts.packages'
    - (result_faillock_conf_check is not skipped and result_faillock_conf_check.stat.exists)
    tags:
    - CJIS-5.5.3
    - NIST-800-171-3.1.8
    - NIST-800-53-AC-7(a)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.1.6
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.4
    - accounts_passwords_pam_faillock_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Lock Accounts After Failed Password Attempts - Ensure the pam_faillock.so
      deny parameter in /etc/security/faillock.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Lock Accounts After Failed Password Attempts - Ensure the pam_faillock.so deny parameter in /etc/security/faillock.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_passwords_pam_faillock_deny | default(true)
    | bool)
- block:
  - name: Lock Accounts After Failed Password Attempts - Ensure the pam_faillock.so deny parameter not in PAM files
    block:
    - name: Lock Accounts After Failed Password Attempts - Check if /etc/pam.d/system-auth file is present
      ansible.builtin.stat:
        path: /etc/pam.d/system-auth
      register: result_pam_file_present
    - name: Lock Accounts After Failed Password Attempts - Check the proper remediation for the system
      block:
      - name: Lock Accounts After Failed Password Attempts - Define the PAM file to be edited as a local fact
        ansible.builtin.set_fact:
          pam_file_path: /etc/pam.d/system-auth
      - name: Lock Accounts After Failed Password Attempts - Check if system relies on authselect tool
        ansible.builtin.stat:
          path: /usr/bin/authselect
        register: result_authselect_present
      - name: Lock Accounts After Failed Password Attempts - Ensure authselect custom profile is used if authselect is present
        block:
        - name: Lock Accounts After Failed Password Attempts - Check integrity of authselect current profile
          ansible.builtin.command:
            cmd: authselect check
          register: result_authselect_check_cmd
          changed_when: false
          check_mode: false
          failed_when: false
        - name: Lock Accounts After Failed Password Attempts - Informative message based on the authselect integrity check
            result
          ansible.builtin.assert:
            that:
            - ansible_check_mode or result_authselect_check_cmd.rc == 0
            fail_msg:
            - authselect integrity check failed. Remediation aborted!
            - This remediation could not be applied because an authselect profile was not selected or the selected profile
              is not intact.
            - It is not recommended to manually edit the PAM files when authselect tool is available.
            - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile
              is recommended.
            success_msg:
            - authselect integrity check passed
        - name: Lock Accounts After Failed Password Attempts - Get authselect current profile
          ansible.builtin.shell:
            cmd: authselect current -r | awk '{ print $1 }'
          register: result_authselect_profile
          changed_when: false
          when:
          - result_authselect_check_cmd is success
        - name: Lock Accounts After Failed Password Attempts - Define the current authselect profile as a local fact
          ansible.builtin.set_fact:
            authselect_current_profile: '{{ result_authselect_profile.stdout }}'
            authselect_custom_profile: '{{ result_authselect_profile.stdout }}'
          when:
          - result_authselect_profile is not skipped
          - result_authselect_profile.stdout is match("custom/")
        - name: Lock Accounts After Failed Password Attempts - Define the new authselect custom profile as a local fact
          ansible.builtin.set_fact:
            authselect_current_profile: '{{ result_authselect_profile.stdout }}'
            authselect_custom_profile: custom/hardening
          when:
          - result_authselect_profile is not skipped
          - result_authselect_profile.stdout is not match("custom/")
        - name: Lock Accounts After Failed Password Attempts - Get authselect current features to also enable them in the
            custom profile
          ansible.builtin.shell:
            cmd: authselect current | tail -n+3 | awk '{ print $2 }'
          register: result_authselect_features
          changed_when: false
          check_mode: false
          when:
          - result_authselect_profile is not skipped
          - authselect_current_profile is not match("custom/")
        - name: Lock Accounts After Failed Password Attempts - Check if any custom profile with the same name was already
            created
          ansible.builtin.stat:
            path: /etc/authselect/{{ authselect_custom_profile }}
          register: result_authselect_custom_profile_present
          changed_when: false
          when:
          - result_authselect_profile is not skipped
          - authselect_current_profile is not match("custom/")
        - name: Lock Accounts After Failed Password Attempts - Create an authselect custom profile based on the current profile
          ansible.builtin.command:
            cmd: authselect create-profile hardening -b {{ authselect_current_profile }}
          when:
          - result_authselect_profile is not skipped
          - result_authselect_check_cmd is success
          - authselect_current_profile is not match("^(custom/|local)")
          - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
        - name: Lock Accounts After Failed Password Attempts - Create an authselect custom profile based on sssd profile
          ansible.builtin.command:
            cmd: authselect create-profile hardening -b sssd
          when:
          - result_authselect_profile is not skipped
          - result_authselect_check_cmd is success
          - authselect_current_profile is match("local")
          - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
        - name: Lock Accounts After Failed Password Attempts - Ensure authselect changes are applied
          ansible.builtin.command:
            cmd: authselect apply-changes -b --backup=before-hardening-custom-profile
          when:
          - result_authselect_check_cmd is success
          - result_authselect_profile is not skipped
          - authselect_current_profile is not match("custom/")
          - authselect_custom_profile is not match(authselect_current_profile)
        - name: Lock Accounts After Failed Password Attempts - Ensure the authselect custom profile is selected
          ansible.builtin.command:
            cmd: authselect select {{ authselect_custom_profile }}
          register: result_pam_authselect_select_profile
          when:
          - result_authselect_check_cmd is success
          - result_authselect_profile is not skipped
          - authselect_current_profile is not match("custom/")
          - authselect_custom_profile is not match(authselect_current_profile)
        - name: Lock Accounts After Failed Password Attempts - Restore the authselect features in the custom profile
          ansible.builtin.command:
            cmd: authselect enable-feature {{ item }}
          loop: '{{ result_authselect_features.stdout_lines }}'
          register: result_pam_authselect_restore_features
          when:
          - result_authselect_profile is not skipped
          - result_authselect_features is not skipped
          - result_pam_authselect_select_profile is not skipped
        - name: Lock Accounts After Failed Password Attempts - Ensure authselect changes are applied
          ansible.builtin.command:
            cmd: authselect apply-changes -b --backup=after-hardening-custom-profile
          when:
          - result_authselect_check_cmd is success
          - result_authselect_profile is not skipped
          - result_pam_authselect_restore_features is not skipped
        - name: Lock Accounts After Failed Password Attempts - Change the PAM file to be edited according to the custom authselect
            profile
          ansible.builtin.set_fact:
            pam_file_path: /etc/authselect/{{ authselect_custom_profile }}/{{ pam_file_path | basename }}
          when:
          - authselect_custom_profile is defined
        when:
        - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
      - name: Lock Accounts After Failed Password Attempts - Define a fact for control already filtered in case filters are
          used
        ansible.builtin.set_fact:
          pam_module_control: ''
      - name: Lock Accounts After Failed Password Attempts - Check if {{ pam_file_path }} file is present
        ansible.builtin.stat:
          path: '{{ pam_file_path }}'
        register: result_pam_file_present
      - name: Lock Accounts After Failed Password Attempts - Ensure the "deny" option from "pam_faillock.so" is not present
          in {{ pam_file_path }}
        ansible.builtin.replace:
          dest: '{{ pam_file_path }}'
          regexp: (.*auth.*pam_faillock.so.*)\bdeny\b=?[0-9a-zA-Z]*(.*)
          replace: \1\2
        register: result_pam_option_removal
        when: (result_pam_file_present is not skipped and result_pam_file_present.stat.exists)
      - name: Lock Accounts After Failed Password Attempts - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b
        when:
        - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
        - result_pam_option_removal is changed
      when:
      - (result_pam_file_present is not skipped and result_pam_file_present.stat.exists)
    - name: Lock Accounts After Failed Password Attempts - Check if /etc/pam.d/password-auth file is present
      ansible.builtin.stat:
        path: /etc/pam.d/password-auth
      register: result_pam_file_present
    - name: Lock Accounts After Failed Password Attempts - Check the proper remediation for the system
      block:
      - name: Lock Accounts After Failed Password Attempts - Define the PAM file to be edited as a local fact
        ansible.builtin.set_fact:
          pam_file_path: /etc/pam.d/password-auth
      - name: Lock Accounts After Failed Password Attempts - Check if system relies on authselect tool
        ansible.builtin.stat:
          path: /usr/bin/authselect
        register: result_authselect_present
      - name: Lock Accounts After Failed Password Attempts - Ensure authselect custom profile is used if authselect is present
        block:
        - name: Lock Accounts After Failed Password Attempts - Check integrity of authselect current profile
          ansible.builtin.command:
            cmd: authselect check
          register: result_authselect_check_cmd
          changed_when: false
          check_mode: false
          failed_when: false
        - name: Lock Accounts After Failed Password Attempts - Informative message based on the authselect integrity check
            result
          ansible.builtin.assert:
            that:
            - ansible_check_mode or result_authselect_check_cmd.rc == 0
            fail_msg:
            - authselect integrity check failed. Remediation aborted!
            - This remediation could not be applied because an authselect profile was not selected or the selected profile
              is not intact.
            - It is not recommended to manually edit the PAM files when authselect tool is available.
            - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile
              is recommended.
            success_msg:
            - authselect integrity check passed
        - name: Lock Accounts After Failed Password Attempts - Get authselect current profile
          ansible.builtin.shell:
            cmd: authselect current -r | awk '{ print $1 }'
          register: result_authselect_profile
          changed_when: false
          when:
          - result_authselect_check_cmd is success
        - name: Lock Accounts After Failed Password Attempts - Define the current authselect profile as a local fact
          ansible.builtin.set_fact:
            authselect_current_profile: '{{ result_authselect_profile.stdout }}'
            authselect_custom_profile: '{{ result_authselect_profile.stdout }}'
          when:
          - result_authselect_profile is not skipped
          - result_authselect_profile.stdout is match("custom/")
        - name: Lock Accounts After Failed Password Attempts - Define the new authselect custom profile as a local fact
          ansible.builtin.set_fact:
            authselect_current_profile: '{{ result_authselect_profile.stdout }}'
            authselect_custom_profile: custom/hardening
          when:
          - result_authselect_profile is not skipped
          - result_authselect_profile.stdout is not match("custom/")
        - name: Lock Accounts After Failed Password Attempts - Get authselect current features to also enable them in the
            custom profile
          ansible.builtin.shell:
            cmd: authselect current | tail -n+3 | awk '{ print $2 }'
          register: result_authselect_features
          changed_when: false
          check_mode: false
          when:
          - result_authselect_profile is not skipped
          - authselect_current_profile is not match("custom/")
        - name: Lock Accounts After Failed Password Attempts - Check if any custom profile with the same name was already
            created
          ansible.builtin.stat:
            path: /etc/authselect/{{ authselect_custom_profile }}
          register: result_authselect_custom_profile_present
          changed_when: false
          when:
          - result_authselect_profile is not skipped
          - authselect_current_profile is not match("custom/")
        - name: Lock Accounts After Failed Password Attempts - Create an authselect custom profile based on the current profile
          ansible.builtin.command:
            cmd: authselect create-profile hardening -b {{ authselect_current_profile }}
          when:
          - result_authselect_profile is not skipped
          - result_authselect_check_cmd is success
          - authselect_current_profile is not match("^(custom/|local)")
          - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
        - name: Lock Accounts After Failed Password Attempts - Create an authselect custom profile based on sssd profile
          ansible.builtin.command:
            cmd: authselect create-profile hardening -b sssd
          when:
          - result_authselect_profile is not skipped
          - result_authselect_check_cmd is success
          - authselect_current_profile is match("local")
          - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
        - name: Lock Accounts After Failed Password Attempts - Ensure authselect changes are applied
          ansible.builtin.command:
            cmd: authselect apply-changes -b --backup=before-hardening-custom-profile
          when:
          - result_authselect_check_cmd is success
          - result_authselect_profile is not skipped
          - authselect_current_profile is not match("custom/")
          - authselect_custom_profile is not match(authselect_current_profile)
        - name: Lock Accounts After Failed Password Attempts - Ensure the authselect custom profile is selected
          ansible.builtin.command:
            cmd: authselect select {{ authselect_custom_profile }}
          register: result_pam_authselect_select_profile
          when:
          - result_authselect_check_cmd is success
          - result_authselect_profile is not skipped
          - authselect_current_profile is not match("custom/")
          - authselect_custom_profile is not match(authselect_current_profile)
        - name: Lock Accounts After Failed Password Attempts - Restore the authselect features in the custom profile
          ansible.builtin.command:
            cmd: authselect enable-feature {{ item }}
          loop: '{{ result_authselect_features.stdout_lines }}'
          register: result_pam_authselect_restore_features
          when:
          - result_authselect_profile is not skipped
          - result_authselect_features is not skipped
          - result_pam_authselect_select_profile is not skipped
        - name: Lock Accounts After Failed Password Attempts - Ensure authselect changes are applied
          ansible.builtin.command:
            cmd: authselect apply-changes -b --backup=after-hardening-custom-profile
          when:
          - result_authselect_check_cmd is success
          - result_authselect_profile is not skipped
          - result_pam_authselect_restore_features is not skipped
        - name: Lock Accounts After Failed Password Attempts - Change the PAM file to be edited according to the custom authselect
            profile
          ansible.builtin.set_fact:
            pam_file_path: /etc/authselect/{{ authselect_custom_profile }}/{{ pam_file_path | basename }}
          when:
          - authselect_custom_profile is defined
        when:
        - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
      - name: Lock Accounts After Failed Password Attempts - Define a fact for control already filtered in case filters are
          used
        ansible.builtin.set_fact:
          pam_module_control: ''
      - name: Lock Accounts After Failed Password Attempts - Check if {{ pam_file_path }} file is present
        ansible.builtin.stat:
          path: '{{ pam_file_path }}'
        register: result_pam_file_present
      - name: Lock Accounts After Failed Password Attempts - Ensure the "deny" option from "pam_faillock.so" is not present
          in {{ pam_file_path }}
        ansible.builtin.replace:
          dest: '{{ pam_file_path }}'
          regexp: (.*auth.*pam_faillock.so.*)\bdeny\b=?[0-9a-zA-Z]*(.*)
          replace: \1\2
        register: result_pam_option_removal
        when: (result_pam_file_present is not skipped and result_pam_file_present.stat.exists)
      - name: Lock Accounts After Failed Password Attempts - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b
        when:
        - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
        - result_pam_option_removal is changed
      when:
      - (result_pam_file_present is not skipped and result_pam_file_present.stat.exists)
    when:
    - '"pam" in ansible_facts.packages'
    - (result_faillock_conf_check is not skipped and result_faillock_conf_check.stat.exists)
    tags:
    - CJIS-5.5.3
    - NIST-800-171-3.1.8
    - NIST-800-53-AC-7(a)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.1.6
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.4
    - accounts_passwords_pam_faillock_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Lock Accounts After Failed Password Attempts - Ensure the pam_faillock.so
      deny parameter not in PAM files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Lock Accounts After Failed Password Attempts - Ensure the pam_faillock.so deny parameter not in PAM
        files'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_passwords_pam_faillock_deny | default(true)
    | bool)
- block:
  - name: Lock Accounts After Failed Password Attempts - Ensure the pam_faillock.so deny parameter in PAM files
    block:
    - name: Lock Accounts After Failed Password Attempts - Check if pam_faillock.so deny parameter is already enabled in pam
        files
      ansible.builtin.lineinfile:
        path: /etc/pam.d/system-auth
        regexp: .*auth.*pam_faillock\.so (preauth|authfail).*deny
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_faillock_deny_parameter_is_present
    - name: Lock Accounts After Failed Password Attempts - Ensure the inclusion of pam_faillock.so preauth deny parameter
        in auth section
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        backrefs: true
        regexp: (^\s*auth\s+)([\w\[].*\b)(\s+pam_faillock.so preauth.*)
        line: \1required\3 deny={{ var_accounts_passwords_pam_faillock_deny }}
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_deny_parameter_is_present.found == 0
    - name: Lock Accounts After Failed Password Attempts - Ensure the inclusion of pam_faillock.so authfail deny parameter
        in auth section
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        backrefs: true
        regexp: (^\s*auth\s+)([\w\[].*\b)(\s+pam_faillock.so authfail.*)
        line: \1required\3 deny={{ var_accounts_passwords_pam_faillock_deny }}
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_deny_parameter_is_present.found == 0
    - name: Lock Accounts After Failed Password Attempts - Ensure the desired value for pam_faillock.so preauth deny parameter
        in auth section
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        backrefs: true
        regexp: (^\s*auth\s+)([\w\[].*\b)(\s+pam_faillock.so preauth.*)(deny)=[0-9]+(.*)
        line: \1required\3\4={{ var_accounts_passwords_pam_faillock_deny }}\5
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_deny_parameter_is_present.found > 0
    - name: Lock Accounts After Failed Password Attempts - Ensure the desired value for pam_faillock.so authfail deny parameter
        in auth section
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        backrefs: true
        regexp: (^\s*auth\s+)([\w\[].*\b)(\s+pam_faillock.so authfail.*)(deny)=[0-9]+(.*)
        line: \1required\3\4={{ var_accounts_passwords_pam_faillock_deny }}\5
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_deny_parameter_is_present.found > 0
    when:
    - '"pam" in ansible_facts.packages'
    - not (result_faillock_conf_check is not skipped and result_faillock_conf_check.stat.exists)
    tags:
    - CJIS-5.5.3
    - NIST-800-171-3.1.8
    - NIST-800-53-AC-7(a)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.1.6
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.4
    - accounts_passwords_pam_faillock_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Lock Accounts After Failed Password Attempts - Ensure the pam_faillock.so
      deny parameter in PAM files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Lock Accounts After Failed Password Attempts - Ensure the pam_faillock.so deny parameter in PAM files''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_passwords_pam_faillock_deny | default(true)
    | bool)
- block:
  - name: Set Lockout Time for Failed Password Attempts - Check if system relies on authselect tool
    ansible.builtin.stat:
      path: /usr/bin/authselect
    register: result_authselect_present
    when: '"pam" in ansible_facts.packages'
    tags:
    - CJIS-5.5.3
    - NIST-800-171-3.1.8
    - NIST-800-53-AC-7(b)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.1.7
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.4
    - accounts_passwords_pam_faillock_unlock_time
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set Lockout Time for Failed Password Attempts - Check if system relies on
      authselect tool'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Lockout Time for Failed Password Attempts - Check if system relies on authselect tool'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_passwords_pam_faillock_unlock_time | default(true)
    | bool)
- block:
  - name: Set Lockout Time for Failed Password Attempts - Remediation where authselect tool is present
    block:
    - name: Set Lockout Time for Failed Password Attempts - Check integrity of authselect current profile
      ansible.builtin.command:
        cmd: authselect check
      register: result_authselect_check_cmd
      changed_when: false
      check_mode: false
      failed_when: false
    - name: Set Lockout Time for Failed Password Attempts - Informative message based on the authselect integrity check result
      ansible.builtin.assert:
        that:
        - ansible_check_mode or result_authselect_check_cmd.rc == 0
        fail_msg:
        - authselect integrity check failed. Remediation aborted!
        - This remediation could not be applied because an authselect profile was not selected or the selected profile is
          not intact.
        - It is not recommended to manually edit the PAM files when authselect tool is available.
        - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile is recommended.
        success_msg:
        - authselect integrity check passed
    - name: Set Lockout Time for Failed Password Attempts - Get authselect current features
      ansible.builtin.shell:
        cmd: authselect current | tail -n+3 | awk '{ print $2 }'
      register: result_authselect_features
      changed_when: false
      check_mode: false
      when:
      - result_authselect_check_cmd is success
    - name: Set Lockout Time for Failed Password Attempts - Ensure "with-faillock" feature is enabled using authselect tool
      ansible.builtin.command:
        cmd: authselect enable-feature with-faillock
      register: result_authselect_enable_feature_cmd
      when:
      - result_authselect_check_cmd is success
      - result_authselect_features.stdout is not search("with-faillock")
    - name: Set Lockout Time for Failed Password Attempts - Ensure authselect changes are applied
      ansible.builtin.command:
        cmd: authselect apply-changes -b
      when:
      - result_authselect_enable_feature_cmd is not skipped
      - result_authselect_enable_feature_cmd is success
    when:
    - '"pam" in ansible_facts.packages'
    - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    tags:
    - CJIS-5.5.3
    - NIST-800-171-3.1.8
    - NIST-800-53-AC-7(b)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.1.7
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.4
    - accounts_passwords_pam_faillock_unlock_time
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set Lockout Time for Failed Password Attempts - Remediation where authselect
      tool is present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Lockout Time for Failed Password Attempts - Remediation where authselect tool is present'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_passwords_pam_faillock_unlock_time | default(true)
    | bool)
- block:
  - name: Set Lockout Time for Failed Password Attempts - Remediation where authselect tool is not present
    block:
    - name: Set Lockout Time for Failed Password Attempts - Check if pam_faillock.so is already enabled
      ansible.builtin.lineinfile:
        path: /etc/pam.d/system-auth
        regexp: .*auth.*pam_faillock\.so (preauth|authfail)
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_faillock_is_enabled
    - name: Set Lockout Time for Failed Password Attempts - Enable pam_faillock.so preauth editing PAM files
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        line: auth        required      pam_faillock.so preauth
        insertbefore: ^auth.*sufficient.*pam_unix\.so.*
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_is_enabled.found == 0
    - name: Set Lockout Time for Failed Password Attempts - Enable pam_faillock.so authfail editing PAM files
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        line: auth        required      pam_faillock.so authfail
        insertbefore: ^auth.*required.*pam_deny\.so.*
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_is_enabled.found == 0
    - name: Set Lockout Time for Failed Password Attempts - Enable pam_faillock.so account section editing PAM files
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        line: account     required      pam_faillock.so
        insertbefore: ^account.*required.*pam_unix\.so.*
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_is_enabled.found == 0
    when:
    - '"pam" in ansible_facts.packages'
    - not (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    tags:
    - CJIS-5.5.3
    - NIST-800-171-3.1.8
    - NIST-800-53-AC-7(b)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.1.7
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.4
    - accounts_passwords_pam_faillock_unlock_time
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set Lockout Time for Failed Password Attempts - Remediation where authselect
      tool is not present'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Lockout Time for Failed Password Attempts - Remediation where authselect tool is not present''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_passwords_pam_faillock_unlock_time | default(true)
    | bool)
- block:
  - name: Set Lockout Time for Failed Password Attempts - Check the presence of /etc/security/faillock.conf file
    ansible.builtin.stat:
      path: /etc/security/faillock.conf
    register: result_faillock_conf_check
    when: '"pam" in ansible_facts.packages'
    tags:
    - CJIS-5.5.3
    - NIST-800-171-3.1.8
    - NIST-800-53-AC-7(b)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.1.7
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.4
    - accounts_passwords_pam_faillock_unlock_time
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set Lockout Time for Failed Password Attempts - Check the presence of /etc/security/faillock.conf
      file'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Lockout Time for Failed Password Attempts - Check the presence of /etc/security/faillock.conf
        file'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_passwords_pam_faillock_unlock_time | default(true)
    | bool)
- block:
  - name: Set Lockout Time for Failed Password Attempts - Ensure the pam_faillock.so unlock_time parameter in /etc/security/faillock.conf
    ansible.builtin.lineinfile:
      path: /etc/security/faillock.conf
      regexp: ^\s*unlock_time\s*=
      line: unlock_time = {{ var_accounts_passwords_pam_faillock_unlock_time }}
      state: present
    when:
    - '"pam" in ansible_facts.packages'
    - (result_faillock_conf_check is not skipped and result_faillock_conf_check.stat.exists)
    tags:
    - CJIS-5.5.3
    - NIST-800-171-3.1.8
    - NIST-800-53-AC-7(b)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.1.7
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.4
    - accounts_passwords_pam_faillock_unlock_time
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set Lockout Time for Failed Password Attempts - Ensure the pam_faillock.so
      unlock_time parameter in /etc/security/faillock.conf'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Lockout Time for Failed Password Attempts - Ensure the pam_faillock.so unlock_time parameter
        in /etc/security/faillock.conf'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_passwords_pam_faillock_unlock_time | default(true)
    | bool)
- block:
  - name: Set Lockout Time for Failed Password Attempts - Ensure the pam_faillock.so unlock_time parameter not in PAM files
    block:
    - name: Set Lockout Time for Failed Password Attempts - Check if /etc/pam.d/system-auth file is present
      ansible.builtin.stat:
        path: /etc/pam.d/system-auth
      register: result_pam_file_present
    - name: Set Lockout Time for Failed Password Attempts - Check the proper remediation for the system
      block:
      - name: Set Lockout Time for Failed Password Attempts - Define the PAM file to be edited as a local fact
        ansible.builtin.set_fact:
          pam_file_path: /etc/pam.d/system-auth
      - name: Set Lockout Time for Failed Password Attempts - Check if system relies on authselect tool
        ansible.builtin.stat:
          path: /usr/bin/authselect
        register: result_authselect_present
      - name: Set Lockout Time for Failed Password Attempts - Ensure authselect custom profile is used if authselect is present
        block:
        - name: Set Lockout Time for Failed Password Attempts - Check integrity of authselect current profile
          ansible.builtin.command:
            cmd: authselect check
          register: result_authselect_check_cmd
          changed_when: false
          check_mode: false
          failed_when: false
        - name: Set Lockout Time for Failed Password Attempts - Informative message based on the authselect integrity check
            result
          ansible.builtin.assert:
            that:
            - ansible_check_mode or result_authselect_check_cmd.rc == 0
            fail_msg:
            - authselect integrity check failed. Remediation aborted!
            - This remediation could not be applied because an authselect profile was not selected or the selected profile
              is not intact.
            - It is not recommended to manually edit the PAM files when authselect tool is available.
            - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile
              is recommended.
            success_msg:
            - authselect integrity check passed
        - name: Set Lockout Time for Failed Password Attempts - Get authselect current profile
          ansible.builtin.shell:
            cmd: authselect current -r | awk '{ print $1 }'
          register: result_authselect_profile
          changed_when: false
          when:
          - result_authselect_check_cmd is success
        - name: Set Lockout Time for Failed Password Attempts - Define the current authselect profile as a local fact
          ansible.builtin.set_fact:
            authselect_current_profile: '{{ result_authselect_profile.stdout }}'
            authselect_custom_profile: '{{ result_authselect_profile.stdout }}'
          when:
          - result_authselect_profile is not skipped
          - result_authselect_profile.stdout is match("custom/")
        - name: Set Lockout Time for Failed Password Attempts - Define the new authselect custom profile as a local fact
          ansible.builtin.set_fact:
            authselect_current_profile: '{{ result_authselect_profile.stdout }}'
            authselect_custom_profile: custom/hardening
          when:
          - result_authselect_profile is not skipped
          - result_authselect_profile.stdout is not match("custom/")
        - name: Set Lockout Time for Failed Password Attempts - Get authselect current features to also enable them in the
            custom profile
          ansible.builtin.shell:
            cmd: authselect current | tail -n+3 | awk '{ print $2 }'
          register: result_authselect_features
          changed_when: false
          check_mode: false
          when:
          - result_authselect_profile is not skipped
          - authselect_current_profile is not match("custom/")
        - name: Set Lockout Time for Failed Password Attempts - Check if any custom profile with the same name was already
            created
          ansible.builtin.stat:
            path: /etc/authselect/{{ authselect_custom_profile }}
          register: result_authselect_custom_profile_present
          changed_when: false
          when:
          - result_authselect_profile is not skipped
          - authselect_current_profile is not match("custom/")
        - name: Set Lockout Time for Failed Password Attempts - Create an authselect custom profile based on the current profile
          ansible.builtin.command:
            cmd: authselect create-profile hardening -b {{ authselect_current_profile }}
          when:
          - result_authselect_profile is not skipped
          - result_authselect_check_cmd is success
          - authselect_current_profile is not match("^(custom/|local)")
          - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
        - name: Set Lockout Time for Failed Password Attempts - Create an authselect custom profile based on sssd profile
          ansible.builtin.command:
            cmd: authselect create-profile hardening -b sssd
          when:
          - result_authselect_profile is not skipped
          - result_authselect_check_cmd is success
          - authselect_current_profile is match("local")
          - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
        - name: Set Lockout Time for Failed Password Attempts - Ensure authselect changes are applied
          ansible.builtin.command:
            cmd: authselect apply-changes -b --backup=before-hardening-custom-profile
          when:
          - result_authselect_check_cmd is success
          - result_authselect_profile is not skipped
          - authselect_current_profile is not match("custom/")
          - authselect_custom_profile is not match(authselect_current_profile)
        - name: Set Lockout Time for Failed Password Attempts - Ensure the authselect custom profile is selected
          ansible.builtin.command:
            cmd: authselect select {{ authselect_custom_profile }}
          register: result_pam_authselect_select_profile
          when:
          - result_authselect_check_cmd is success
          - result_authselect_profile is not skipped
          - authselect_current_profile is not match("custom/")
          - authselect_custom_profile is not match(authselect_current_profile)
        - name: Set Lockout Time for Failed Password Attempts - Restore the authselect features in the custom profile
          ansible.builtin.command:
            cmd: authselect enable-feature {{ item }}
          loop: '{{ result_authselect_features.stdout_lines }}'
          register: result_pam_authselect_restore_features
          when:
          - result_authselect_profile is not skipped
          - result_authselect_features is not skipped
          - result_pam_authselect_select_profile is not skipped
        - name: Set Lockout Time for Failed Password Attempts - Ensure authselect changes are applied
          ansible.builtin.command:
            cmd: authselect apply-changes -b --backup=after-hardening-custom-profile
          when:
          - result_authselect_check_cmd is success
          - result_authselect_profile is not skipped
          - result_pam_authselect_restore_features is not skipped
        - name: Set Lockout Time for Failed Password Attempts - Change the PAM file to be edited according to the custom authselect
            profile
          ansible.builtin.set_fact:
            pam_file_path: /etc/authselect/{{ authselect_custom_profile }}/{{ pam_file_path | basename }}
          when:
          - authselect_custom_profile is defined
        when:
        - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
      - name: Set Lockout Time for Failed Password Attempts - Define a fact for control already filtered in case filters are
          used
        ansible.builtin.set_fact:
          pam_module_control: ''
      - name: Set Lockout Time for Failed Password Attempts - Check if {{ pam_file_path }} file is present
        ansible.builtin.stat:
          path: '{{ pam_file_path }}'
        register: result_pam_file_present
      - name: Set Lockout Time for Failed Password Attempts - Ensure the "unlock_time" option from "pam_faillock.so" is not
          present in {{ pam_file_path }}
        ansible.builtin.replace:
          dest: '{{ pam_file_path }}'
          regexp: (.*auth.*pam_faillock.so.*)\bunlock_time\b=?[0-9a-zA-Z]*(.*)
          replace: \1\2
        register: result_pam_option_removal
        when: (result_pam_file_present is not skipped and result_pam_file_present.stat.exists)
      - name: Set Lockout Time for Failed Password Attempts - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b
        when:
        - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
        - result_pam_option_removal is changed
      when:
      - (result_pam_file_present is not skipped and result_pam_file_present.stat.exists)
    - name: Set Lockout Time for Failed Password Attempts - Check if /etc/pam.d/password-auth file is present
      ansible.builtin.stat:
        path: /etc/pam.d/password-auth
      register: result_pam_file_present
    - name: Set Lockout Time for Failed Password Attempts - Check the proper remediation for the system
      block:
      - name: Set Lockout Time for Failed Password Attempts - Define the PAM file to be edited as a local fact
        ansible.builtin.set_fact:
          pam_file_path: /etc/pam.d/password-auth
      - name: Set Lockout Time for Failed Password Attempts - Check if system relies on authselect tool
        ansible.builtin.stat:
          path: /usr/bin/authselect
        register: result_authselect_present
      - name: Set Lockout Time for Failed Password Attempts - Ensure authselect custom profile is used if authselect is present
        block:
        - name: Set Lockout Time for Failed Password Attempts - Check integrity of authselect current profile
          ansible.builtin.command:
            cmd: authselect check
          register: result_authselect_check_cmd
          changed_when: false
          check_mode: false
          failed_when: false
        - name: Set Lockout Time for Failed Password Attempts - Informative message based on the authselect integrity check
            result
          ansible.builtin.assert:
            that:
            - ansible_check_mode or result_authselect_check_cmd.rc == 0
            fail_msg:
            - authselect integrity check failed. Remediation aborted!
            - This remediation could not be applied because an authselect profile was not selected or the selected profile
              is not intact.
            - It is not recommended to manually edit the PAM files when authselect tool is available.
            - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile
              is recommended.
            success_msg:
            - authselect integrity check passed
        - name: Set Lockout Time for Failed Password Attempts - Get authselect current profile
          ansible.builtin.shell:
            cmd: authselect current -r | awk '{ print $1 }'
          register: result_authselect_profile
          changed_when: false
          when:
          - result_authselect_check_cmd is success
        - name: Set Lockout Time for Failed Password Attempts - Define the current authselect profile as a local fact
          ansible.builtin.set_fact:
            authselect_current_profile: '{{ result_authselect_profile.stdout }}'
            authselect_custom_profile: '{{ result_authselect_profile.stdout }}'
          when:
          - result_authselect_profile is not skipped
          - result_authselect_profile.stdout is match("custom/")
        - name: Set Lockout Time for Failed Password Attempts - Define the new authselect custom profile as a local fact
          ansible.builtin.set_fact:
            authselect_current_profile: '{{ result_authselect_profile.stdout }}'
            authselect_custom_profile: custom/hardening
          when:
          - result_authselect_profile is not skipped
          - result_authselect_profile.stdout is not match("custom/")
        - name: Set Lockout Time for Failed Password Attempts - Get authselect current features to also enable them in the
            custom profile
          ansible.builtin.shell:
            cmd: authselect current | tail -n+3 | awk '{ print $2 }'
          register: result_authselect_features
          changed_when: false
          check_mode: false
          when:
          - result_authselect_profile is not skipped
          - authselect_current_profile is not match("custom/")
        - name: Set Lockout Time for Failed Password Attempts - Check if any custom profile with the same name was already
            created
          ansible.builtin.stat:
            path: /etc/authselect/{{ authselect_custom_profile }}
          register: result_authselect_custom_profile_present
          changed_when: false
          when:
          - result_authselect_profile is not skipped
          - authselect_current_profile is not match("custom/")
        - name: Set Lockout Time for Failed Password Attempts - Create an authselect custom profile based on the current profile
          ansible.builtin.command:
            cmd: authselect create-profile hardening -b {{ authselect_current_profile }}
          when:
          - result_authselect_profile is not skipped
          - result_authselect_check_cmd is success
          - authselect_current_profile is not match("^(custom/|local)")
          - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
        - name: Set Lockout Time for Failed Password Attempts - Create an authselect custom profile based on sssd profile
          ansible.builtin.command:
            cmd: authselect create-profile hardening -b sssd
          when:
          - result_authselect_profile is not skipped
          - result_authselect_check_cmd is success
          - authselect_current_profile is match("local")
          - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
        - name: Set Lockout Time for Failed Password Attempts - Ensure authselect changes are applied
          ansible.builtin.command:
            cmd: authselect apply-changes -b --backup=before-hardening-custom-profile
          when:
          - result_authselect_check_cmd is success
          - result_authselect_profile is not skipped
          - authselect_current_profile is not match("custom/")
          - authselect_custom_profile is not match(authselect_current_profile)
        - name: Set Lockout Time for Failed Password Attempts - Ensure the authselect custom profile is selected
          ansible.builtin.command:
            cmd: authselect select {{ authselect_custom_profile }}
          register: result_pam_authselect_select_profile
          when:
          - result_authselect_check_cmd is success
          - result_authselect_profile is not skipped
          - authselect_current_profile is not match("custom/")
          - authselect_custom_profile is not match(authselect_current_profile)
        - name: Set Lockout Time for Failed Password Attempts - Restore the authselect features in the custom profile
          ansible.builtin.command:
            cmd: authselect enable-feature {{ item }}
          loop: '{{ result_authselect_features.stdout_lines }}'
          register: result_pam_authselect_restore_features
          when:
          - result_authselect_profile is not skipped
          - result_authselect_features is not skipped
          - result_pam_authselect_select_profile is not skipped
        - name: Set Lockout Time for Failed Password Attempts - Ensure authselect changes are applied
          ansible.builtin.command:
            cmd: authselect apply-changes -b --backup=after-hardening-custom-profile
          when:
          - result_authselect_check_cmd is success
          - result_authselect_profile is not skipped
          - result_pam_authselect_restore_features is not skipped
        - name: Set Lockout Time for Failed Password Attempts - Change the PAM file to be edited according to the custom authselect
            profile
          ansible.builtin.set_fact:
            pam_file_path: /etc/authselect/{{ authselect_custom_profile }}/{{ pam_file_path | basename }}
          when:
          - authselect_custom_profile is defined
        when:
        - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
      - name: Set Lockout Time for Failed Password Attempts - Define a fact for control already filtered in case filters are
          used
        ansible.builtin.set_fact:
          pam_module_control: ''
      - name: Set Lockout Time for Failed Password Attempts - Check if {{ pam_file_path }} file is present
        ansible.builtin.stat:
          path: '{{ pam_file_path }}'
        register: result_pam_file_present
      - name: Set Lockout Time for Failed Password Attempts - Ensure the "unlock_time" option from "pam_faillock.so" is not
          present in {{ pam_file_path }}
        ansible.builtin.replace:
          dest: '{{ pam_file_path }}'
          regexp: (.*auth.*pam_faillock.so.*)\bunlock_time\b=?[0-9a-zA-Z]*(.*)
          replace: \1\2
        register: result_pam_option_removal
        when: (result_pam_file_present is not skipped and result_pam_file_present.stat.exists)
      - name: Set Lockout Time for Failed Password Attempts - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b
        when:
        - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
        - result_pam_option_removal is changed
      when:
      - (result_pam_file_present is not skipped and result_pam_file_present.stat.exists)
    when:
    - '"pam" in ansible_facts.packages'
    - (result_faillock_conf_check is not skipped and result_faillock_conf_check.stat.exists)
    tags:
    - CJIS-5.5.3
    - NIST-800-171-3.1.8
    - NIST-800-53-AC-7(b)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.1.7
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.4
    - accounts_passwords_pam_faillock_unlock_time
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set Lockout Time for Failed Password Attempts - Ensure the pam_faillock.so
      unlock_time parameter not in PAM files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Lockout Time for Failed Password Attempts - Ensure the pam_faillock.so unlock_time parameter
        not in PAM files'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_passwords_pam_faillock_unlock_time | default(true)
    | bool)
- block:
  - name: Set Lockout Time for Failed Password Attempts - Ensure the pam_faillock.so unlock_time parameter in PAM files
    block:
    - name: Set Lockout Time for Failed Password Attempts - Check if pam_faillock.so unlock_time parameter is already enabled
        in pam files
      ansible.builtin.lineinfile:
        path: /etc/pam.d/system-auth
        regexp: .*auth.*pam_faillock\.so (preauth|authfail).*unlock_time
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_faillock_unlock_time_parameter_is_present
    - name: Set Lockout Time for Failed Password Attempts - Ensure the inclusion of pam_faillock.so preauth unlock_time parameter
        in auth section
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        backrefs: true
        regexp: (^\s*auth\s+)([\w\[].*\b)(\s+pam_faillock.so preauth.*)
        line: \1required\3 unlock_time={{ var_accounts_passwords_pam_faillock_unlock_time }}
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_unlock_time_parameter_is_present.found == 0
    - name: Set Lockout Time for Failed Password Attempts - Ensure the inclusion of pam_faillock.so authfail unlock_time parameter
        in auth section
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        backrefs: true
        regexp: (^\s*auth\s+)([\w\[].*\b)(\s+pam_faillock.so authfail.*)
        line: \1required\3 unlock_time={{ var_accounts_passwords_pam_faillock_unlock_time }}
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_unlock_time_parameter_is_present.found == 0
    - name: Set Lockout Time for Failed Password Attempts - Ensure the desired value for pam_faillock.so preauth unlock_time
        parameter in auth section
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        backrefs: true
        regexp: (^\s*auth\s+)([\w\[].*\b)(\s+pam_faillock.so preauth.*)(unlock_time)=[0-9]+(.*)
        line: \1required\3\4={{ var_accounts_passwords_pam_faillock_unlock_time }}\5
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_unlock_time_parameter_is_present.found > 0
    - name: Set Lockout Time for Failed Password Attempts - Ensure the desired value for pam_faillock.so authfail unlock_time
        parameter in auth section
      ansible.builtin.lineinfile:
        path: '{{ item }}'
        backrefs: true
        regexp: (^\s*auth\s+)([\w\[].*\b)(\s+pam_faillock.so authfail.*)(unlock_time)=[0-9]+(.*)
        line: \1required\3\4={{ var_accounts_passwords_pam_faillock_unlock_time }}\5
        state: present
      loop:
      - /etc/pam.d/system-auth
      - /etc/pam.d/password-auth
      when:
      - result_pam_faillock_unlock_time_parameter_is_present.found > 0
    when:
    - '"pam" in ansible_facts.packages'
    - not (result_faillock_conf_check is not skipped and result_faillock_conf_check.stat.exists)
    tags:
    - CJIS-5.5.3
    - NIST-800-171-3.1.8
    - NIST-800-53-AC-7(b)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.1.7
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.4
    - accounts_passwords_pam_faillock_unlock_time
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set Lockout Time for Failed Password Attempts - Ensure the pam_faillock.so
      unlock_time parameter in PAM files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Lockout Time for Failed Password Attempts - Ensure the pam_faillock.so unlock_time parameter
        in PAM files'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_passwords_pam_faillock_unlock_time | default(true)
    | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Different Categories - Ensure PAM variable minclass is set accordingly
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/security/pwquality.conf
      regexp: ^#?\s*minclass
      line: minclass = {{ var_password_pam_minclass }}
    when: '"libpwquality" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - accounts_password_pam_minclass
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Different Categories -
      Ensure PAM variable minclass is set accordingly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Different Categories - Ensure PAM variable minclass
        is set accordingly'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_minclass | default(true) | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Minimum Length - Ensure PAM variable minlen is set accordingly
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/security/pwquality.conf
      regexp: ^#?\s*minlen
      line: minlen = {{ var_password_pam_minlen }}
    when: '"libpwquality" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(a)
    - NIST-800-53-IA-5(4)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.3
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.6
    - accounts_password_pam_minlen
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Minimum Length - Ensure PAM variable
      minlen is set accordingly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Minimum Length - Ensure PAM variable minlen is set accordingly''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_minlen | default(true) | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted Per-Session - Define a fact for
      control already filtered in case filters are used
    ansible.builtin.set_fact:
      pam_module_control: requisite
    when: '"libpwquality" in ansible_facts.packages'
    tags:
    - CJIS-5.5.3
    - NIST-800-53-AC-7(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(4)
    - accounts_password_pam_retry
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted
      Per-Session - Define a fact for control already filtered in case filters are used'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted Per-Session -
        Define a fact for control already filtered in case filters are used'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_retry | default(true) | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted Per-Session - Check if expected
      PAM module line is present in /etc/pam.d/system-auth
    ansible.builtin.lineinfile:
      path: /etc/pam.d/system-auth
      regexp: ^\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_pwquality.so\s*.*
      state: absent
    check_mode: true
    changed_when: false
    register: result_pam_line_present
    when: '"libpwquality" in ansible_facts.packages'
    tags:
    - CJIS-5.5.3
    - NIST-800-53-AC-7(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(4)
    - accounts_password_pam_retry
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted
      Per-Session - Check if expected PAM module line is present in /etc/pam.d/system-auth'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted Per-Session -
        Check if expected PAM module line is present in /etc/pam.d/system-auth'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_retry | default(true) | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted Per-Session - Include or update
      the PAM module line in /etc/pam.d/system-auth
    block:
    - name: Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted Per-Session - Check if required
        PAM module line is present in /etc/pam.d/system-auth with different control
      ansible.builtin.lineinfile:
        path: /etc/pam.d/system-auth
        regexp: ^\s*password\s+.*\s+pam_pwquality.so\s*
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_line_other_control_present
    - name: Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted Per-Session - Ensure the correct
        control for the required PAM module line in /etc/pam.d/system-auth
      ansible.builtin.replace:
        dest: /etc/pam.d/system-auth
        regexp: ^(\s*password\s+).*(\bpam_pwquality.so.*)
        replace: \1{{ pam_module_control }} \2
      register: result_pam_module_edit
      when:
      - result_pam_line_other_control_present.found == 1
    - name: Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted Per-Session - Ensure the required
        PAM module line is included in /etc/pam.d/system-auth
      ansible.builtin.lineinfile:
        dest: /etc/pam.d/system-auth
        insertafter: ^\s*account
        line: password    {{ pam_module_control }}    pam_pwquality.so
      register: result_pam_module_add
      when:
      - result_pam_line_other_control_present.found == 0 or result_pam_line_other_control_present.found > 1
    - name: Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted Per-Session - Ensure authselect
        changes are applied
      ansible.builtin.command:
        cmd: authselect apply-changes -b
      when:
      - result_authselect_present is defined
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
      - "(result_pam_module_add is defined and result_pam_module_add.changed)\n or (result_pam_module_edit is defined and\
        \ result_pam_module_edit.changed)"
    when:
    - '"libpwquality" in ansible_facts.packages'
    - result_pam_line_present.found is defined
    - result_pam_line_present.found == 0
    tags:
    - CJIS-5.5.3
    - NIST-800-53-AC-7(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(4)
    - accounts_password_pam_retry
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted
      Per-Session - Include or update the PAM module line in /etc/pam.d/system-auth'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted Per-Session -
        Include or update the PAM module line in /etc/pam.d/system-auth'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_retry | default(true) | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted Per-Session - Define a fact for
      control already filtered in case filters are used
    ansible.builtin.set_fact:
      pam_module_control: requisite
    when: '"libpwquality" in ansible_facts.packages'
    tags:
    - CJIS-5.5.3
    - NIST-800-53-AC-7(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(4)
    - accounts_password_pam_retry
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted
      Per-Session - Define a fact for control already filtered in case filters are used'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted Per-Session -
        Define a fact for control already filtered in case filters are used'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_retry | default(true) | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted Per-Session - Check if the required
      PAM module option is present in /etc/pam.d/system-auth
    ansible.builtin.lineinfile:
      path: /etc/pam.d/system-auth
      regexp: ^\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_pwquality.so\s*.*\sretry\b
      state: absent
    check_mode: true
    changed_when: false
    register: result_pam_module_accounts_password_pam_retry_option_present
    when: '"libpwquality" in ansible_facts.packages'
    tags:
    - CJIS-5.5.3
    - NIST-800-53-AC-7(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(4)
    - accounts_password_pam_retry
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted
      Per-Session - Check if the required PAM module option is present in /etc/pam.d/system-auth'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted Per-Session -
        Check if the required PAM module option is present in /etc/pam.d/system-auth'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_retry | default(true) | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted Per-Session - Ensure the "retry"
      PAM option for "pam_pwquality.so" is included in /etc/pam.d/system-auth
    ansible.builtin.lineinfile:
      path: /etc/pam.d/system-auth
      backrefs: true
      regexp: ^(\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_pwquality.so.*)
      line: \1 retry={{ var_password_pam_retry }}
      state: present
    register: result_pam_accounts_password_pam_retry_add
    when:
    - '"libpwquality" in ansible_facts.packages'
    - result_pam_module_accounts_password_pam_retry_option_present.found is defined
    - result_pam_module_accounts_password_pam_retry_option_present.found == 0
    tags:
    - CJIS-5.5.3
    - NIST-800-53-AC-7(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(4)
    - accounts_password_pam_retry
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted
      Per-Session - Ensure the "retry" PAM option for "pam_pwquality.so" is included in /etc/pam.d/system-auth'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted Per-Session -
        Ensure the "retry" PAM option for "pam_pwquality.so" is included in /etc/pam.d/system-auth'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_retry | default(true) | bool)
- block:
  - name: Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted Per-Session - Ensure the required
      value for "retry" PAM option from "pam_pwquality.so" in /etc/pam.d/system-auth
    ansible.builtin.lineinfile:
      path: /etc/pam.d/system-auth
      backrefs: true
      regexp: ^(\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_pwquality.so\s+.*)(retry)=[0-9a-zA-Z]*\s*(.*)
      line: \1\2={{ var_password_pam_retry }} \3
    register: result_pam_accounts_password_pam_retry_edit
    when:
    - '"libpwquality" in ansible_facts.packages'
    - result_pam_module_accounts_password_pam_retry_option_present.found > 0
    tags:
    - CJIS-5.5.3
    - NIST-800-53-AC-7(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(4)
    - accounts_password_pam_retry
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted
      Per-Session - Ensure the required value for "retry" PAM option from "pam_pwquality.so" in /etc/pam.d/system-auth'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure PAM Enforces Password Requirements - Authentication Retry Prompts Permitted Per-Session -
        Ensure the required value for "retry" PAM option from "pam_pwquality.so" in /etc/pam.d/system-auth'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_pam_retry | default(true) | bool)
- block:
  - name: Set Password Hashing Algorithm in /etc/login.defs
    ansible.builtin.lineinfile:
      dest: /etc/login.defs
      regexp: ^#?ENCRYPT_METHOD
      line: ENCRYPT_METHOD {{ var_password_hashing_algorithm.split('|')[0] }}
      state: present
      create: true
    when: '"shadow-utils" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.2
    - NIST-800-171-3.13.11
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(c)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.1
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.2
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_password_hashing_algorithm_logindefs
  rescue:
  - name: 'StigForge: continue after error in ''Set Password Hashing Algorithm in /etc/login.defs'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Password Hashing Algorithm in /etc/login.defs'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_set_password_hashing_algorithm_logindefs | default(true)
    | bool)
- block:
  - name: Set PAM's Password Hashing Algorithm - password-auth - Check if /etc/pam.d/password-auth file is present
    ansible.builtin.stat:
      path: /etc/pam.d/password-auth
    register: result_pam_file_present
    when: '"pam" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.2
    - NIST-800-171-3.13.11
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(c)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.1
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - set_password_hashing_algorithm_passwordauth
  rescue:
  - name: 'StigForge: continue after error in "Set PAM''s Password Hashing Algorithm - password-auth - Check if /etc/pam.d/password-auth
      file is present" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Set PAM''s Password Hashing Algorithm - password-auth - Check if /etc/pam.d/password-auth file is
        present" raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_set_password_hashing_algorithm_passwordauth | default(true)
    | bool)
- block:
  - name: Set PAM's Password Hashing Algorithm - password-auth - Check the proper remediation for the system
    block:
    - name: Set PAM's Password Hashing Algorithm - password-auth - Define the PAM file to be edited as a local fact
      ansible.builtin.set_fact:
        pam_file_path: /etc/pam.d/password-auth
    - name: Set PAM's Password Hashing Algorithm - password-auth - Check if system relies on authselect tool
      ansible.builtin.stat:
        path: /usr/bin/authselect
      register: result_authselect_present
    - name: Set PAM's Password Hashing Algorithm - password-auth - Ensure authselect custom profile is used if authselect
        is present
      block:
      - name: Set PAM's Password Hashing Algorithm - password-auth - Check integrity of authselect current profile
        ansible.builtin.command:
          cmd: authselect check
        register: result_authselect_check_cmd
        changed_when: false
        check_mode: false
        failed_when: false
      - name: Set PAM's Password Hashing Algorithm - password-auth - Informative message based on the authselect integrity
          check result
        ansible.builtin.assert:
          that:
          - ansible_check_mode or result_authselect_check_cmd.rc == 0
          fail_msg:
          - authselect integrity check failed. Remediation aborted!
          - This remediation could not be applied because an authselect profile was not selected or the selected profile is
            not intact.
          - It is not recommended to manually edit the PAM files when authselect tool is available.
          - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile is
            recommended.
          success_msg:
          - authselect integrity check passed
      - name: Set PAM's Password Hashing Algorithm - password-auth - Get authselect current profile
        ansible.builtin.shell:
          cmd: authselect current -r | awk '{ print $1 }'
        register: result_authselect_profile
        changed_when: false
        when:
        - result_authselect_check_cmd is success
      - name: Set PAM's Password Hashing Algorithm - password-auth - Define the current authselect profile as a local fact
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: '{{ result_authselect_profile.stdout }}'
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is match("custom/")
      - name: Set PAM's Password Hashing Algorithm - password-auth - Define the new authselect custom profile as a local fact
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: custom/hardening
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is not match("custom/")
      - name: Set PAM's Password Hashing Algorithm - password-auth - Get authselect current features to also enable them in
          the custom profile
        ansible.builtin.shell:
          cmd: authselect current | tail -n+3 | awk '{ print $2 }'
        register: result_authselect_features
        changed_when: false
        check_mode: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: Set PAM's Password Hashing Algorithm - password-auth - Check if any custom profile with the same name was already
          created
        ansible.builtin.stat:
          path: /etc/authselect/{{ authselect_custom_profile }}
        register: result_authselect_custom_profile_present
        changed_when: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: Set PAM's Password Hashing Algorithm - password-auth - Create an authselect custom profile based on the current
          profile
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b {{ authselect_current_profile }}
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is not match("^(custom/|local)")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: Set PAM's Password Hashing Algorithm - password-auth - Create an authselect custom profile based on sssd profile
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b sssd
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is match("local")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: Set PAM's Password Hashing Algorithm - password-auth - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=before-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: Set PAM's Password Hashing Algorithm - password-auth - Ensure the authselect custom profile is selected
        ansible.builtin.command:
          cmd: authselect select {{ authselect_custom_profile }}
        register: result_pam_authselect_select_profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: Set PAM's Password Hashing Algorithm - password-auth - Restore the authselect features in the custom profile
        ansible.builtin.command:
          cmd: authselect enable-feature {{ item }}
        loop: '{{ result_authselect_features.stdout_lines }}'
        register: result_pam_authselect_restore_features
        when:
        - result_authselect_profile is not skipped
        - result_authselect_features is not skipped
        - result_pam_authselect_select_profile is not skipped
      - name: Set PAM's Password Hashing Algorithm - password-auth - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=after-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - result_pam_authselect_restore_features is not skipped
      - name: Set PAM's Password Hashing Algorithm - password-auth - Change the PAM file to be edited according to the custom
          authselect profile
        ansible.builtin.set_fact:
          pam_file_path: /etc/authselect/{{ authselect_custom_profile }}/{{ pam_file_path | basename }}
        when:
        - authselect_custom_profile is defined
      when:
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    - name: Set PAM's Password Hashing Algorithm - password-auth - Define a fact for control already filtered in case filters
        are used
      ansible.builtin.set_fact:
        pam_module_control: sufficient
    - name: Set PAM's Password Hashing Algorithm - password-auth - Check if expected PAM module line is present in {{ pam_file_path
        }}
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        regexp: ^\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_unix.so\s*.*
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_line_present
    - name: Set PAM's Password Hashing Algorithm - password-auth - Include or update the PAM module line in {{ pam_file_path
        }}
      block:
      - name: Set PAM's Password Hashing Algorithm - password-auth - Check if required PAM module line is present in {{ pam_file_path
          }} with different control
        ansible.builtin.lineinfile:
          path: '{{ pam_file_path }}'
          regexp: ^\s*password\s+.*\s+pam_unix.so\s*
          state: absent
        check_mode: true
        changed_when: false
        register: result_pam_line_other_control_present
      - name: Set PAM's Password Hashing Algorithm - password-auth - Ensure the correct control for the required PAM module
          line in {{ pam_file_path }}
        ansible.builtin.replace:
          dest: '{{ pam_file_path }}'
          regexp: ^(\s*password\s+).*(\bpam_unix.so.*)
          replace: \1{{ pam_module_control }} \2
        register: result_pam_module_edit
        when:
        - result_pam_line_other_control_present.found == 1
      - name: Set PAM's Password Hashing Algorithm - password-auth - Ensure the required PAM module line is included in {{
          pam_file_path }}
        ansible.builtin.lineinfile:
          dest: '{{ pam_file_path }}'
          line: password    {{ pam_module_control }}    pam_unix.so
        register: result_pam_module_add
        when:
        - result_pam_line_other_control_present.found == 0 or result_pam_line_other_control_present.found > 1
      - name: Set PAM's Password Hashing Algorithm - password-auth - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b
        when:
        - result_authselect_present is defined
        - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
        - "(result_pam_module_add is defined and result_pam_module_add.changed)\n or (result_pam_module_edit is defined and\
          \ result_pam_module_edit.changed)"
      when:
      - result_pam_line_present.found is defined
      - result_pam_line_present.found == 0
    - name: Set PAM's Password Hashing Algorithm - password-auth - Define a fact for control already filtered in case filters
        are used
      ansible.builtin.set_fact:
        pam_module_control: sufficient
    - name: Set PAM's Password Hashing Algorithm - password-auth - Check if the required PAM module option is present in {{
        pam_file_path }}
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        regexp: ^\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_unix.so\s*.*\s{{ var_password_hashing_algorithm_pam
          }}\b
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_module_set_password_hashing_algorithm_passwordauth_option_present
    - name: Set PAM's Password Hashing Algorithm - password-auth - Ensure the "{{ var_password_hashing_algorithm_pam }}" PAM
        option for "pam_unix.so" is included in {{ pam_file_path }}
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        backrefs: true
        regexp: ^(\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_unix.so.*)
        line: \1 {{ var_password_hashing_algorithm_pam }}
        state: present
      register: result_pam_set_password_hashing_algorithm_passwordauth_add
      when:
      - result_pam_module_set_password_hashing_algorithm_passwordauth_option_present.found is defined
      - result_pam_module_set_password_hashing_algorithm_passwordauth_option_present.found == 0
    - name: Set PAM's Password Hashing Algorithm - password-auth - Ensure authselect changes are applied
      ansible.builtin.command:
        cmd: authselect apply-changes -b
      when:
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
      - "(result_pam_set_password_hashing_algorithm_passwordauth_add is defined and result_pam_set_password_hashing_algorithm_passwordauth_add.changed)\n\
        \ or (result_pam_set_password_hashing_algorithm_passwordauth_edit is defined and result_pam_set_password_hashing_algorithm_passwordauth_edit.changed)"
    when:
    - '"pam" in ansible_facts.packages'
    - (result_pam_file_present is not skipped and result_pam_file_present.stat.exists)
    tags:
    - CJIS-5.6.2.2
    - NIST-800-171-3.13.11
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(c)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.1
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - set_password_hashing_algorithm_passwordauth
  rescue:
  - name: 'StigForge: continue after error in "Set PAM''s Password Hashing Algorithm - password-auth - Check the proper remediation
      for the system" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Set PAM''s Password Hashing Algorithm - password-auth - Check the proper remediation for the system"
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_set_password_hashing_algorithm_passwordauth | default(true)
    | bool)
- block:
  - name: Set PAM's Password Hashing Algorithm - password-auth - Check if /etc/pam.d/password-auth File is Present
    ansible.builtin.stat:
      path: /etc/pam.d/password-auth
    register: result_pam_file_present
    when: '"pam" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.2
    - NIST-800-171-3.13.11
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(c)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.1
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - set_password_hashing_algorithm_passwordauth
  rescue:
  - name: 'StigForge: continue after error in "Set PAM''s Password Hashing Algorithm - password-auth - Check if /etc/pam.d/password-auth
      File is Present" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Set PAM''s Password Hashing Algorithm - password-auth - Check if /etc/pam.d/password-auth File is
        Present" raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_set_password_hashing_algorithm_passwordauth | default(true)
    | bool)
- block:
  - name: Set PAM's Password Hashing Algorithm - password-auth - Check The Proper Remediation For The System
    block:
    - name: Set PAM's Password Hashing Algorithm - password-auth - Define the PAM file to be edited as a local fact
      ansible.builtin.set_fact:
        pam_file_path: /etc/pam.d/password-auth
    - name: Set PAM's Password Hashing Algorithm - password-auth - Check if system relies on authselect tool
      ansible.builtin.stat:
        path: /usr/bin/authselect
      register: result_authselect_present
    - name: Set PAM's Password Hashing Algorithm - password-auth - Ensure authselect custom profile is used if authselect
        is present
      block:
      - name: Set PAM's Password Hashing Algorithm - password-auth - Check integrity of authselect current profile
        ansible.builtin.command:
          cmd: authselect check
        register: result_authselect_check_cmd
        changed_when: false
        check_mode: false
        failed_when: false
      - name: Set PAM's Password Hashing Algorithm - password-auth - Informative message based on the authselect integrity
          check result
        ansible.builtin.assert:
          that:
          - ansible_check_mode or result_authselect_check_cmd.rc == 0
          fail_msg:
          - authselect integrity check failed. Remediation aborted!
          - This remediation could not be applied because an authselect profile was not selected or the selected profile is
            not intact.
          - It is not recommended to manually edit the PAM files when authselect tool is available.
          - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile is
            recommended.
          success_msg:
          - authselect integrity check passed
      - name: Set PAM's Password Hashing Algorithm - password-auth - Get authselect current profile
        ansible.builtin.shell:
          cmd: authselect current -r | awk '{ print $1 }'
        register: result_authselect_profile
        changed_when: false
        when:
        - result_authselect_check_cmd is success
      - name: Set PAM's Password Hashing Algorithm - password-auth - Define the current authselect profile as a local fact
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: '{{ result_authselect_profile.stdout }}'
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is match("custom/")
      - name: Set PAM's Password Hashing Algorithm - password-auth - Define the new authselect custom profile as a local fact
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: custom/hardening
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is not match("custom/")
      - name: Set PAM's Password Hashing Algorithm - password-auth - Get authselect current features to also enable them in
          the custom profile
        ansible.builtin.shell:
          cmd: authselect current | tail -n+3 | awk '{ print $2 }'
        register: result_authselect_features
        changed_when: false
        check_mode: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: Set PAM's Password Hashing Algorithm - password-auth - Check if any custom profile with the same name was already
          created
        ansible.builtin.stat:
          path: /etc/authselect/{{ authselect_custom_profile }}
        register: result_authselect_custom_profile_present
        changed_when: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: Set PAM's Password Hashing Algorithm - password-auth - Create an authselect custom profile based on the current
          profile
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b {{ authselect_current_profile }}
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is not match("^(custom/|local)")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: Set PAM's Password Hashing Algorithm - password-auth - Create an authselect custom profile based on sssd profile
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b sssd
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is match("local")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: Set PAM's Password Hashing Algorithm - password-auth - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=before-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: Set PAM's Password Hashing Algorithm - password-auth - Ensure the authselect custom profile is selected
        ansible.builtin.command:
          cmd: authselect select {{ authselect_custom_profile }}
        register: result_pam_authselect_select_profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: Set PAM's Password Hashing Algorithm - password-auth - Restore the authselect features in the custom profile
        ansible.builtin.command:
          cmd: authselect enable-feature {{ item }}
        loop: '{{ result_authselect_features.stdout_lines }}'
        register: result_pam_authselect_restore_features
        when:
        - result_authselect_profile is not skipped
        - result_authselect_features is not skipped
        - result_pam_authselect_select_profile is not skipped
      - name: Set PAM's Password Hashing Algorithm - password-auth - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=after-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - result_pam_authselect_restore_features is not skipped
      - name: Set PAM's Password Hashing Algorithm - password-auth - Change the PAM file to be edited according to the custom
          authselect profile
        ansible.builtin.set_fact:
          pam_file_path: /etc/authselect/{{ authselect_custom_profile }}/{{ pam_file_path | basename }}
        when:
        - authselect_custom_profile is defined
      when:
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    - name: Set PAM's Password Hashing Algorithm - password-auth - Check if "{{ pam_file_path }}" File is Present
      ansible.builtin.stat:
        path: '{{ pam_file_path }}'
      register: pam_file_path_present
    - name: Set PAM's Password Hashing Algorithm - password-auth - Ensure That Only the Correct Hashing Algorithm Option For
        pam_unix.so Is Used in {{ pam_file_path }}
      ansible.builtin.replace:
        dest: '{{ pam_file_path }}'
        regexp: (^\s*password.*pam_unix\.so.*)\b{{ item }}\b\s*(.*)
        replace: \1\2
      when:
      - item != var_password_hashing_algorithm_pam
      - (pam_file_path_present is not skipped and pam_file_path_present.stat.exists)
      loop:
      - sha512
      - yescrypt
      - gost_yescrypt
      - blowfish
      - sha256
      - md5
      - bigcrypt
      register: result_pam_hashing_options_removal
    - name: Set PAM's Password Hashing Algorithm - password-auth - Ensure authselect changes are applied
      ansible.builtin.command:
        cmd: authselect apply-changes -b
      when:
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
      - result_pam_hashing_options_removal is changed
    when:
    - '"pam" in ansible_facts.packages'
    - (result_pam_file_present is not skipped and result_pam_file_present.stat.exists)
    tags:
    - CJIS-5.6.2.2
    - NIST-800-171-3.13.11
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(c)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.1
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - set_password_hashing_algorithm_passwordauth
  rescue:
  - name: 'StigForge: continue after error in "Set PAM''s Password Hashing Algorithm - password-auth - Check The Proper Remediation
      For The System" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Set PAM''s Password Hashing Algorithm - password-auth - Check The Proper Remediation For The System"
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_set_password_hashing_algorithm_passwordauth | default(true)
    | bool)
- block:
  - name: Set PAM's Password Hashing Algorithm - Check if /etc/pam.d/system-auth file is present
    ansible.builtin.stat:
      path: /etc/pam.d/system-auth
    register: result_pam_file_present
    when: '"pam" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.2
    - NIST-800-171-3.13.11
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(c)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.1
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - set_password_hashing_algorithm_systemauth
  rescue:
  - name: 'StigForge: continue after error in "Set PAM''s Password Hashing Algorithm - Check if /etc/pam.d/system-auth file
      is present" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Set PAM''s Password Hashing Algorithm - Check if /etc/pam.d/system-auth file is present" raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_set_password_hashing_algorithm_systemauth | default(true)
    | bool)
- block:
  - name: Set PAM's Password Hashing Algorithm - Check the proper remediation for the system
    block:
    - name: Set PAM's Password Hashing Algorithm - Define the PAM file to be edited as a local fact
      ansible.builtin.set_fact:
        pam_file_path: /etc/pam.d/system-auth
    - name: Set PAM's Password Hashing Algorithm - Check if system relies on authselect tool
      ansible.builtin.stat:
        path: /usr/bin/authselect
      register: result_authselect_present
    - name: Set PAM's Password Hashing Algorithm - Ensure authselect custom profile is used if authselect is present
      block:
      - name: Set PAM's Password Hashing Algorithm - Check integrity of authselect current profile
        ansible.builtin.command:
          cmd: authselect check
        register: result_authselect_check_cmd
        changed_when: false
        check_mode: false
        failed_when: false
      - name: Set PAM's Password Hashing Algorithm - Informative message based on the authselect integrity check result
        ansible.builtin.assert:
          that:
          - ansible_check_mode or result_authselect_check_cmd.rc == 0
          fail_msg:
          - authselect integrity check failed. Remediation aborted!
          - This remediation could not be applied because an authselect profile was not selected or the selected profile is
            not intact.
          - It is not recommended to manually edit the PAM files when authselect tool is available.
          - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile is
            recommended.
          success_msg:
          - authselect integrity check passed
      - name: Set PAM's Password Hashing Algorithm - Get authselect current profile
        ansible.builtin.shell:
          cmd: authselect current -r | awk '{ print $1 }'
        register: result_authselect_profile
        changed_when: false
        when:
        - result_authselect_check_cmd is success
      - name: Set PAM's Password Hashing Algorithm - Define the current authselect profile as a local fact
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: '{{ result_authselect_profile.stdout }}'
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is match("custom/")
      - name: Set PAM's Password Hashing Algorithm - Define the new authselect custom profile as a local fact
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: custom/hardening
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is not match("custom/")
      - name: Set PAM's Password Hashing Algorithm - Get authselect current features to also enable them in the custom profile
        ansible.builtin.shell:
          cmd: authselect current | tail -n+3 | awk '{ print $2 }'
        register: result_authselect_features
        changed_when: false
        check_mode: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: Set PAM's Password Hashing Algorithm - Check if any custom profile with the same name was already created
        ansible.builtin.stat:
          path: /etc/authselect/{{ authselect_custom_profile }}
        register: result_authselect_custom_profile_present
        changed_when: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: Set PAM's Password Hashing Algorithm - Create an authselect custom profile based on the current profile
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b {{ authselect_current_profile }}
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is not match("^(custom/|local)")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: Set PAM's Password Hashing Algorithm - Create an authselect custom profile based on sssd profile
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b sssd
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is match("local")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: Set PAM's Password Hashing Algorithm - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=before-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: Set PAM's Password Hashing Algorithm - Ensure the authselect custom profile is selected
        ansible.builtin.command:
          cmd: authselect select {{ authselect_custom_profile }}
        register: result_pam_authselect_select_profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: Set PAM's Password Hashing Algorithm - Restore the authselect features in the custom profile
        ansible.builtin.command:
          cmd: authselect enable-feature {{ item }}
        loop: '{{ result_authselect_features.stdout_lines }}'
        register: result_pam_authselect_restore_features
        when:
        - result_authselect_profile is not skipped
        - result_authselect_features is not skipped
        - result_pam_authselect_select_profile is not skipped
      - name: Set PAM's Password Hashing Algorithm - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=after-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - result_pam_authselect_restore_features is not skipped
      - name: Set PAM's Password Hashing Algorithm - Change the PAM file to be edited according to the custom authselect profile
        ansible.builtin.set_fact:
          pam_file_path: /etc/authselect/{{ authselect_custom_profile }}/{{ pam_file_path | basename }}
        when:
        - authselect_custom_profile is defined
      when:
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    - name: Set PAM's Password Hashing Algorithm - Define a fact for control already filtered in case filters are used
      ansible.builtin.set_fact:
        pam_module_control: sufficient
    - name: Set PAM's Password Hashing Algorithm - Check if expected PAM module line is present in {{ pam_file_path }}
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        regexp: ^\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_unix.so\s*.*
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_line_present
    - name: Set PAM's Password Hashing Algorithm - Include or update the PAM module line in {{ pam_file_path }}
      block:
      - name: Set PAM's Password Hashing Algorithm - Check if required PAM module line is present in {{ pam_file_path }} with
          different control
        ansible.builtin.lineinfile:
          path: '{{ pam_file_path }}'
          regexp: ^\s*password\s+.*\s+pam_unix.so\s*
          state: absent
        check_mode: true
        changed_when: false
        register: result_pam_line_other_control_present
      - name: Set PAM's Password Hashing Algorithm - Ensure the correct control for the required PAM module line in {{ pam_file_path
          }}
        ansible.builtin.replace:
          dest: '{{ pam_file_path }}'
          regexp: ^(\s*password\s+).*(\bpam_unix.so.*)
          replace: \1{{ pam_module_control }} \2
        register: result_pam_module_edit
        when:
        - result_pam_line_other_control_present.found == 1
      - name: Set PAM's Password Hashing Algorithm - Ensure the required PAM module line is included in {{ pam_file_path }}
        ansible.builtin.lineinfile:
          dest: '{{ pam_file_path }}'
          line: password    {{ pam_module_control }}    pam_unix.so
        register: result_pam_module_add
        when:
        - result_pam_line_other_control_present.found == 0 or result_pam_line_other_control_present.found > 1
      - name: Set PAM's Password Hashing Algorithm - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b
        when:
        - result_authselect_present is defined
        - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
        - "(result_pam_module_add is defined and result_pam_module_add.changed)\n or (result_pam_module_edit is defined and\
          \ result_pam_module_edit.changed)"
      when:
      - result_pam_line_present.found is defined
      - result_pam_line_present.found == 0
    - name: Set PAM's Password Hashing Algorithm - Define a fact for control already filtered in case filters are used
      ansible.builtin.set_fact:
        pam_module_control: sufficient
    - name: Set PAM's Password Hashing Algorithm - Check if the required PAM module option is present in {{ pam_file_path
        }}
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        regexp: ^\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_unix.so\s*.*\s{{ var_password_hashing_algorithm_pam
          }}\b
        state: absent
      check_mode: true
      changed_when: false
      register: result_pam_module_set_password_hashing_algorithm_systemauth_option_present
    - name: Set PAM's Password Hashing Algorithm - Ensure the "{{ var_password_hashing_algorithm_pam }}" PAM option for "pam_unix.so"
        is included in {{ pam_file_path }}
      ansible.builtin.lineinfile:
        path: '{{ pam_file_path }}'
        backrefs: true
        regexp: ^(\s*password\s+{{ pam_module_control | regex_escape() }}\s+pam_unix.so.*)
        line: \1 {{ var_password_hashing_algorithm_pam }}
        state: present
      register: result_pam_set_password_hashing_algorithm_systemauth_add
      when:
      - result_pam_module_set_password_hashing_algorithm_systemauth_option_present.found is defined
      - result_pam_module_set_password_hashing_algorithm_systemauth_option_present.found == 0
    - name: Set PAM's Password Hashing Algorithm - Ensure authselect changes are applied
      ansible.builtin.command:
        cmd: authselect apply-changes -b
      when:
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
      - "(result_pam_set_password_hashing_algorithm_systemauth_add is defined and result_pam_set_password_hashing_algorithm_systemauth_add.changed)\n\
        \ or (result_pam_set_password_hashing_algorithm_systemauth_edit is defined and result_pam_set_password_hashing_algorithm_systemauth_edit.changed)"
    when:
    - '"pam" in ansible_facts.packages'
    - (result_pam_file_present is not skipped and result_pam_file_present.stat.exists)
    tags:
    - CJIS-5.6.2.2
    - NIST-800-171-3.13.11
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(c)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.1
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - set_password_hashing_algorithm_systemauth
  rescue:
  - name: 'StigForge: continue after error in "Set PAM''s Password Hashing Algorithm - Check the proper remediation for the
      system" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Set PAM''s Password Hashing Algorithm - Check the proper remediation for the system" raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_set_password_hashing_algorithm_systemauth | default(true)
    | bool)
- block:
  - name: Set PAM's Password Hashing Algorithm - Check if /etc/pam.d/system-auth File is Present
    ansible.builtin.stat:
      path: /etc/pam.d/system-auth
    register: result_pam_file_present
    when: '"pam" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.2
    - NIST-800-171-3.13.11
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(c)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.1
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - set_password_hashing_algorithm_systemauth
  rescue:
  - name: 'StigForge: continue after error in "Set PAM''s Password Hashing Algorithm - Check if /etc/pam.d/system-auth File
      is Present" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Set PAM''s Password Hashing Algorithm - Check if /etc/pam.d/system-auth File is Present" raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_set_password_hashing_algorithm_systemauth | default(true)
    | bool)
- block:
  - name: Set PAM's Password Hashing Algorithm - Check The Proper Remediation For The System
    block:
    - name: Set PAM's Password Hashing Algorithm - Define the PAM file to be edited as a local fact
      ansible.builtin.set_fact:
        pam_file_path: /etc/pam.d/system-auth
    - name: Set PAM's Password Hashing Algorithm - Check if system relies on authselect tool
      ansible.builtin.stat:
        path: /usr/bin/authselect
      register: result_authselect_present
    - name: Set PAM's Password Hashing Algorithm - Ensure authselect custom profile is used if authselect is present
      block:
      - name: Set PAM's Password Hashing Algorithm - Check integrity of authselect current profile
        ansible.builtin.command:
          cmd: authselect check
        register: result_authselect_check_cmd
        changed_when: false
        check_mode: false
        failed_when: false
      - name: Set PAM's Password Hashing Algorithm - Informative message based on the authselect integrity check result
        ansible.builtin.assert:
          that:
          - ansible_check_mode or result_authselect_check_cmd.rc == 0
          fail_msg:
          - authselect integrity check failed. Remediation aborted!
          - This remediation could not be applied because an authselect profile was not selected or the selected profile is
            not intact.
          - It is not recommended to manually edit the PAM files when authselect tool is available.
          - In cases where the default authselect profile does not cover a specific demand, a custom authselect profile is
            recommended.
          success_msg:
          - authselect integrity check passed
      - name: Set PAM's Password Hashing Algorithm - Get authselect current profile
        ansible.builtin.shell:
          cmd: authselect current -r | awk '{ print $1 }'
        register: result_authselect_profile
        changed_when: false
        when:
        - result_authselect_check_cmd is success
      - name: Set PAM's Password Hashing Algorithm - Define the current authselect profile as a local fact
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: '{{ result_authselect_profile.stdout }}'
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is match("custom/")
      - name: Set PAM's Password Hashing Algorithm - Define the new authselect custom profile as a local fact
        ansible.builtin.set_fact:
          authselect_current_profile: '{{ result_authselect_profile.stdout }}'
          authselect_custom_profile: custom/hardening
        when:
        - result_authselect_profile is not skipped
        - result_authselect_profile.stdout is not match("custom/")
      - name: Set PAM's Password Hashing Algorithm - Get authselect current features to also enable them in the custom profile
        ansible.builtin.shell:
          cmd: authselect current | tail -n+3 | awk '{ print $2 }'
        register: result_authselect_features
        changed_when: false
        check_mode: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: Set PAM's Password Hashing Algorithm - Check if any custom profile with the same name was already created
        ansible.builtin.stat:
          path: /etc/authselect/{{ authselect_custom_profile }}
        register: result_authselect_custom_profile_present
        changed_when: false
        when:
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
      - name: Set PAM's Password Hashing Algorithm - Create an authselect custom profile based on the current profile
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b {{ authselect_current_profile }}
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is not match("^(custom/|local)")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: Set PAM's Password Hashing Algorithm - Create an authselect custom profile based on sssd profile
        ansible.builtin.command:
          cmd: authselect create-profile hardening -b sssd
        when:
        - result_authselect_profile is not skipped
        - result_authselect_check_cmd is success
        - authselect_current_profile is match("local")
        - not (result_authselect_custom_profile_present is not skipped and result_authselect_custom_profile_present.stat.exists)
      - name: Set PAM's Password Hashing Algorithm - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=before-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: Set PAM's Password Hashing Algorithm - Ensure the authselect custom profile is selected
        ansible.builtin.command:
          cmd: authselect select {{ authselect_custom_profile }}
        register: result_pam_authselect_select_profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - authselect_current_profile is not match("custom/")
        - authselect_custom_profile is not match(authselect_current_profile)
      - name: Set PAM's Password Hashing Algorithm - Restore the authselect features in the custom profile
        ansible.builtin.command:
          cmd: authselect enable-feature {{ item }}
        loop: '{{ result_authselect_features.stdout_lines }}'
        register: result_pam_authselect_restore_features
        when:
        - result_authselect_profile is not skipped
        - result_authselect_features is not skipped
        - result_pam_authselect_select_profile is not skipped
      - name: Set PAM's Password Hashing Algorithm - Ensure authselect changes are applied
        ansible.builtin.command:
          cmd: authselect apply-changes -b --backup=after-hardening-custom-profile
        when:
        - result_authselect_check_cmd is success
        - result_authselect_profile is not skipped
        - result_pam_authselect_restore_features is not skipped
      - name: Set PAM's Password Hashing Algorithm - Change the PAM file to be edited according to the custom authselect profile
        ansible.builtin.set_fact:
          pam_file_path: /etc/authselect/{{ authselect_custom_profile }}/{{ pam_file_path | basename }}
        when:
        - authselect_custom_profile is defined
      when:
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
    - name: Set PAM's Password Hashing Algorithm - Check if "{{ pam_file_path }}" File is Present
      ansible.builtin.stat:
        path: '{{ pam_file_path }}'
      register: pam_file_path_present
    - name: Set PAM's Password Hashing Algorithm - Ensure That Only the Correct Hashing Algorithm Option For pam_unix.so Is
        Used in {{ pam_file_path }}
      ansible.builtin.replace:
        dest: '{{ pam_file_path }}'
        regexp: (^\s*password.*pam_unix\.so.*)\b{{ item }}\b\s*(.*)
        replace: \1\2
      when:
      - item != var_password_hashing_algorithm_pam
      - (pam_file_path_present is not skipped and pam_file_path_present.stat.exists)
      loop:
      - sha512
      - yescrypt
      - gost_yescrypt
      - blowfish
      - sha256
      - md5
      - bigcrypt
      register: result_pam_hashing_options_removal
    - name: Set PAM's Password Hashing Algorithm - Ensure authselect changes are applied
      ansible.builtin.command:
        cmd: authselect apply-changes -b
      when:
      - (result_authselect_present is not skipped and result_authselect_present.stat.exists)
      - result_pam_hashing_options_removal is changed
    when:
    - '"pam" in ansible_facts.packages'
    - (result_pam_file_present is not skipped and result_pam_file_present.stat.exists)
    tags:
    - CJIS-5.6.2.2
    - NIST-800-171-3.13.11
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(c)
    - NIST-800-53-IA-5(c)
    - PCI-DSS-Req-8.2.1
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - set_password_hashing_algorithm_systemauth
  rescue:
  - name: 'StigForge: continue after error in "Set PAM''s Password Hashing Algorithm - Check The Proper Remediation For The
      System" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Set PAM''s Password Hashing Algorithm - Check The Proper Remediation For The System" raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_set_password_hashing_algorithm_systemauth | default(true)
    | bool)
- block:
  - name: Set Account Expiration Following Inactivity
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/default/useradd
      regexp: ^INACTIVE
      line: INACTIVE={{ var_account_disable_post_pw_expiration }}
    when: '"shadow-utils" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.6
    - NIST-800-53-AC-2(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-4(e)
    - PCI-DSS-Req-8.1.4
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.6
    - account_disable_post_pw_expiration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set Account Expiration Following Inactivity'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Account Expiration Following Inactivity'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_account_disable_post_pw_expiration | default(true)
    | bool)
- block:
  - name: Set Password Maximum Age
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/login.defs
      regexp: ^#?PASS_MAX_DAYS
      line: PASS_MAX_DAYS {{ var_accounts_maximum_age_login_defs }}
    when: '"shadow-utils" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.1
    - NIST-800-171-3.5.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(d)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.4
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.9
    - accounts_maximum_age_login_defs
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set Password Maximum Age'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Password Maximum Age'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_maximum_age_login_defs | default(true) |
    bool)
- block:
  - name: Set Password Minimum Age
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/login.defs
      regexp: ^#?PASS_MIN_DAYS
      line: PASS_MIN_DAYS {{ var_accounts_minimum_age_login_defs }}
    when: '"shadow-utils" in ansible_facts.packages'
    tags:
    - CJIS-5.6.2.1.1
    - NIST-800-171-3.5.8
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(d)
    - NIST-800-53-IA-5(f)
    - accounts_minimum_age_login_defs
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set Password Minimum Age'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Password Minimum Age'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_minimum_age_login_defs | default(true) |
    bool)
- block:
  - name: Collect users with not correct maximum time period between password changes
    ansible.builtin.command:
      cmd: awk -F':' '(/^[^:]+:[^!*]/ && ($5 > {{ var_accounts_maximum_age_login_defs }} || $5 == "")) {print $1}' /etc/shadow
    register: user_names
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(d)
    - NIST-800-53-IA-5(f)
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.9
    - accounts_password_set_max_life_existing
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Collect users with not correct maximum time period between password changes''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Collect users with not correct maximum time period between password changes'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_set_max_life_existing | default(true)
    | bool)
- block:
  - name: Change the maximum time period between password changes
    ansible.builtin.user:
      user: '{{ item }}'
      password_expire_max: '{{ var_accounts_maximum_age_login_defs }}'
    with_items: '{{ user_names.stdout_lines }}'
    when: user_names.stdout_lines | length > 0
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(d)
    - NIST-800-53-IA-5(f)
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.9
    - accounts_password_set_max_life_existing
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Change the maximum time period between password changes'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Change the maximum time period between password changes'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_set_max_life_existing | default(true)
    | bool)
- block:
  - name: Collect users with not correct minimum time period between password changes
    ansible.builtin.command: 'awk -F'':'' ''(/^[^:]+:[^!*]/ && ($4 < {{ var_accounts_minimum_age_login_defs }} || $4 == ""))
      {print $1}'' /etc/shadow

      '
    register: user_names
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(d)
    - NIST-800-53-IA-5(f)
    - accounts_password_set_min_life_existing
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Collect users with not correct minimum time period between password changes''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Collect users with not correct minimum time period between password changes'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_set_min_life_existing | default(true)
    | bool)
- block:
  - name: Change the minimum time period between password changes
    ansible.builtin.command: 'chage -m {{ var_accounts_minimum_age_login_defs }} {{ item }}

      '
    with_items: '{{ user_names.stdout_lines }}'
    when: user_names.stdout_lines | length > 0
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(d)
    - NIST-800-53-IA-5(f)
    - accounts_password_set_min_life_existing
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Change the minimum time period between password changes'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Change the minimum time period between password changes'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_set_min_life_existing | default(true)
    | bool)
- block:
  - name: Set Existing Passwords Warning Age - Collect Users With Incorrect Number of Days of Warning Before Password Expires
    ansible.builtin.command:
      cmd: awk -F':' '(($6 < {{ var_accounts_password_warn_age_login_defs }} || $6 == "") && $2 ~ /^\$/) {print $1}' /etc/shadow
    register: result_pass_warn_age_user_names
    changed_when: false
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(d)
    - NIST-800-53-IA-5(f)
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.9
    - accounts_password_set_warn_age_existing
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set Existing Passwords Warning Age - Collect Users With Incorrect Number of
      Days of Warning Before Password Expires'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Existing Passwords Warning Age - Collect Users With Incorrect Number of Days of Warning Before
        Password Expires'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_set_warn_age_existing | default(true)
    | bool)
- block:
  - name: Set Existing Passwords Warning Age - Ensure the Number of Days of Warning Before Password Expires
    ansible.builtin.command:
      cmd: chage --warndays {{ var_accounts_password_warn_age_login_defs }} {{ item }}
    with_items: '{{ result_pass_warn_age_user_names.stdout_lines }}'
    when: result_pass_warn_age_user_names is not skipped and result_pass_warn_age_user_names.stdout_lines | length > 0
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(d)
    - NIST-800-53-IA-5(f)
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.9
    - accounts_password_set_warn_age_existing
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set Existing Passwords Warning Age - Ensure the Number of Days of Warning
      Before Password Expires'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Existing Passwords Warning Age - Ensure the Number of Days of Warning Before Password Expires''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_set_warn_age_existing | default(true)
    | bool)
- block:
  - name: Set Password Warning Age
    ansible.builtin.lineinfile:
      dest: /etc/login.defs
      regexp: ^PASS_WARN_AGE *[0-9]*
      state: present
      line: PASS_WARN_AGE        {{ var_accounts_password_warn_age_login_defs }}
      create: true
    when: '"shadow-utils" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.5.8
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)(d)
    - NIST-800-53-IA-5(f)
    - PCI-DSS-Req-8.2.4
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.9
    - accounts_password_warn_age_login_defs
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set Password Warning Age'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Password Warning Age'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_password_warn_age_login_defs | default(true)
    | bool)
- block:
  - name: Collect users with not correct INACTIVE parameter set
    ansible.builtin.command:
      cmd: awk -F':' '(($7 > {{ var_account_disable_post_pw_expiration }} || $7 == "") && $2 ~ /^\$/) {print $1}' /etc/shadow
    register: user_names
    changed_when: false
    tags:
    - NIST-800-171-3.5.6
    - NIST-800-53-AC-2(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-4(e)
    - PCI-DSS-Req-8.1.4
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.6
    - accounts_set_post_pw_existing
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Collect users with not correct INACTIVE parameter set'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Collect users with not correct INACTIVE parameter set'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_set_post_pw_existing | default(true) | bool)
- block:
  - name: Change the period of inactivity
    ansible.builtin.command:
      cmd: chage --inactive {{ var_account_disable_post_pw_expiration }} {{ item }}
    with_items: '{{ user_names.stdout_lines }}'
    when: user_names is not skipped and user_names.stdout_lines | length > 0
    tags:
    - NIST-800-171-3.5.6
    - NIST-800-53-AC-2(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-4(e)
    - PCI-DSS-Req-8.1.4
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.6
    - accounts_set_post_pw_existing
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Change the period of inactivity'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Change the period of inactivity'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_set_post_pw_existing | default(true) | bool)
- block:
  - name: Collect users with no password
    ansible.builtin.command: 'awk -F: ''!$2 {print $1}'' /etc/shadow

      '
    register: users_nopasswd
    changed_when: false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.2
    - high_severity
    - low_complexity
    - low_disruption
    - no_empty_passwords_etc_shadow
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Collect users with no password'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Collect users with no password'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_no_empty_passwords_etc_shadow | default(true) | bool)
- block:
  - name: Lock users with no password
    ansible.builtin.command: 'passwd -l {{ item }}

      '
    with_items: '{{ users_nopasswd.stdout_lines }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - users_nopasswd is not skipped and users_nopasswd.stdout_lines | length > 0
    tags:
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.2
    - high_severity
    - low_complexity
    - low_disruption
    - no_empty_passwords_etc_shadow
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Lock users with no password'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Lock users with no password'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_no_empty_passwords_etc_shadow | default(true) | bool)
- block:
  - name: Get all /etc/passwd file entries
    ansible.builtin.getent:
      database: passwd
      split: ':'
    tags:
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-6(5)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-4(b)
    - PCI-DSS-Req-8.5
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.1
    - accounts_no_uid_except_zero
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get all /etc/passwd file entries'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get all /etc/passwd file entries'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_no_uid_except_zero | default(true) | bool)
- block:
  - name: Lock the password of the user accounts other than root with uid 0
    ansible.builtin.command: passwd -l {{ item.key }}
    loop: '{{ getent_passwd | dict2items | rejectattr(''key'', ''search'', ''root'') | list }}'
    when: item.value.1  == '0'
    tags:
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-6(5)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-4(b)
    - PCI-DSS-Req-8.5
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.1
    - accounts_no_uid_except_zero
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Lock the password of the user accounts other than root with uid 0'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Lock the password of the user accounts other than root with uid 0'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_no_uid_except_zero | default(true) | bool)
- block:
  - name: Ensure that System Accounts Are Locked - Get All Local Users From /etc/passwd
    ansible.builtin.getent:
      database: passwd
      split: ':'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.2
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_password_auth_for_systemaccounts
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure that System Accounts Are Locked - Get All Local Users From /etc/passwd''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure that System Accounts Are Locked - Get All Local Users From /etc/passwd'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_no_password_auth_for_systemaccounts | default(true)
    | bool)
- block:
  - name: Ensure that System Accounts Are Locked - Create local_users Variable From getent_passwd Facts
    ansible.builtin.set_fact:
      local_users: '{{ ansible_facts.getent_passwd | dict2items }}'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.2
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_password_auth_for_systemaccounts
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure that System Accounts Are Locked - Create local_users Variable From
      getent_passwd Facts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure that System Accounts Are Locked - Create local_users Variable From getent_passwd Facts'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_no_password_auth_for_systemaccounts | default(true)
    | bool)
- block:
  - name: Ensure that System Accounts Are Locked - Lock System Accounts
    ansible.builtin.user:
      name: '{{ item.key }}'
      password_lock: true
    loop: '{{ local_users }}'
    when:
    - item.value[1]|int < 1000
    - item.key not in ['root', 'halt', 'sync', 'shutdown', 'nfsnobody']
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.2
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_password_auth_for_systemaccounts
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure that System Accounts Are Locked - Lock System Accounts'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure that System Accounts Are Locked - Lock System Accounts'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_no_password_auth_for_systemaccounts | default(true)
    | bool)
- block:
  - name: Ensure that System Accounts Do Not Run a Shell Upon Login - Get All Local Users From /etc/passwd
    ansible.builtin.getent:
      database: passwd
      split: ':'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.2
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - no_shelllogin_for_systemaccounts
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure that System Accounts Do Not Run a Shell Upon Login - Get All Local
      Users From /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure that System Accounts Do Not Run a Shell Upon Login - Get All Local Users From /etc/passwd''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Ensure that System Accounts Do Not Run a Shell Upon Login - Create local_users Variable From getent_passwd Facts
    ansible.builtin.set_fact:
      local_users: '{{ ansible_facts.getent_passwd | dict2items }}'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.2
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - no_shelllogin_for_systemaccounts
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure that System Accounts Do Not Run a Shell Upon Login - Create local_users
      Variable From getent_passwd Facts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure that System Accounts Do Not Run a Shell Upon Login - Create local_users Variable From getent_passwd
        Facts'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
- block:
  - name: Ensure that System Accounts Do Not Run a Shell Upon Login -  Disable Login Shell for System Accounts
    ansible.builtin.user:
      name: '{{ item.key }}'
      shell: /sbin/nologin
    loop: '{{ local_users }}'
    when:
    - item.key not in ['root']
    - item.value[1]|int < 1000
    - item.value[5] not in ['/sbin/shutdown', '/sbin/halt', '/bin/sync']
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.2
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - no_shelllogin_for_systemaccounts
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure that System Accounts Do Not Run a Shell Upon Login -  Disable Login
      Shell for System Accounts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure that System Accounts Do Not Run a Shell Upon Login -  Disable Login Shell for System Accounts''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
- block:
  - name: Correct any occurrence of TMOUT in /etc/profile
    ansible.builtin.replace:
      path: /etc/profile
      regexp: ^[^#].*TMOUT=.*
      replace: typeset -xr TMOUT={{ var_accounts_tmout }}
    register: profile_replaced
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSSv4-8.6
    - PCI-DSSv4-8.6.1
    - accounts_tmout
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Correct any occurrence of TMOUT in /etc/profile'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Correct any occurrence of TMOUT in /etc/profile'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_tmout | default(true) | bool)
- block:
  - name: Set Interactive Session Timeout
    ansible.builtin.lineinfile:
      path: /etc/profile.d/tmout.sh
      create: true
      regexp: TMOUT=
      line: typeset -xr TMOUT={{ var_accounts_tmout }}
      state: present
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSSv4-8.6
    - PCI-DSSv4-8.6.1
    - accounts_tmout
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set Interactive Session Timeout'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Interactive Session Timeout'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_tmout | default(true) | bool)
- block:
  - name: Ensure interactive local users are the group-owners of their respective initialization files
    ansible.builtin.shell:
      cmd: '

        awk -F: ''{if ($4 >= 1000 && $4 != 65534) print $4":"$6}'' /etc/passwd | while IFS=: read -r gid home; do find -P
        "$home" -maxdepth 1 -type f -name "\.[^.]*" -exec chgrp -f --no-dereference -- $gid "{}" \;; done'
    tags:
    - accounts_user_dot_group_ownership
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure interactive local users are the group-owners of their respective initialization
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure interactive local users are the group-owners of their respective initialization files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_user_dot_group_ownership | default(true)
    | bool)
- block:
  - name: Ensure interactive local users are the owners of their respective initialization files
    ansible.builtin.shell:
      cmd: 'awk -F: ''{if ($3 >= 1000 && $3 != 65534) print $3":"$6}'' /etc/passwd | while IFS=: read -r uid home; do find
        -P "$home" -maxdepth 1 -type f -name "\.[^.]*" -exec chown -f --no-dereference -- $uid "{}" \;; done'
    tags:
    - accounts_user_dot_user_ownership
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure interactive local users are the owners of their respective initialization
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure interactive local users are the owners of their respective initialization files'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_user_dot_user_ownership | default(true) |
    bool)
- block:
  - name: Get all local users from /etc/passwd
    ansible.builtin.getent:
      database: passwd
      split: ':'
    tags:
    - accounts_user_interactive_home_directory_exists
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get all local users from /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get all local users from /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_user_interactive_home_directory_exists |
    default(true) | bool)
- block:
  - name: Create local_users variable from the getent output
    ansible.builtin.set_fact:
      local_users: '{{ ansible_facts.getent_passwd|dict2items }}'
    tags:
    - accounts_user_interactive_home_directory_exists
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Create local_users variable from the getent output'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Create local_users variable from the getent output'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_user_interactive_home_directory_exists |
    default(true) | bool)
- block:
  - name: Ensure interactive users have a home directory exists
    ansible.builtin.user:
      name: '{{ item.key }}'
      create_home: true
    loop: '{{ local_users }}'
    when:
    - item.value[1]|int >= 1000
    - item.value[1]|int != 65534
    tags:
    - accounts_user_interactive_home_directory_exists
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure interactive users have a home directory exists'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure interactive users have a home directory exists'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_user_interactive_home_directory_exists |
    default(true) | bool)
- block:
  - name: Get all local users from /etc/passwd
    ansible.builtin.getent:
      database: passwd
      split: ':'
    tags:
    - accounts_users_netrc_file_permissions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get all local users from /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get all local users from /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_users_netrc_file_permissions | default(true)
    | bool)
- block:
  - name: Create local_users variable from the getent output
    ansible.builtin.set_fact:
      local_users: '{{ ansible_facts.getent_passwd|dict2items }}'
    tags:
    - accounts_users_netrc_file_permissions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Create local_users variable from the getent output'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Create local_users variable from the getent output'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_users_netrc_file_permissions | default(true)
    | bool)
- block:
  - name: Test for existence of .netrc file in home directories to avoid creating them, but only fixing permissions
    ansible.builtin.stat:
      path: '{{ item.value[4] }}/.netrc'
    register: path_exists
    loop: '{{ local_users }}'
    when:
    - item.value[1]|int >= 1000
    - item.value[1]|int != 65534
    tags:
    - accounts_users_netrc_file_permissions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence of .netrc file in home directories to avoid creating them,
      but only fixing permissions'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence of .netrc file in home directories to avoid creating them, but only fixing permissions''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_users_netrc_file_permissions | default(true)
    | bool)
- block:
  - name: Ensure group and world cannot access respective .netrc files
    ansible.builtin.file:
      path: '{{ item.item.value[4] }}/.netrc'
      mode: '0600'
      state: file
    loop: '{{ path_exists.results }}'
    when: item.stat is defined and (item is not skipped and item.stat.exists)
    tags:
    - accounts_users_netrc_file_permissions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group and world cannot access respective .netrc files'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group and world cannot access respective .netrc files'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_users_netrc_file_permissions | default(true)
    | bool)
- block:
  - name: Get all local users from /etc/passwd
    ansible.builtin.getent:
      database: passwd
      split: ':'
    tags:
    - file_groupownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get all local users from /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get all local users from /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_home_directories | default(true)
    | bool)
- block:
  - name: Create local_users variable from the getent output
    ansible.builtin.set_fact:
      local_users: '{{ ansible_facts.getent_passwd|dict2items }}'
    tags:
    - file_groupownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Create local_users variable from the getent output'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Create local_users variable from the getent output'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_home_directories | default(true)
    | bool)
- block:
  - name: Test for existence of home directories to avoid creating them, but only fixing group ownership
    ansible.builtin.stat:
      path: '{{ item.value[4] }}'
    register: path_exists
    loop: '{{ local_users }}'
    when:
    - item.value[1]|int >= 1000
    - item.value[1]|int != 65534
    tags:
    - file_groupownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence of home directories to avoid creating them, but only fixing
      group ownership'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence of home directories to avoid creating them, but only fixing group ownership''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_home_directories | default(true)
    | bool)
- block:
  - name: Ensure interactive local users are the group-owners of their respective home directories
    ansible.builtin.file:
      path: '{{ item.0.value[4] }}'
      group: '{{ item.0.value[2] }}'
    loop: '{{ local_users|zip(path_exists.results)|list }}'
    when: item.1.stat is defined and item.1.stat.exists
    tags:
    - file_groupownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure interactive local users are the group-owners of their respective home
      directories'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure interactive local users are the group-owners of their respective home directories'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_home_directories | default(true)
    | bool)
- block:
  - name: Get all local users from /etc/passwd
    ansible.builtin.getent:
      database: passwd
      split: ':'
    tags:
    - file_ownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get all local users from /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get all local users from /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_home_directories | default(true) |
    bool)
- block:
  - name: Create local_users variable from the getent output
    ansible.builtin.set_fact:
      local_users: '{{ ansible_facts.getent_passwd|dict2items }}'
    tags:
    - file_ownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Create local_users variable from the getent output'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Create local_users variable from the getent output'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_home_directories | default(true) |
    bool)
- block:
  - name: Test for existence of home directories to avoid creating them, but only fixing ownership
    ansible.builtin.stat:
      path: '{{ item.value[4] }}'
    register: path_exists
    loop: '{{ local_users }}'
    when:
    - item.value[1]|int >= 1000
    - item.value[1]|int != 65534
    tags:
    - file_ownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence of home directories to avoid creating them, but only fixing
      ownership'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence of home directories to avoid creating them, but only fixing ownership'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_home_directories | default(true) |
    bool)
- block:
  - name: Ensure interactive local users are the owners of their respective home directories
    ansible.builtin.file:
      path: '{{ item.0.value[4] }}'
      owner: '{{ item.0.value[1] }}'
    loop: '{{ local_users|zip(path_exists.results)|list }}'
    when: item.1.stat is defined and item.1.stat.exists
    tags:
    - file_ownership_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure interactive local users are the owners of their respective home directories''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure interactive local users are the owners of their respective home directories'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_home_directories | default(true) |
    bool)
- block:
  - name: Ensure All User Initialization Files Have Mode 0740 Or Less Permissive - Gather User Info
    ansible.builtin.getent:
      database: passwd
    tags:
    - file_permission_user_init_files
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure All User Initialization Files Have Mode 0740 Or Less Permissive - Gather
      User Info'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure All User Initialization Files Have Mode 0740 Or Less Permissive - Gather User Info'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permission_user_init_files | default(true) |
    bool)
- block:
  - name: Ensure All User Initialization Files Have Mode 0740 Or Less Permissive - Find Init Files
    ansible.builtin.find:
      paths: '{{ item.value[4] }}'
      pattern: '{{ var_user_initialization_files_regex }}'
      hidden: true
      use_regex: true
    with_dict: '{{ ansible_facts.getent_passwd }}'
    when:
    - item.value[4] != "/sbin/nologin"
    - item.key not in ["nobody", "nfsnobody"]
    - item.value[1] | int >= 1000
    register: found_init_files
    tags:
    - file_permission_user_init_files
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure All User Initialization Files Have Mode 0740 Or Less Permissive - Find
      Init Files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure All User Initialization Files Have Mode 0740 Or Less Permissive - Find Init Files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permission_user_init_files | default(true) |
    bool)
- block:
  - name: Ensure All User Initialization Files Have Mode 0740 Or Less Permissive - Fix Init Files Permissions
    ansible.builtin.file:
      path: '{{ item.1.path }}'
      mode: u-s,g-wxs,o=
    loop: '{{ q(''ansible.builtin.subelements'', found_init_files.results, ''files'', {''skip_missing'': True}) }}'
    tags:
    - file_permission_user_init_files
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure All User Initialization Files Have Mode 0740 Or Less Permissive - Fix
      Init Files Permissions'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure All User Initialization Files Have Mode 0740 Or Less Permissive - Fix Init Files Permissions''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permission_user_init_files | default(true) |
    bool)
- block:
  - name: Get all local users from /etc/passwd
    ansible.builtin.getent:
      database: passwd
      split: ':'
    tags:
    - file_permissions_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get all local users from /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get all local users from /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_home_directories | default(true)
    | bool)
- block:
  - name: Create local_users variable from the getent output
    ansible.builtin.set_fact:
      local_users: '{{ ansible_facts.getent_passwd|dict2items }}'
    tags:
    - file_permissions_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Create local_users variable from the getent output'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Create local_users variable from the getent output'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_home_directories | default(true)
    | bool)
- block:
  - name: Test for existence home directories to avoid creating them.
    ansible.builtin.stat:
      path: '{{ item.value[4] }}'
    register: path_exists
    loop: '{{ local_users }}'
    when:
    - item.value[1]|int >= 1000
    - item.value[1]|int != 65534
    - item.value[4] != "/"
    tags:
    - file_permissions_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence home directories to avoid creating them.'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence home directories to avoid creating them.'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_home_directories | default(true)
    | bool)
- block:
  - name: Ensure interactive local users have proper permissions on their respective home directories
    ansible.builtin.file:
      path: '{{ item.0.value[4] }}'
      mode: u-s,g-w-s,o=-
      follow: false
      recurse: false
    loop: '{{ local_users|zip(path_exists.results)|list }}'
    when: item.1.stat is defined and item.1.stat.exists
    tags:
    - file_permissions_home_directories
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure interactive local users have proper permissions on their respective
      home directories'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure interactive local users have proper permissions on their respective home directories'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_home_directories | default(true)
    | bool)
- block:
  - name: Get root paths which are not symbolic links
    ansible.builtin.stat:
      path: '{{ item }}'
    changed_when: false
    failed_when: false
    register: root_paths
    with_items: '{{ ansible_env.PATH.split('':'') }}'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(a)
    - accounts_root_path_dirs_no_write
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get root paths which are not symbolic links'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get root paths which are not symbolic links'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_root_path_dirs_no_write | default(true) |
    bool)
- block:
  - name: Disable writability to root directories
    ansible.builtin.file:
      path: '{{ item.item }}'
      mode: g-w,o-w
    with_items: '{{ root_paths.results }}'
    when:
    - root_paths.results is defined
    - (item is not skipped and item.stat.exists)
    - not (item is not skipped and item.stat.islnk)
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(a)
    - accounts_root_path_dirs_no_write
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable writability to root directories'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable writability to root directories'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_root_path_dirs_no_write | default(true) |
    bool)
- block:
  - name: Check if umask in /etc/bashrc is already set
    ansible.builtin.lineinfile:
      path: /etc/bashrc
      regexp: ^[^#]*\bumask\s+\d+$
      state: absent
    check_mode: true
    changed_when: false
    register: umask_replace
    when: '"bash" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_bashrc
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Check if umask in /etc/bashrc is already set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check if umask in /etc/bashrc is already set'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_umask_etc_bashrc | default(true) | bool)
- block:
  - name: Replace user umask in /etc/bashrc
    ansible.builtin.replace:
      path: /etc/bashrc
      regexp: ^([^#]*\b)umask\s+\d+$
      replace: \g<1>umask {{ var_accounts_user_umask }}
    when:
    - '"bash" in ansible_facts.packages'
    - umask_replace.found > 0
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_bashrc
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Replace user umask in /etc/bashrc'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Replace user umask in /etc/bashrc'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_umask_etc_bashrc | default(true) | bool)
- block:
  - name: Ensure the Default umask is Appended Correctly
    ansible.builtin.lineinfile:
      create: true
      path: /etc/bashrc
      line: umask {{ var_accounts_user_umask }}
    when:
    - '"bash" in ansible_facts.packages'
    - umask_replace.found == 0
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_bashrc
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Default umask is Appended Correctly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Default umask is Appended Correctly'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_umask_etc_bashrc | default(true) | bool)
- block:
  - name: Check if UMASK is already set
    ansible.builtin.lineinfile:
      path: /etc/login.defs
      regexp: ^(\s*)UMASK\s+.*
      state: absent
    check_mode: true
    changed_when: false
    register: result_umask_is_set
    when: '"shadow-utils" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_login_defs
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Check if UMASK is already set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check if UMASK is already set'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_umask_etc_login_defs | default(true) | bool)
- block:
  - name: Replace user UMASK in /etc/login.defs
    ansible.builtin.replace:
      path: /etc/login.defs
      regexp: ^(\s*)UMASK(\s+).*
      replace: \g<1>UMASK\g<2>{{ var_accounts_user_umask }}
    when:
    - '"shadow-utils" in ansible_facts.packages'
    - result_umask_is_set.found > 0
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_login_defs
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Replace user UMASK in /etc/login.defs'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Replace user UMASK in /etc/login.defs'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_umask_etc_login_defs | default(true) | bool)
- block:
  - name: Ensure the Default UMASK is Appended Correctly
    ansible.builtin.lineinfile:
      create: true
      path: /etc/login.defs
      line: UMASK {{ var_accounts_user_umask }}
    when:
    - '"shadow-utils" in ansible_facts.packages'
    - result_umask_is_set.found == 0
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_login_defs
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Default UMASK is Appended Correctly'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Default UMASK is Appended Correctly'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_umask_etc_login_defs | default(true) | bool)
- block:
  - name: Ensure the Default Umask is Set Correctly in /etc/profile - Locate Profile Configuration Files Where umask Is Defined
    ansible.builtin.find:
      paths:
      - /etc/profile.d
      patterns:
      - sh.local
      - '*.sh'
      contains: ^[\s]*umask\s+\d+
    register: result_profile_d_files
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_profile
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Default Umask is Set Correctly in /etc/profile - Locate Profile
      Configuration Files Where umask Is Defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Default Umask is Set Correctly in /etc/profile - Locate Profile Configuration Files Where
        umask Is Defined'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_umask_etc_profile | default(true) | bool)
- block:
  - name: Ensure the Default Umask is Set Correctly in /etc/profile - Replace Existing umask Value in Files From /etc/profile.d
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^(\s*)umask\s+\d+
      replace: \1umask {{ var_accounts_user_umask }}
    loop: '{{ result_profile_d_files.files }}'
    register: result_umask_replaced_profile_d
    when: result_profile_d_files.matched
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_profile
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Default Umask is Set Correctly in /etc/profile - Replace Existing
      umask Value in Files From /etc/profile.d'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Default Umask is Set Correctly in /etc/profile - Replace Existing umask Value in Files
        From /etc/profile.d'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_umask_etc_profile | default(true) | bool)
- block:
  - name: Ensure the Default Umask is Set Correctly in /etc/profile - Ensure umask Is Set in /etc/profile if Not Already Set
      Elsewhere
    ansible.builtin.lineinfile:
      create: true
      mode: 420
      path: /etc/profile
      line: umask {{ var_accounts_user_umask }}
    when: not result_profile_d_files.matched
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_profile
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Default Umask is Set Correctly in /etc/profile - Ensure umask Is
      Set in /etc/profile if Not Already Set Elsewhere'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Default Umask is Set Correctly in /etc/profile - Ensure umask Is Set in /etc/profile if
        Not Already Set Elsewhere'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_umask_etc_profile | default(true) | bool)
- block:
  - name: Ensure the Default Umask is Set Correctly in /etc/profile - Ensure umask Value For All Existing umask Definition
      in /etc/profile
    ansible.builtin.replace:
      path: /etc/profile
      regexp: ^(\s*)umask\s+\d+
      replace: \1umask {{ var_accounts_user_umask }}
    register: result_umask_replaced_profile
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - accounts_umask_etc_profile
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure the Default Umask is Set Correctly in /etc/profile - Ensure umask Value
      For All Existing umask Definition in /etc/profile'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure the Default Umask is Set Correctly in /etc/profile - Ensure umask Value For All Existing umask
        Definition in /etc/profile'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_accounts_umask_etc_profile | default(true) | bool)
- block:
  - name: Set the file_groupowner_grub2_cfg_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_grub2_cfg_newgroup: '0'
    when:
    - ( "grub2-common" in ansible_facts.packages and "kernel" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - CJIS-5.5.2.2
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-7.1
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_grub2_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_grub2_cfg_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_grub2_cfg_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_grub2_cfg | default(true) | bool)
- block:
  - name: Test for existence /boot/grub2/grub.cfg
    ansible.builtin.stat:
      path: /boot/grub2/grub.cfg
    register: file_exists
    when:
    - ( "grub2-common" in ansible_facts.packages and "kernel" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - CJIS-5.5.2.2
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-7.1
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_grub2_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /boot/grub2/grub.cfg'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /boot/grub2/grub.cfg'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_grub2_cfg | default(true) | bool)
- block:
  - name: Ensure group owner on /boot/grub2/grub.cfg
    ansible.builtin.file:
      path: /boot/grub2/grub.cfg
      follow: false
      group: '{{ file_groupowner_grub2_cfg_newgroup }}'
    when:
    - ( "grub2-common" in ansible_facts.packages and "kernel" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-7.1
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_grub2_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /boot/grub2/grub.cfg'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /boot/grub2/grub.cfg'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_grub2_cfg | default(true) | bool)
- block:
  - name: Set the file_groupowner_user_cfg_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_user_cfg_newgroup: '0'
    when:
    - ( "grub2-common" in ansible_facts.packages and "kernel" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - CJIS-5.5.2.2
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-7.1
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_user_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_user_cfg_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_user_cfg_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_user_cfg | default(true) | bool)
- block:
  - name: Test for existence /boot/grub2/user.cfg
    ansible.builtin.stat:
      path: /boot/grub2/user.cfg
    register: file_exists
    when:
    - ( "grub2-common" in ansible_facts.packages and "kernel" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - CJIS-5.5.2.2
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-7.1
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_user_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /boot/grub2/user.cfg'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /boot/grub2/user.cfg'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_user_cfg | default(true) | bool)
- block:
  - name: Ensure group owner on /boot/grub2/user.cfg
    ansible.builtin.file:
      path: /boot/grub2/user.cfg
      follow: false
      group: '{{ file_groupowner_user_cfg_newgroup }}'
    when:
    - ( "grub2-common" in ansible_facts.packages and "kernel" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-7.1
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_user_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /boot/grub2/user.cfg'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /boot/grub2/user.cfg'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_user_cfg | default(true) | bool)
- block:
  - name: Set the file_owner_grub2_cfg_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_grub2_cfg_newown: '0'
    when:
    - ( "grub2-common" in ansible_facts.packages and "kernel" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - CJIS-5.5.2.2
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-7.1
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_grub2_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_grub2_cfg_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_grub2_cfg_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_grub2_cfg | default(true) | bool)
- block:
  - name: Test for existence /boot/grub2/grub.cfg
    ansible.builtin.stat:
      path: /boot/grub2/grub.cfg
    register: file_exists
    when:
    - ( "grub2-common" in ansible_facts.packages and "kernel" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - CJIS-5.5.2.2
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-7.1
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_grub2_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /boot/grub2/grub.cfg'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /boot/grub2/grub.cfg'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_grub2_cfg | default(true) | bool)
- block:
  - name: Ensure owner on /boot/grub2/grub.cfg
    ansible.builtin.file:
      path: /boot/grub2/grub.cfg
      follow: false
      owner: '{{ file_owner_grub2_cfg_newown }}'
    when:
    - ( "grub2-common" in ansible_facts.packages and "kernel" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-7.1
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_grub2_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /boot/grub2/grub.cfg'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /boot/grub2/grub.cfg'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_grub2_cfg | default(true) | bool)
- block:
  - name: Set the file_owner_user_cfg_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_user_cfg_newown: '0'
    when:
    - ( "grub2-common" in ansible_facts.packages and "kernel" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - CJIS-5.5.2.2
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-7.1
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_user_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_user_cfg_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_user_cfg_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_user_cfg | default(true) | bool)
- block:
  - name: Test for existence /boot/grub2/user.cfg
    ansible.builtin.stat:
      path: /boot/grub2/user.cfg
    register: file_exists
    when:
    - ( "grub2-common" in ansible_facts.packages and "kernel" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - CJIS-5.5.2.2
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-7.1
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_user_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /boot/grub2/user.cfg'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /boot/grub2/user.cfg'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_user_cfg | default(true) | bool)
- block:
  - name: Ensure owner on /boot/grub2/user.cfg
    ansible.builtin.file:
      path: /boot/grub2/user.cfg
      follow: false
      owner: '{{ file_owner_user_cfg_newown }}'
    when:
    - ( "grub2-common" in ansible_facts.packages and "kernel" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-7.1
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_user_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /boot/grub2/user.cfg'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /boot/grub2/user.cfg'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_user_cfg | default(true) | bool)
- block:
  - name: Test for existence /boot/grub2/grub.cfg
    ansible.builtin.stat:
      path: /boot/grub2/grub.cfg
    register: file_exists
    when:
    - ( "grub2-common" in ansible_facts.packages and "kernel" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_grub2_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /boot/grub2/grub.cfg'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /boot/grub2/grub.cfg'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_grub2_cfg | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xwrs,o-xwrt on /boot/grub2/grub.cfg
    ansible.builtin.file:
      path: /boot/grub2/grub.cfg
      mode: u-xs,g-xwrs,o-xwrt
    when:
    - ( "grub2-common" in ansible_facts.packages and "kernel" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_grub2_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xwrs,o-xwrt on /boot/grub2/grub.cfg'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xwrs,o-xwrt on /boot/grub2/grub.cfg'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_grub2_cfg | default(true) | bool)
- block:
  - name: Test for existence /boot/grub2/user.cfg
    ansible.builtin.stat:
      path: /boot/grub2/user.cfg
    register: file_exists
    when:
    - ( "grub2-common" in ansible_facts.packages and "kernel" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    tags:
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_user_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /boot/grub2/user.cfg'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /boot/grub2/user.cfg'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_user_cfg | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xwrs,o-xwrt on /boot/grub2/user.cfg
    ansible.builtin.file:
      path: /boot/grub2/user.cfg
      mode: u-xs,g-xwrs,o-xwrt
    when:
    - ( "grub2-common" in ansible_facts.packages and "kernel" in ansible_facts.packages )
    - not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] )
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_user_cfg
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xwrs,o-xwrt on /boot/grub2/user.cfg'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xwrs,o-xwrt on /boot/grub2/user.cfg'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_user_cfg | default(true) | bool)
- block:
  - name: Ensure rsyslog Default File Permissions Configured - Search for $FileCreateMode Parameter in rsyslog Main Config
      File
    ansible.builtin.find:
      paths: /etc
      pattern: rsyslog.conf
      contains: ^\s*\$FileCreateMode\s*\d+
    register: rsyslog_main_file_with_filecreatemode
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_filecreatemode
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Default File Permissions Configured - Search for $FileCreateMode
      Parameter in rsyslog Main Config File'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Default File Permissions Configured - Search for $FileCreateMode Parameter in rsyslog
        Main Config File'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_filecreatemode | default(true) | bool)
- block:
  - name: Ensure rsyslog Default File Permissions Configured - Search for $FileCreateMode Parameter in rsyslog Include Files
    ansible.builtin.find:
      paths: /etc/rsyslog.d/
      pattern: '*.conf'
      contains: ^\s*\$FileCreateMode\s*\d+
    register: rsyslog_includes_with_filecreatemode
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_filecreatemode
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Default File Permissions Configured - Search for $FileCreateMode
      Parameter in rsyslog Include Files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Default File Permissions Configured - Search for $FileCreateMode Parameter in rsyslog
        Include Files'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_filecreatemode | default(true) | bool)
- block:
  - name: Ensure rsyslog Default File Permissions Configured - Assemble List of rsyslog Configuration Files with $FileCreateMode
      Parameter
    ansible.builtin.set_fact:
      rsyslog_filecreatemode_files: '{{ rsyslog_main_file_with_filecreatemode.files | map(attribute=''path'') | list + rsyslog_includes_with_filecreatemode.files
        | map(attribute=''path'') | list }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_filecreatemode
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Default File Permissions Configured - Assemble List of rsyslog
      Configuration Files with $FileCreateMode Parameter'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Default File Permissions Configured - Assemble List of rsyslog Configuration Files
        with $FileCreateMode Parameter'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_filecreatemode | default(true) | bool)
- block:
  - name: Ensure rsyslog Default File Permissions Configured - Remove $FileCreateMode Parameter from Multiple Files to Avoid
      Conflicts
    ansible.builtin.lineinfile:
      path: '{{ item }}'
      regexp: \$FileCreateMode.*
      state: absent
    register: result_rsyslog_filecreatemode_removed
    loop: '{{ rsyslog_filecreatemode_files }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - rsyslog_filecreatemode_files | length > 1
    tags:
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_filecreatemode
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Default File Permissions Configured - Remove $FileCreateMode
      Parameter from Multiple Files to Avoid Conflicts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Default File Permissions Configured - Remove $FileCreateMode Parameter from Multiple
        Files to Avoid Conflicts'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_filecreatemode | default(true) | bool)
- block:
  - name: Ensure rsyslog Default File Permissions Configured - Add $FileCreateMode Parameter and Expected Value
    ansible.builtin.lineinfile:
      path: /etc/rsyslog.d/00-rsyslog_filecreatemode.conf
      line: $FileCreateMode 0640
      mode: 416
      create: true
    when:
    - '"kernel" in ansible_facts.packages'
    - rsyslog_filecreatemode_files | length == 0 or result_rsyslog_filecreatemode_removed is not skipped
    tags:
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_filecreatemode
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Default File Permissions Configured - Add $FileCreateMode Parameter
      and Expected Value'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Default File Permissions Configured - Add $FileCreateMode Parameter and Expected Value''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_filecreatemode | default(true) | bool)
- block:
  - name: Ensure rsyslog Default File Permissions Configured - Ensure Correct Value of Existing $FileCreateMode Parameter
    ansible.builtin.lineinfile:
      path: '{{ item }}'
      regexp: ^\$FileCreateMode
      line: $FileCreateMode 0640
    loop: '{{ rsyslog_filecreatemode_files }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - rsyslog_filecreatemode_files | length == 1
    tags:
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_filecreatemode
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Default File Permissions Configured - Ensure Correct Value
      of Existing $FileCreateMode Parameter'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Default File Permissions Configured - Ensure Correct Value of Existing $FileCreateMode
        Parameter'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_filecreatemode | default(true) | bool)
- block:
  - name: Ensure Log Files Are Owned By Appropriate Group - Set rsyslog logfile configuration facts
    ansible.builtin.set_fact:
      rsyslog_etc_config: /etc/rsyslog.conf
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_groupownership
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Log Files Are Owned By Appropriate Group - Set rsyslog logfile configuration
      facts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Log Files Are Owned By Appropriate Group - Set rsyslog logfile configuration facts'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_groupownership | default(true) | bool)
- block:
  - name: Ensure Log Files Are Owned By Appropriate Group - Get IncludeConfig directive
    ansible.builtin.shell: 'set -o pipefail

      grep -e ''$IncludeConfig'' {{ rsyslog_etc_config }} | cut -d '' '' -f 2 || true

      '
    register: rsyslog_old_inc
    changed_when: false
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_groupownership
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Log Files Are Owned By Appropriate Group - Get IncludeConfig directive''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Log Files Are Owned By Appropriate Group - Get IncludeConfig directive'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_groupownership | default(true) | bool)
- block:
  - name: Ensure Log Files Are Owned By Appropriate Group - Get include files directives
    ansible.builtin.shell: 'set -o pipefail

      awk ''/)/{f=0} /include\(/{f=1} f{ nf=gensub("^(include\\(|\\s*)file=\"(\\S+)\".*","\\2",1); if($0!=nf){ print nf }}''
      {{ rsyslog_etc_config }} || true

      '
    register: rsyslog_new_inc
    changed_when: false
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_groupownership
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Log Files Are Owned By Appropriate Group - Get include files directives''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Log Files Are Owned By Appropriate Group - Get include files directives'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_groupownership | default(true) | bool)
- block:
  - name: Ensure Log Files Are Owned By Appropriate Group - Aggregate rsyslog includes
    ansible.builtin.set_fact:
      include_config_output: '{{ rsyslog_old_inc.stdout_lines + rsyslog_new_inc.stdout_lines }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    - rsyslog_old_inc is not skipped and rsyslog_new_inc is not skipped
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_groupownership
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Log Files Are Owned By Appropriate Group - Aggregate rsyslog includes''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Log Files Are Owned By Appropriate Group - Aggregate rsyslog includes'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_groupownership | default(true) | bool)
- block:
  - name: Ensure Log Files Are Owned By Appropriate Group - List all config files
    ansible.builtin.find:
      paths: '{{ item | dirname }}'
      patterns: '{{ item | basename }}'
      hidden: false
      follow: true
    loop: '{{ include_config_output | list + [rsyslog_etc_config] }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    - include_config_output is defined
    register: rsyslog_config_files
    failed_when: false
    changed_when: false
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_groupownership
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Log Files Are Owned By Appropriate Group - List all config files''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Log Files Are Owned By Appropriate Group - List all config files'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_groupownership | default(true) | bool)
- block:
  - name: Ensure Log Files Are Owned By Appropriate Group - Extract log files old format
    ansible.builtin.shell: 'set -o pipefail

      grep -oP ''^[^(\s|#|\$)]+[\s]+.*[\s]+-?(/+[^:;\s]+);*\.*$'' {{ item.1.path }} | \

      awk ''{print $NF}'' | \

      sed -e ''s/^-//'' || true

      '
    loop: '{{ rsyslog_config_files.results | default([]) | subelements(''files'') }}'
    register: log_files_old
    changed_when: false
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    - rsyslog_config_files is not skipped
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_groupownership
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Log Files Are Owned By Appropriate Group - Extract log files old format''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Log Files Are Owned By Appropriate Group - Extract log files old format'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_groupownership | default(true) | bool)
- block:
  - name: Ensure Log Files Are Owned By Appropriate Group - Extract log files new format
    ansible.builtin.shell: 'set -o pipefail

      grep -ozP "action\s*\(\s*type\s*=\s*\"omfile\"[^\)]*\)" {{ item.1.path }} | \

      grep -aoP "\bFile\s*=\s*\"([/[:alnum:][:punct:]]*)\"\s*\)" | \

      grep -oE "\"([/[:alnum:][:punct:]]*)\"" | \

      tr -d "\""|| true

      '
    loop: '{{ rsyslog_config_files.results | default([]) | subelements(''files'') }}'
    register: log_files_new
    changed_when: false
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    - rsyslog_config_files is not skipped
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_groupownership
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Log Files Are Owned By Appropriate Group - Extract log files new format''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Log Files Are Owned By Appropriate Group - Extract log files new format'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_groupownership | default(true) | bool)
- block:
  - name: Ensure Log Files Are Owned By Appropriate Group - Sum all log files found
    ansible.builtin.set_fact:
      log_files: '{{ log_files_new.results | map(attribute=''stdout_lines'') | list | flatten | unique + log_files_old.results
        | map(attribute=''stdout_lines'') | list | flatten | unique }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_groupownership
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Log Files Are Owned By Appropriate Group - Sum all log files found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Log Files Are Owned By Appropriate Group - Sum all log files found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_groupownership | default(true) | bool)
- block:
  - name: Ensure Log Files Are Owned By Appropriate Group -Setup log files attribute
    ansible.builtin.file:
      path: '{{ item }}'
      group: root
      state: file
    loop: '{{ log_files | list | flatten | unique }}'
    failed_when: false
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_groupownership
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Log Files Are Owned By Appropriate Group -Setup log files attribute''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Log Files Are Owned By Appropriate Group -Setup log files attribute'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_groupownership | default(true) | bool)
- block:
  - name: Ensure Log Files Are Owned By Appropriate User - Set rsyslog logfile configuration facts
    ansible.builtin.set_fact:
      rsyslog_etc_config: /etc/rsyslog.conf
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_ownership
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Log Files Are Owned By Appropriate User - Set rsyslog logfile configuration
      facts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Log Files Are Owned By Appropriate User - Set rsyslog logfile configuration facts'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_ownership | default(true) | bool)
- block:
  - name: Ensure Log Files Are Owned By Appropriate User - Get IncludeConfig directive
    ansible.builtin.shell: 'set -o pipefail

      grep -e ''$IncludeConfig'' {{ rsyslog_etc_config }} | cut -d '' '' -f 2 || true

      '
    register: rsyslog_old_inc
    changed_when: false
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_ownership
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Log Files Are Owned By Appropriate User - Get IncludeConfig directive''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Log Files Are Owned By Appropriate User - Get IncludeConfig directive'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_ownership | default(true) | bool)
- block:
  - name: Ensure Log Files Are Owned By Appropriate User - Get include files directives
    ansible.builtin.shell: 'set -o pipefail

      awk ''/)/{f=0} /include\(/{f=1} f{ nf=gensub("^(include\\(|\\s*)file=\"(\\S+)\".*","\\2",1); if($0!=nf){ print nf }}''
      {{ rsyslog_etc_config }} || true

      '
    register: rsyslog_new_inc
    changed_when: false
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_ownership
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Log Files Are Owned By Appropriate User - Get include files directives''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Log Files Are Owned By Appropriate User - Get include files directives'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_ownership | default(true) | bool)
- block:
  - name: Ensure Log Files Are Owned By Appropriate User - Aggregate rsyslog includes
    ansible.builtin.set_fact:
      include_config_output: '{{ rsyslog_old_inc.stdout_lines + rsyslog_new_inc.stdout_lines }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    - rsyslog_old_inc is not skipped and rsyslog_new_inc is not skipped
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_ownership
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Log Files Are Owned By Appropriate User - Aggregate rsyslog includes''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Log Files Are Owned By Appropriate User - Aggregate rsyslog includes'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_ownership | default(true) | bool)
- block:
  - name: Ensure Log Files Are Owned By Appropriate User - List all config files
    ansible.builtin.find:
      paths: '{{ item | dirname }}'
      patterns: '{{ item | basename }}'
      hidden: false
      follow: true
    loop: '{{ include_config_output | list + [rsyslog_etc_config] }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    - include_config_output is defined
    register: rsyslog_config_files
    failed_when: false
    changed_when: false
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_ownership
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Log Files Are Owned By Appropriate User - List all config files'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Log Files Are Owned By Appropriate User - List all config files'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_ownership | default(true) | bool)
- block:
  - name: Ensure Log Files Are Owned By Appropriate User - Extract log files old format
    ansible.builtin.shell: 'set -o pipefail

      grep -oP ''^[^(\s|#|\$)]+[\s]+.*[\s]+-?(/+[^:;\s]+);*\.*$'' {{ item.1.path }} | \

      awk ''{print $NF}'' | \

      sed -e ''s/^-//'' || true

      '
    loop: '{{ rsyslog_config_files.results | default([]) | subelements(''files'') }}'
    register: log_files_old
    changed_when: false
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    - rsyslog_config_files is not skipped
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_ownership
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Log Files Are Owned By Appropriate User - Extract log files old format''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Log Files Are Owned By Appropriate User - Extract log files old format'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_ownership | default(true) | bool)
- block:
  - name: Ensure Log Files Are Owned By Appropriate User - Extract log files new format
    ansible.builtin.shell: 'set -o pipefail

      grep -ozP "action\s*\(\s*type\s*=\s*\"omfile\"[^\)]*\)" {{ item.1.path }} | \

      grep -aoP "\bFile\s*=\s*\"([/[:alnum:][:punct:]]*)\"\s*\)" | \

      grep -oE "\"([/[:alnum:][:punct:]]*)\"" | \

      tr -d "\""|| true

      '
    loop: '{{ rsyslog_config_files.results | default([]) | subelements(''files'') }}'
    register: log_files_new
    changed_when: false
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    - rsyslog_config_files is not skipped
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_ownership
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Log Files Are Owned By Appropriate User - Extract log files new format''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Log Files Are Owned By Appropriate User - Extract log files new format'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_ownership | default(true) | bool)
- block:
  - name: Ensure Log Files Are Owned By Appropriate User - Sum all log files found
    ansible.builtin.set_fact:
      log_files: '{{ log_files_new.results | map(attribute=''stdout_lines'') | list | flatten | unique + log_files_old.results
        | map(attribute=''stdout_lines'') | list | flatten | unique }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_ownership
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Log Files Are Owned By Appropriate User - Sum all log files found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Log Files Are Owned By Appropriate User - Sum all log files found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_ownership | default(true) | bool)
- block:
  - name: Ensure Log Files Are Owned By Appropriate User -Setup log files attribute
    ansible.builtin.file:
      path: '{{ item }}'
      owner: root
      state: file
    loop: '{{ log_files | list | flatten | unique }}'
    failed_when: false
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_ownership
  rescue:
  - name: 'StigForge: continue after error in ''Ensure Log Files Are Owned By Appropriate User -Setup log files attribute''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure Log Files Are Owned By Appropriate User -Setup log files attribute'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_ownership | default(true) | bool)
- block:
  - name: Ensure System Log Files Have Correct Permissions - Set rsyslog logfile configuration facts
    ansible.builtin.set_fact:
      rsyslog_etc_config: /etc/rsyslog.conf
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_permissions
  rescue:
  - name: 'StigForge: continue after error in ''Ensure System Log Files Have Correct Permissions - Set rsyslog logfile configuration
      facts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure System Log Files Have Correct Permissions - Set rsyslog logfile configuration facts'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_permissions | default(true) | bool)
- block:
  - name: Ensure System Log Files Have Correct Permissions - Get IncludeConfig directive
    ansible.builtin.shell: 'set -o pipefail

      grep -e ''$IncludeConfig'' {{ rsyslog_etc_config }} | cut -d '' '' -f 2 || true

      '
    register: rsyslog_old_inc
    changed_when: false
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_permissions
  rescue:
  - name: 'StigForge: continue after error in ''Ensure System Log Files Have Correct Permissions - Get IncludeConfig directive''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure System Log Files Have Correct Permissions - Get IncludeConfig directive'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_permissions | default(true) | bool)
- block:
  - name: Ensure System Log Files Have Correct Permissions - Get include files directives
    ansible.builtin.shell: 'set -o pipefail

      awk ''/)/{f=0} /include\(/{f=1} f{ nf=gensub("^(include\\(|\\s*)file=\"(\\S+)\".*","\\2",1); if($0!=nf){ print nf }}''
      {{ rsyslog_etc_config }} || true

      '
    register: rsyslog_new_inc
    changed_when: false
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_permissions
  rescue:
  - name: 'StigForge: continue after error in ''Ensure System Log Files Have Correct Permissions - Get include files directives''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure System Log Files Have Correct Permissions - Get include files directives'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_permissions | default(true) | bool)
- block:
  - name: Ensure System Log Files Have Correct Permissions - Aggregate rsyslog includes
    ansible.builtin.set_fact:
      include_config_output: '{{ rsyslog_old_inc.stdout_lines + rsyslog_new_inc.stdout_lines }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    - rsyslog_old_inc is not skipped and rsyslog_new_inc is not skipped
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_permissions
  rescue:
  - name: 'StigForge: continue after error in ''Ensure System Log Files Have Correct Permissions - Aggregate rsyslog includes''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure System Log Files Have Correct Permissions - Aggregate rsyslog includes'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_permissions | default(true) | bool)
- block:
  - name: Ensure System Log Files Have Correct Permissions - List all config files
    ansible.builtin.find:
      paths: '{{ item | dirname }}'
      patterns: '{{ item | basename }}'
      hidden: false
      follow: true
    loop: '{{ include_config_output | list + [rsyslog_etc_config] }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    - include_config_output is defined
    register: rsyslog_config_files
    failed_when: false
    changed_when: false
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_permissions
  rescue:
  - name: 'StigForge: continue after error in ''Ensure System Log Files Have Correct Permissions - List all config files''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure System Log Files Have Correct Permissions - List all config files'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_permissions | default(true) | bool)
- block:
  - name: Ensure System Log Files Have Correct Permissions - Extract log files old format
    ansible.builtin.shell: 'set -o pipefail

      grep -oP ''^[^(\s|#|\$)]+[\s]+.*[\s]+-?(/+[^:;\s]+);*\.*$'' {{ item.1.path }} | \

      awk ''{print $NF}'' | \

      sed -e ''s/^-//'' || true

      '
    loop: '{{ rsyslog_config_files.results | default([]) | subelements(''files'') }}'
    register: log_files_old
    changed_when: false
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    - rsyslog_config_files is not skipped
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_permissions
  rescue:
  - name: 'StigForge: continue after error in ''Ensure System Log Files Have Correct Permissions - Extract log files old format''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure System Log Files Have Correct Permissions - Extract log files old format'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_permissions | default(true) | bool)
- block:
  - name: Ensure System Log Files Have Correct Permissions - Extract log files new format
    ansible.builtin.shell: 'set -o pipefail

      grep -ozP "action\s*\(\s*type\s*=\s*\"omfile\"[^\)]*\)" {{ item.1.path }} | \

      grep -aoP "\bFile\s*=\s*\"([/[:alnum:][:punct:]]*)\"\s*\)" | \

      grep -oE "\"([/[:alnum:][:punct:]]*)\"" | \

      tr -d "\""|| true

      '
    loop: '{{ rsyslog_config_files.results | default([]) | subelements(''files'') }}'
    register: log_files_new
    changed_when: false
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    - rsyslog_config_files is not skipped
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_permissions
  rescue:
  - name: 'StigForge: continue after error in ''Ensure System Log Files Have Correct Permissions - Extract log files new format''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure System Log Files Have Correct Permissions - Extract log files new format'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_permissions | default(true) | bool)
- block:
  - name: Ensure System Log Files Have Correct Permissions - Sum all log files found
    ansible.builtin.set_fact:
      log_files: '{{ log_files_new.results | map(attribute=''stdout_lines'') | list | flatten | unique + log_files_old.results
        | map(attribute=''stdout_lines'') | list | flatten | unique }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_permissions
  rescue:
  - name: 'StigForge: continue after error in ''Ensure System Log Files Have Correct Permissions - Sum all log files found''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure System Log Files Have Correct Permissions - Sum all log files found'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_permissions | default(true) | bool)
- block:
  - name: Ensure System Log Files Have Correct Permissions -Setup log files attribute
    ansible.builtin.file:
      path: '{{ item }}'
      mode: '0640'
      state: file
    loop: '{{ log_files | list | flatten | unique }}'
    failed_when: false
    when:
    - '"kernel" in ansible_facts.packages'
    - '"rsyslog" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.1
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - configure_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_files_permissions
  rescue:
  - name: 'StigForge: continue after error in ''Ensure System Log Files Have Correct Permissions -Setup log files attribute''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure System Log Files Have Correct Permissions -Setup log files attribute'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_files_permissions | default(true) | bool)
- block:
  - name: Setting unquoted shell-style assignment of 'Compress' to 'yes' in '/etc/systemd/journald.conf'
    block:
    - name: Check for duplicate values
      ansible.builtin.lineinfile:
        path: /etc/systemd/journald.conf
        create: true
        regexp: (?i)^\s*Compress=
        state: absent
      check_mode: true
      changed_when: false
      register: dupes
    - name: Deduplicate values from /etc/systemd/journald.conf
      ansible.builtin.lineinfile:
        path: /etc/systemd/journald.conf
        create: true
        regexp: (?i)^\s*Compress=
        state: absent
      when: dupes.found is defined and dupes.found > 1
    - name: Insert correct line to /etc/systemd/journald.conf
      ansible.builtin.lineinfile:
        path: /etc/systemd/journald.conf
        create: true
        regexp: (?i)^\s*Compress=
        line: Compress=yes
        state: present
        insertbefore: ^# Compress
        validate: /usr/bin/bash -n %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - journald_compress
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Setting unquoted shell-style assignment of ''Compress'' to ''yes'' in ''/etc/systemd/journald.conf''"
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Setting unquoted shell-style assignment of ''Compress'' to ''yes'' in ''/etc/systemd/journald.conf''"
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_journald_compress | default(true) | bool)
- block:
  - name: Setting unquoted shell-style assignment of 'ForwardToSyslog' to 'yes' in '/etc/systemd/journald.conf'
    block:
    - name: Check for duplicate values
      ansible.builtin.lineinfile:
        path: /etc/systemd/journald.conf
        create: true
        regexp: (?i)^\s*ForwardToSyslog=
        state: absent
      check_mode: true
      changed_when: false
      register: dupes
    - name: Deduplicate values from /etc/systemd/journald.conf
      ansible.builtin.lineinfile:
        path: /etc/systemd/journald.conf
        create: true
        regexp: (?i)^\s*ForwardToSyslog=
        state: absent
      when: dupes.found is defined and dupes.found > 1
    - name: Insert correct line to /etc/systemd/journald.conf
      ansible.builtin.lineinfile:
        path: /etc/systemd/journald.conf
        create: true
        regexp: (?i)^\s*ForwardToSyslog=
        line: ForwardToSyslog=yes
        state: present
        insertbefore: ^# ForwardToSyslog
        validate: /usr/bin/bash -n %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - journald_forward_to_syslog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Setting unquoted shell-style assignment of ''ForwardToSyslog'' to ''yes'' in
      ''/etc/systemd/journald.conf''" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Setting unquoted shell-style assignment of ''ForwardToSyslog'' to ''yes'' in ''/etc/systemd/journald.conf''"
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_journald_forward_to_syslog | default(true) | bool)
- block:
  - name: Setting unquoted shell-style assignment of 'Storage' to 'persistent' in '/etc/systemd/journald.conf'
    block:
    - name: Check for duplicate values
      ansible.builtin.lineinfile:
        path: /etc/systemd/journald.conf
        create: true
        regexp: (?i)^\s*Storage=
        state: absent
      check_mode: true
      changed_when: false
      register: dupes
    - name: Deduplicate values from /etc/systemd/journald.conf
      ansible.builtin.lineinfile:
        path: /etc/systemd/journald.conf
        create: true
        regexp: (?i)^\s*Storage=
        state: absent
      when: dupes.found is defined and dupes.found > 1
    - name: Insert correct line to /etc/systemd/journald.conf
      ansible.builtin.lineinfile:
        path: /etc/systemd/journald.conf
        create: true
        regexp: (?i)^\s*Storage=
        line: Storage=persistent
        state: present
        insertbefore: ^# Storage
        validate: /usr/bin/bash -n %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - journald_storage
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Setting unquoted shell-style assignment of ''Storage'' to ''persistent'' in
      ''/etc/systemd/journald.conf''" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Setting unquoted shell-style assignment of ''Storage'' to ''persistent'' in ''/etc/systemd/journald.conf''"
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_journald_storage | default(true) | bool)
- block:
  - name: Disable systemd-journal-remote Socket - Collect systemd Socket Units Present in the System
    ansible.builtin.command:
      cmd: systemctl -q list-unit-files --type socket
    register: result_systemd_unit_files
    changed_when: false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - socket_systemd-journal-remote_disabled
  rescue:
  - name: 'StigForge: continue after error in ''Disable systemd-journal-remote Socket - Collect systemd Socket Units Present
      in the System'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable systemd-journal-remote Socket - Collect systemd Socket Units Present in the System'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Disable systemd-journal-remote Socket - Ensure systemd-journal-remote.socket is Masked
    ansible.builtin.systemd:
      name: systemd-journal-remote.socket
      state: stopped
      enabled: false
      masked: true
    when:
    - '"kernel" in ansible_facts.packages'
    - result_systemd_unit_files.stdout_lines is search("systemd-journal-remote.socket")
    tags:
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - socket_systemd-journal-remote_disabled
  rescue:
  - name: 'StigForge: continue after error in ''Disable systemd-journal-remote Socket - Ensure systemd-journal-remote.socket
      is Masked'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable systemd-journal-remote Socket - Ensure systemd-journal-remote.socket is Masked'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Define Rsyslog Config Lines Regex in
      Legacy Syntax
    ansible.builtin.set_fact:
      rsyslog_listen_legacy_regex: ^\s*\$(((Input(TCP|RELP)|UDP)ServerRun)|ModLoad\s+(imtcp|imudp|imrelp))
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Define Rsyslog Config Lines Regex in Legacy Syntax'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Define Rsyslog Config
        Lines Regex in Legacy Syntax'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_nolisten | default(true) | bool)
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Search for Legacy Config Lines in Rsyslog
      Main Config File
    ansible.builtin.find:
      paths: /etc
      pattern: rsyslog.conf
      contains: '{{ rsyslog_listen_legacy_regex }}'
    register: rsyslog_listen_legacy_main_file
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Search for Legacy Config Lines in Rsyslog Main Config File'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Search for Legacy Config
        Lines in Rsyslog Main Config File'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_nolisten | default(true) | bool)
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Search for Legacy Config Lines in Rsyslog
      Include Files
    ansible.builtin.find:
      paths: /etc/rsyslog.d/
      pattern: '*.conf'
      contains: '{{ rsyslog_listen_legacy_regex }}'
    register: rsyslog_listen_legacy_include_files
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Search for Legacy Config Lines in Rsyslog Include Files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Search for Legacy Config
        Lines in Rsyslog Include Files'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_nolisten | default(true) | bool)
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Assemble List of Config Files With
      Listen Lines in Legacy Syntax
    ansible.builtin.set_fact:
      rsyslog_legacy_remote_listen_files: '{{ rsyslog_listen_legacy_main_file.files | map(attribute=''path'') | list + rsyslog_listen_legacy_include_files.files
        | map(attribute=''path'') | list }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Assemble List of Config Files With Listen Lines in Legacy Syntax'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Assemble List of Config
        Files With Listen Lines in Legacy Syntax'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_nolisten | default(true) | bool)
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Comment Listen Config Lines Wherever
      Defined Using Legacy Syntax
    ansible.builtin.replace:
      path: '{{ item }}'
      regexp: '{{ rsyslog_listen_legacy_regex }}'
      replace: '# \1'
    loop: '{{ rsyslog_legacy_remote_listen_files }}'
    register: rsyslog_listen_legacy_comment
    when:
    - '"kernel" in ansible_facts.packages'
    - rsyslog_legacy_remote_listen_files | length > 0
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Comment Listen Config Lines Wherever Defined Using Legacy Syntax'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Comment Listen Config
        Lines Wherever Defined Using Legacy Syntax'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_nolisten | default(true) | bool)
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Define Rsyslog Config Lines Regex in
      RainerScript Syntax
    ansible.builtin.set_fact:
      rsyslog_listen_rainer_regex: ^\s*(module|input)\((load|type)="(imtcp|imudp)".*$
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Define Rsyslog Config Lines Regex in RainerScript Syntax'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Define Rsyslog Config
        Lines Regex in RainerScript Syntax'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_nolisten | default(true) | bool)
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Search for RainerScript Config Lines
      in Rsyslog Main Config File
    ansible.builtin.find:
      paths: /etc
      pattern: rsyslog.conf
      contains: '{{ rsyslog_listen_rainer_regex }}'
    register: rsyslog_rainer_remote_main_file
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Search for RainerScript Config Lines in Rsyslog Main Config File'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Search for RainerScript
        Config Lines in Rsyslog Main Config File'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_nolisten | default(true) | bool)
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Search for RainerScript Config Lines
      in Rsyslog Include Files
    ansible.builtin.find:
      paths: /etc/rsyslog.d/
      pattern: '*.conf'
      contains: '{{ rsyslog_listen_rainer_regex }}'
    register: rsyslog_rainer_remote_include_files
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Search for RainerScript Config Lines in Rsyslog Include Files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Search for RainerScript
        Config Lines in Rsyslog Include Files'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_nolisten | default(true) | bool)
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Assemble List of Config Files With
      Listen Lines in RainerScript
    ansible.builtin.set_fact:
      rsyslog_rainer_remote_listen_files: '{{ rsyslog_rainer_remote_main_file.files | map(attribute=''path'') | list + rsyslog_rainer_remote_include_files.files
        | map(attribute=''path'') | list }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Assemble List of Config Files With Listen Lines in RainerScript'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Assemble List of Config
        Files With Listen Lines in RainerScript'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_nolisten | default(true) | bool)
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Comment Listen Config Lines Wherever
      Defined Using RainerScript
    ansible.builtin.replace:
      path: '{{ item }}'
      regexp: '{{ rsyslog_listen_rainer_regex }}'
      replace: '# \1'
    loop: '{{ rsyslog_rainer_remote_listen_files }}'
    register: rsyslog_listen_rainer_comment
    when:
    - '"kernel" in ansible_facts.packages'
    - rsyslog_rainer_remote_listen_files | length > 0
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Comment Listen Config Lines Wherever Defined Using RainerScript'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Comment Listen Config
        Lines Wherever Defined Using RainerScript'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_nolisten | default(true) | bool)
- block:
  - name: Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Restart Rsyslog if Any Line Were Commented
      Out
    ansible.builtin.service:
      name: rsyslog
      state: restarted
    when:
    - '"kernel" in ansible_facts.packages'
    - rsyslog_listen_legacy_comment is changed or rsyslog_listen_rainer_comment is changed
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - rsyslog_nolisten
  rescue:
  - name: 'StigForge: continue after error in ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server
      - Restart Rsyslog if Any Line Were Commented Out'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure rsyslog Does Not Accept Remote Messages Unless Acting As Log Server - Restart Rsyslog if Any
        Line Were Commented Out'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_rsyslog_nolisten | default(true) | bool)
- block:
  - name: Configure Firewalld to Restrict Loopback Traffic - Remediation is Applicable if firewalld Service is Running
    block:
    - name: Configure Firewalld to Restrict Loopback Traffic - Ensure firewalld trusted Zone Restricts IPv4 Loopback Traffic
      ansible.builtin.command:
        cmd: firewall-cmd --permanent --zone=trusted --add-rich-rule='rule family=ipv4 source address="127.0.0.1" destination
          not address="127.0.0.1" drop'
      register: result_trusted_ipv4_restriction
      changed_when:
      - '''ALREADY_ENABLED'' not in result_trusted_ipv4_restriction.stderr'
    - name: Configure Firewalld to Restrict Loopback Traffic - Ensure firewalld trusted Zone Restricts IPv6 Loopback Traffic
      ansible.builtin.command:
        cmd: firewall-cmd --permanent --zone=trusted --add-rich-rule='rule family=ipv6 source address="::1" destination not
          address="::1" drop'
      register: result_trusted_ipv6_restriction
      changed_when:
      - '''ALREADY_ENABLED'' not in result_trusted_ipv6_restriction.stderr'
    - name: Configure Firewalld to Restrict Loopback Traffic - Ensure firewalld Changes are Applied
      ansible.builtin.service:
        name: firewalld
        state: reloaded
      when:
      - result_trusted_ipv4_restriction is changed or result_trusted_ipv6_restriction is changed
    when:
    - '"kernel" in ansible_facts.packages'
    - ansible_facts.services['firewalld.service'].state == 'running'
    tags:
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - configure_strategy
    - firewalld_loopback_traffic_restricted
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Configure Firewalld to Restrict Loopback Traffic - Remediation is Applicable
      if firewalld Service is Running'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Firewalld to Restrict Loopback Traffic - Remediation is Applicable if firewalld Service
        is Running'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_firewalld_loopback_traffic_restricted | default(true)
    | bool)
- block:
  - name: Configure Firewalld to Restrict Loopback Traffic - Informative Message Based on Service State
    ansible.builtin.assert:
      that:
      - ansible_check_mode or ansible_facts.services['firewalld.service'].state == 'running'
      fail_msg:
      - firewalld service is not active. Remediation aborted!
      - This remediation could not be applied because it depends on firewalld service running.
      - The service is not started by this remediation in order to prevent connection issues.
      success_msg:
      - Configure Firewalld to Restrict Loopback Traffic remediation successfully executed
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - configure_strategy
    - firewalld_loopback_traffic_restricted
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Configure Firewalld to Restrict Loopback Traffic - Informative Message Based
      on Service State'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Firewalld to Restrict Loopback Traffic - Informative Message Based on Service State'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_firewalld_loopback_traffic_restricted | default(true)
    | bool)
- block:
  - name: Configure Firewalld to Trust Loopback Traffic - Remediation is Applicable if firewalld Service is Running
    block:
    - name: Configure Firewalld to Trust Loopback Traffic - Ensure firewalld trusted Zone Includes lo Interface
      ansible.builtin.command:
        cmd: firewall-cmd --permanent --zone=trusted --add-interface=lo
      register: result_lo_interface_assignment
      changed_when:
      - '''ALREADY_ENABLED'' not in result_lo_interface_assignment.stderr'
    - name: Configure Firewalld to Trust Loopback Traffic - Ensure firewalld Changes are Applied
      ansible.builtin.service:
        name: firewalld
        state: reloaded
      when:
      - result_lo_interface_assignment is changed
    when:
    - '"kernel" in ansible_facts.packages'
    - ansible_facts.services['firewalld.service'].state == 'running'
    tags:
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - configure_strategy
    - firewalld_loopback_traffic_trusted
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Configure Firewalld to Trust Loopback Traffic - Remediation is Applicable
      if firewalld Service is Running'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Firewalld to Trust Loopback Traffic - Remediation is Applicable if firewalld Service is
        Running'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_firewalld_loopback_traffic_trusted | default(true)
    | bool)
- block:
  - name: Configure Firewalld to Trust Loopback Traffic - Informative Message Based on Service State
    ansible.builtin.assert:
      that:
      - ansible_check_mode or ansible_facts.services['firewalld.service'].state == 'running'
      fail_msg:
      - firewalld service is not active. Remediation aborted!
      - This remediation could not be applied because it depends on firewalld service running.
      - The service is not started by this remediation in order to prevent connection issues.
      success_msg:
      - Configure Firewalld to Trust Loopback Traffic remediation successfully executed
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.1
    - configure_strategy
    - firewalld_loopback_traffic_trusted
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Configure Firewalld to Trust Loopback Traffic - Informative Message Based
      on Service State'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure Firewalld to Trust Loopback Traffic - Informative Message Based on Service State'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_firewalld_loopback_traffic_trusted | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv6.conf.all.accept_ra.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_all_accept_ra | default(true)
    | bool)
- block:
  - name: Comment out any occurrences of net.ipv6.conf.all.accept_ra from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv6.conf.all.accept_ra
      replace: '#net.ipv6.conf.all.accept_ra'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.all.accept_ra from config files''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.all.accept_ra from config files'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_all_accept_ra | default(true)
    | bool)
- block:
  - name: Ensure sysctl net.ipv6.conf.all.accept_ra is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.all.accept_ra
      value: '{{ sysctl_net_ipv6_conf_all_accept_ra_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv6.conf.all.accept_ra is set'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv6.conf.all.accept_ra is set'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_all_accept_ra | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv6.conf.all.accept_redirects.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_all_accept_redirects | default(true)
    | bool)
- block:
  - name: Comment out any occurrences of net.ipv6.conf.all.accept_redirects from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv6.conf.all.accept_redirects
      replace: '#net.ipv6.conf.all.accept_redirects'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.all.accept_redirects from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.all.accept_redirects from config files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_all_accept_redirects | default(true)
    | bool)
- block:
  - name: Ensure sysctl net.ipv6.conf.all.accept_redirects is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.all.accept_redirects
      value: '{{ sysctl_net_ipv6_conf_all_accept_redirects_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv6.conf.all.accept_redirects is set'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv6.conf.all.accept_redirects is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_all_accept_redirects | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv6.conf.all.accept_source_route.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_all_accept_source_route | default(true)
    | bool)
- block:
  - name: Comment out any occurrences of net.ipv6.conf.all.accept_source_route from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv6.conf.all.accept_source_route
      replace: '#net.ipv6.conf.all.accept_source_route'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.all.accept_source_route from
      config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.all.accept_source_route from config files'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_all_accept_source_route | default(true)
    | bool)
- block:
  - name: Ensure sysctl net.ipv6.conf.all.accept_source_route is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.all.accept_source_route
      value: '{{ sysctl_net_ipv6_conf_all_accept_source_route_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv6.conf.all.accept_source_route is set'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv6.conf.all.accept_source_route is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_all_accept_source_route | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv6.conf.all.forwarding.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_all_forwarding | default(true)
    | bool)
- block:
  - name: Comment out any occurrences of net.ipv6.conf.all.forwarding from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv6.conf.all.forwarding
      replace: '#net.ipv6.conf.all.forwarding'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.all.forwarding from config files''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.all.forwarding from config files'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_all_forwarding | default(true)
    | bool)
- block:
  - name: Ensure sysctl net.ipv6.conf.all.forwarding is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.all.forwarding
      value: '{{ sysctl_net_ipv6_conf_all_forwarding_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_all_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv6.conf.all.forwarding is set'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv6.conf.all.forwarding is set'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_all_forwarding | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv6.conf.default.accept_ra.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_default_accept_ra | default(true)
    | bool)
- block:
  - name: Comment out any occurrences of net.ipv6.conf.default.accept_ra from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv6.conf.default.accept_ra
      replace: '#net.ipv6.conf.default.accept_ra'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.default.accept_ra from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.default.accept_ra from config files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_default_accept_ra | default(true)
    | bool)
- block:
  - name: Ensure sysctl net.ipv6.conf.default.accept_ra is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.default.accept_ra
      value: '{{ sysctl_net_ipv6_conf_default_accept_ra_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_ra
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv6.conf.default.accept_ra is set'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv6.conf.default.accept_ra is set'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_default_accept_ra | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv6.conf.default.accept_redirects.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_default_accept_redirects | default(true)
    | bool)
- block:
  - name: Comment out any occurrences of net.ipv6.conf.default.accept_redirects from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv6.conf.default.accept_redirects
      replace: '#net.ipv6.conf.default.accept_redirects'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.default.accept_redirects from
      config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.default.accept_redirects from config files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_default_accept_redirects | default(true)
    | bool)
- block:
  - name: Ensure sysctl net.ipv6.conf.default.accept_redirects is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.default.accept_redirects
      value: '{{ sysctl_net_ipv6_conf_default_accept_redirects_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv6.conf.default.accept_redirects is set'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv6.conf.default.accept_redirects is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_default_accept_redirects | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv6.conf.default.accept_source_route.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_default_accept_source_route |
    default(true) | bool)
- block:
  - name: Comment out any occurrences of net.ipv6.conf.default.accept_source_route from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv6.conf.default.accept_source_route
      replace: '#net.ipv6.conf.default.accept_source_route'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv6.conf.default.accept_source_route from
      config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv6.conf.default.accept_source_route from config files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_default_accept_source_route |
    default(true) | bool)
- block:
  - name: Ensure sysctl net.ipv6.conf.default.accept_source_route is set
    ansible.posix.sysctl:
      name: net.ipv6.conf.default.accept_source_route
      value: '{{ sysctl_net_ipv6_conf_default_accept_source_route_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(b)
    - NIST-800-53-CM-6.1(iv)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv6_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv6.conf.default.accept_source_route is set'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv6.conf.default.accept_source_route is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv6_conf_default_accept_source_route |
    default(true) | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.all.accept_redirects.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_all_accept_redirects | default(true)
    | bool)
- block:
  - name: Comment out any occurrences of net.ipv4.conf.all.accept_redirects from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.all.accept_redirects
      replace: '#net.ipv4.conf.all.accept_redirects'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.all.accept_redirects from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.all.accept_redirects from config files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_all_accept_redirects | default(true)
    | bool)
- block:
  - name: Ensure sysctl net.ipv4.conf.all.accept_redirects is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.all.accept_redirects
      value: '{{ sysctl_net_ipv4_conf_all_accept_redirects_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.all.accept_redirects is set'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.all.accept_redirects is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_all_accept_redirects | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.all.accept_source_route.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_all_accept_source_route | default(true)
    | bool)
- block:
  - name: Comment out any occurrences of net.ipv4.conf.all.accept_source_route from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.all.accept_source_route
      replace: '#net.ipv4.conf.all.accept_source_route'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.all.accept_source_route from
      config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.all.accept_source_route from config files'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_all_accept_source_route | default(true)
    | bool)
- block:
  - name: Ensure sysctl net.ipv4.conf.all.accept_source_route is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.all.accept_source_route
      value: '{{ sysctl_net_ipv4_conf_all_accept_source_route_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.all.accept_source_route is set'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.all.accept_source_route is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_all_accept_source_route | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.all.log_martians.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_all_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_all_log_martians | default(true)
    | bool)
- block:
  - name: Comment out any occurrences of net.ipv4.conf.all.log_martians from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.all.log_martians
      replace: '#net.ipv4.conf.all.log_martians'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_all_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.all.log_martians from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.all.log_martians from config files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_all_log_martians | default(true)
    | bool)
- block:
  - name: Ensure sysctl net.ipv4.conf.all.log_martians is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.all.log_martians
      value: '{{ sysctl_net_ipv4_conf_all_log_martians_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_all_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.all.log_martians is set'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.all.log_martians is set'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_all_log_martians | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.all.rp_filter.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_all_rp_filter | default(true)
    | bool)
- block:
  - name: Comment out any occurrences of net.ipv4.conf.all.rp_filter from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.all.rp_filter
      replace: '#net.ipv4.conf.all.rp_filter'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.all.rp_filter from config files''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.all.rp_filter from config files'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_all_rp_filter | default(true)
    | bool)
- block:
  - name: Ensure sysctl net.ipv4.conf.all.rp_filter is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.all.rp_filter
      value: '{{ sysctl_net_ipv4_conf_all_rp_filter_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.all.rp_filter is set'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.all.rp_filter is set'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_all_rp_filter | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.all.secure_redirects.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_all_secure_redirects | default(true)
    | bool)
- block:
  - name: Comment out any occurrences of net.ipv4.conf.all.secure_redirects from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.all.secure_redirects
      replace: '#net.ipv4.conf.all.secure_redirects'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.all.secure_redirects from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.all.secure_redirects from config files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_all_secure_redirects | default(true)
    | bool)
- block:
  - name: Ensure sysctl net.ipv4.conf.all.secure_redirects is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.all.secure_redirects
      value: '{{ sysctl_net_ipv4_conf_all_secure_redirects_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.all.secure_redirects is set'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.all.secure_redirects is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_all_secure_redirects | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.default.accept_redirects.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_default_accept_redirects | default(true)
    | bool)
- block:
  - name: Comment out any occurrences of net.ipv4.conf.default.accept_redirects from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.default.accept_redirects
      replace: '#net.ipv4.conf.default.accept_redirects'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.default.accept_redirects from
      config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.default.accept_redirects from config files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_default_accept_redirects | default(true)
    | bool)
- block:
  - name: Ensure sysctl net.ipv4.conf.default.accept_redirects is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.default.accept_redirects
      value: '{{ sysctl_net_ipv4_conf_default_accept_redirects_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.default.accept_redirects is set'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.default.accept_redirects is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_default_accept_redirects | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.default.accept_source_route.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_default_accept_source_route |
    default(true) | bool)
- block:
  - name: Comment out any occurrences of net.ipv4.conf.default.accept_source_route from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.default.accept_source_route
      replace: '#net.ipv4.conf.default.accept_source_route'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.default.accept_source_route from
      config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.default.accept_source_route from config files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_default_accept_source_route |
    default(true) | bool)
- block:
  - name: Ensure sysctl net.ipv4.conf.default.accept_source_route is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.default.accept_source_route
      value: '{{ sysctl_net_ipv4_conf_default_accept_source_route_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_accept_source_route
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.default.accept_source_route is set'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.default.accept_source_route is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_default_accept_source_route |
    default(true) | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.default.log_martians.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_default_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_default_log_martians | default(true)
    | bool)
- block:
  - name: Comment out any occurrences of net.ipv4.conf.default.log_martians from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.default.log_martians
      replace: '#net.ipv4.conf.default.log_martians'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_default_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.default.log_martians from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.default.log_martians from config files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_default_log_martians | default(true)
    | bool)
- block:
  - name: Ensure sysctl net.ipv4.conf.default.log_martians is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.default.log_martians
      value: '{{ sysctl_net_ipv4_conf_default_log_martians_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(3)(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_conf_default_log_martians
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.default.log_martians is set'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.default.log_martians is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_default_log_martians | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.default.rp_filter.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_default_rp_filter | default(true)
    | bool)
- block:
  - name: Comment out any occurrences of net.ipv4.conf.default.rp_filter from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.default.rp_filter
      replace: '#net.ipv4.conf.default.rp_filter'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.default.rp_filter from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.default.rp_filter from config files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_default_rp_filter | default(true)
    | bool)
- block:
  - name: Ensure sysctl net.ipv4.conf.default.rp_filter is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.default.rp_filter
      value: '{{ sysctl_net_ipv4_conf_default_rp_filter_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_rp_filter
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.default.rp_filter is set'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.default.rp_filter is set'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_default_rp_filter | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.default.secure_redirects.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_default_secure_redirects | default(true)
    | bool)
- block:
  - name: Comment out any occurrences of net.ipv4.conf.default.secure_redirects from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.default.secure_redirects
      replace: '#net.ipv4.conf.default.secure_redirects'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.default.secure_redirects from
      config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.default.secure_redirects from config files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_default_secure_redirects | default(true)
    | bool)
- block:
  - name: Ensure sysctl net.ipv4.conf.default.secure_redirects is set
    ansible.posix.sysctl:
      name: net.ipv4.conf.default.secure_redirects
      value: '{{ sysctl_net_ipv4_conf_default_secure_redirects_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_secure_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.default.secure_redirects is set'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.default.secure_redirects is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_default_secure_redirects | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.icmp_echo_ignore_broadcasts.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_icmp_echo_ignore_broadcasts
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_icmp_echo_ignore_broadcasts | default(true)
    | bool)
- block:
  - name: Comment out any occurrences of net.ipv4.icmp_echo_ignore_broadcasts from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.icmp_echo_ignore_broadcasts
      replace: '#net.ipv4.icmp_echo_ignore_broadcasts'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_icmp_echo_ignore_broadcasts
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.icmp_echo_ignore_broadcasts from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.icmp_echo_ignore_broadcasts from config files'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_icmp_echo_ignore_broadcasts | default(true)
    | bool)
- block:
  - name: Ensure sysctl net.ipv4.icmp_echo_ignore_broadcasts is set
    ansible.posix.sysctl:
      name: net.ipv4.icmp_echo_ignore_broadcasts
      value: '{{ sysctl_net_ipv4_icmp_echo_ignore_broadcasts_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_icmp_echo_ignore_broadcasts
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.icmp_echo_ignore_broadcasts is set'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.icmp_echo_ignore_broadcasts is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_icmp_echo_ignore_broadcasts | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.icmp_ignore_bogus_error_responses.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    | default(true) | bool)
- block:
  - name: Comment out any occurrences of net.ipv4.icmp_ignore_bogus_error_responses from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.icmp_ignore_bogus_error_responses
      replace: '#net.ipv4.icmp_ignore_bogus_error_responses'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.icmp_ignore_bogus_error_responses
      from config files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.icmp_ignore_bogus_error_responses from config files'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    | default(true) | bool)
- block:
  - name: Ensure sysctl net.ipv4.icmp_ignore_bogus_error_responses is set
    ansible.posix.sysctl:
      name: net.ipv4.icmp_ignore_bogus_error_responses
      value: '{{ sysctl_net_ipv4_icmp_ignore_bogus_error_responses_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - PCI-DSS-Req-1.4.3
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - low_complexity
    - medium_disruption
    - reboot_required
    - sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.icmp_ignore_bogus_error_responses is set'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.icmp_ignore_bogus_error_responses is set'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_icmp_ignore_bogus_error_responses
    | default(true) | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.tcp_syncookies.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(1)
    - NIST-800-53-SC-5(2)
    - NIST-800-53-SC-5(3)(a)
    - PCI-DSS-Req-1.4.1
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_tcp_syncookies
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_tcp_syncookies | default(true) | bool)
- block:
  - name: Comment out any occurrences of net.ipv4.tcp_syncookies from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.tcp_syncookies
      replace: '#net.ipv4.tcp_syncookies'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(1)
    - NIST-800-53-SC-5(2)
    - NIST-800-53-SC-5(3)(a)
    - PCI-DSS-Req-1.4.1
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_tcp_syncookies
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.tcp_syncookies from config files''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.tcp_syncookies from config files'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_tcp_syncookies | default(true) | bool)
- block:
  - name: Ensure sysctl net.ipv4.tcp_syncookies is set
    ansible.posix.sysctl:
      name: net.ipv4.tcp_syncookies
      value: '{{ sysctl_net_ipv4_tcp_syncookies_value }}'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5(1)
    - NIST-800-53-SC-5(2)
    - NIST-800-53-SC-5(3)(a)
    - PCI-DSS-Req-1.4.1
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_tcp_syncookies
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.tcp_syncookies is set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.tcp_syncookies is set'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_tcp_syncookies | default(true) | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.all.send_redirects.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_all_send_redirects | default(true)
    | bool)
- block:
  - name: Comment out any occurrences of net.ipv4.conf.all.send_redirects from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.all.send_redirects
      replace: '#net.ipv4.conf.all.send_redirects'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.all.send_redirects from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.all.send_redirects from config files'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_all_send_redirects | default(true)
    | bool)
- block:
  - name: Ensure sysctl net.ipv4.conf.all.send_redirects is set to 0
    ansible.posix.sysctl:
      name: net.ipv4.conf.all.send_redirects
      value: '0'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_all_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.all.send_redirects is set to 0'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.all.send_redirects is set to 0'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_all_send_redirects | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.conf.default.send_redirects.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_default_send_redirects | default(true)
    | bool)
- block:
  - name: Comment out any occurrences of net.ipv4.conf.default.send_redirects from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.conf.default.send_redirects
      replace: '#net.ipv4.conf.default.send_redirects'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.conf.default.send_redirects from config
      files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.conf.default.send_redirects from config files'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_default_send_redirects | default(true)
    | bool)
- block:
  - name: Ensure sysctl net.ipv4.conf.default.send_redirects is set to 0
    ansible.posix.sysctl:
      name: net.ipv4.conf.default.send_redirects
      value: '0'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1.1
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.5
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_conf_default_send_redirects
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.conf.default.send_redirects is set to 0'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.conf.default.send_redirects is set to 0'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_conf_default_send_redirects | default(true)
    | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*net.ipv4.ip_forward.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.3.1
    - PCI-DSS-Req-1.3.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_ip_forward
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_ip_forward | default(true) | bool)
- block:
  - name: Comment out any occurrences of net.ipv4.ip_forward from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*net.ipv4.ip_forward
      replace: '#net.ipv4.ip_forward'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.3.1
    - PCI-DSS-Req-1.3.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_ip_forward
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of net.ipv4.ip_forward from config files'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of net.ipv4.ip_forward from config files'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_ip_forward | default(true) | bool)
- block:
  - name: Ensure sysctl net.ipv4.ip_forward is set to 0
    ansible.posix.sysctl:
      name: net.ipv4.ip_forward
      value: '0'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.20
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-SC-5
    - NIST-800-53-SC-7(a)
    - PCI-DSS-Req-1.3.1
    - PCI-DSS-Req-1.3.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.3
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_net_ipv4_ip_forward
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl net.ipv4.ip_forward is set to 0'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl net.ipv4.ip_forward is set to 0'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_net_ipv4_ip_forward | default(true) | bool)
- block:
  - name: Collect Existing Nftables
    ansible.builtin.command: nft list table {{ var_nftables_family }} {{ var_nftables_table }}
    register: result_nftables_table_family
    changed_when: false
    failed_when: result_nftables_table_family.rc not in [0, 1]
    when: '"nftables" in ansible_facts.packages'
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_table
  rescue:
  - name: 'StigForge: continue after error in ''Collect Existing Nftables'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Collect Existing Nftables'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_set_nftables_table | default(true) | bool)
- block:
  - name: Set Nftable Table
    ansible.builtin.command: nft create table {{ var_nftables_family }} {{ var_nftables_table }}
    when:
    - '"nftables" in ansible_facts.packages'
    - result_nftables_table_family is not skipped
    - result_nftables_table_family.rc != 0
    tags:
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - set_nftables_table
  rescue:
  - name: 'StigForge: continue after error in ''Set Nftable Table'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set Nftable Table'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_set_nftables_table | default(true) | bool)
- block:
  - name: Ensure kernel module 'dccp' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/dccp.conf
      regexp: install\s+dccp
      line: install dccp /bin/false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - kernel_module_dccp_disabled
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''dccp'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''dccp'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_kernel_module_dccp_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'rds' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/rds.conf
      regexp: install\s+rds
      line: install rds /bin/false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_rds_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''rds'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''rds'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_kernel_module_rds_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'sctp' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/sctp.conf
      regexp: install\s+sctp
      line: install sctp /bin/false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.10.1
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-1.4.2
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - disable_strategy
    - kernel_module_sctp_disabled
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''sctp'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''sctp'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_kernel_module_sctp_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'tipc' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/tipc.conf
      regexp: install\s+tipc
      line: install tipc /bin/false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_tipc_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''tipc'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''tipc'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_kernel_module_tipc_disabled | default(true) | bool)
- block:
  - name: Verify that All World-Writable Directories Have Sticky Bits Set - Define Excluded (Non-Local) File Systems and Paths
    ansible.builtin.set_fact:
      excluded_fstypes:
      - afs
      - autofs
      - ceph
      - cifs
      - smb3
      - smbfs
      - sshfs
      - ncpfs
      - ncp
      - nfs
      - nfs4
      - gfs
      - gfs2
      - glusterfs
      - gpfs
      - pvfs2
      - ocfs2
      - lustre
      - davfs
      - fuse.sshfs
      excluded_paths:
      - dev
      - proc
      - run
      - sys
      search_paths: []
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - dir_perms_world_writable_sticky_bits
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Verify that All World-Writable Directories Have Sticky Bits Set - Define Excluded
      (Non-Local) File Systems and Paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Verify that All World-Writable Directories Have Sticky Bits Set - Define Excluded (Non-Local) File
        Systems and Paths'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_dir_perms_world_writable_sticky_bits | default(true)
    | bool)
- block:
  - name: Verify that All World-Writable Directories Have Sticky Bits Set - Find Relevant Root Directories Ignoring Pre-Defined
      Excluded Paths
    ansible.builtin.find:
      paths: /
      file_type: directory
      excludes: '{{ excluded_paths }}'
      hidden: true
      recurse: false
    register: result_relevant_root_dirs
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - dir_perms_world_writable_sticky_bits
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Verify that All World-Writable Directories Have Sticky Bits Set - Find Relevant
      Root Directories Ignoring Pre-Defined Excluded Paths'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Verify that All World-Writable Directories Have Sticky Bits Set - Find Relevant Root Directories
        Ignoring Pre-Defined Excluded Paths'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_dir_perms_world_writable_sticky_bits | default(true)
    | bool)
- block:
  - name: Verify that All World-Writable Directories Have Sticky Bits Set - Include Relevant Root Directories in a List of
      Paths to be Searched
    ansible.builtin.set_fact:
      search_paths: '{{ search_paths | union([item.path]) }}'
    loop: '{{ result_relevant_root_dirs.files }}'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - dir_perms_world_writable_sticky_bits
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Verify that All World-Writable Directories Have Sticky Bits Set - Include
      Relevant Root Directories in a List of Paths to be Searched'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Verify that All World-Writable Directories Have Sticky Bits Set - Include Relevant Root Directories
        in a List of Paths to be Searched'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_dir_perms_world_writable_sticky_bits | default(true)
    | bool)
- block:
  - name: Verify that All World-Writable Directories Have Sticky Bits Set - Increment Search Paths List with Local Partitions
      Mount Points
    ansible.builtin.set_fact:
      search_paths: '{{ search_paths | union([item.mount]) }}'
    loop: '{{ ansible_mounts }}'
    when:
    - item.fstype not in excluded_fstypes
    - item.mount != '/'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - dir_perms_world_writable_sticky_bits
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Verify that All World-Writable Directories Have Sticky Bits Set - Increment
      Search Paths List with Local Partitions Mount Points'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Verify that All World-Writable Directories Have Sticky Bits Set - Increment Search Paths List with
        Local Partitions Mount Points'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_dir_perms_world_writable_sticky_bits | default(true)
    | bool)
- block:
  - name: Verify that All World-Writable Directories Have Sticky Bits Set - Increment Search Paths List with Local NFS File
      System Targets
    ansible.builtin.set_fact:
      search_paths: '{{ search_paths | union([item.device.split('':'')[1]]) }}'
    loop: '{{ ansible_mounts }}'
    when: item.device is search("localhost:")
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - dir_perms_world_writable_sticky_bits
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Verify that All World-Writable Directories Have Sticky Bits Set - Increment
      Search Paths List with Local NFS File System Targets'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Verify that All World-Writable Directories Have Sticky Bits Set - Increment Search Paths List with
        Local NFS File System Targets'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_dir_perms_world_writable_sticky_bits | default(true)
    | bool)
- block:
  - name: Verify that All World-Writable Directories Have Sticky Bits Set - Define Rule Specific Facts
    ansible.builtin.set_fact:
      world_writable_dirs: []
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - dir_perms_world_writable_sticky_bits
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Verify that All World-Writable Directories Have Sticky Bits Set - Define Rule
      Specific Facts'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Verify that All World-Writable Directories Have Sticky Bits Set - Define Rule Specific Facts'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_dir_perms_world_writable_sticky_bits | default(true)
    | bool)
- block:
  - name: Verify that All World-Writable Directories Have Sticky Bits Set - Find All Uncompliant Directories in Local File
      Systems
    ansible.builtin.command:
      cmd: find {{ item }} -xdev -type d ( -perm -0002 -a ! -perm -1000 )
    loop: '{{ search_paths }}'
    changed_when: false
    register: result_found_dirs
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - dir_perms_world_writable_sticky_bits
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Verify that All World-Writable Directories Have Sticky Bits Set - Find All
      Uncompliant Directories in Local File Systems'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Verify that All World-Writable Directories Have Sticky Bits Set - Find All Uncompliant Directories
        in Local File Systems'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_dir_perms_world_writable_sticky_bits | default(true)
    | bool)
- block:
  - name: Verify that All World-Writable Directories Have Sticky Bits Set - Create List of World Writable Directories Without
      Sticky Bit
    ansible.builtin.set_fact:
      world_writable_dirs: '{{ world_writable_dirs | union(item.stdout_lines) | list }}'
    loop: '{{ result_found_dirs.results }}'
    when: result_found_dirs is not skipped and item is not skipped
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - dir_perms_world_writable_sticky_bits
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Verify that All World-Writable Directories Have Sticky Bits Set - Create List
      of World Writable Directories Without Sticky Bit'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Verify that All World-Writable Directories Have Sticky Bits Set - Create List of World Writable Directories
        Without Sticky Bit'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_dir_perms_world_writable_sticky_bits | default(true)
    | bool)
- block:
  - name: Verify that All World-Writable Directories Have Sticky Bits Set - Ensure Sticky Bit is Set on Local World Writable
      Directories
    ansible.builtin.file:
      path: '{{ item }}'
      mode: a+t
    loop: '{{ world_writable_dirs }}'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - dir_perms_world_writable_sticky_bits
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Verify that All World-Writable Directories Have Sticky Bits Set - Ensure Sticky
      Bit is Set on Local World Writable Directories'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Verify that All World-Writable Directories Have Sticky Bits Set - Ensure Sticky Bit is Set on Local
        World Writable Directories'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_dir_perms_world_writable_sticky_bits | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_backup_etc_group_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_backup_etc_group_newgroup: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_backup_etc_group_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_backup_etc_group_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_backup_etc_group | default(true) |
    bool)
- block:
  - name: Test for existence /etc/group-
    ansible.builtin.stat:
      path: /etc/group-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/group-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/group-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_backup_etc_group | default(true) |
    bool)
- block:
  - name: Ensure group owner on /etc/group-
    ansible.builtin.file:
      path: /etc/group-
      follow: false
      group: '{{ file_groupowner_backup_etc_group_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/group-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/group-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_backup_etc_group | default(true) |
    bool)
- block:
  - name: Set the file_groupowner_backup_etc_gshadow_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_backup_etc_gshadow_newgroup: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - configure_strategy
    - file_groupowner_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_backup_etc_gshadow_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_backup_etc_gshadow_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_backup_etc_gshadow | default(true)
    | bool)
- block:
  - name: Test for existence /etc/gshadow-
    ansible.builtin.stat:
      path: /etc/gshadow-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - configure_strategy
    - file_groupowner_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/gshadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/gshadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_backup_etc_gshadow | default(true)
    | bool)
- block:
  - name: Ensure group owner on /etc/gshadow-
    ansible.builtin.file:
      path: /etc/gshadow-
      follow: false
      group: '{{ file_groupowner_backup_etc_gshadow_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - configure_strategy
    - file_groupowner_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/gshadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/gshadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_backup_etc_gshadow | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_backup_etc_passwd_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_backup_etc_passwd_newgroup: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_backup_etc_passwd_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_backup_etc_passwd_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_backup_etc_passwd | default(true)
    | bool)
- block:
  - name: Test for existence /etc/passwd-
    ansible.builtin.stat:
      path: /etc/passwd-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/passwd-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/passwd-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_backup_etc_passwd | default(true)
    | bool)
- block:
  - name: Ensure group owner on /etc/passwd-
    ansible.builtin.file:
      path: /etc/passwd-
      follow: false
      group: '{{ file_groupowner_backup_etc_passwd_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/passwd-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/passwd-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_backup_etc_passwd | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_backup_etc_shadow_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_backup_etc_shadow_newgroup: '0'
    tags:
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_backup_etc_shadow_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_backup_etc_shadow_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_backup_etc_shadow | default(true)
    | bool)
- block:
  - name: Test for existence /etc/shadow-
    ansible.builtin.stat:
      path: /etc/shadow-
    register: file_exists
    tags:
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_backup_etc_shadow | default(true)
    | bool)
- block:
  - name: Ensure group owner on /etc/shadow-
    ansible.builtin.file:
      path: /etc/shadow-
      follow: false
      group: '{{ file_groupowner_backup_etc_shadow_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSS-Req-8.7
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/shadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/shadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_backup_etc_shadow | default(true)
    | bool)
- block:
  - name: Set the file_groupowner_etc_group_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_group_newgroup: '0'
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_group_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_group_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_group | default(true) | bool)
- block:
  - name: Test for existence /etc/group
    ansible.builtin.stat:
      path: /etc/group
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/group'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/group'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_group | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/group
    ansible.builtin.file:
      path: /etc/group
      follow: false
      group: '{{ file_groupowner_etc_group_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/group'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/group'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_group | default(true) | bool)
- block:
  - name: Set the file_groupowner_etc_gshadow_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_gshadow_newgroup: '0'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_gshadow_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_gshadow_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_gshadow | default(true) | bool)
- block:
  - name: Test for existence /etc/gshadow
    ansible.builtin.stat:
      path: /etc/gshadow
    register: file_exists
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/gshadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/gshadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_gshadow | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/gshadow
    ansible.builtin.file:
      path: /etc/gshadow
      follow: false
      group: '{{ file_groupowner_etc_gshadow_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/gshadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/gshadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_gshadow | default(true) | bool)
- block:
  - name: Set the file_groupowner_etc_passwd_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_passwd_newgroup: '0'
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_passwd_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_passwd_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_passwd | default(true) | bool)
- block:
  - name: Test for existence /etc/passwd
    ansible.builtin.stat:
      path: /etc/passwd
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_passwd | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/passwd
    ansible.builtin.file:
      path: /etc/passwd
      follow: false
      group: '{{ file_groupowner_etc_passwd_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_passwd | default(true) | bool)
- block:
  - name: Set the file_groupowner_etc_shadow_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_etc_shadow_newgroup: '0'
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_etc_shadow_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_etc_shadow_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_shadow | default(true) | bool)
- block:
  - name: Test for existence /etc/shadow
    ansible.builtin.stat:
      path: /etc/shadow
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_shadow | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/shadow
    ansible.builtin.file:
      path: /etc/shadow
      follow: false
      group: '{{ file_groupowner_etc_shadow_newgroup }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/shadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/shadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_etc_shadow | default(true) | bool)
- block:
  - name: Set the file_owner_backup_etc_group_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_backup_etc_group_newown: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_backup_etc_group_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_backup_etc_group_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_backup_etc_group | default(true) | bool)
- block:
  - name: Test for existence /etc/group-
    ansible.builtin.stat:
      path: /etc/group-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/group-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/group-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_backup_etc_group | default(true) | bool)
- block:
  - name: Ensure owner on /etc/group-
    ansible.builtin.file:
      path: /etc/group-
      follow: false
      owner: '{{ file_owner_backup_etc_group_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/group-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/group-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_backup_etc_group | default(true) | bool)
- block:
  - name: Set the file_owner_backup_etc_gshadow_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_backup_etc_gshadow_newown: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - configure_strategy
    - file_owner_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_backup_etc_gshadow_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_backup_etc_gshadow_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_backup_etc_gshadow | default(true) | bool)
- block:
  - name: Test for existence /etc/gshadow-
    ansible.builtin.stat:
      path: /etc/gshadow-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - configure_strategy
    - file_owner_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/gshadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/gshadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_backup_etc_gshadow | default(true) | bool)
- block:
  - name: Ensure owner on /etc/gshadow-
    ansible.builtin.file:
      path: /etc/gshadow-
      follow: false
      owner: '{{ file_owner_backup_etc_gshadow_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7
    - configure_strategy
    - file_owner_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/gshadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/gshadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_backup_etc_gshadow | default(true) | bool)
- block:
  - name: Set the file_owner_backup_etc_passwd_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_backup_etc_passwd_newown: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_backup_etc_passwd_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_backup_etc_passwd_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_backup_etc_passwd | default(true) | bool)
- block:
  - name: Test for existence /etc/passwd-
    ansible.builtin.stat:
      path: /etc/passwd-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/passwd-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/passwd-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_backup_etc_passwd | default(true) | bool)
- block:
  - name: Ensure owner on /etc/passwd-
    ansible.builtin.file:
      path: /etc/passwd-
      follow: false
      owner: '{{ file_owner_backup_etc_passwd_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/passwd-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/passwd-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_backup_etc_passwd | default(true) | bool)
- block:
  - name: Set the file_owner_backup_etc_shadow_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_backup_etc_shadow_newown: '0'
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_backup_etc_shadow_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_backup_etc_shadow_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_backup_etc_shadow | default(true) | bool)
- block:
  - name: Test for existence /etc/shadow-
    ansible.builtin.stat:
      path: /etc/shadow-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_backup_etc_shadow | default(true) | bool)
- block:
  - name: Ensure owner on /etc/shadow-
    ansible.builtin.file:
      path: /etc/shadow-
      follow: false
      owner: '{{ file_owner_backup_etc_shadow_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/shadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/shadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_backup_etc_shadow | default(true) | bool)
- block:
  - name: Set the file_owner_etc_group_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_group_newown: '0'
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_group_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_group_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_group | default(true) | bool)
- block:
  - name: Test for existence /etc/group
    ansible.builtin.stat:
      path: /etc/group
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/group'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/group'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_group | default(true) | bool)
- block:
  - name: Ensure owner on /etc/group
    ansible.builtin.file:
      path: /etc/group
      follow: false
      owner: '{{ file_owner_etc_group_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/group'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/group'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_group | default(true) | bool)
- block:
  - name: Set the file_owner_etc_gshadow_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_gshadow_newown: '0'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_gshadow_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_gshadow_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_gshadow | default(true) | bool)
- block:
  - name: Test for existence /etc/gshadow
    ansible.builtin.stat:
      path: /etc/gshadow
    register: file_exists
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/gshadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/gshadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_gshadow | default(true) | bool)
- block:
  - name: Ensure owner on /etc/gshadow
    ansible.builtin.file:
      path: /etc/gshadow
      follow: false
      owner: '{{ file_owner_etc_gshadow_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/gshadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/gshadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_gshadow | default(true) | bool)
- block:
  - name: Set the file_owner_etc_passwd_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_passwd_newown: '0'
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_passwd_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_passwd_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_passwd | default(true) | bool)
- block:
  - name: Test for existence /etc/passwd
    ansible.builtin.stat:
      path: /etc/passwd
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_passwd | default(true) | bool)
- block:
  - name: Ensure owner on /etc/passwd
    ansible.builtin.file:
      path: /etc/passwd
      follow: false
      owner: '{{ file_owner_etc_passwd_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_passwd | default(true) | bool)
- block:
  - name: Set the file_owner_etc_shadow_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_etc_shadow_newown: '0'
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_etc_shadow_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_etc_shadow_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_shadow | default(true) | bool)
- block:
  - name: Test for existence /etc/shadow
    ansible.builtin.stat:
      path: /etc/shadow
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_shadow | default(true) | bool)
- block:
  - name: Ensure owner on /etc/shadow
    ansible.builtin.file:
      path: /etc/shadow
      follow: false
      owner: '{{ file_owner_etc_shadow_newown }}'
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/shadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/shadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_etc_shadow | default(true) | bool)
- block:
  - name: Test for existence /etc/group-
    ansible.builtin.stat:
      path: /etc/group-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/group-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/group-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_backup_etc_group | default(true)
    | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/group-
    ansible.builtin.file:
      path: /etc/group-
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_backup_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/group-'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/group-'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_backup_etc_group | default(true)
    | bool)
- block:
  - name: Test for existence /etc/gshadow-
    ansible.builtin.stat:
      path: /etc/gshadow-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - configure_strategy
    - file_permissions_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/gshadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/gshadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_backup_etc_gshadow | default(true)
    | bool)
- block:
  - name: Ensure permission u-xwrs,g-xwrs,o-xwrt on /etc/gshadow-
    ansible.builtin.file:
      path: /etc/gshadow-
      mode: u-xwrs,g-xwrs,o-xwrt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - configure_strategy
    - file_permissions_backup_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xwrs,g-xwrs,o-xwrt on /etc/gshadow-'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xwrs,g-xwrs,o-xwrt on /etc/gshadow-'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_backup_etc_gshadow | default(true)
    | bool)
- block:
  - name: Test for existence /etc/passwd-
    ansible.builtin.stat:
      path: /etc/passwd-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/passwd-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/passwd-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_backup_etc_passwd | default(true)
    | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/passwd-
    ansible.builtin.file:
      path: /etc/passwd-
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_backup_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/passwd-'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/passwd-'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_backup_etc_passwd | default(true)
    | bool)
- block:
  - name: Test for existence /etc/shadow-
    ansible.builtin.stat:
      path: /etc/shadow-
    register: file_exists
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shadow-'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shadow-'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_backup_etc_shadow | default(true)
    | bool)
- block:
  - name: Ensure permission u-xwrs,g-xwrs,o-xwrt on /etc/shadow-
    ansible.builtin.file:
      path: /etc/shadow-
      mode: u-xwrs,g-xwrs,o-xwrt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6 (1)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_backup_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xwrs,g-xwrs,o-xwrt on /etc/shadow-'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xwrs,g-xwrs,o-xwrt on /etc/shadow-'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_backup_etc_shadow | default(true)
    | bool)
- block:
  - name: Test for existence /etc/group
    ansible.builtin.stat:
      path: /etc/group
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/group'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/group'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_etc_group | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/group
    ansible.builtin.file:
      path: /etc/group
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_etc_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/group'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/group'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_etc_group | default(true) | bool)
- block:
  - name: Test for existence /etc/gshadow
    ansible.builtin.stat:
      path: /etc/gshadow
    register: file_exists
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_permissions_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/gshadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/gshadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_etc_gshadow | default(true) | bool)
- block:
  - name: Ensure permission u-xwrs,g-xwrs,o-xwrt on /etc/gshadow
    ansible.builtin.file:
      path: /etc/gshadow
      mode: u-xwrs,g-xwrs,o-xwrt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_permissions_etc_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xwrs,g-xwrs,o-xwrt on /etc/gshadow'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xwrs,g-xwrs,o-xwrt on /etc/gshadow'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_etc_gshadow | default(true) | bool)
- block:
  - name: Test for existence /etc/passwd
    ansible.builtin.stat:
      path: /etc/passwd
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/passwd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_etc_passwd | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwt on /etc/passwd
    ansible.builtin.file:
      path: /etc/passwd
      mode: u-xs,g-xws,o-xwt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_etc_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwt on /etc/passwd'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwt on /etc/passwd'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_etc_passwd | default(true) | bool)
- block:
  - name: Test for existence /etc/shadow
    ansible.builtin.stat:
      path: /etc/shadow
    register: file_exists
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/shadow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/shadow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_etc_shadow | default(true) | bool)
- block:
  - name: Ensure permission u-xwrs,g-xwrs,o-xwrt on /etc/shadow
    ansible.builtin.file:
      path: /etc/shadow
      mode: u-xwrs,g-xwrs,o-xwrt
    when: file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - CJIS-5.5.2.2
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.7.c
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_etc_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xwrs,g-xwrs,o-xwrt on /etc/shadow'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xwrs,g-xwrs,o-xwrt on /etc/shadow'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_etc_shadow | default(true) | bool)
- block:
  - name: Ensure kernel module 'cramfs' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/cramfs.conf
      regexp: install\s+cramfs
      line: install cramfs /bin/false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_cramfs_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''cramfs'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''cramfs'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_kernel_module_cramfs_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'freevxfs' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/freevxfs.conf
      regexp: install\s+freevxfs
      line: install freevxfs /bin/false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_freevxfs_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''freevxfs'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''freevxfs'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_kernel_module_freevxfs_disabled | default(true) |
    bool)
- block:
  - name: Ensure kernel module 'hfs' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/hfs.conf
      regexp: install\s+hfs
      line: install hfs /bin/false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_hfs_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''hfs'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''hfs'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_kernel_module_hfs_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'hfsplus' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/hfsplus.conf
      regexp: install\s+hfsplus
      line: install hfsplus /bin/false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_hfsplus_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''hfsplus'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''hfsplus'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_kernel_module_hfsplus_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'jffs2' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/jffs2.conf
      regexp: install\s+jffs2
      line: install jffs2 /bin/false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_jffs2_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''jffs2'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''jffs2'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_kernel_module_jffs2_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'squashfs' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/squashfs.conf
      regexp: install\s+squashfs
      line: install squashfs /bin/false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_squashfs_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''squashfs'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''squashfs'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_kernel_module_squashfs_disabled | default(true) |
    bool)
- block:
  - name: Ensure kernel module 'udf' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/udf.conf
      regexp: install\s+udf
      line: install udf /bin/false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - kernel_module_udf_disabled
    - low_complexity
    - low_severity
    - medium_disruption
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''udf'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''udf'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_kernel_module_udf_disabled | default(true) | bool)
- block:
  - name: Ensure kernel module 'usb-storage' is disabled
    ansible.builtin.lineinfile:
      create: true
      dest: /etc/modprobe.d/usb-storage.conf
      regexp: install\s+usb-storage
      line: install usb-storage /bin/false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.21
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - PCI-DSSv4-3.4
    - PCI-DSSv4-3.4.2
    - disable_strategy
    - kernel_module_usb-storage_disabled
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
  rescue:
  - name: 'StigForge: continue after error in "Ensure kernel module ''usb-storage'' is disabled" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Ensure kernel module ''usb-storage'' is disabled" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
- block:
  - name: 'Add nodev Option to /dev/shm: Check information associated to mountpoint'
    command: findmnt  '/dev/shm'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when: ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /dev/shm: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /dev/shm: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_dev_shm_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /dev/shm: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /dev/shm: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /dev/shm: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_dev_shm_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /dev/shm
      - tmpfs
      - tmpfs
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - ("" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_dev_shm_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /dev/shm: Make sure nodev option is part of the to /dev/shm options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nodev'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - mount_info is defined and "nodev" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /dev/shm: Make sure nodev option is part of the to /dev/shm
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /dev/shm: Make sure nodev option is part of the to /dev/shm options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_dev_shm_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /dev/shm: Ensure /dev/shm is mounted with nodev option'
    mount:
      path: /dev/shm
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /dev/shm: Ensure /dev/shm is mounted with nodev option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /dev/shm: Ensure /dev/shm is mounted with nodev option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_dev_shm_nodev | default(true) | bool)
- block:
  - name: 'Add noexec Option to /dev/shm: Check information associated to mountpoint'
    command: findmnt  '/dev/shm'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when: ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /dev/shm: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /dev/shm: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_dev_shm_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /dev/shm: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /dev/shm: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /dev/shm: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_dev_shm_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /dev/shm
      - tmpfs
      - tmpfs
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - ("" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_dev_shm_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /dev/shm: Make sure noexec option is part of the to /dev/shm options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',noexec'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - mount_info is defined and "noexec" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /dev/shm: Make sure noexec option is part of the to /dev/shm
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /dev/shm: Make sure noexec option is part of the to /dev/shm options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_dev_shm_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /dev/shm: Ensure /dev/shm is mounted with noexec option'
    mount:
      path: /dev/shm
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /dev/shm: Ensure /dev/shm is mounted with noexec option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /dev/shm: Ensure /dev/shm is mounted with noexec option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_dev_shm_noexec | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /dev/shm: Check information associated to mountpoint'
    command: findmnt  '/dev/shm'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when: ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /dev/shm: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /dev/shm: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_dev_shm_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /dev/shm: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /dev/shm: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /dev/shm: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_dev_shm_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /dev/shm
      - tmpfs
      - tmpfs
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - ("" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /dev/shm: If /dev/shm not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_dev_shm_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /dev/shm: Make sure nosuid option is part of the to /dev/shm options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nosuid'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - mount_info is defined and "nosuid" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /dev/shm: Make sure nosuid option is part of the to /dev/shm
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /dev/shm: Make sure nosuid option is part of the to /dev/shm options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_dev_shm_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /dev/shm: Ensure /dev/shm is mounted with nosuid option'
    mount:
      path: /dev/shm
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_dev_shm_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /dev/shm: Ensure /dev/shm is mounted with nosuid option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /dev/shm: Ensure /dev/shm is mounted with nosuid option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_dev_shm_nosuid | default(true) | bool)
- block:
  - name: 'Add nodev Option to /home: Check information associated to mountpoint'
    command: findmnt --fstab '/home'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - mount_option_home_nodev
    - no_reboot_needed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /home: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /home: Check information associated to mountpoint'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_home_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /home: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - mount_option_home_nodev
    - no_reboot_needed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /home: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /home: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_home_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /home: If /home not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /home
      - ''
      - ''
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - mount_option_home_nodev
    - no_reboot_needed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /home: If /home not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /home: If /home not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_home_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /home: Make sure nodev option is part of the to /home options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nodev'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nodev" not in mount_info.options
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - mount_option_home_nodev
    - no_reboot_needed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /home: Make sure nodev option is part of the to /home
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /home: Make sure nodev option is part of the to /home options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_home_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /home: Ensure /home is mounted with nodev option'
    mount:
      path: /home
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - mount_option_home_nodev
    - no_reboot_needed
    - unknown_severity
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /home: Ensure /home is mounted with nodev option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /home: Ensure /home is mounted with nodev option'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_home_nodev | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /home: Check information associated to mountpoint'
    command: findmnt --fstab '/home'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_home_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /home: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /home: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_home_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /home: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_home_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /home: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /home: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_home_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /home: If /home not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /home
      - ''
      - ''
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_home_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /home: If /home not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /home: If /home not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_home_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /home: Make sure nosuid option is part of the to /home options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nosuid'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nosuid" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_home_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /home: Make sure nosuid option is part of the to /home
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /home: Make sure nosuid option is part of the to /home options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_home_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /home: Ensure /home is mounted with nosuid option'
    mount:
      path: /home
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/home" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_home_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /home: Ensure /home is mounted with nosuid option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /home: Ensure /home is mounted with nosuid option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_home_nosuid | default(true) | bool)
- block:
  - name: 'Add nodev Option to /tmp: Check information associated to mountpoint'
    command: findmnt --fstab '/tmp'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /tmp: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /tmp: Check information associated to mountpoint'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /tmp: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /tmp: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /tmp: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /tmp: If /tmp not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /tmp
      - ''
      - ''
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /tmp: If /tmp not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /tmp: If /tmp not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /tmp: Make sure nodev option is part of the to /tmp options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nodev'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nodev" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /tmp: Make sure nodev option is part of the to /tmp options''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /tmp: Make sure nodev option is part of the to /tmp options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /tmp: Ensure /tmp is mounted with nodev option'
    mount:
      path: /tmp
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /tmp: Ensure /tmp is mounted with nodev option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /tmp: Ensure /tmp is mounted with nodev option'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add noexec Option to /tmp: Check information associated to mountpoint'
    command: findmnt --fstab '/tmp'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /tmp: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /tmp: Check information associated to mountpoint'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /tmp: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /tmp: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /tmp: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /tmp: If /tmp not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /tmp
      - ''
      - ''
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /tmp: If /tmp not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /tmp: If /tmp not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /tmp: Make sure noexec option is part of the to /tmp options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',noexec'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "noexec" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /tmp: Make sure noexec option is part of the to /tmp
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /tmp: Make sure noexec option is part of the to /tmp options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /tmp: Ensure /tmp is mounted with noexec option'
    mount:
      path: /tmp
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /tmp: Ensure /tmp is mounted with noexec option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /tmp: Ensure /tmp is mounted with noexec option'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /tmp: Check information associated to mountpoint'
    command: findmnt --fstab '/tmp'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /tmp: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /tmp: Check information associated to mountpoint'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_tmp_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /tmp: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /tmp: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /tmp: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_tmp_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /tmp: If /tmp not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /tmp
      - ''
      - ''
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /tmp: If /tmp not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /tmp: If /tmp not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_tmp_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /tmp: Make sure nosuid option is part of the to /tmp options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nosuid'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nosuid" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /tmp: Make sure nosuid option is part of the to /tmp
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /tmp: Make sure nosuid option is part of the to /tmp options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_tmp_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /tmp: Ensure /tmp is mounted with nosuid option'
    mount:
      path: /tmp
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /tmp: Ensure /tmp is mounted with nosuid option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /tmp: Ensure /tmp is mounted with nosuid option'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_tmp_nosuid | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/log/audit: Check information associated to mountpoint'
    command: findmnt --fstab '/var/log/audit'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log/audit: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log/audit: Check information associated to mountpoint'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_audit_nodev | default(true) |
    bool)
- block:
  - name: 'Add nodev Option to /var/log/audit: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log/audit: Create mount_info dictionary variable''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log/audit: Create mount_info dictionary variable'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_audit_nodev | default(true) |
    bool)
- block:
  - name: 'Add nodev Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/log/audit
      - ''
      - ''
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info
      manually'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info manually'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_audit_nodev | default(true) |
    bool)
- block:
  - name: 'Add nodev Option to /var/log/audit: Make sure nodev option is part of the to /var/log/audit options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nodev'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nodev" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log/audit: Make sure nodev option is part of the
      to /var/log/audit options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log/audit: Make sure nodev option is part of the to /var/log/audit options''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_audit_nodev | default(true) |
    bool)
- block:
  - name: 'Add nodev Option to /var/log/audit: Ensure /var/log/audit is mounted with nodev option'
    mount:
      path: /var/log/audit
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log/audit: Ensure /var/log/audit is mounted with
      nodev option'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log/audit: Ensure /var/log/audit is mounted with nodev option'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_audit_nodev | default(true) |
    bool)
- block:
  - name: 'Add noexec Option to /var/log/audit: Check information associated to mountpoint'
    command: findmnt --fstab '/var/log/audit'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log/audit: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log/audit: Check information associated to mountpoint'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_audit_noexec | default(true)
    | bool)
- block:
  - name: 'Add noexec Option to /var/log/audit: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log/audit: Create mount_info dictionary variable''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log/audit: Create mount_info dictionary variable'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_audit_noexec | default(true)
    | bool)
- block:
  - name: 'Add noexec Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/log/audit
      - ''
      - ''
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log/audit: If /var/log/audit not mounted, craft
      mount_info manually'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info manually'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_audit_noexec | default(true)
    | bool)
- block:
  - name: 'Add noexec Option to /var/log/audit: Make sure noexec option is part of the to /var/log/audit options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',noexec'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "noexec" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log/audit: Make sure noexec option is part of the
      to /var/log/audit options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log/audit: Make sure noexec option is part of the to /var/log/audit options''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_audit_noexec | default(true)
    | bool)
- block:
  - name: 'Add noexec Option to /var/log/audit: Ensure /var/log/audit is mounted with noexec option'
    mount:
      path: /var/log/audit
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log/audit: Ensure /var/log/audit is mounted with
      noexec option'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log/audit: Ensure /var/log/audit is mounted with noexec option'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_audit_noexec | default(true)
    | bool)
- block:
  - name: 'Add nosuid Option to /var/log/audit: Check information associated to mountpoint'
    command: findmnt --fstab '/var/log/audit'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log/audit: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log/audit: Check information associated to mountpoint'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_audit_nosuid | default(true)
    | bool)
- block:
  - name: 'Add nosuid Option to /var/log/audit: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log/audit: Create mount_info dictionary variable''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log/audit: Create mount_info dictionary variable'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_audit_nosuid | default(true)
    | bool)
- block:
  - name: 'Add nosuid Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/log/audit
      - ''
      - ''
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log/audit: If /var/log/audit not mounted, craft
      mount_info manually'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log/audit: If /var/log/audit not mounted, craft mount_info manually'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_audit_nosuid | default(true)
    | bool)
- block:
  - name: 'Add nosuid Option to /var/log/audit: Make sure nosuid option is part of the to /var/log/audit options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nosuid'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nosuid" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log/audit: Make sure nosuid option is part of the
      to /var/log/audit options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log/audit: Make sure nosuid option is part of the to /var/log/audit options''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_audit_nosuid | default(true)
    | bool)
- block:
  - name: 'Add nosuid Option to /var/log/audit: Ensure /var/log/audit is mounted with nosuid option'
    mount:
      path: /var/log/audit
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log/audit" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_audit_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log/audit: Ensure /var/log/audit is mounted with
      nosuid option'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log/audit: Ensure /var/log/audit is mounted with nosuid option'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_audit_nosuid | default(true)
    | bool)
- block:
  - name: 'Add nodev Option to /var/log: Check information associated to mountpoint'
    command: findmnt --fstab '/var/log'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/log: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/log: If /var/log not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/log
      - ''
      - ''
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log: If /var/log not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log: If /var/log not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/log: Make sure nodev option is part of the to /var/log options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nodev'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nodev" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log: Make sure nodev option is part of the to /var/log
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log: Make sure nodev option is part of the to /var/log options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/log: Ensure /var/log is mounted with nodev option'
    mount:
      path: /var/log
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/log: Ensure /var/log is mounted with nodev option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/log: Ensure /var/log is mounted with nodev option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_nodev | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/log: Check information associated to mountpoint'
    command: findmnt --fstab '/var/log'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/log: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/log: If /var/log not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/log
      - ''
      - ''
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log: If /var/log not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log: If /var/log not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/log: Make sure noexec option is part of the to /var/log options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',noexec'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "noexec" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log: Make sure noexec option is part of the to /var/log
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log: Make sure noexec option is part of the to /var/log options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/log: Ensure /var/log is mounted with noexec option'
    mount:
      path: /var/log
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/log: Ensure /var/log is mounted with noexec option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/log: Ensure /var/log is mounted with noexec option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_noexec | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/log: Check information associated to mountpoint'
    command: findmnt --fstab '/var/log'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/log: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/log: If /var/log not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/log
      - ''
      - ''
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log: If /var/log not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log: If /var/log not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/log: Make sure nosuid option is part of the to /var/log options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nosuid'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nosuid" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log: Make sure nosuid option is part of the to /var/log
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log: Make sure nosuid option is part of the to /var/log options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/log: Ensure /var/log is mounted with nosuid option'
    mount:
      path: /var/log
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/log" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_log_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/log: Ensure /var/log is mounted with nosuid option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/log: Ensure /var/log is mounted with nosuid option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_log_nosuid | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var: Check information associated to mountpoint'
    command: findmnt --fstab '/var'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var: Check information associated to mountpoint'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var: If /var not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var
      - ''
      - ''
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var: If /var not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var: If /var not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var: Make sure nodev option is part of the to /var options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nodev'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nodev" not in mount_info.options
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var: Make sure nodev option is part of the to /var options''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var: Make sure nodev option is part of the to /var options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var: Ensure /var is mounted with nodev option'
    mount:
      path: /var
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - NIST-800-53-AC-6
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var: Ensure /var is mounted with nodev option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var: Ensure /var is mounted with nodev option'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_nodev | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var: Check information associated to mountpoint'
    command: findmnt --fstab '/var'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var: Check information associated to mountpoint'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var: Check information associated to mountpoint'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var: Create mount_info dictionary variable'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var: If /var not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var
      - ''
      - ''
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var: If /var not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var: If /var not mounted, craft mount_info manually'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var: Make sure nosuid option is part of the to /var options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nosuid'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nosuid" not in mount_info.options
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var: Make sure nosuid option is part of the to /var
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var: Make sure nosuid option is part of the to /var options'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var: Ensure /var is mounted with nosuid option'
    mount:
      path: /var
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var: Ensure /var is mounted with nosuid option'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var: Ensure /var is mounted with nosuid option'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_nosuid | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/tmp: Check information associated to mountpoint'
    command: findmnt --fstab '/var/tmp'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/tmp: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/tmp: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/tmp: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/tmp: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/tmp: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/tmp
      - ''
      - ''
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/tmp: Make sure nodev option is part of the to /var/tmp options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nodev'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nodev" not in mount_info.options
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/tmp: Make sure nodev option is part of the to /var/tmp
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/tmp: Make sure nodev option is part of the to /var/tmp options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add nodev Option to /var/tmp: Ensure /var/tmp is mounted with nodev option'
    mount:
      path: /var/tmp
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nodev
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nodev Option to /var/tmp: Ensure /var/tmp is mounted with nodev option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nodev Option to /var/tmp: Ensure /var/tmp is mounted with nodev option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_tmp_nodev | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/tmp: Check information associated to mountpoint'
    command: findmnt --fstab '/var/tmp'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/tmp: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/tmp: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/tmp: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/tmp: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/tmp: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/tmp
      - ''
      - ''
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/tmp: Make sure noexec option is part of the to /var/tmp options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',noexec'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "noexec" not in mount_info.options
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/tmp: Make sure noexec option is part of the to /var/tmp
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/tmp: Make sure noexec option is part of the to /var/tmp options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add noexec Option to /var/tmp: Ensure /var/tmp is mounted with noexec option'
    mount:
      path: /var/tmp
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_noexec
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add noexec Option to /var/tmp: Ensure /var/tmp is mounted with noexec option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add noexec Option to /var/tmp: Ensure /var/tmp is mounted with noexec option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_tmp_noexec | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/tmp: Check information associated to mountpoint'
    command: findmnt --fstab '/var/tmp'
    register: device_name
    failed_when: device_name.rc > 1
    changed_when: false
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/tmp: Check information associated to mountpoint''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/tmp: Check information associated to mountpoint'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_tmp_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/tmp: Create mount_info dictionary variable'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - '{{ device_name.stdout_lines[0].split() | list | lower }}'
    - '{{ device_name.stdout_lines[1].split() | list }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length > 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/tmp: Create mount_info dictionary variable'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/tmp: Create mount_info dictionary variable'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_tmp_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually'
    set_fact:
      mount_info: '{{ mount_info|default({})|combine({item.0: item.1}) }}'
    with_together:
    - - target
      - source
      - fstype
      - options
    - - /var/tmp
      - ''
      - ''
      - defaults
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - ("--fstab" | length == 0)
    - device_name.stdout is defined and device_name.stdout_lines is defined
    - (device_name.stdout | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/tmp: If /var/tmp not mounted, craft mount_info manually'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_tmp_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/tmp: Make sure nosuid option is part of the to /var/tmp options'
    set_fact:
      mount_info: '{{ mount_info | combine( {''options'':''''~mount_info.options~'',nosuid'' }) }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined and "nosuid" not in mount_info.options
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/tmp: Make sure nosuid option is part of the to /var/tmp
      options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/tmp: Make sure nosuid option is part of the to /var/tmp options'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_tmp_nosuid | default(true) | bool)
- block:
  - name: 'Add nosuid Option to /var/tmp: Ensure /var/tmp is mounted with nosuid option'
    mount:
      path: /var/tmp
      src: '{{ mount_info.source }}'
      opts: '{{ mount_info.options }}'
      state: mounted
      fstype: '{{ mount_info.fstype }}'
    when:
    - ( not ( "kernel" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages
      and not "openshift-kubelet" in ansible_facts.packages ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz",
      "podman", "container"] ) )
    - '"/var/tmp" in ansible_mounts | map(attribute="mount") | list'
    - mount_info is defined
    - (device_name.stdout is defined and (device_name.stdout | length > 0)) or ("--fstab" | length == 0)
    tags:
    - configure_strategy
    - high_disruption
    - low_complexity
    - medium_severity
    - mount_option_var_tmp_nosuid
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add nosuid Option to /var/tmp: Ensure /var/tmp is mounted with nosuid option''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add nosuid Option to /var/tmp: Ensure /var/tmp is mounted with nosuid option'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_mount_option_var_tmp_nosuid | default(true) | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*kernel.yama.ptrace_scope.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-SC-7(10)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_yama_ptrace_scope
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_kernel_yama_ptrace_scope | default(true) |
    bool)
- block:
  - name: Comment out any occurrences of kernel.yama.ptrace_scope from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*kernel.yama.ptrace_scope
      replace: '#kernel.yama.ptrace_scope'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-SC-7(10)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_yama_ptrace_scope
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of kernel.yama.ptrace_scope from config files''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of kernel.yama.ptrace_scope from config files'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_kernel_yama_ptrace_scope | default(true) |
    bool)
- block:
  - name: Ensure sysctl kernel.yama.ptrace_scope is set to 1
    ansible.posix.sysctl:
      name: kernel.yama.ptrace_scope
      value: '1'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-SC-7(10)
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_yama_ptrace_scope
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl kernel.yama.ptrace_scope is set to 1'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl kernel.yama.ptrace_scope is set to 1'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_kernel_yama_ptrace_scope | default(true) |
    bool)
- block:
  - name: Disable core dump backtraces - Search for a section in files
    ansible.builtin.find:
      paths: '{{item.path}}'
      patterns: '{{item.pattern}}'
      contains: ^\s*\[Coredump\]
      read_whole_file: true
      use_regex: true
    register: systemd_dropin_files_with_section
    loop:
    - path: '{{ ''/etc/systemd/coredump.conf'' | dirname }}'
      pattern: '{{ ''/etc/systemd/coredump.conf'' | basename | regex_escape }}'
    - path: /etc/systemd/coredump.conf.d
      pattern: .*\.conf
    when: '"systemd" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6
    - PCI-DSS-Req-3.2
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - coredump_disable_backtraces
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable core dump backtraces - Search for a section in files'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable core dump backtraces - Search for a section in files'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_coredump_disable_backtraces | default(true) | bool)
- block:
  - name: Disable core dump backtraces - Count number of files which contain the correct section
    ansible.builtin.set_fact:
      count_of_systemd_dropin_files_with_section: '{{systemd_dropin_files_with_section.results | map(attribute=''matched'')
        | list | map(''int'') | sum}}'
    when: '"systemd" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6
    - PCI-DSS-Req-3.2
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - coredump_disable_backtraces
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable core dump backtraces - Count number of files which contain the correct
      section'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable core dump backtraces - Count number of files which contain the correct section'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_coredump_disable_backtraces | default(true) | bool)
- block:
  - name: Disable core dump backtraces - Add missing configuration to correct section
    community.general.ini_file:
      path: '{{item}}'
      section: Coredump
      option: ProcessSizeMax
      value: '0'
      state: present
      no_extra_spaces: true
    when:
    - '"systemd" in ansible_facts.packages'
    - count_of_systemd_dropin_files_with_section | int > 0
    loop: '{{systemd_dropin_files_with_section.results | sum(attribute=''files'', start=[]) | map(attribute=''path'') | list
      }}'
    tags:
    - NIST-800-53-CM-6
    - PCI-DSS-Req-3.2
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - coredump_disable_backtraces
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable core dump backtraces - Add missing configuration to correct section''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable core dump backtraces - Add missing configuration to correct section'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_coredump_disable_backtraces | default(true) | bool)
- block:
  - name: Disable core dump backtraces - Add configuration to new remediation file
    community.general.ini_file:
      path: /etc/systemd/coredump.conf.d/complianceascode_hardening.conf
      section: Coredump
      option: ProcessSizeMax
      value: '0'
      state: present
      no_extra_spaces: true
      create: true
    when:
    - '"systemd" in ansible_facts.packages'
    - count_of_systemd_dropin_files_with_section | int == 0
    tags:
    - NIST-800-53-CM-6
    - PCI-DSS-Req-3.2
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - coredump_disable_backtraces
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable core dump backtraces - Add configuration to new remediation file''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable core dump backtraces - Add configuration to new remediation file'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_coredump_disable_backtraces | default(true) | bool)
- block:
  - name: Disable storing core dump - Search for a section in files
    ansible.builtin.find:
      paths: '{{item.path}}'
      patterns: '{{item.pattern}}'
      contains: ^\s*\[Coredump\]
      read_whole_file: true
      use_regex: true
    register: systemd_dropin_files_with_section
    loop:
    - path: '{{ ''/etc/systemd/coredump.conf'' | dirname }}'
      pattern: '{{ ''/etc/systemd/coredump.conf'' | basename | regex_escape }}'
    - path: /etc/systemd/coredump.conf.d
      pattern: .*\.conf
    when: '"systemd" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6
    - PCI-DSS-Req-3.2
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - coredump_disable_storage
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable storing core dump - Search for a section in files'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable storing core dump - Search for a section in files'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_coredump_disable_storage | default(true) | bool)
- block:
  - name: Disable storing core dump - Count number of files which contain the correct section
    ansible.builtin.set_fact:
      count_of_systemd_dropin_files_with_section: '{{systemd_dropin_files_with_section.results | map(attribute=''matched'')
        | list | map(''int'') | sum}}'
    when: '"systemd" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6
    - PCI-DSS-Req-3.2
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - coredump_disable_storage
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable storing core dump - Count number of files which contain the correct
      section'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable storing core dump - Count number of files which contain the correct section'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_coredump_disable_storage | default(true) | bool)
- block:
  - name: Disable storing core dump - Add missing configuration to correct section
    community.general.ini_file:
      path: '{{item}}'
      section: Coredump
      option: Storage
      value: none
      state: present
      no_extra_spaces: true
    when:
    - '"systemd" in ansible_facts.packages'
    - count_of_systemd_dropin_files_with_section | int > 0
    loop: '{{systemd_dropin_files_with_section.results | sum(attribute=''files'', start=[]) | map(attribute=''path'') | list
      }}'
    tags:
    - NIST-800-53-CM-6
    - PCI-DSS-Req-3.2
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - coredump_disable_storage
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable storing core dump - Add missing configuration to correct section''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable storing core dump - Add missing configuration to correct section'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_coredump_disable_storage | default(true) | bool)
- block:
  - name: Disable storing core dump - Add configuration to new remediation file
    community.general.ini_file:
      path: /etc/systemd/coredump.conf.d/complianceascode_hardening.conf
      section: Coredump
      option: Storage
      value: none
      state: present
      no_extra_spaces: true
      create: true
    when:
    - '"systemd" in ansible_facts.packages'
    - count_of_systemd_dropin_files_with_section | int == 0
    tags:
    - NIST-800-53-CM-6
    - PCI-DSS-Req-3.2
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - coredump_disable_storage
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable storing core dump - Add configuration to new remediation file'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable storing core dump - Add configuration to new remediation file'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_coredump_disable_storage | default(true) | bool)
- block:
  - name: List /etc/sysctl.d/*.conf files
    ansible.builtin.find:
      paths:
      - /etc/sysctl.d/
      - /run/sysctl.d/
      - /usr/local/lib/sysctl.d/
      - /usr/lib/sysctl.d/
      contains: ^[\s]*kernel.randomize_va_space.*$
      patterns: '*.conf'
      file_type: any
    register: find_sysctl_d
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-30
    - NIST-800-53-SC-30(2)
    - PCI-DSS-Req-2.2.1
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_randomize_va_space
  rescue:
  - name: 'StigForge: continue after error in ''List /etc/sysctl.d/*.conf files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List /etc/sysctl.d/*.conf files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_kernel_randomize_va_space | default(true) |
    bool)
- block:
  - name: Comment out any occurrences of kernel.randomize_va_space from config files
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^[\s]*kernel.randomize_va_space
      replace: '#kernel.randomize_va_space'
    loop: '{{ find_sysctl_d.files }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-30
    - NIST-800-53-SC-30(2)
    - PCI-DSS-Req-2.2.1
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_randomize_va_space
  rescue:
  - name: 'StigForge: continue after error in ''Comment out any occurrences of kernel.randomize_va_space from config files''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Comment out any occurrences of kernel.randomize_va_space from config files'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_kernel_randomize_va_space | default(true) |
    bool)
- block:
  - name: Ensure sysctl kernel.randomize_va_space is set to 2
    ansible.posix.sysctl:
      name: kernel.randomize_va_space
      value: '2'
      sysctl_file: /etc/sysctl.conf
      state: present
      reload: true
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-30
    - NIST-800-53-SC-30(2)
    - PCI-DSS-Req-2.2.1
    - PCI-DSSv4-3.3
    - PCI-DSSv4-3.3.1
    - PCI-DSSv4-3.3.1.1
    - disable_strategy
    - low_complexity
    - medium_disruption
    - medium_severity
    - reboot_required
    - sysctl_kernel_randomize_va_space
  rescue:
  - name: 'StigForge: continue after error in ''Ensure sysctl kernel.randomize_va_space is set to 2'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure sysctl kernel.randomize_va_space is set to 2'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sysctl_kernel_randomize_va_space | default(true) |
    bool)
- block:
  - name: Ensure SELinux Not Disabled in /etc/default/grub - Find /etc/grub.d/ files
    ansible.builtin.find:
      paths:
      - /etc/grub.d/
      follow: true
    register: result_grub_d
    when:
    - '"kernel" in ansible_facts.packages'
    - '"grub2-common" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.2
    - NIST-800-171-3.7.2
    - NIST-800-53-AC-3
    - NIST-800-53-AC-3(3)(a)
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.6
    - grub2_enable_selinux
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SELinux Not Disabled in /etc/default/grub - Find /etc/grub.d/ files''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SELinux Not Disabled in /etc/default/grub - Find /etc/grub.d/ files'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_grub2_enable_selinux | default(true) | bool)
- block:
  - name: Ensure SELinux Not Disabled in /etc/default/grub - Ensure SELinux Not Disabled in /etc/grub.d/ files
    ansible.builtin.replace:
      dest: '{{ item.path }}'
      regexp: (selinux|enforcing)=0
    with_items:
    - '{{ result_grub_d.files }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - '"grub2-common" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.2
    - NIST-800-171-3.7.2
    - NIST-800-53-AC-3
    - NIST-800-53-AC-3(3)(a)
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.6
    - grub2_enable_selinux
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SELinux Not Disabled in /etc/default/grub - Ensure SELinux Not Disabled
      in /etc/grub.d/ files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SELinux Not Disabled in /etc/default/grub - Ensure SELinux Not Disabled in /etc/grub.d/ files''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_grub2_enable_selinux | default(true) | bool)
- block:
  - name: Ensure SELinux Not Disabled in /etc/default/grub - Check if /etc/grub2.cfg exists
    ansible.builtin.stat:
      path: /etc/grub2.cfg
    register: result_grub2_cfg_present
    when:
    - '"kernel" in ansible_facts.packages'
    - '"grub2-common" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.2
    - NIST-800-171-3.7.2
    - NIST-800-53-AC-3
    - NIST-800-53-AC-3(3)(a)
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.6
    - grub2_enable_selinux
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SELinux Not Disabled in /etc/default/grub - Check if /etc/grub2.cfg
      exists'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SELinux Not Disabled in /etc/default/grub - Check if /etc/grub2.cfg exists'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_grub2_enable_selinux | default(true) | bool)
- block:
  - name: Ensure SELinux Not Disabled in /etc/default/grub - Check if /etc/default/grub exists
    ansible.builtin.stat:
      path: /etc/default/grub
    register: result_default_grub_present
    when:
    - '"kernel" in ansible_facts.packages'
    - '"grub2-common" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.2
    - NIST-800-171-3.7.2
    - NIST-800-53-AC-3
    - NIST-800-53-AC-3(3)(a)
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.6
    - grub2_enable_selinux
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SELinux Not Disabled in /etc/default/grub - Check if /etc/default/grub
      exists'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SELinux Not Disabled in /etc/default/grub - Check if /etc/default/grub exists'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_grub2_enable_selinux | default(true) | bool)
- block:
  - name: Ensure SELinux Not Disabled in /etc/default/grub - Ensure SELinux Not Disabled in /etc/grub2.cfg
    ansible.builtin.replace:
      dest: /etc/grub2.cfg
      regexp: (selinux|enforcing)=0
    when:
    - '"kernel" in ansible_facts.packages'
    - '"grub2-common" in ansible_facts.packages'
    - (result_grub2_cfg_present is not skipped and result_grub2_cfg_present.stat.exists)
    tags:
    - NIST-800-171-3.1.2
    - NIST-800-171-3.7.2
    - NIST-800-53-AC-3
    - NIST-800-53-AC-3(3)(a)
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.6
    - grub2_enable_selinux
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SELinux Not Disabled in /etc/default/grub - Ensure SELinux Not Disabled
      in /etc/grub2.cfg'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SELinux Not Disabled in /etc/default/grub - Ensure SELinux Not Disabled in /etc/grub2.cfg''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_grub2_enable_selinux | default(true) | bool)
- block:
  - name: Ensure SELinux Not Disabled in /etc/default/grub - Ensure SELinux Not Disabled in /etc/default/grub
    ansible.builtin.replace:
      dest: /etc/default/grub
      regexp: (selinux|enforcing)=0
    when:
    - '"kernel" in ansible_facts.packages'
    - '"grub2-common" in ansible_facts.packages'
    - (result_default_grub_present is not skipped and result_default_grub_present.stat.exists)
    tags:
    - NIST-800-171-3.1.2
    - NIST-800-171-3.7.2
    - NIST-800-53-AC-3
    - NIST-800-53-AC-3(3)(a)
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.6
    - grub2_enable_selinux
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SELinux Not Disabled in /etc/default/grub - Ensure SELinux Not Disabled
      in /etc/default/grub'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SELinux Not Disabled in /etc/default/grub - Ensure SELinux Not Disabled in /etc/default/grub''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_grub2_enable_selinux | default(true) | bool)
- block:
  - name: Ensure SELinux is Not Disabled
    block:
    - name: Check for duplicate values
      ansible.builtin.lineinfile:
        path: /etc/selinux/config
        create: true
        regexp: (?i)^SELINUX=
        state: absent
      check_mode: true
      changed_when: false
      register: dupes
    - name: Deduplicate values from /etc/selinux/config
      ansible.builtin.lineinfile:
        path: /etc/selinux/config
        create: true
        regexp: (?i)^SELINUX=
        state: absent
      when: dupes.found is defined and dupes.found > 1
    - name: Insert correct line to /etc/selinux/config
      ansible.builtin.lineinfile:
        path: /etc/selinux/config
        create: true
        regexp: (?i)^SELINUX=
        line: SELINUX=permissive
        state: present
    when: '"kernel" in ansible_facts.packages'
    tags:
    - high_severity
    - low_complexity
    - low_disruption
    - reboot_required
    - restrict_strategy
    - selinux_not_disabled
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SELinux is Not Disabled'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SELinux is Not Disabled'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_selinux_not_disabled | default(true) | bool)
- block:
  - name: ' - Mark system to relabel SELinux on next boot'
    ansible.builtin.file:
      path: /.autorelabel
      state: touch
    when: '"kernel" in ansible_facts.packages'
    tags:
    - high_severity
    - low_complexity
    - low_disruption
    - reboot_required
    - restrict_strategy
    - selinux_not_disabled
  rescue:
  - name: 'StigForge: continue after error in '' - Mark system to relabel SELinux on next boot'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: '' - Mark system to relabel SELinux on next boot'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_selinux_not_disabled | default(true) | bool)
- block:
  - name: Configure SELinux Policy
    block:
    - name: Check for duplicate values
      ansible.builtin.lineinfile:
        path: /etc/selinux/config
        create: true
        regexp: (?i)^SELINUXTYPE=
        state: absent
      check_mode: true
      changed_when: false
      register: dupes
    - name: Deduplicate values from /etc/selinux/config
      ansible.builtin.lineinfile:
        path: /etc/selinux/config
        create: true
        regexp: (?i)^SELINUXTYPE=
        state: absent
      when: dupes.found is defined and dupes.found > 1
    - name: Insert correct line to /etc/selinux/config
      ansible.builtin.lineinfile:
        path: /etc/selinux/config
        create: true
        regexp: (?i)^SELINUXTYPE=
        line: SELINUXTYPE={{ var_selinux_policy_name }}
        state: present
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.2
    - NIST-800-171-3.7.2
    - NIST-800-53-AC-3
    - NIST-800-53-AC-3(3)(a)
    - NIST-800-53-AU-9
    - NIST-800-53-SC-7(21)
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.6
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - selinux_policytype
  rescue:
  - name: 'StigForge: continue after error in ''Configure SELinux Policy'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure SELinux Policy'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_selinux_policytype | default(true) | bool)
- block:
  - name: Ensure SELinux State is Enforcing
    block:
    - name: Check for duplicate values
      ansible.builtin.lineinfile:
        path: /etc/selinux/config
        create: true
        regexp: (?i)^SELINUX=
        state: absent
      check_mode: true
      changed_when: false
      register: dupes
    - name: Deduplicate values from /etc/selinux/config
      ansible.builtin.lineinfile:
        path: /etc/selinux/config
        create: true
        regexp: (?i)^SELINUX=
        state: absent
      when: dupes.found is defined and dupes.found > 1
    - name: Insert correct line to /etc/selinux/config
      ansible.builtin.lineinfile:
        path: /etc/selinux/config
        create: true
        regexp: (?i)^SELINUX=
        line: SELINUX={{ var_selinux_state }}
        state: present
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.2
    - NIST-800-171-3.7.2
    - NIST-800-53-AC-3
    - NIST-800-53-AC-3(3)(a)
    - NIST-800-53-AU-9
    - NIST-800-53-SC-7(21)
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - selinux_state
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SELinux State is Enforcing'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SELinux State is Enforcing'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_selinux_state | default(true) | bool)
- block:
  - name: ' - Mark system to relabel SELinux on next boot'
    ansible.builtin.file:
      path: /.autorelabel
      state: touch
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.2
    - NIST-800-171-3.7.2
    - NIST-800-53-AC-3
    - NIST-800-53-AC-3(3)(a)
    - NIST-800-53-AU-9
    - NIST-800-53-SC-7(21)
    - PCI-DSSv4-1.2
    - PCI-DSSv4-1.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - selinux_state
  rescue:
  - name: 'StigForge: continue after error in '' - Mark system to relabel SELinux on next boot'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: '' - Mark system to relabel SELinux on next boot'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_selinux_state | default(true) | bool)
- block:
  - name: Set the file_groupowner_cron_d_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_cron_d_newgroup: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_cron_d_newgroup variable if represented by gid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_cron_d_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_cron_d | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/cron.d/
    ansible.builtin.file:
      path: /etc/cron.d/
      follow: false
      state: directory
      group: '{{ file_groupowner_cron_d_newgroup }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/cron.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/cron.d/'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_cron_d | default(true) | bool)
- block:
  - name: Set the file_groupowner_cron_daily_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_cron_daily_newgroup: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_daily
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_cron_daily_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_cron_daily_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_cron_daily | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/cron.daily/
    ansible.builtin.file:
      path: /etc/cron.daily/
      follow: false
      state: directory
      group: '{{ file_groupowner_cron_daily_newgroup }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_daily
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/cron.daily/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/cron.daily/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_cron_daily | default(true) | bool)
- block:
  - name: Set the file_groupowner_cron_hourly_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_cron_hourly_newgroup: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_hourly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_cron_hourly_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_cron_hourly_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_cron_hourly | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/cron.hourly/
    ansible.builtin.file:
      path: /etc/cron.hourly/
      follow: false
      state: directory
      group: '{{ file_groupowner_cron_hourly_newgroup }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_hourly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/cron.hourly/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/cron.hourly/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_cron_hourly | default(true) | bool)
- block:
  - name: Set the file_groupowner_cron_monthly_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_cron_monthly_newgroup: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_monthly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_cron_monthly_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_cron_monthly_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_cron_monthly | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/cron.monthly/
    ansible.builtin.file:
      path: /etc/cron.monthly/
      follow: false
      state: directory
      group: '{{ file_groupowner_cron_monthly_newgroup }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_monthly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/cron.monthly/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/cron.monthly/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_cron_monthly | default(true) | bool)
- block:
  - name: Set the file_groupowner_cron_weekly_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_cron_weekly_newgroup: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_weekly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_cron_weekly_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_cron_weekly_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_cron_weekly | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/cron.weekly/
    ansible.builtin.file:
      path: /etc/cron.weekly/
      follow: false
      state: directory
      group: '{{ file_groupowner_cron_weekly_newgroup }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_weekly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/cron.weekly/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/cron.weekly/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_cron_weekly | default(true) | bool)
- block:
  - name: Set the file_groupowner_crontab_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_crontab_newgroup: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_crontab_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_crontab_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_crontab | default(true) | bool)
- block:
  - name: Test for existence /etc/crontab
    ansible.builtin.stat:
      path: /etc/crontab
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/crontab'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/crontab'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_crontab | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/crontab
    ansible.builtin.file:
      path: /etc/crontab
      follow: false
      group: '{{ file_groupowner_crontab_newgroup }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/crontab'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/crontab'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_crontab | default(true) | bool)
- block:
  - name: Set the file_owner_cron_d_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_cron_d_newown: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_cron_d_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_cron_d_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_cron_d | default(true) | bool)
- block:
  - name: Ensure owner on directory /etc/cron.d/
    ansible.builtin.file:
      path: /etc/cron.d/
      follow: false
      state: directory
      owner: '{{ file_owner_cron_d_newown }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on directory /etc/cron.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on directory /etc/cron.d/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_cron_d | default(true) | bool)
- block:
  - name: Set the file_owner_cron_daily_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_cron_daily_newown: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_daily
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_cron_daily_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_cron_daily_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_cron_daily | default(true) | bool)
- block:
  - name: Ensure owner on directory /etc/cron.daily/
    ansible.builtin.file:
      path: /etc/cron.daily/
      follow: false
      state: directory
      owner: '{{ file_owner_cron_daily_newown }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_daily
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on directory /etc/cron.daily/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on directory /etc/cron.daily/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_cron_daily | default(true) | bool)
- block:
  - name: Set the file_owner_cron_hourly_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_cron_hourly_newown: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_hourly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_cron_hourly_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_cron_hourly_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_cron_hourly | default(true) | bool)
- block:
  - name: Ensure owner on directory /etc/cron.hourly/
    ansible.builtin.file:
      path: /etc/cron.hourly/
      follow: false
      state: directory
      owner: '{{ file_owner_cron_hourly_newown }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_hourly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on directory /etc/cron.hourly/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on directory /etc/cron.hourly/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_cron_hourly | default(true) | bool)
- block:
  - name: Set the file_owner_cron_monthly_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_cron_monthly_newown: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_monthly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_cron_monthly_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_cron_monthly_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_cron_monthly | default(true) | bool)
- block:
  - name: Ensure owner on directory /etc/cron.monthly/
    ansible.builtin.file:
      path: /etc/cron.monthly/
      follow: false
      state: directory
      owner: '{{ file_owner_cron_monthly_newown }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_monthly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on directory /etc/cron.monthly/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on directory /etc/cron.monthly/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_cron_monthly | default(true) | bool)
- block:
  - name: Set the file_owner_cron_weekly_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_cron_weekly_newown: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_weekly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_cron_weekly_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_cron_weekly_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_cron_weekly | default(true) | bool)
- block:
  - name: Ensure owner on directory /etc/cron.weekly/
    ansible.builtin.file:
      path: /etc/cron.weekly/
      follow: false
      state: directory
      owner: '{{ file_owner_cron_weekly_newown }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_weekly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on directory /etc/cron.weekly/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on directory /etc/cron.weekly/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_cron_weekly | default(true) | bool)
- block:
  - name: Set the file_owner_crontab_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_crontab_newown: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_crontab_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_crontab_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_crontab | default(true) | bool)
- block:
  - name: Test for existence /etc/crontab
    ansible.builtin.stat:
      path: /etc/crontab
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/crontab'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/crontab'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_crontab | default(true) | bool)
- block:
  - name: Ensure owner on /etc/crontab
    ansible.builtin.file:
      path: /etc/crontab
      follow: false
      owner: '{{ file_owner_crontab_newown }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/crontab'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/crontab'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_crontab | default(true) | bool)
- block:
  - name: Find /etc/cron.d/ file(s)
    ansible.builtin.command: 'find -P /etc/cron.d/ -maxdepth 0 -perm /u+s,g+xwrs,o+xwrt  -type d '
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/cron.d/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/cron.d/ file(s)'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_cron_d | default(true) | bool)
- block:
  - name: Set permissions for /etc/cron.d/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-s,g-xwrs,o-xwrt
      state: directory
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_d
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/cron.d/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/cron.d/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_cron_d | default(true) | bool)
- block:
  - name: Find /etc/cron.daily/ file(s)
    ansible.builtin.command: 'find -P /etc/cron.daily/ -maxdepth 0 -perm /u+s,g+xwrs,o+xwrt  -type d '
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_daily
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/cron.daily/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/cron.daily/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_cron_daily | default(true) | bool)
- block:
  - name: Set permissions for /etc/cron.daily/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-s,g-xwrs,o-xwrt
      state: directory
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_daily
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/cron.daily/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/cron.daily/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_cron_daily | default(true) | bool)
- block:
  - name: Find /etc/cron.hourly/ file(s)
    ansible.builtin.command: 'find -P /etc/cron.hourly/ -maxdepth 0 -perm /u+s,g+xwrs,o+xwrt  -type d '
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_hourly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/cron.hourly/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/cron.hourly/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_cron_hourly | default(true) | bool)
- block:
  - name: Set permissions for /etc/cron.hourly/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-s,g-xwrs,o-xwrt
      state: directory
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_hourly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/cron.hourly/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/cron.hourly/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_cron_hourly | default(true) | bool)
- block:
  - name: Find /etc/cron.monthly/ file(s)
    ansible.builtin.command: 'find -P /etc/cron.monthly/ -maxdepth 0 -perm /u+s,g+xwrs,o+xwrt  -type d '
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_monthly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/cron.monthly/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/cron.monthly/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_cron_monthly | default(true) | bool)
- block:
  - name: Set permissions for /etc/cron.monthly/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-s,g-xwrs,o-xwrt
      state: directory
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_monthly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/cron.monthly/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/cron.monthly/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_cron_monthly | default(true) | bool)
- block:
  - name: Find /etc/cron.weekly/ file(s)
    ansible.builtin.command: 'find -P /etc/cron.weekly/ -maxdepth 0 -perm /u+s,g+xwrs,o+xwrt  -type d '
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_weekly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/cron.weekly/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/cron.weekly/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_cron_weekly | default(true) | bool)
- block:
  - name: Set permissions for /etc/cron.weekly/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-s,g-xwrs,o-xwrt
      state: directory
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_weekly
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/cron.weekly/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/cron.weekly/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_cron_weekly | default(true) | bool)
- block:
  - name: Test for existence /etc/crontab
    ansible.builtin.stat:
      path: /etc/crontab
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/crontab'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/crontab'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_crontab | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xwrs,o-xwrt on /etc/crontab
    ansible.builtin.file:
      path: /etc/crontab
      mode: u-xs,g-xwrs,o-xwrt
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_crontab
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xwrs,o-xwrt on /etc/crontab'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xwrs,o-xwrt on /etc/crontab'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_crontab | default(true) | bool)
- block:
  - name: Remove /etc/at.deny
    ansible.builtin.file:
      path: /etc/at.deny
      state: absent
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - disable_strategy
    - file_at_deny_not_exist
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Remove /etc/at.deny'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove /etc/at.deny'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_at_deny_not_exist | default(true) | bool)
- block:
  - name: Add empty /etc/cron.allow
    ansible.builtin.file:
      path: /etc/cron.allow
      state: touch
      owner: '0'
      mode: '0600'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - disable_strategy
    - file_cron_allow_exists
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Add empty /etc/cron.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add empty /etc/cron.allow'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_cron_allow_exists | default(true) | bool)
- block:
  - name: Remove /etc/cron.deny
    ansible.builtin.file:
      path: /etc/cron.deny
      state: absent
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - disable_strategy
    - file_cron_deny_not_exist
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Remove /etc/cron.deny'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove /etc/cron.deny'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_cron_deny_not_exist | default(true) | bool)
- block:
  - name: Set the file_groupowner_at_allow_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_at_allow_newgroup: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_at_allow_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_at_allow_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_at_allow | default(true) | bool)
- block:
  - name: Test for existence /etc/at.allow
    ansible.builtin.stat:
      path: /etc/at.allow
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/at.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/at.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_at_allow | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/at.allow
    ansible.builtin.file:
      path: /etc/at.allow
      follow: false
      group: '{{ file_groupowner_at_allow_newgroup }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/at.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/at.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_at_allow | default(true) | bool)
- block:
  - name: Set the file_groupowner_cron_allow_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_cron_allow_newgroup: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_cron_allow_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_cron_allow_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_cron_allow | default(true) | bool)
- block:
  - name: Test for existence /etc/cron.allow
    ansible.builtin.stat:
      path: /etc/cron.allow
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/cron.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/cron.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_cron_allow | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/cron.allow
    ansible.builtin.file:
      path: /etc/cron.allow
      follow: false
      group: '{{ file_groupowner_cron_allow_newgroup }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_groupowner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/cron.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/cron.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_cron_allow | default(true) | bool)
- block:
  - name: Set the file_owner_at_allow_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_at_allow_newown: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_at_allow_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_at_allow_newown variable if represented by uid'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_at_allow | default(true) | bool)
- block:
  - name: Test for existence /etc/at.allow
    ansible.builtin.stat:
      path: /etc/at.allow
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/at.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/at.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_at_allow | default(true) | bool)
- block:
  - name: Ensure owner on /etc/at.allow
    ansible.builtin.file:
      path: /etc/at.allow
      follow: false
      owner: '{{ file_owner_at_allow_newown }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/at.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/at.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_at_allow | default(true) | bool)
- block:
  - name: Set the file_owner_cron_allow_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_cron_allow_newown: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_cron_allow_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_cron_allow_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_cron_allow | default(true) | bool)
- block:
  - name: Test for existence /etc/cron.allow
    ansible.builtin.stat:
      path: /etc/cron.allow
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/cron.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/cron.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_cron_allow | default(true) | bool)
- block:
  - name: Ensure owner on /etc/cron.allow
    ansible.builtin.file:
      path: /etc/cron.allow
      follow: false
      owner: '{{ file_owner_cron_allow_newown }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_owner_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/cron.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/cron.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_cron_allow | default(true) | bool)
- block:
  - name: Test for existence /etc/at.allow
    ansible.builtin.stat:
      path: /etc/at.allow
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/at.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/at.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_at_allow | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /etc/at.allow
    ansible.builtin.file:
      path: /etc/at.allow
      mode: u-xs,g-xws,o-xwrt
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_at_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /etc/at.allow'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /etc/at.allow'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_at_allow | default(true) | bool)
- block:
  - name: Test for existence /etc/cron.allow
    ansible.builtin.stat:
      path: /etc/cron.allow
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/cron.allow'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/cron.allow'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_cron_allow | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xws,o-xwrt on /etc/cron.allow
    ansible.builtin.file:
      path: /etc/cron.allow
      mode: u-xs,g-xws,o-xwrt
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_cron_allow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xws,o-xwrt on /etc/cron.allow'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xws,o-xwrt on /etc/cron.allow'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_cron_allow | default(true) | bool)
- block:
  - name: Make changes to Postfix configuration file
    ansible.builtin.lineinfile:
      path: /etc/postfix/main.cf
      create: false
      regexp: (?i)^inet_interfaces\s*=\s.*
      line: inet_interfaces = {{ var_postfix_inet_interfaces }}
      state: present
      insertafter: ^inet_interfaces\s*=\s.*
    when:
    - '"kernel" in ansible_facts.packages'
    - '"postfix" in ansible_facts.packages'
    - '"postfix" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-1.4
    - PCI-DSSv4-1.4.2
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - postfix_network_listening_disabled
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Make changes to Postfix configuration file'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Make changes to Postfix configuration file'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_postfix_network_listening_disabled | default(true)
    | bool)
- block:
  - name: Detect if chrony is already configured with pools or servers
    ansible.builtin.find:
      path: /etc
      patterns: chrony.conf
      contains: ^[\s]*(?:server|pool)[\s]+[\w]+
    register: chrony_servers
    when:
    - '"kernel" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
    tags:
    - NIST-800-53-AU-8(1)(a)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.3
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.2
    - chronyd_specify_remote_server
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Detect if chrony is already configured with pools or servers'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Detect if chrony is already configured with pools or servers'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_chronyd_specify_remote_server | default(true) | bool)
- block:
  - name: Configure remote time servers
    ansible.builtin.lineinfile:
      path: /etc/chrony.conf
      line: server {{ item }}
      state: present
      create: true
    loop: '{{ var_multiple_time_servers.split(",") }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
    - chrony_servers.matched == 0
    tags:
    - NIST-800-53-AU-8(1)(a)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.3
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.2
    - chronyd_specify_remote_server
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Configure remote time servers'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure remote time servers'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_chronyd_specify_remote_server | default(true) | bool)
- block:
  - name: Detect if file /etc/sysconfig/chronyd is not empty or missing
    ansible.builtin.find:
      path: /etc/sysconfig/
      patterns: chronyd
      contains: ^([\s]*OPTIONS=["]?[^"]*)("?)
    register: chronyd_file
    when:
    - '"kernel" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - chronyd_run_as_chrony_user
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Detect if file /etc/sysconfig/chronyd is not empty or missing'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Detect if file /etc/sysconfig/chronyd is not empty or missing'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_chronyd_run_as_chrony_user | default(true) | bool)
- block:
  - name: Remove any previous configuration of user used to run chronyd process
    ansible.builtin.replace:
      path: /etc/sysconfig/chronyd
      regexp: \s*-u\s*\w+\s*
      replace: ' '
    when:
    - '"kernel" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
    - chronyd_file is defined and chronyd_file.matched > 0
    tags:
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - chronyd_run_as_chrony_user
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Remove any previous configuration of user used to run chronyd process'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove any previous configuration of user used to run chronyd process'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_chronyd_run_as_chrony_user | default(true) | bool)
- block:
  - name: Correct existing line in /etc/sysconfig/chronyd to run chronyd as chrony user
    ansible.builtin.lineinfile:
      path: /etc/sysconfig/chronyd
      regexp: ^([\s]*OPTIONS=["]?[^"]*)("?)
      line: \1 -u chrony\2
      state: present
      backrefs: true
    when:
    - '"kernel" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
    - chronyd_file is defined and chronyd_file.matched > 0
    tags:
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - chronyd_run_as_chrony_user
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Correct existing line in /etc/sysconfig/chronyd to run chronyd as chrony user''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Correct existing line in /etc/sysconfig/chronyd to run chronyd as chrony user'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_chronyd_run_as_chrony_user | default(true) | bool)
- block:
  - name: Insert correct line into /etc/sysconfig/chronyd ensuring chronyd runs as chrony user
    ansible.builtin.lineinfile:
      path: /etc/sysconfig/chronyd
      line: OPTIONS="-u chrony"
      state: present
      create: true
    when:
    - '"kernel" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'
    - chronyd_file is defined and chronyd_file.matched == 0
    tags:
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - chronyd_run_as_chrony_user
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Insert correct line into /etc/sysconfig/chronyd ensuring chronyd runs as chrony
      user'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Insert correct line into /etc/sysconfig/chronyd ensuring chronyd runs as chrony user'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_chronyd_run_as_chrony_user | default(true) | bool)
- block:
  - name: Detect .rhosts files in users home directories
    ansible.builtin.find:
      paths:
      - /root
      - /home
      recurse: true
      patterns: .rhosts
      hidden: true
      file_type: file
    check_mode: false
    register: rhosts_locations
    when: '"rsh-server" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - no_rsh_trust_files
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Detect .rhosts files in users home directories'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Detect .rhosts files in users home directories'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_no_rsh_trust_files | default(true) | bool)
- block:
  - name: Remove .rhosts files
    ansible.builtin.file:
      path: '{{ item }}'
      state: absent
    with_items: '{{ rhosts_locations.files | map(attribute=''path'') | list }}'
    when:
    - '"rsh-server" in ansible_facts.packages'
    - rhosts_locations is success
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - no_rsh_trust_files
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove .rhosts files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove .rhosts files'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_no_rsh_trust_files | default(true) | bool)
- block:
  - name: Remove /etc/hosts.equiv file
    ansible.builtin.file:
      path: /etc/hosts.equiv
      state: absent
    when: '"rsh-server" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - no_rsh_trust_files
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove /etc/hosts.equiv file'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove /etc/hosts.equiv file'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_no_rsh_trust_files | default(true) | bool)
- block:
  - name: Set the file_groupowner_sshd_config_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupowner_sshd_config_newgroup: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupowner_sshd_config_newgroup variable if represented by gid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupowner_sshd_config_newgroup variable if represented by gid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_sshd_config | default(true) | bool)
- block:
  - name: Test for existence /etc/ssh/sshd_config
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_sshd_config | default(true) | bool)
- block:
  - name: Ensure group owner on /etc/ssh/sshd_config
    ansible.builtin.file:
      path: /etc/ssh/sshd_config
      follow: false
      group: '{{ file_groupowner_sshd_config_newgroup }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_groupowner_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupowner_sshd_config | default(true) | bool)
- block:
  - name: Check that the ssh_keys group is defined
    ansible.builtin.getent:
      database: group
      key: ssh_keys
    ignore_errors: true
    when:
    - '"kernel" in ansible_facts.packages'
    - file_groupownership_sshd_private_key_newgroup is undefined
    tags:
    - configure_strategy
    - file_groupownership_sshd_private_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Check that the ssh_keys group is defined'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Check that the ssh_keys group is defined'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_sshd_private_key | default(true)
    | bool)
- block:
  - name: Set the file_groupownership_sshd_private_key_newgroup variable if ssh_keys found
    ansible.builtin.set_fact:
      file_groupownership_sshd_private_key_newgroup: ssh_keys
    when:
    - '"kernel" in ansible_facts.packages'
    - ansible_facts.getent_group["ssh_keys"] is defined
    tags:
    - configure_strategy
    - file_groupownership_sshd_private_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownership_sshd_private_key_newgroup variable if ssh_keys
      found'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownership_sshd_private_key_newgroup variable if ssh_keys found'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_sshd_private_key | default(true)
    | bool)
- block:
  - name: Find /etc/ssh/ file(s) matching ^.*_key$
    ansible.builtin.command: find -P /etc/ssh/ -maxdepth 1 -type f  ! -group ssh_keys -regextype posix-extended -regex "^.*_key$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_sshd_private_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/ssh/ file(s) matching ^.*_key$'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/ssh/ file(s) matching ^.*_key$'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_sshd_private_key | default(true)
    | bool)
- block:
  - name: Ensure group owner on /etc/ssh/ file(s) matching ^.*_key$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupownership_sshd_private_key_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_sshd_private_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/ssh/ file(s) matching ^.*_key$'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/ssh/ file(s) matching ^.*_key$'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_sshd_private_key | default(true)
    | bool)
- block:
  - name: Set the file_groupownership_sshd_pub_key_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupownership_sshd_pub_key_newgroup: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_sshd_pub_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownership_sshd_pub_key_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownership_sshd_pub_key_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_sshd_pub_key | default(true) |
    bool)
- block:
  - name: Find /etc/ssh/ file(s) matching ^.*\.pub$
    ansible.builtin.command: find -P /etc/ssh/ -maxdepth 1 -type f  ! -group 0 -regextype posix-extended -regex "^.*\.pub$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_sshd_pub_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/ssh/ file(s) matching ^.*\\.pub$'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/ssh/ file(s) matching ^.*\\.pub$'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_sshd_pub_key | default(true) |
    bool)
- block:
  - name: Ensure group owner on /etc/ssh/ file(s) matching ^.*\.pub$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupownership_sshd_pub_key_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_sshd_pub_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/ssh/ file(s) matching ^.*\\.pub$'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/ssh/ file(s) matching ^.*\\.pub$'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_sshd_pub_key | default(true) |
    bool)
- block:
  - name: Set the file_owner_sshd_config_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_owner_sshd_config_newown: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_owner_sshd_config_newown variable if represented by uid'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_owner_sshd_config_newown variable if represented by uid'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_sshd_config | default(true) | bool)
- block:
  - name: Test for existence /etc/ssh/sshd_config
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_sshd_config | default(true) | bool)
- block:
  - name: Ensure owner on /etc/ssh/sshd_config
    ansible.builtin.file:
      path: /etc/ssh/sshd_config
      follow: false
      owner: '{{ file_owner_sshd_config_newown }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - configure_strategy
    - file_owner_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_owner_sshd_config | default(true) | bool)
- block:
  - name: Set the file_ownership_sshd_private_key_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_ownership_sshd_private_key_newown: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_sshd_private_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_ownership_sshd_private_key_newown variable if represented by
      uid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_ownership_sshd_private_key_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_sshd_private_key | default(true) |
    bool)
- block:
  - name: Find /etc/ssh/ file(s) matching ^.*_key$
    ansible.builtin.command: find -P /etc/ssh/ -maxdepth 1 -type f  ! -user 0 -regextype posix-extended -regex "^.*_key$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_sshd_private_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/ssh/ file(s) matching ^.*_key$'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/ssh/ file(s) matching ^.*_key$'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_sshd_private_key | default(true) |
    bool)
- block:
  - name: Ensure owner on /etc/ssh/ file(s) matching ^.*_key$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_ownership_sshd_private_key_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_sshd_private_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/ssh/ file(s) matching ^.*_key$'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/ssh/ file(s) matching ^.*_key$'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_sshd_private_key | default(true) |
    bool)
- block:
  - name: Set the file_ownership_sshd_pub_key_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_ownership_sshd_pub_key_newown: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_sshd_pub_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_ownership_sshd_pub_key_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_ownership_sshd_pub_key_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_sshd_pub_key | default(true) | bool)
- block:
  - name: Find /etc/ssh/ file(s) matching ^.*\.pub$
    ansible.builtin.command: find -P /etc/ssh/ -maxdepth 1 -type f  ! -user 0 -regextype posix-extended -regex "^.*\.pub$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_sshd_pub_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/ssh/ file(s) matching ^.*\\.pub$'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/ssh/ file(s) matching ^.*\\.pub$'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_sshd_pub_key | default(true) | bool)
- block:
  - name: Ensure owner on /etc/ssh/ file(s) matching ^.*\.pub$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_ownership_sshd_pub_key_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_sshd_pub_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/ssh/ file(s) matching ^.*\\.pub$'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/ssh/ file(s) matching ^.*\\.pub$'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_sshd_pub_key | default(true) | bool)
- block:
  - name: Test for existence /etc/ssh/sshd_config
    ansible.builtin.stat:
      path: /etc/ssh/sshd_config
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /etc/ssh/sshd_config'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /etc/ssh/sshd_config'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_sshd_config | default(true) | bool)
- block:
  - name: Ensure permission u-xs,g-xwrs,o-xwrt on /etc/ssh/sshd_config
    ansible.builtin.file:
      path: /etc/ssh/sshd_config
      mode: u-xs,g-xwrs,o-xwrt
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_config
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-xs,g-xwrs,o-xwrt on /etc/ssh/sshd_config'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-xs,g-xwrs,o-xwrt on /etc/ssh/sshd_config'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_sshd_config | default(true) | bool)
- block:
  - name: Find root:root-owned keys
    ansible.builtin.command: find -H /etc/ssh/ -maxdepth 1 -user root -regex ".*_key$" -type f -group root -perm /u+xs,g+xwrs,o+xwrt
    register: root_owned_keys
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.13
    - NIST-800-171-3.13.10
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_private_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find root:root-owned keys'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find root:root-owned keys'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_sshd_private_key | default(true)
    | bool)
- block:
  - name: Set permissions for root:root-owned keys
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xwrs,o-xwrt
      state: file
    with_items:
    - '{{ root_owned_keys.stdout_lines }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.13
    - NIST-800-171-3.13.10
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_private_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for root:root-owned keys'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for root:root-owned keys'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_sshd_private_key | default(true)
    | bool)
- block:
  - name: Find root:ssh_keys-owned keys
    ansible.builtin.command: find -H /etc/ssh/ -maxdepth 1 -user root -regex ".*_key$" -type f -group ssh_keys -perm /u+xs,g+xws,o+xwrt
    register: dedicated_group_owned_keys
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.13
    - NIST-800-171-3.13.10
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_private_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find root:ssh_keys-owned keys'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find root:ssh_keys-owned keys'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_sshd_private_key | default(true)
    | bool)
- block:
  - name: Set permissions for root:ssh_keys-owned keys
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xws,o-xwrt
      state: file
    with_items:
    - '{{ dedicated_group_owned_keys.stdout_lines }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.13
    - NIST-800-171-3.13.10
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_private_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for root:ssh_keys-owned keys'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for root:ssh_keys-owned keys'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_sshd_private_key | default(true)
    | bool)
- block:
  - name: Find /etc/ssh/ file(s)
    ansible.builtin.command: find -P /etc/ssh/ -maxdepth 1 -perm /u+xs,g+xws,o+xwt  -type f -regextype posix-extended -regex
      "^.*\.pub$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.13
    - NIST-800-171-3.13.10
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_pub_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/ssh/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/ssh/ file(s)'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_sshd_pub_key | default(true) | bool)
- block:
  - name: Set permissions for /etc/ssh/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xws,o-xwt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.13
    - NIST-800-171-3.13.10
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - configure_strategy
    - file_permissions_sshd_pub_key
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/ssh/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/ssh/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_sshd_pub_key | default(true) | bool)
- block:
  - name: Set SSH Client Alive Count Max
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "ClientAliveCountMax"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter ClientAliveCountMax is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "ClientAliveCountMax"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "ClientAliveCountMax"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "ClientAliveCountMax"| regex_escape }}\s+
        line: ClientAliveCountMax {{ var_sshd_set_keepalive }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_keepalive
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Count Max'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Count Max'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_set_keepalive | default(true) | bool)
- block:
  - name: Set SSH Client Alive Count Max - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_keepalive
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Count Max - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Count Max - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_set_keepalive | default(true) | bool)
- block:
  - name: Set SSH Client Alive Interval
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "ClientAliveInterval"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter ClientAliveInterval is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "ClientAliveInterval"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "ClientAliveInterval"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "ClientAliveInterval"| regex_escape }}\s+
        line: ClientAliveInterval {{ sshd_idle_timeout_value }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_idle_timeout
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Interval'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Interval'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_set_idle_timeout | default(true) | bool)
- block:
  - name: Set SSH Client Alive Interval - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - PCI-DSSv4-8.2
    - PCI-DSSv4-8.2.8
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_idle_timeout
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Client Alive Interval - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Client Alive Interval - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_set_idle_timeout | default(true) | bool)
- block:
  - name: Disable Host-Based Authentication
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "HostbasedAuthentication"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter HostbasedAuthentication is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "HostbasedAuthentication"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "HostbasedAuthentication"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "HostbasedAuthentication"| regex_escape }}\s+
        line: HostbasedAuthentication no
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.1
    - disable_host_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Host-Based Authentication'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Host-Based Authentication'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_disable_host_auth | default(true) | bool)
- block:
  - name: Disable Host-Based Authentication - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      mode: '0600'
      state: touch
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-8.3
    - PCI-DSSv4-8.3.1
    - disable_host_auth
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Disable Host-Based Authentication - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable Host-Based Authentication - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_disable_host_auth | default(true) | bool)
- block:
  - name: Disable SSH Access via Empty Passwords
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter PermitEmptyPasswords is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "PermitEmptyPasswords"| regex_escape }}\s+
        line: PermitEmptyPasswords no
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_empty_passwords
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Access via Empty Passwords'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Access via Empty Passwords'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_disable_empty_passwords | default(true) | bool)
- block:
  - name: Disable SSH Access via Empty Passwords - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      mode: '0600'
      state: touch
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_empty_passwords
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Access via Empty Passwords - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Access via Empty Passwords - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_disable_empty_passwords | default(true) | bool)
- block:
  - name: Disable SSH Support for .rhosts Files
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "IgnoreRhosts"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter IgnoreRhosts is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "IgnoreRhosts"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "IgnoreRhosts"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "IgnoreRhosts"| regex_escape }}\s+
        line: IgnoreRhosts yes
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_rhosts
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Support for .rhosts Files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Support for .rhosts Files'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_disable_rhosts | default(true) | bool)
- block:
  - name: Disable SSH Support for .rhosts Files - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      mode: '0600'
      state: touch
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_rhosts
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Support for .rhosts Files - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Support for .rhosts Files - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_disable_rhosts | default(true) | bool)
- block:
  - name: Disable SSH Root Login
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitRootLogin"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter PermitRootLogin is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "PermitRootLogin"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitRootLogin"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "PermitRootLogin"| regex_escape }}\s+
        line: PermitRootLogin no
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(2)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-2(5)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_root_login
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Root Login'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Root Login'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_disable_root_login | default(true) | bool)
- block:
  - name: Disable SSH Root Login - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.1
    - NIST-800-171-3.1.5
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-6(2)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-IA-2
    - NIST-800-53-IA-2(5)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_root_login
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH Root Login - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH Root Login - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_disable_root_login | default(true) | bool)
- block:
  - name: Disable SSH TCP Forwarding
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "AllowTcpForwarding"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter AllowTcpForwarding is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "AllowTcpForwarding"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "AllowTcpForwarding"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "AllowTcpForwarding"| regex_escape }}\s+
        line: AllowTcpForwarding no
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_tcp_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH TCP Forwarding'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH TCP Forwarding'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_disable_tcp_forwarding | default(true) | bool)
- block:
  - name: Disable SSH TCP Forwarding - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_tcp_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Disable SSH TCP Forwarding - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable SSH TCP Forwarding - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_disable_tcp_forwarding | default(true) | bool)
- block:
  - name: Disable X11 Forwarding
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "X11Forwarding"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter X11Forwarding is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "X11Forwarding"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "X11Forwarding"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "X11Forwarding"| regex_escape }}\s+
        line: X11Forwarding no
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_x11_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Disable X11 Forwarding'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable X11 Forwarding'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_disable_x11_forwarding | default(true) | bool)
- block:
  - name: Disable X11 Forwarding - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      mode: '0600'
      state: touch
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_disable_x11_forwarding
  rescue:
  - name: 'StigForge: continue after error in ''Disable X11 Forwarding - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Disable X11 Forwarding - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_disable_x11_forwarding | default(true) | bool)
- block:
  - name: Do Not Allow SSH Environment Options
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitUserEnvironment"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter PermitUserEnvironment is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "PermitUserEnvironment"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "PermitUserEnvironment"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "PermitUserEnvironment"| regex_escape }}\s+
        line: PermitUserEnvironment no
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_do_not_permit_user_env
  rescue:
  - name: 'StigForge: continue after error in ''Do Not Allow SSH Environment Options'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Do Not Allow SSH Environment Options'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_do_not_permit_user_env | default(true) | bool)
- block:
  - name: Do Not Allow SSH Environment Options - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf
      mode: '0600'
      state: touch
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_do_not_permit_user_env
  rescue:
  - name: 'StigForge: continue after error in ''Do Not Allow SSH Environment Options - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Do Not Allow SSH Environment Options - set file mode for /etc/ssh/sshd_config.d/01-complianceascode-reinforce-os-defaults.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_do_not_permit_user_env | default(true) | bool)
- block:
  - name: Enable PAM
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "UsePAM"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter UsePAM is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "UsePAM"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "UsePAM"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "UsePAM"| regex_escape }}\s+
        line: UsePAM yes
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_pam
  rescue:
  - name: 'StigForge: continue after error in ''Enable PAM'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable PAM'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_enable_pam | default(true) | bool)
- block:
  - name: Enable PAM - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_pam
  rescue:
  - name: 'StigForge: continue after error in ''Enable PAM - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable PAM - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_enable_pam | default(true) | bool)
- block:
  - name: Enable SSH Warning Banner
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "Banner"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter Banner is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "Banner"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "Banner"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "Banner"| regex_escape }}\s+
        line: Banner /etc/issue.net
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.9
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-8(a)
    - NIST-800-53-AC-8(c)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_warning_banner_net
  rescue:
  - name: 'StigForge: continue after error in ''Enable SSH Warning Banner'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable SSH Warning Banner'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_enable_warning_banner_net | default(true) | bool)
- block:
  - name: Enable SSH Warning Banner - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.5.6
    - NIST-800-171-3.1.9
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-8(a)
    - NIST-800-53-AC-8(c)
    - NIST-800-53-CM-6(a)
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_enable_warning_banner_net
  rescue:
  - name: 'StigForge: continue after error in ''Enable SSH Warning Banner - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Enable SSH Warning Banner - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_enable_warning_banner_net | default(true) | bool)
- block:
  - name: Ensure SSH LoginGraceTime is configured
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "LoginGraceTime"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter LoginGraceTime is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "LoginGraceTime"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "LoginGraceTime"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "LoginGraceTime"| regex_escape }}\s+
        line: LoginGraceTime {{ var_sshd_set_login_grace_time }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_login_grace_time
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH LoginGraceTime is configured'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH LoginGraceTime is configured'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_set_login_grace_time | default(true) | bool)
- block:
  - name: Ensure SSH LoginGraceTime is configured - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_login_grace_time
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH LoginGraceTime is configured - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH LoginGraceTime is configured - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_set_login_grace_time | default(true) | bool)
- block:
  - name: Set SSH Daemon LogLevel to VERBOSE
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "LogLevel"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter LogLevel is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "LogLevel"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "LogLevel"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "LogLevel"| regex_escape }}\s+
        line: LogLevel VERBOSE
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(1)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_loglevel_verbose
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Daemon LogLevel to VERBOSE'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Daemon LogLevel to VERBOSE'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_set_loglevel_verbose | default(true) | bool)
- block:
  - name: Set SSH Daemon LogLevel to VERBOSE - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AC-17(1)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-2.2.4
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_loglevel_verbose
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH Daemon LogLevel to VERBOSE - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH Daemon LogLevel to VERBOSE - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_set_loglevel_verbose | default(true) | bool)
- block:
  - name: Set SSH authentication attempt limit
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "MaxAuthTries"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter MaxAuthTries is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "MaxAuthTries"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "MaxAuthTries"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "MaxAuthTries"| regex_escape }}\s+
        line: MaxAuthTries {{ sshd_max_auth_tries_value }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_auth_tries
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH authentication attempt limit'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH authentication attempt limit'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_set_max_auth_tries | default(true) | bool)
- block:
  - name: Set SSH authentication attempt limit - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_auth_tries
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH authentication attempt limit - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH authentication attempt limit - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_set_max_auth_tries | default(true) | bool)
- block:
  - name: Set SSH MaxSessions limit
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "MaxSessions"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter MaxSessions is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "MaxSessions"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "MaxSessions"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "MaxSessions"| regex_escape }}\s+
        line: MaxSessions {{ var_sshd_max_sessions }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_sessions
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH MaxSessions limit'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH MaxSessions limit'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_set_max_sessions | default(true) | bool)
- block:
  - name: Set SSH MaxSessions limit - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_max_sessions
  rescue:
  - name: 'StigForge: continue after error in ''Set SSH MaxSessions limit - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set SSH MaxSessions limit - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_set_max_sessions | default(true) | bool)
- block:
  - name: Ensure SSH MaxStartups is configured
    block:
    - name: Deduplicate values from /etc/ssh/sshd_config
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        create: false
        regexp: (?i)(?i)^\s*{{ "MaxStartups"| regex_escape }}\s+
        state: absent
    - name: Check if /etc/ssh/sshd_config.d exists
      ansible.builtin.stat:
        path: /etc/ssh/sshd_config.d
      register: _etc_ssh_sshd_config_d_exists
    - name: Check if the parameter MaxStartups is present in /etc/ssh/sshd_config.d
      ansible.builtin.find:
        paths: /etc/ssh/sshd_config.d
        recurse: 'yes'
        follow: 'no'
        contains: (?i)^\s*{{ "MaxStartups"| regex_escape }}\s+
      register: _etc_ssh_sshd_config_d_has_parameter
      when: (_etc_ssh_sshd_config_d_exists is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir) is defined and (_etc_ssh_sshd_config_d_exists
        is not skipped and _etc_ssh_sshd_config_d_exists.stat.isdir)
    - name: Remove parameter from files in /etc/ssh/sshd_config.d
      ansible.builtin.lineinfile:
        path: '{{ item.path }}'
        create: false
        regexp: (?i)(?i)^\s*{{ "MaxStartups"| regex_escape }}\s+
        state: absent
      with_items: '{{ _etc_ssh_sshd_config_d_has_parameter.files }}'
      when: _etc_ssh_sshd_config_d_has_parameter.matched
    - name: Insert correct line to /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
        create: true
        regexp: (?i)(?i)^\s*{{ "MaxStartups"| regex_escape }}\s+
        line: MaxStartups {{ var_sshd_set_maxstartups }}
        state: present
        insertbefore: BOF
        validate: /usr/sbin/sshd -t -f %s
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_maxstartups
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH MaxStartups is configured'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH MaxStartups is configured'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_set_maxstartups | default(true) | bool)
- block:
  - name: Ensure SSH MaxStartups is configured - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
    ansible.builtin.file:
      path: /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf
      mode: '0600'
      state: touch
    when: '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
    - sshd_set_maxstartups
  rescue:
  - name: 'StigForge: continue after error in ''Ensure SSH MaxStartups is configured - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure SSH MaxStartups is configured - set file mode for /etc/ssh/sshd_config.d/00-complianceascode-hardening.conf''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_sshd_set_maxstartups | default(true) | bool)
- block:
  - name: Update grub defaults and the bootloader menu
    ansible.builtin.command: /sbin/grubby --update-kernel=ALL --args="audit=1"
    when:
    - '"kernel" in ansible_facts.packages'
    - '"grub2-common" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-17(1)
    - NIST-800-53-AU-10
    - NIST-800-53-AU-14(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IR-5(1)
    - PCI-DSS-Req-10.3
    - PCI-DSSv4-10.7
    - PCI-DSSv4-10.7.2
    - grub2_audit_argument
    - low_disruption
    - low_severity
    - medium_complexity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Update grub defaults and the bootloader menu'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Update grub defaults and the bootloader menu'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_grub2_audit_argument | default(true) | bool)
- block:
  - name: Update grub defaults and the bootloader menu
    ansible.builtin.command: /sbin/grubby --update-kernel=ALL --args="audit_backlog_limit=8192"
    when:
    - '"kernel" in ansible_facts.packages'
    - '"grub2-common" in ansible_facts.packages'
    tags:
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-10.7
    - PCI-DSSv4-10.7.2
    - grub2_audit_backlog_limit_argument
    - low_disruption
    - low_severity
    - medium_complexity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Update grub defaults and the bootloader menu'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Update grub defaults and the bootloader menu'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_grub2_audit_backlog_limit_argument | default(true)
    | bool)
- block:
  - name: Collect all files from /etc/audit/rules.d with .rules extension
    ansible.builtin.find:
      paths: /etc/audit/rules.d/
      patterns: '*.rules'
    register: find_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-171-3.4.3
    - NIST-800-53-AC-6(9)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - audit_rules_immutable
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Collect all files from /etc/audit/rules.d with .rules extension'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Collect all files from /etc/audit/rules.d with .rules extension'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_immutable | default(true) | bool)
- block:
  - name: Remove the -e option from all Audit config files
    ansible.builtin.lineinfile:
      path: '{{ item }}'
      regexp: ^\s*(?:-e)\s+.*$
      state: absent
    loop: '{{ find_rules_d.files | map(attribute=''path'') | list + [''/etc/audit/audit.rules''] }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-171-3.4.3
    - NIST-800-53-AC-6(9)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - audit_rules_immutable
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remove the -e option from all Audit config files'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remove the -e option from all Audit config files'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_immutable | default(true) | bool)
- block:
  - name: Add Audit -e option into /etc/audit/rules.d/immutable.rules and /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      path: '{{ item }}'
      create: true
      line: -e 2
      mode: g-rwx,o-rwx
    loop:
    - /etc/audit/audit.rules
    - /etc/audit/rules.d/immutable.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-171-3.4.3
    - NIST-800-53-AC-6(9)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.2
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.2
    - audit_rules_immutable
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Add Audit -e option into /etc/audit/rules.d/immutable.rules and /etc/audit/audit.rules''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Add Audit -e option into /etc/audit/rules.d/immutable.rules and /etc/audit/audit.rules'' raised an
        error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_immutable | default(true) | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls - Check if watch rule for /etc/selinux/ already
      exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/selinux/\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.8
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_mac_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls - Check if
      watch rule for /etc/selinux/ already exists in /etc/audit/rules.d/" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls - Check if watch rule for /etc/selinux/
        already exists in /etc/audit/rules.d/" raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_mac_modification | default(true) | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls - Search /etc/audit/rules.d for other rules with
      specified key MAC-policy
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)MAC-policy$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.8
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_mac_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls - Search
      /etc/audit/rules.d for other rules with specified key MAC-policy" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls - Search /etc/audit/rules.d for
        other rules with specified key MAC-policy" raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_mac_modification | default(true) | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls - Use /etc/audit/rules.d/MAC-policy.rules as the
      recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/MAC-policy.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.8
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_mac_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls - Use /etc/audit/rules.d/MAC-policy.rules
      as the recipient for the rule" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls - Use /etc/audit/rules.d/MAC-policy.rules
        as the recipient for the rule" raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_mac_modification | default(true) | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.8
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_mac_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls - Use matched
      file as the recipient for the rule" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls - Use matched file as the recipient
        for the rule" raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_mac_modification | default(true) | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls - Add watch rule for /etc/selinux/ in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/selinux/ -p wa -k MAC-policy
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.8
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_mac_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls - Add watch
      rule for /etc/selinux/ in /etc/audit/rules.d/" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls - Add watch rule for /etc/selinux/
        in /etc/audit/rules.d/" raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_mac_modification | default(true) | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls - Check if watch rule for /etc/selinux/ already
      exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/selinux/\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.8
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_mac_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls - Check if
      watch rule for /etc/selinux/ already exists in /etc/audit/audit.rules" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls - Check if watch rule for /etc/selinux/
        already exists in /etc/audit/audit.rules" raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_mac_modification | default(true) | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls - Add watch rule for /etc/selinux/ in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/selinux/ -p wa -k MAC-policy
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.8
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_mac_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls - Add watch
      rule for /etc/selinux/ in /etc/audit/audit.rules" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls - Add watch rule for /etc/selinux/
        in /etc/audit/audit.rules" raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_mac_modification | default(true) | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls in usr/share - Check if watch rule for /usr/share/selinux/
      already exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/usr/share/selinux/\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.8
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - audit_rules_mac_modification_usr_share
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls in usr/share
      - Check if watch rule for /usr/share/selinux/ already exists in /etc/audit/rules.d/" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls in usr/share - Check if watch rule
        for /usr/share/selinux/ already exists in /etc/audit/rules.d/" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_mac_modification_usr_share | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls in usr/share - Search /etc/audit/rules.d for other
      rules with specified key MAC-policy
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)MAC-policy$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - NIST-800-171-3.1.8
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - audit_rules_mac_modification_usr_share
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls in usr/share
      - Search /etc/audit/rules.d for other rules with specified key MAC-policy" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls in usr/share - Search /etc/audit/rules.d
        for other rules with specified key MAC-policy" raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_mac_modification_usr_share | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls in usr/share - Use /etc/audit/rules.d/MAC-policy.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/MAC-policy.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - NIST-800-171-3.1.8
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - audit_rules_mac_modification_usr_share
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls in usr/share
      - Use /etc/audit/rules.d/MAC-policy.rules as the recipient for the rule" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls in usr/share - Use /etc/audit/rules.d/MAC-policy.rules
        as the recipient for the rule" raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_mac_modification_usr_share | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls in usr/share - Use matched file as the recipient
      for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - NIST-800-171-3.1.8
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - audit_rules_mac_modification_usr_share
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls in usr/share
      - Use matched file as the recipient for the rule" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls in usr/share - Use matched file
        as the recipient for the rule" raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_mac_modification_usr_share | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls in usr/share - Add watch rule for /usr/share/selinux/
      in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /usr/share/selinux/ -p wa -k MAC-policy
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - NIST-800-171-3.1.8
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - audit_rules_mac_modification_usr_share
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls in usr/share
      - Add watch rule for /usr/share/selinux/ in /etc/audit/rules.d/" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls in usr/share - Add watch rule for
        /usr/share/selinux/ in /etc/audit/rules.d/" raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_mac_modification_usr_share | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls in usr/share - Check if watch rule for /usr/share/selinux/
      already exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/usr/share/selinux/\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.8
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - audit_rules_mac_modification_usr_share
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls in usr/share
      - Check if watch rule for /usr/share/selinux/ already exists in /etc/audit/audit.rules" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls in usr/share - Check if watch rule
        for /usr/share/selinux/ already exists in /etc/audit/audit.rules" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_mac_modification_usr_share | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Mandatory Access Controls in usr/share - Add watch rule for /usr/share/selinux/
      in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /usr/share/selinux/ -p wa -k MAC-policy
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - NIST-800-171-3.1.8
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - audit_rules_mac_modification_usr_share
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Mandatory Access Controls in usr/share
      - Add watch rule for /usr/share/selinux/ in /etc/audit/audit.rules" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Mandatory Access Controls in usr/share - Add watch rule for
        /usr/share/selinux/ in /etc/audit/audit.rules" raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_mac_modification_usr_share | default(true)
    | bool)
- block:
  - name: Set architecture for audit mount tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_media_export
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit mount tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit mount tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_media_export | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for mount for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - mount
        syscall_grouping: []
    - name: Check existence of mount in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/export.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/export.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=export
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - mount
        syscall_grouping: []
    - name: Check existence of mount in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=export
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_media_export
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for mount for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for mount for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_media_export | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for mount for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - mount
        syscall_grouping: []
    - name: Check existence of mount in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/export.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/export.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=export
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - mount
        syscall_grouping: []
    - name: Check existence of mount in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=export
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_media_export
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for mount for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for mount for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_media_export | default(true) | bool)
- block:
  - name: Set architecture for audit tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit tasks'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Remediate audit rules for network configuration for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - sethostname
        - setdomainname
        syscall_grouping:
        - sethostname
        - setdomainname
    - name: Check existence of sethostname, setdomainname in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/audit_rules_networkconfig_modification.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/audit_rules_networkconfig_modification.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=audit_rules_networkconfig_modification
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - sethostname
        - setdomainname
        syscall_grouping:
        - sethostname
        - setdomainname
    - name: Check existence of sethostname, setdomainname in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=audit_rules_networkconfig_modification
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remediate audit rules for network configuration for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remediate audit rules for network configuration for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Remediate audit rules for network configuration for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - sethostname
        - setdomainname
        syscall_grouping:
        - sethostname
        - setdomainname
    - name: Check existence of sethostname, setdomainname in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/audit_rules_networkconfig_modification.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/audit_rules_networkconfig_modification.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F key=audit_rules_networkconfig_modification
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - sethostname
        - setdomainname
        syscall_grouping:
        - sethostname
        - setdomainname
    - name: Check existence of sethostname, setdomainname in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F key=audit_rules_networkconfig_modification
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Remediate audit rules for network configuration for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Remediate audit rules for network configuration for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Check if watch rule for /etc/issue already exists in
      /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/issue\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Check if watch
      rule for /etc/issue already exists in /etc/audit/rules.d/" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Check if watch rule for /etc/issue already
        exists in /etc/audit/rules.d/" raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Search /etc/audit/rules.d for other rules with specified
      key audit_rules_networkconfig_modification
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_rules_networkconfig_modification$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Search /etc/audit/rules.d
      for other rules with specified key audit_rules_networkconfig_modification" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Search /etc/audit/rules.d for other
        rules with specified key audit_rules_networkconfig_modification" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Use /etc/audit/rules.d/audit_rules_networkconfig_modification.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_rules_networkconfig_modification.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Use /etc/audit/rules.d/audit_rules_networkconfig_modification.rules
      as the recipient for the rule" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Use /etc/audit/rules.d/audit_rules_networkconfig_modification.rules
        as the recipient for the rule" raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Use matched file
      as the recipient for the rule" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Use matched file as the recipient for
        the rule" raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Add watch rule for /etc/issue in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/issue -p wa -k audit_rules_networkconfig_modification
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Add watch rule
      for /etc/issue in /etc/audit/rules.d/" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Add watch rule for /etc/issue in /etc/audit/rules.d/"
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Check if watch rule for /etc/issue already exists in
      /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/issue\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Check if watch
      rule for /etc/issue already exists in /etc/audit/audit.rules" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Check if watch rule for /etc/issue already
        exists in /etc/audit/audit.rules" raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Add watch rule for /etc/issue in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/issue -p wa -k audit_rules_networkconfig_modification
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Add watch rule
      for /etc/issue in /etc/audit/audit.rules" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Add watch rule for /etc/issue in /etc/audit/audit.rules"
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Check if watch rule for /etc/issue.net already exists
      in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/issue.net\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Check if watch
      rule for /etc/issue.net already exists in /etc/audit/rules.d/" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Check if watch rule for /etc/issue.net
        already exists in /etc/audit/rules.d/" raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Search /etc/audit/rules.d for other rules with specified
      key audit_rules_networkconfig_modification
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_rules_networkconfig_modification$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Search /etc/audit/rules.d
      for other rules with specified key audit_rules_networkconfig_modification" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Search /etc/audit/rules.d for other
        rules with specified key audit_rules_networkconfig_modification" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Use /etc/audit/rules.d/audit_rules_networkconfig_modification.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_rules_networkconfig_modification.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Use /etc/audit/rules.d/audit_rules_networkconfig_modification.rules
      as the recipient for the rule" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Use /etc/audit/rules.d/audit_rules_networkconfig_modification.rules
        as the recipient for the rule" raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Use matched file
      as the recipient for the rule" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Use matched file as the recipient for
        the rule" raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Add watch rule for /etc/issue.net in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/issue.net -p wa -k audit_rules_networkconfig_modification
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Add watch rule
      for /etc/issue.net in /etc/audit/rules.d/" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Add watch rule for /etc/issue.net in
        /etc/audit/rules.d/" raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Check if watch rule for /etc/issue.net already exists
      in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/issue.net\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Check if watch
      rule for /etc/issue.net already exists in /etc/audit/audit.rules" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Check if watch rule for /etc/issue.net
        already exists in /etc/audit/audit.rules" raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Add watch rule for /etc/issue.net in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/issue.net -p wa -k audit_rules_networkconfig_modification
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Add watch rule
      for /etc/issue.net in /etc/audit/audit.rules" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Add watch rule for /etc/issue.net in
        /etc/audit/audit.rules" raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Check if watch rule for /etc/hosts already exists in
      /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/hosts\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Check if watch
      rule for /etc/hosts already exists in /etc/audit/rules.d/" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Check if watch rule for /etc/hosts already
        exists in /etc/audit/rules.d/" raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Search /etc/audit/rules.d for other rules with specified
      key audit_rules_networkconfig_modification
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_rules_networkconfig_modification$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Search /etc/audit/rules.d
      for other rules with specified key audit_rules_networkconfig_modification" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Search /etc/audit/rules.d for other
        rules with specified key audit_rules_networkconfig_modification" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Use /etc/audit/rules.d/audit_rules_networkconfig_modification.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_rules_networkconfig_modification.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Use /etc/audit/rules.d/audit_rules_networkconfig_modification.rules
      as the recipient for the rule" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Use /etc/audit/rules.d/audit_rules_networkconfig_modification.rules
        as the recipient for the rule" raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Use matched file
      as the recipient for the rule" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Use matched file as the recipient for
        the rule" raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Add watch rule for /etc/hosts in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/hosts -p wa -k audit_rules_networkconfig_modification
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Add watch rule
      for /etc/hosts in /etc/audit/rules.d/" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Add watch rule for /etc/hosts in /etc/audit/rules.d/"
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Check if watch rule for /etc/hosts already exists in
      /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/hosts\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Check if watch
      rule for /etc/hosts already exists in /etc/audit/audit.rules" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Check if watch rule for /etc/hosts already
        exists in /etc/audit/audit.rules" raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Add watch rule for /etc/hosts in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/hosts -p wa -k audit_rules_networkconfig_modification
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Add watch rule
      for /etc/hosts in /etc/audit/audit.rules" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Add watch rule for /etc/hosts in /etc/audit/audit.rules"
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Check if watch rule for /etc/sysconfig/network already
      exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/sysconfig/network\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Check if watch
      rule for /etc/sysconfig/network already exists in /etc/audit/rules.d/" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Check if watch rule for /etc/sysconfig/network
        already exists in /etc/audit/rules.d/" raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Search /etc/audit/rules.d for other rules with specified
      key audit_rules_networkconfig_modification
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_rules_networkconfig_modification$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Search /etc/audit/rules.d
      for other rules with specified key audit_rules_networkconfig_modification" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Search /etc/audit/rules.d for other
        rules with specified key audit_rules_networkconfig_modification" raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Use /etc/audit/rules.d/audit_rules_networkconfig_modification.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_rules_networkconfig_modification.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Use /etc/audit/rules.d/audit_rules_networkconfig_modification.rules
      as the recipient for the rule" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Use /etc/audit/rules.d/audit_rules_networkconfig_modification.rules
        as the recipient for the rule" raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Use matched file
      as the recipient for the rule" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Use matched file as the recipient for
        the rule" raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Add watch rule for /etc/sysconfig/network in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/sysconfig/network -p wa -k audit_rules_networkconfig_modification
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Add watch rule
      for /etc/sysconfig/network in /etc/audit/rules.d/" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Add watch rule for /etc/sysconfig/network
        in /etc/audit/rules.d/" raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Check if watch rule for /etc/sysconfig/network already
      exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/sysconfig/network\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Check if watch
      rule for /etc/sysconfig/network already exists in /etc/audit/audit.rules" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Check if watch rule for /etc/sysconfig/network
        already exists in /etc/audit/audit.rules" raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Add watch rule for /etc/sysconfig/network in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/sysconfig/network -p wa -k audit_rules_networkconfig_modification
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_networkconfig_modification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Add watch rule
      for /etc/sysconfig/network in /etc/audit/audit.rules" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Add watch rule for /etc/sysconfig/network
        in /etc/audit/audit.rules" raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification | default(true)
    | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Check if watch rule for /etc/sysconfig/network-scripts
      already exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/sysconfig/network-scripts\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - audit_rules_networkconfig_modification_network_scripts
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Check if watch
      rule for /etc/sysconfig/network-scripts already exists in /etc/audit/rules.d/" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Check if watch rule for /etc/sysconfig/network-scripts
        already exists in /etc/audit/rules.d/" raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification_network_scripts
    | default(true) | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Search /etc/audit/rules.d for other rules with specified
      key audit_rules_networkconfig_modification_network_scripts
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_rules_networkconfig_modification_network_scripts$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_networkconfig_modification_network_scripts
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Search /etc/audit/rules.d
      for other rules with specified key audit_rules_networkconfig_modification_network_scripts" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Search /etc/audit/rules.d for other
        rules with specified key audit_rules_networkconfig_modification_network_scripts" raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification_network_scripts
    | default(true) | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Use /etc/audit/rules.d/audit_rules_networkconfig_modification_network_scripts.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_rules_networkconfig_modification_network_scripts.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_networkconfig_modification_network_scripts
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Use /etc/audit/rules.d/audit_rules_networkconfig_modification_network_scripts.rules
      as the recipient for the rule" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Use /etc/audit/rules.d/audit_rules_networkconfig_modification_network_scripts.rules
        as the recipient for the rule" raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification_network_scripts
    | default(true) | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_networkconfig_modification_network_scripts
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Use matched file
      as the recipient for the rule" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Use matched file as the recipient for
        the rule" raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification_network_scripts
    | default(true) | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Add watch rule for /etc/sysconfig/network-scripts in
      /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/sysconfig/network-scripts -p wa -k audit_rules_networkconfig_modification_network_scripts
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - audit_rules_networkconfig_modification_network_scripts
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Add watch rule
      for /etc/sysconfig/network-scripts in /etc/audit/rules.d/" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Add watch rule for /etc/sysconfig/network-scripts
        in /etc/audit/rules.d/" raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification_network_scripts
    | default(true) | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Check if watch rule for /etc/sysconfig/network-scripts
      already exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/sysconfig/network-scripts\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - audit_rules_networkconfig_modification_network_scripts
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Check if watch
      rule for /etc/sysconfig/network-scripts already exists in /etc/audit/audit.rules" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Check if watch rule for /etc/sysconfig/network-scripts
        already exists in /etc/audit/audit.rules" raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification_network_scripts
    | default(true) | bool)
- block:
  - name: Record Events that Modify the System's Network Environment - Add watch rule for /etc/sysconfig/network-scripts in
      /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/sysconfig/network-scripts -p wa -k audit_rules_networkconfig_modification_network_scripts
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - audit_rules_networkconfig_modification_network_scripts
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in "Record Events that Modify the System''s Network Environment - Add watch rule
      for /etc/sysconfig/network-scripts in /etc/audit/audit.rules" (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: "Record Events that Modify the System''s Network Environment - Add watch rule for /etc/sysconfig/network-scripts
        in /etc/audit/audit.rules" raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_networkconfig_modification_network_scripts
    | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Check if watch rule for /var/run/utmp already
      exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/var/run/utmp\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Check
      if watch rule for /var/run/utmp already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Check if watch rule for /var/run/utmp
        already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Search /etc/audit/rules.d for other rules
      with specified key session
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)session$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Search
      /etc/audit/rules.d for other rules with specified key session'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Search /etc/audit/rules.d for
        other rules with specified key session'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Use /etc/audit/rules.d/session.rules as the
      recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/session.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Use
      /etc/audit/rules.d/session.rules as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Use /etc/audit/rules.d/session.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Use matched file as the recipient for the
      rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Use
      matched file as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Use matched file as the recipient
        for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Add watch rule for /var/run/utmp in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /var/run/utmp -p wa -k session
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Add
      watch rule for /var/run/utmp in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Add watch rule for /var/run/utmp
        in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Check if watch rule for /var/run/utmp already
      exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/var/run/utmp\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Check
      if watch rule for /var/run/utmp already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Check if watch rule for /var/run/utmp
        already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Add watch rule for /var/run/utmp in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /var/run/utmp -p wa -k session
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Add
      watch rule for /var/run/utmp in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Add watch rule for /var/run/utmp
        in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Check if watch rule for /var/log/btmp already
      exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/var/log/btmp\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Check
      if watch rule for /var/log/btmp already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Check if watch rule for /var/log/btmp
        already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Search /etc/audit/rules.d for other rules
      with specified key session
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)session$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Search
      /etc/audit/rules.d for other rules with specified key session'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Search /etc/audit/rules.d for
        other rules with specified key session'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Use /etc/audit/rules.d/session.rules as the
      recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/session.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Use
      /etc/audit/rules.d/session.rules as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Use /etc/audit/rules.d/session.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Use matched file as the recipient for the
      rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Use
      matched file as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Use matched file as the recipient
        for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Add watch rule for /var/log/btmp in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /var/log/btmp -p wa -k session
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Add
      watch rule for /var/log/btmp in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Add watch rule for /var/log/btmp
        in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Check if watch rule for /var/log/btmp already
      exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/var/log/btmp\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Check
      if watch rule for /var/log/btmp already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Check if watch rule for /var/log/btmp
        already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Add watch rule for /var/log/btmp in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /var/log/btmp -p wa -k session
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Add
      watch rule for /var/log/btmp in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Add watch rule for /var/log/btmp
        in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Check if watch rule for /var/log/wtmp already
      exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/var/log/wtmp\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Check
      if watch rule for /var/log/wtmp already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Check if watch rule for /var/log/wtmp
        already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Search /etc/audit/rules.d for other rules
      with specified key session
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)session$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Search
      /etc/audit/rules.d for other rules with specified key session'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Search /etc/audit/rules.d for
        other rules with specified key session'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Use /etc/audit/rules.d/session.rules as the
      recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/session.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Use
      /etc/audit/rules.d/session.rules as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Use /etc/audit/rules.d/session.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Use matched file as the recipient for the
      rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Use
      matched file as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Use matched file as the recipient
        for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Add watch rule for /var/log/wtmp in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /var/log/wtmp -p wa -k session
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Add
      watch rule for /var/log/wtmp in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Add watch rule for /var/log/wtmp
        in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Check if watch rule for /var/log/wtmp already
      exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/var/log/wtmp\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Check
      if watch rule for /var/log/wtmp already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Check if watch rule for /var/log/wtmp
        already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Record Attempts to Alter Process and Session Initiation Information - Add watch rule for /var/log/wtmp in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /var/log/wtmp -p wa -k session
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - audit_rules_session_events
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Process and Session Initiation Information - Add
      watch rule for /var/log/wtmp in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Process and Session Initiation Information - Add watch rule for /var/log/wtmp
        in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_session_events | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Check if watch rule for /etc/sudoers already exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/sudoers\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Check if watch rule
      for /etc/sudoers already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Check if watch rule for /etc/sudoers already
        exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/sudoers -p wa -k actions
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers
      in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers in /etc/audit/audit.rules''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Check if watch rule for /etc/sudoers already exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/sudoers\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Check if watch rule
      for /etc/sudoers already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Check if watch rule for /etc/sudoers already
        exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Search /etc/audit/rules.d for other rules with specified key
      actions
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)actions$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Search /etc/audit/rules.d
      for other rules with specified key actions'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Search /etc/audit/rules.d for other rules with
        specified key actions'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Use /etc/audit/rules.d/actions.rules as the recipient for
      the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/actions.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Use /etc/audit/rules.d/actions.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Use /etc/audit/rules.d/actions.rules as the
        recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Use matched file as
      the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Use matched file as the recipient for the rule''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/sudoers -p wa -k actions
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers
      in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers in /etc/audit/rules.d/''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Check if watch rule for /etc/sudoers.d/ already exists in
      /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/sudoers.d/\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Check if watch rule
      for /etc/sudoers.d/ already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Check if watch rule for /etc/sudoers.d/ already
        exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers.d/ in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/sudoers.d/ -p wa -k actions
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers.d/
      in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers.d/ in /etc/audit/audit.rules''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Check if watch rule for /etc/sudoers.d/ already exists in
      /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/sudoers.d/\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Check if watch rule
      for /etc/sudoers.d/ already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Check if watch rule for /etc/sudoers.d/ already
        exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Search /etc/audit/rules.d for other rules with specified key
      actions
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)actions$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Search /etc/audit/rules.d
      for other rules with specified key actions'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Search /etc/audit/rules.d for other rules with
        specified key actions'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Use /etc/audit/rules.d/actions.rules as the recipient for
      the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/actions.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Use /etc/audit/rules.d/actions.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Use /etc/audit/rules.d/actions.rules as the
        recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Use matched file as
      the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Use matched file as the recipient for the rule''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers.d/ in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/sudoers.d/ -p wa -k actions
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(7)(b)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_sysadmin_actions
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers.d/
      in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects System Administrator Actions - Add watch rule for /etc/sudoers.d/ in /etc/audit/rules.d/''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_sysadmin_actions | default(true) | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/group - Check if watch rule for /etc/group already exists
      in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/group\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/group - Check if watch
      rule for /etc/group already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/group - Check if watch rule for /etc/group
        already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_group | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/group - Search /etc/audit/rules.d for other rules with specified
      key audit_rules_usergroup_modification
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_rules_usergroup_modification$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/group - Search /etc/audit/rules.d
      for other rules with specified key audit_rules_usergroup_modification'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/group - Search /etc/audit/rules.d for other
        rules with specified key audit_rules_usergroup_modification'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_group | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/group - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_rules_usergroup_modification.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/group - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/group - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_group | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/group - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/group - Use matched
      file as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/group - Use matched file as the recipient
        for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_group | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/group - Add watch rule for /etc/group in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/group -p wa -k audit_rules_usergroup_modification
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/group - Add watch
      rule for /etc/group in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/group - Add watch rule for /etc/group in
        /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_group | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/group - Check if watch rule for /etc/group already exists
      in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/group\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/group - Check if watch
      rule for /etc/group already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/group - Check if watch rule for /etc/group
        already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_group | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/group - Add watch rule for /etc/group in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/group -p wa -k audit_rules_usergroup_modification
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_group
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/group - Add watch
      rule for /etc/group in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/group - Add watch rule for /etc/group in
        /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_group | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/gshadow - Check if watch rule for /etc/gshadow already exists
      in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/gshadow\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/gshadow - Check if
      watch rule for /etc/gshadow already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/gshadow - Check if watch rule for /etc/gshadow
        already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_gshadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/gshadow - Search /etc/audit/rules.d for other rules with
      specified key audit_rules_usergroup_modification
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_rules_usergroup_modification$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/gshadow - Search /etc/audit/rules.d
      for other rules with specified key audit_rules_usergroup_modification'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/gshadow - Search /etc/audit/rules.d for other
        rules with specified key audit_rules_usergroup_modification'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_gshadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/gshadow - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_rules_usergroup_modification.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/gshadow - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/gshadow - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_gshadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/gshadow - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/gshadow - Use matched
      file as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/gshadow - Use matched file as the recipient
        for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_gshadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/gshadow - Add watch rule for /etc/gshadow in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/gshadow -p wa -k audit_rules_usergroup_modification
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/gshadow - Add watch
      rule for /etc/gshadow in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/gshadow - Add watch rule for /etc/gshadow
        in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_gshadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/gshadow - Check if watch rule for /etc/gshadow already exists
      in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/gshadow\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/gshadow - Check if
      watch rule for /etc/gshadow already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/gshadow - Check if watch rule for /etc/gshadow
        already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_gshadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/gshadow - Add watch rule for /etc/gshadow in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/gshadow -p wa -k audit_rules_usergroup_modification
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_gshadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/gshadow - Add watch
      rule for /etc/gshadow in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/gshadow - Add watch rule for /etc/gshadow
        in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_gshadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/security/opasswd - Check if watch rule for /etc/security/opasswd
      already exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/security/opasswd\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/security/opasswd -
      Check if watch rule for /etc/security/opasswd already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/security/opasswd - Check if watch rule for
        /etc/security/opasswd already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_opasswd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/security/opasswd - Search /etc/audit/rules.d for other rules
      with specified key audit_rules_usergroup_modification
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_rules_usergroup_modification$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/security/opasswd -
      Search /etc/audit/rules.d for other rules with specified key audit_rules_usergroup_modification'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/security/opasswd - Search /etc/audit/rules.d
        for other rules with specified key audit_rules_usergroup_modification'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_opasswd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/security/opasswd - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_rules_usergroup_modification.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/security/opasswd -
      Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules as the recipient for the rule'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/security/opasswd - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_opasswd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/security/opasswd - Use matched file as the recipient for
      the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/security/opasswd -
      Use matched file as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/security/opasswd - Use matched file as the
        recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_opasswd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/security/opasswd - Add watch rule for /etc/security/opasswd
      in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/security/opasswd -p wa -k audit_rules_usergroup_modification
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/security/opasswd -
      Add watch rule for /etc/security/opasswd in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/security/opasswd - Add watch rule for /etc/security/opasswd
        in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_opasswd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/security/opasswd - Check if watch rule for /etc/security/opasswd
      already exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/security/opasswd\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/security/opasswd -
      Check if watch rule for /etc/security/opasswd already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/security/opasswd - Check if watch rule for
        /etc/security/opasswd already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_opasswd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/security/opasswd - Add watch rule for /etc/security/opasswd
      in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/security/opasswd -p wa -k audit_rules_usergroup_modification
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_opasswd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/security/opasswd -
      Add watch rule for /etc/security/opasswd in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/security/opasswd - Add watch rule for /etc/security/opasswd
        in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_opasswd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/passwd - Check if watch rule for /etc/passwd already exists
      in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/passwd\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/passwd - Check if
      watch rule for /etc/passwd already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/passwd - Check if watch rule for /etc/passwd
        already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_passwd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/passwd - Search /etc/audit/rules.d for other rules with
      specified key audit_rules_usergroup_modification_passwd
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_rules_usergroup_modification_passwd$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/passwd - Search /etc/audit/rules.d
      for other rules with specified key audit_rules_usergroup_modification_passwd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/passwd - Search /etc/audit/rules.d for other
        rules with specified key audit_rules_usergroup_modification_passwd'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_passwd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/passwd - Use /etc/audit/rules.d/audit_rules_usergroup_modification_passwd.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_rules_usergroup_modification_passwd.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/passwd - Use /etc/audit/rules.d/audit_rules_usergroup_modification_passwd.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/passwd - Use /etc/audit/rules.d/audit_rules_usergroup_modification_passwd.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_passwd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/passwd - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/passwd - Use matched
      file as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/passwd - Use matched file as the recipient
        for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_passwd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/passwd - Add watch rule for /etc/passwd in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/passwd -p wa -k audit_rules_usergroup_modification_passwd
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/passwd - Add watch
      rule for /etc/passwd in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/passwd - Add watch rule for /etc/passwd in
        /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_passwd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/passwd - Check if watch rule for /etc/passwd already exists
      in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/passwd\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/passwd - Check if
      watch rule for /etc/passwd already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/passwd - Check if watch rule for /etc/passwd
        already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_passwd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/passwd - Add watch rule for /etc/passwd in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/passwd -p wa -k audit_rules_usergroup_modification_passwd
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_passwd
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/passwd - Add watch
      rule for /etc/passwd in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/passwd - Add watch rule for /etc/passwd in
        /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_passwd | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/shadow - Check if watch rule for /etc/shadow already exists
      in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/shadow\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/shadow - Check if
      watch rule for /etc/shadow already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/shadow - Check if watch rule for /etc/shadow
        already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_shadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/shadow - Search /etc/audit/rules.d for other rules with
      specified key audit_rules_usergroup_modification
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_rules_usergroup_modification$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/shadow - Search /etc/audit/rules.d
      for other rules with specified key audit_rules_usergroup_modification'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/shadow - Search /etc/audit/rules.d for other
        rules with specified key audit_rules_usergroup_modification'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_shadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/shadow - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_rules_usergroup_modification.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/shadow - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/shadow - Use /etc/audit/rules.d/audit_rules_usergroup_modification.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_shadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/shadow - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/shadow - Use matched
      file as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/shadow - Use matched file as the recipient
        for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_shadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/shadow - Add watch rule for /etc/shadow in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/shadow -p wa -k audit_rules_usergroup_modification
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/shadow - Add watch
      rule for /etc/shadow in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/shadow - Add watch rule for /etc/shadow in
        /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_shadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/shadow - Check if watch rule for /etc/shadow already exists
      in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/shadow\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/shadow - Check if
      watch rule for /etc/shadow already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/shadow - Check if watch rule for /etc/shadow
        already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_shadow | default(true)
    | bool)
- block:
  - name: Record Events that Modify User/Group Information - /etc/shadow - Add watch rule for /etc/shadow in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/shadow -p wa -k audit_rules_usergroup_modification
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.5
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.5
    - audit_rules_usergroup_modification_shadow
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Events that Modify User/Group Information - /etc/shadow - Add watch
      rule for /etc/shadow in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Events that Modify User/Group Information - /etc/shadow - Add watch rule for /etc/shadow in
        /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_usergroup_modification_shadow | default(true)
    | bool)
- block:
  - name: Record Attempts to perform maintenance activities - Check if watch rule for /var/log/sudo.log already exists in
      /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/var/log/sudo.log\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_sudo_log_events
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to perform maintenance activities - Check if watch rule for
      /var/log/sudo.log already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to perform maintenance activities - Check if watch rule for /var/log/sudo.log already
        exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_sudo_log_events | default(true) | bool)
- block:
  - name: Record Attempts to perform maintenance activities - Search /etc/audit/rules.d for other rules with specified key
      maintenance
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)maintenance$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_sudo_log_events
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to perform maintenance activities - Search /etc/audit/rules.d
      for other rules with specified key maintenance'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to perform maintenance activities - Search /etc/audit/rules.d for other rules with
        specified key maintenance'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_sudo_log_events | default(true) | bool)
- block:
  - name: Record Attempts to perform maintenance activities - Use /etc/audit/rules.d/maintenance.rules as the recipient for
      the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/maintenance.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_sudo_log_events
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to perform maintenance activities - Use /etc/audit/rules.d/maintenance.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to perform maintenance activities - Use /etc/audit/rules.d/maintenance.rules as the
        recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_sudo_log_events | default(true) | bool)
- block:
  - name: Record Attempts to perform maintenance activities - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_sudo_log_events
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to perform maintenance activities - Use matched file as the
      recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to perform maintenance activities - Use matched file as the recipient for the rule''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_sudo_log_events | default(true) | bool)
- block:
  - name: Record Attempts to perform maintenance activities - Add watch rule for /var/log/sudo.log in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /var/log/sudo.log -p wa -k maintenance
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_sudo_log_events
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to perform maintenance activities - Add watch rule for /var/log/sudo.log
      in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to perform maintenance activities - Add watch rule for /var/log/sudo.log in /etc/audit/rules.d/''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_sudo_log_events | default(true) | bool)
- block:
  - name: Record Attempts to perform maintenance activities - Check if watch rule for /var/log/sudo.log already exists in
      /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/var/log/sudo.log\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_sudo_log_events
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to perform maintenance activities - Check if watch rule for
      /var/log/sudo.log already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to perform maintenance activities - Check if watch rule for /var/log/sudo.log already
        exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_sudo_log_events | default(true) | bool)
- block:
  - name: Record Attempts to perform maintenance activities - Add watch rule for /var/log/sudo.log in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /var/log/sudo.log -p wa -k maintenance
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - PCI-DSS-Req-10.2.2
    - PCI-DSS-Req-10.2.5.b
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_sudo_log_events
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to perform maintenance activities - Add watch rule for /var/log/sudo.log
      in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to perform maintenance activities - Add watch rule for /var/log/sudo.log in /etc/audit/audit.rules''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_sudo_log_events | default(true) | bool)
- block:
  - name: Set the file_groupownership_audit_configuration_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupownership_audit_configuration_newgroup: '0'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownership_audit_configuration_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownership_audit_configuration_newgroup variable if represented by gid'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Find /etc/audit/ file(s) matching ^.*audit(\.rules|d\.conf)$
    ansible.builtin.command: find -P /etc/audit/ -maxdepth 1 -type f  ! -group 0 -regextype posix-extended -regex "^.*audit(\.rules|d\.conf)$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Ensure group owner on /etc/audit/ file(s) matching ^.*audit(\.rules|d\.conf)$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupownership_audit_configuration_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Find /etc/audit/rules.d/ file(s) matching ^.*\.rules$
    ansible.builtin.command: find -P /etc/audit/rules.d/ -maxdepth 1 -type f  ! -group 0 -regextype posix-extended -regex
      "^.*\.rules$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Ensure group owner on /etc/audit/rules.d/ file(s) matching ^.*\.rules$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      group: '{{ file_groupownership_audit_configuration_newgroup }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /etc/audit/rules.d/ file(s) matching ^.*\\.rules$''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Set the file_ownership_audit_configuration_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_ownership_audit_configuration_newown: '0'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_ownership_audit_configuration_newown variable if represented
      by uid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_ownership_audit_configuration_newown variable if represented by uid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Find /etc/audit/ file(s) matching ^.*audit(\.rules|d\.conf)$
    ansible.builtin.command: find -P /etc/audit/ -maxdepth 1 -type f  ! -user 0 -regextype posix-extended -regex "^.*audit(\.rules|d\.conf)$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Ensure owner on /etc/audit/ file(s) matching ^.*audit(\.rules|d\.conf)$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_ownership_audit_configuration_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/audit/ file(s) matching ^.*audit(\\.rules|d\\.conf)$'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Find /etc/audit/rules.d/ file(s) matching ^.*\.rules$
    ansible.builtin.command: find -P /etc/audit/rules.d/ -maxdepth 1 -type f  ! -user 0 -regextype posix-extended -regex "^.*\.rules$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' (OpenSCAP is the
      score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Ensure owner on /etc/audit/rules.d/ file(s) matching ^.*\.rules$
    ansible.builtin.file:
      path: '{{ item }}'
      follow: false
      owner: '{{ file_ownership_audit_configuration_newown }}'
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /etc/audit/rules.d/ file(s) matching ^.*\\.rules$'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_configuration | default(true)
    | bool)
- block:
  - name: Find /etc/audit/ file(s)
    ansible.builtin.command: find -P /etc/audit/ -maxdepth 1 -perm /u+xs,g+xws,o+xwrt  -type f -regextype posix-extended -regex
      "^.*audit(\.rules|d\.conf)$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AU-12 b
    - configure_strategy
    - file_permissions_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/audit/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/audit/ file(s)'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_audit_configuration | default(true)
    | bool)
- block:
  - name: Set permissions for /etc/audit/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xws,o-xwrt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AU-12 b
    - configure_strategy
    - file_permissions_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/audit/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/audit/ file(s)'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_audit_configuration | default(true)
    | bool)
- block:
  - name: Find /etc/audit/rules.d/ file(s)
    ansible.builtin.command: find -P /etc/audit/rules.d/ -maxdepth 1 -perm /u+xs,g+xws,o+xwrt  -type f -regextype posix-extended
      -regex "^.*\.rules$"
    register: files_found
    changed_when: false
    failed_when: false
    check_mode: false
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AU-12 b
    - configure_strategy
    - file_permissions_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Find /etc/audit/rules.d/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Find /etc/audit/rules.d/ file(s)'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_audit_configuration | default(true)
    | bool)
- block:
  - name: Set permissions for /etc/audit/rules.d/ file(s)
    ansible.builtin.file:
      path: '{{ item }}'
      mode: u-xs,g-xws,o-xwrt
      state: file
    with_items:
    - '{{ files_found.stdout_lines }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AU-12 b
    - configure_strategy
    - file_permissions_audit_configuration
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set permissions for /etc/audit/rules.d/ file(s)'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set permissions for /etc/audit/rules.d/ file(s)'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_audit_configuration | default(true)
    | bool)
- block:
  - name: Get audit log files
    ansible.builtin.command: grep -iw ^log_file /etc/audit/auditd.conf
    failed_when: false
    register: log_file_exists
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - file_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get audit log files'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get audit log files'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_var_log_audit | default(true) | bool)
- block:
  - name: Parse log file line
    ansible.builtin.command: awk -F '=' '/^log_file/ {print $2}' /etc/audit/auditd.conf
    register: log_file_line
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - log_file_exists is not skipped and (log_file_exists.stdout | length > 0)
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - file_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Parse log file line'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Parse log file line'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_var_log_audit | default(true) | bool)
- block:
  - name: Set default log_file if not set
    ansible.builtin.set_fact:
      log_file: /var/log/audit/audit.log
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - (log_file_exists is skipped) or (log_file_exists is undefined) or (log_file_exists.stdout | length == 0)
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - file_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set default log_file if not set'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set default log_file if not set'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_var_log_audit | default(true) | bool)
- block:
  - name: Set log_file from log_file_line if not set already
    ansible.builtin.set_fact:
      log_file: '{{ log_file_line.stdout | trim }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - (log_file_exists is not skipped) and (log_file_line.stdout is defined) and (log_file_line.stdout | length > 0)
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - file_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set log_file from log_file_line if not set already'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set log_file from log_file_line if not set already'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_var_log_audit | default(true) | bool)
- block:
  - name: Get log files group
    ansible.builtin.command: grep -m 1 ^log_group /etc/audit/auditd.conf
    failed_when: false
    register: log_group_line
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - file_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Get log files group'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Get log files group'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_var_log_audit | default(true) | bool)
- block:
  - name: Parse log group line
    ansible.builtin.command: awk -F '=' '/log_group/ {print $2}' /etc/audit/auditd.conf
    register: log_group
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - (log_group_line is not skipped) and (log_group_line.stdout | length > 0)
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - file_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Parse log group line'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Parse log group line'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_var_log_audit | default(true) | bool)
- block:
  - name: Apply mode to log file when group root
    ansible.builtin.file:
      path: '{{ log_file }}'
      mode: (( log_group is defined ) and ( ( log_group.stdout | trim ) == 'root' )) | ternary( '0600', '0640')
    failed_when: false
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - log_group is not skipped
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - file_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Apply mode to log file when group root'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Apply mode to log file when group root'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_var_log_audit | default(true) | bool)
- block:
  - name: List all log file backups
    ansible.builtin.find:
      path: '{{ log_file | dirname }}'
      patterns: '{{ log_file | basename }}.*'
    register: backup_files
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - file_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''List all log file backups'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''List all log file backups'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_var_log_audit | default(true) | bool)
- block:
  - name: Apply mode to log file when group not root
    ansible.builtin.file:
      path: '{{ item }}'
      mode: (( log_group is defined ) and ( ( log_group.stdout | trim ) == 'root' ))  | ternary( '0400', '0440')
    loop: '{{ backup_files.files| map(attribute=''path'') | list }}'
    failed_when: false
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - backup_files is not skipped
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AC-6(1)
    - NIST-800-53-AU-9(4)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.1
    - file_permissions_var_log_audit
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Apply mode to log file when group not root'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Apply mode to log file when group not root'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_var_log_audit | default(true) | bool)
- block:
  - name: Set architecture for audit chmod tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_chmod
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit chmod tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit chmod tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_chmod | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for chmod for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - chmod
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of chmod in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - chmod
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of chmod in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_chmod
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for chmod for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for chmod for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_chmod | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for chmod for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - chmod
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of chmod in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - chmod
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of chmod in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_chmod
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for chmod for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for chmod for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_chmod | default(true)
    | bool)
- block:
  - name: Set architecture for audit chown tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_chown
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit chown tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit chown tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_chown | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for chown for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - chown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of chown in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - chown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of chown in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_chown
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for chown for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for chown for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_chown | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for chown for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - chown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of chown in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - chown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of chown in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_chown
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for chown for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for chown for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_chown | default(true)
    | bool)
- block:
  - name: Set architecture for audit fchmod tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchmod
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit fchmod tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit fchmod tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_fchmod | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fchmod for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchmod
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of fchmod in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchmod
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of fchmod in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchmod
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fchmod for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fchmod for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_fchmod | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fchmod for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchmod
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of fchmod in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchmod
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of fchmod in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchmod
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fchmod for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fchmod for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_fchmod | default(true)
    | bool)
- block:
  - name: Set architecture for audit fchmodat tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchmodat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit fchmodat tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit fchmodat tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_fchmodat | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fchmodat for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchmodat
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of fchmodat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchmodat
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of fchmodat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchmodat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fchmodat for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fchmodat for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_fchmodat | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fchmodat for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchmodat
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of fchmodat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchmodat
        syscall_grouping:
        - chmod
        - fchmod
        - fchmodat
    - name: Check existence of fchmodat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchmodat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fchmodat for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fchmodat for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_fchmodat | default(true)
    | bool)
- block:
  - name: Set architecture for audit fchown tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchown
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit fchown tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit fchown tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_fchown | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fchown for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of fchown in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of fchown in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchown
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fchown for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fchown for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_fchown | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fchown for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of fchown in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of fchown in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchown
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fchown for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fchown for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_fchown | default(true)
    | bool)
- block:
  - name: Set architecture for audit fchownat tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchownat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit fchownat tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit fchownat tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_fchownat | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fchownat for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchownat
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of fchownat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchownat
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of fchownat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchownat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fchownat for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fchownat for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_fchownat | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fchownat for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchownat
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of fchownat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fchownat
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of fchownat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fchownat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fchownat for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fchownat for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_fchownat | default(true)
    | bool)
- block:
  - name: Set architecture for audit fremovexattr tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fremovexattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit fremovexattr tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit fremovexattr tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_fremovexattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fremovexattr for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fremovexattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fremovexattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fremovexattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fremovexattr for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fremovexattr for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_fremovexattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fremovexattr for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fremovexattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fremovexattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fremovexattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fremovexattr for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fremovexattr for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_fremovexattr | default(true)
    | bool)
- block:
  - name: Set architecture for audit fsetxattr tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fsetxattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit fsetxattr tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit fsetxattr tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_fsetxattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fsetxattr for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fsetxattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fsetxattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fsetxattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fsetxattr for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fsetxattr for 32bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_fsetxattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for fsetxattr for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fsetxattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - fsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of fsetxattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_fsetxattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for fsetxattr for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for fsetxattr for 64bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_fsetxattr | default(true)
    | bool)
- block:
  - name: Set architecture for audit lchown tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_lchown
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit lchown tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit lchown tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_lchown | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for lchown for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lchown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of lchown in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lchown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of lchown in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_lchown
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for lchown for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for lchown for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_lchown | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for lchown for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lchown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of lchown in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lchown
        syscall_grouping:
        - chown
        - fchown
        - fchownat
        - lchown
    - name: Check existence of lchown in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_lchown
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for lchown for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for lchown for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_lchown | default(true)
    | bool)
- block:
  - name: Set architecture for audit lremovexattr tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_lremovexattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit lremovexattr tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit lremovexattr tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_lremovexattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for lremovexattr for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lremovexattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lremovexattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_lremovexattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for lremovexattr for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for lremovexattr for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_lremovexattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for lremovexattr for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lremovexattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lremovexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lremovexattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_lremovexattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for lremovexattr for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for lremovexattr for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_lremovexattr | default(true)
    | bool)
- block:
  - name: Set architecture for audit lsetxattr tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_lsetxattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit lsetxattr tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit lsetxattr tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_lsetxattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for lsetxattr for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lsetxattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lsetxattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_lsetxattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for lsetxattr for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for lsetxattr for 32bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_lsetxattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for lsetxattr for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lsetxattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - lsetxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of lsetxattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_lsetxattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for lsetxattr for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for lsetxattr for 64bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_lsetxattr | default(true)
    | bool)
- block:
  - name: Set architecture for audit removexattr tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_removexattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit removexattr tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit removexattr tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_removexattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for removexattr for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - removexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of removexattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - removexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of removexattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_removexattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for removexattr for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for removexattr for 32bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_removexattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for removexattr for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - removexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of removexattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - removexattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of removexattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_removexattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for removexattr for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for removexattr for 64bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_removexattr | default(true)
    | bool)
- block:
  - name: Set architecture for audit setxattr tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_setxattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit setxattr tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit setxattr tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_setxattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for setxattr for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - setxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of setxattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - setxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of setxattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_setxattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for setxattr for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for setxattr for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_setxattr | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for setxattr for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - setxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of setxattr in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/perm_mod.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/perm_mod.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - setxattr
        syscall_grouping:
        - fremovexattr
        - lremovexattr
        - removexattr
        - fsetxattr
        - lsetxattr
        - setxattr
    - name: Check existence of setxattr in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=perm_mod
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.5.5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.4
    - audit_rules_dac_modification_setxattr
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for setxattr for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for setxattr for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_dac_modification_setxattr | default(true)
    | bool)
- block:
  - name: Record Any Attempts to Run chacl - Perform remediation of Audit rules for /usr/bin/chacl
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/bin/chacl -F auid>=1000 -F
          auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/privileged.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/privileged.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:( -S |,)\w+)+)(
          -F path=/usr/bin/chacl -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/bin/chacl -F auid>=1000 -F auid!=unset -F key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/bin/chacl -F auid>=1000 -F
          auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F path=/usr/bin/chacl
          -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/bin/chacl -F auid>=1000 -F auid!=unset -F key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - audit_rules_execution_chacl
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Any Attempts to Run chacl - Perform remediation of Audit rules for
      /usr/bin/chacl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Any Attempts to Run chacl - Perform remediation of Audit rules for /usr/bin/chacl'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_execution_chacl | default(true) | bool)
- block:
  - name: Record Any Attempts to Run setfacl - Perform remediation of Audit rules for /usr/bin/setfacl
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/bin/setfacl -F auid>=1000 -F
          auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/privileged.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/privileged.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:( -S |,)\w+)+)(
          -F path=/usr/bin/setfacl -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/bin/setfacl -F auid>=1000 -F auid!=unset -F key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/bin/setfacl -F auid>=1000 -F
          auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F path=/usr/bin/setfacl
          -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/bin/setfacl -F auid>=1000 -F auid!=unset -F key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - audit_rules_execution_setfacl
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Any Attempts to Run setfacl - Perform remediation of Audit rules for
      /usr/bin/setfacl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Any Attempts to Run setfacl - Perform remediation of Audit rules for /usr/bin/setfacl'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_execution_setfacl | default(true) | bool)
- block:
  - name: Record Any Attempts to Run chcon - Perform remediation of Audit rules for /usr/bin/chcon
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/bin/chcon -F auid>=1000 -F
          auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/privileged.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/privileged.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:( -S |,)\w+)+)(
          -F path=/usr/bin/chcon -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/bin/chcon -F auid>=1000 -F auid!=unset -F key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/bin/chcon -F auid>=1000 -F
          auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F path=/usr/bin/chcon
          -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/bin/chcon -F auid>=1000 -F auid!=unset -F key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - audit_rules_execution_chcon
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Any Attempts to Run chcon - Perform remediation of Audit rules for
      /usr/bin/chcon'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Any Attempts to Run chcon - Perform remediation of Audit rules for /usr/bin/chcon'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_execution_chcon | default(true) | bool)
- block:
  - name: Set architecture for audit rename tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_rename
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit rename tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit rename tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_file_deletion_events_rename | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for rename for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - rename
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of rename in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/delete.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/delete.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - rename
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of rename in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_rename
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for rename for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for rename for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_file_deletion_events_rename | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for rename for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - rename
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of rename in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/delete.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/delete.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - rename
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of rename in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_rename
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for rename for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for rename for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_file_deletion_events_rename | default(true)
    | bool)
- block:
  - name: Set architecture for audit renameat tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_renameat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit renameat tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit renameat tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_file_deletion_events_renameat | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for renameat for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - renameat
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of renameat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/delete.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/delete.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - renameat
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of renameat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_renameat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for renameat for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for renameat for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_file_deletion_events_renameat | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for renameat for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - renameat
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of renameat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/delete.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/delete.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - renameat
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of renameat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_renameat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for renameat for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for renameat for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_file_deletion_events_renameat | default(true)
    | bool)
- block:
  - name: Set architecture for audit unlink tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_unlink
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit unlink tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit unlink tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_file_deletion_events_unlink | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for unlink for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - unlink
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of unlink in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/delete.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/delete.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - unlink
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of unlink in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_unlink
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for unlink for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for unlink for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_file_deletion_events_unlink | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for unlink for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - unlink
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of unlink in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/delete.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/delete.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - unlink
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of unlink in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_unlink
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for unlink for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for unlink for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_file_deletion_events_unlink | default(true)
    | bool)
- block:
  - name: Set architecture for audit unlinkat tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_unlinkat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit unlinkat tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit unlinkat tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_file_deletion_events_unlinkat | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for unlinkat for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - unlinkat
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of unlinkat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/delete.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/delete.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - unlinkat
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of unlinkat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_unlinkat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for unlinkat for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for unlinkat for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_file_deletion_events_unlinkat | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for unlinkat for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - unlinkat
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of unlinkat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/delete.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/delete.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - unlinkat
        syscall_grouping:
        - unlink
        - unlinkat
        - rename
        - renameat
        - renameat2
        - rmdir
    - name: Check existence of unlinkat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=delete
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.7
    - audit_rules_file_deletion_events_unlinkat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for unlinkat for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for unlinkat for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_file_deletion_events_unlinkat | default(true)
    | bool)
- block:
  - name: Set architecture for audit creat tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_creat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit creat tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit creat tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_creat |
    default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for creat EACCES for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - creat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of creat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - creat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of creat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_creat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for creat EACCES for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for creat EACCES for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_creat |
    default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for creat EACCES for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - creat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of creat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - creat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of creat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_creat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for creat EACCES for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for creat EACCES for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_creat |
    default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for creat EPERM for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - creat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of creat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - creat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of creat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_creat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for creat EPERM for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for creat EPERM for 32bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_creat |
    default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for creat EPERM for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - creat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of creat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - creat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of creat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_creat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for creat EPERM for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for creat EPERM for 64bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_creat |
    default(true) | bool)
- block:
  - name: Set architecture for audit ftruncate tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_ftruncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit ftruncate tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit ftruncate tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_ftruncate
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for ftruncate EACCES for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - ftruncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of ftruncate in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - ftruncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of ftruncate in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_ftruncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for ftruncate EACCES for 32bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for ftruncate EACCES for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_ftruncate
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for ftruncate EACCES for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - ftruncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of ftruncate in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - ftruncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of ftruncate in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_ftruncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for ftruncate EACCES for 64bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for ftruncate EACCES for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_ftruncate
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for ftruncate EPERM for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - ftruncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of ftruncate in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - ftruncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of ftruncate in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_ftruncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for ftruncate EPERM for 32bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for ftruncate EPERM for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_ftruncate
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for ftruncate EPERM for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - ftruncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of ftruncate in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - ftruncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of ftruncate in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_ftruncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for ftruncate EPERM for 64bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for ftruncate EPERM for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_ftruncate
    | default(true) | bool)
- block:
  - name: Set architecture for audit open tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_open
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit open tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit open tasks'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_open |
    default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for open EACCES for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - open
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of open in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - open
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of open in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_open
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for open EACCES for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for open EACCES for 32bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_open |
    default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for open EACCES for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - open
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of open in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - open
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of open in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_open
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for open EACCES for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for open EACCES for 64bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_open |
    default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for open EPERM for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - open
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of open in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - open
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of open in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_open
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for open EPERM for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for open EPERM for 32bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_open |
    default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for open EPERM for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - open
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of open in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - open
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of open in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_open
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for open EPERM for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for open EPERM for 64bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_open |
    default(true) | bool)
- block:
  - name: Set architecture for audit openat tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_openat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit openat tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit openat tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_openat
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for openat EACCES for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - openat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of openat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - openat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of openat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_openat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for openat EACCES for 32bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for openat EACCES for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_openat
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for openat EACCES for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - openat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of openat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - openat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of openat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_openat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for openat EACCES for 64bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for openat EACCES for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_openat
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for openat EPERM for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - openat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of openat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - openat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of openat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_openat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for openat EPERM for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for openat EPERM for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_openat
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for openat EPERM for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - openat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of openat in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - openat
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of openat in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_openat
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for openat EPERM for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for openat EPERM for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_openat
    | default(true) | bool)
- block:
  - name: Set architecture for audit truncate tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_truncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit truncate tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit truncate tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_truncate
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for truncate EACCES for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - truncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of truncate in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - truncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of truncate in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_truncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for truncate EACCES for 32bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for truncate EACCES for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_truncate
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for truncate EACCES for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - truncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of truncate in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - truncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of truncate in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EACCES -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EACCES -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EACCES -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_truncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for truncate EACCES for 64bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for truncate EACCES for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_truncate
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for truncate EPERM for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - truncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of truncate in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - truncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of truncate in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_truncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for truncate EPERM for 32bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for truncate EPERM for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_truncate
    | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for truncate EPERM for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - truncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of truncate in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/access.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/access.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - truncate
        syscall_grouping:
        - creat
        - ftruncate
        - truncate
        - open
        - openat
        - open_by_handle_at
    - name: Check existence of truncate in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F exit=-EPERM -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          exit=-EPERM -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F exit=-EPERM -F auid>=1000 -F auid!=unset -F key=access
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.1
    - PCI-DSS-Req-10.2.4
    - audit_rules_unsuccessful_file_modification_truncate
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for truncate EPERM for 64bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for truncate EPERM for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_unsuccessful_file_modification_truncate
    | default(true) | bool)
- block:
  - name: Set architecture for audit finit_module tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - audit_rules_kernel_module_loading_create
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit finit_module tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit finit_module tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_kernel_module_loading_create | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for finit_module for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - create_module
        syscall_grouping: []
    - name: Check existence of create_module in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/module-change.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/module-change.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - create_module
        syscall_grouping: []
    - name: Check existence of create_module in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - audit_rules_kernel_module_loading_create
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for finit_module for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for finit_module for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_kernel_module_loading_create | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for finit_module for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - create_module
        syscall_grouping: []
    - name: Check existence of create_module in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/module-change.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/module-change.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - create_module
        syscall_grouping: []
    - name: Check existence of create_module in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - audit_rules_kernel_module_loading_create
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for finit_module for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for finit_module for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_kernel_module_loading_create | default(true)
    | bool)
- block:
  - name: Set architecture for audit delete_module tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - audit_rules_kernel_module_loading_delete
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit delete_module tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit delete_module tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_kernel_module_loading_delete | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for delete_module for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - delete_module
        syscall_grouping: []
    - name: Check existence of delete_module in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/module-change.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/module-change.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - delete_module
        syscall_grouping: []
    - name: Check existence of delete_module in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - audit_rules_kernel_module_loading_delete
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for delete_module for 32bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for delete_module for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_kernel_module_loading_delete | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for delete_module for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - delete_module
        syscall_grouping: []
    - name: Check existence of delete_module in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/module-change.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/module-change.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - delete_module
        syscall_grouping: []
    - name: Check existence of delete_module in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - audit_rules_kernel_module_loading_delete
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for delete_module for 64bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for delete_module for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_kernel_module_loading_delete | default(true)
    | bool)
- block:
  - name: Set architecture for audit finit_module tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - audit_rules_kernel_module_loading_finit
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit finit_module tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit finit_module tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_kernel_module_loading_finit | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for finit_module for x86 platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - finit_module
        syscall_grouping:
        - init_module
        - finit_module
    - name: Check existence of finit_module in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/module-change.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/module-change.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - finit_module
        syscall_grouping:
        - init_module
        - finit_module
    - name: Check existence of finit_module in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - audit_rules_kernel_module_loading_finit
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for finit_module for x86 platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for finit_module for x86 platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_kernel_module_loading_finit | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for finit_module for x86_64 platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - finit_module
        syscall_grouping:
        - init_module
        - finit_module
    - name: Check existence of finit_module in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/module-change.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/module-change.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - finit_module
        syscall_grouping:
        - init_module
        - finit_module
    - name: Check existence of finit_module in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - audit_rules_kernel_module_loading_finit
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for finit_module for x86_64 platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for finit_module for x86_64 platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_kernel_module_loading_finit | default(true)
    | bool)
- block:
  - name: Set architecture for audit init_module tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - audit_rules_kernel_module_loading_init
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit init_module tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit init_module tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_kernel_module_loading_init | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for init_module for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - init_module
        syscall_grouping:
        - init_module
        - finit_module
    - name: Check existence of init_module in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/module-change.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/module-change.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - init_module
        syscall_grouping:
        - init_module
        - finit_module
    - name: Check existence of init_module in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - audit_rules_kernel_module_loading_init
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for init_module for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for init_module for 32bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_kernel_module_loading_init | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for init_module for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - init_module
        syscall_grouping:
        - init_module
        - finit_module
    - name: Check existence of init_module in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/module-change.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/module-change.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - init_module
        syscall_grouping:
        - init_module
        - finit_module
    - name: Check existence of init_module in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.7
    - audit_rules_kernel_module_loading_init
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for init_module for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for init_module for 64bit platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_kernel_module_loading_init | default(true)
    | bool)
- block:
  - name: Set architecture for audit query_module tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - audit_rules_kernel_module_loading_query
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit query_module tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit query_module tasks'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_kernel_module_loading_query | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for query_module for x86 platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - query_module
        syscall_grouping:
        - init_module
        - query_module
    - name: Check existence of query_module in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/module-change.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/module-change.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - query_module
        syscall_grouping:
        - init_module
        - query_module
    - name: Check existence of query_module in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    tags:
    - audit_rules_kernel_module_loading_query
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for query_module for x86 platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for query_module for x86 platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_kernel_module_loading_query | default(true)
    | bool)
- block:
  - name: Perform remediation of Audit rules for query_module for x86_64 platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - query_module
        syscall_grouping:
        - init_module
        - query_module
    - name: Check existence of query_module in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/module-change.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/module-change.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - query_module
        syscall_grouping:
        - init_module
        - query_module
    - name: Check existence of query_module in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F auid>=1000 -F auid!=unset -F key=module-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - not ( ansible_architecture == "aarch64" )
    - audit_arch == "b64"
    tags:
    - audit_rules_kernel_module_loading_query
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for query_module for x86_64 platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for query_module for x86_64 platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_kernel_module_loading_query | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - faillock - Check if watch rule for {{ var_accounts_passwords_pam_faillock_dir
      }} already exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+{{ var_accounts_passwords_pam_faillock_dir }}\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_faillock
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - faillock - Check if watch
      rule for {{ var_accounts_passwords_pam_faillock_dir }} already exists in /etc/audit/rules.d/'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - faillock - Check if watch rule for {{ var_accounts_passwords_pam_faillock_dir
        }} already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_login_events_faillock | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - faillock - Search /etc/audit/rules.d for other rules with specified
      key logins
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)logins$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_faillock
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - faillock - Search /etc/audit/rules.d
      for other rules with specified key logins'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - faillock - Search /etc/audit/rules.d for other
        rules with specified key logins'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_login_events_faillock | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - faillock - Use /etc/audit/rules.d/logins.rules as the recipient
      for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/logins.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_faillock
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - faillock - Use /etc/audit/rules.d/logins.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - faillock - Use /etc/audit/rules.d/logins.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_login_events_faillock | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - faillock - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_faillock
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - faillock - Use matched
      file as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - faillock - Use matched file as the recipient for
        the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_login_events_faillock | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - faillock - Add watch rule for {{ var_accounts_passwords_pam_faillock_dir
      }} in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w {{ var_accounts_passwords_pam_faillock_dir }} -p wa -k logins
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_faillock
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - faillock - Add watch rule
      for {{ var_accounts_passwords_pam_faillock_dir }} in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - faillock - Add watch rule for {{ var_accounts_passwords_pam_faillock_dir
        }} in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_login_events_faillock | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - faillock - Check if watch rule for {{ var_accounts_passwords_pam_faillock_dir
      }} already exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+{{ var_accounts_passwords_pam_faillock_dir }}\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_faillock
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - faillock - Check if watch
      rule for {{ var_accounts_passwords_pam_faillock_dir }} already exists in /etc/audit/audit.rules'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - faillock - Check if watch rule for {{ var_accounts_passwords_pam_faillock_dir
        }} already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so
        later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_login_events_faillock | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - faillock - Add watch rule for {{ var_accounts_passwords_pam_faillock_dir
      }} in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w {{ var_accounts_passwords_pam_faillock_dir }} -p wa -k logins
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_faillock
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - faillock - Add watch rule
      for {{ var_accounts_passwords_pam_faillock_dir }} in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - faillock - Add watch rule for {{ var_accounts_passwords_pam_faillock_dir
        }} in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_login_events_faillock | default(true)
    | bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - lastlog - Check if watch rule for /var/log/lastlog already exists
      in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/var/log/lastlog\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - lastlog - Check if watch
      rule for /var/log/lastlog already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - lastlog - Check if watch rule for /var/log/lastlog
        already exists in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_login_events_lastlog | default(true) |
    bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - lastlog - Search /etc/audit/rules.d for other rules with specified
      key logins
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)logins$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - lastlog - Search /etc/audit/rules.d
      for other rules with specified key logins'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - lastlog - Search /etc/audit/rules.d for other
        rules with specified key logins'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_login_events_lastlog | default(true) |
    bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - lastlog - Use /etc/audit/rules.d/logins.rules as the recipient
      for the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/logins.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - lastlog - Use /etc/audit/rules.d/logins.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - lastlog - Use /etc/audit/rules.d/logins.rules
        as the recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_login_events_lastlog | default(true) |
    bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - lastlog - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - lastlog - Use matched file
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - lastlog - Use matched file as the recipient for
        the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_login_events_lastlog | default(true) |
    bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - lastlog - Add watch rule for /var/log/lastlog in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /var/log/lastlog -p wa -k logins
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - lastlog - Add watch rule
      for /var/log/lastlog in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - lastlog - Add watch rule for /var/log/lastlog
        in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_login_events_lastlog | default(true) |
    bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - lastlog - Check if watch rule for /var/log/lastlog already exists
      in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/var/log/lastlog\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - lastlog - Check if watch
      rule for /var/log/lastlog already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - lastlog - Check if watch rule for /var/log/lastlog
        already exists in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_login_events_lastlog | default(true) |
    bool)
- block:
  - name: Record Attempts to Alter Logon and Logout Events - lastlog - Add watch rule for /var/log/lastlog in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /var/log/lastlog -p wa -k logins
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.3
    - PCI-DSSv4-10.2
    - PCI-DSSv4-10.2.1
    - PCI-DSSv4-10.2.1.3
    - audit_rules_login_events_lastlog
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter Logon and Logout Events - lastlog - Add watch rule
      for /var/log/lastlog in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter Logon and Logout Events - lastlog - Add watch rule for /var/log/lastlog
        in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_login_events_lastlog | default(true) |
    bool)
- block:
  - name: Ensure auditd Collects Information on the Use of Privileged Commands - Set List of Mount Points Which Permits Execution
      of Privileged Commands
    ansible.builtin.set_fact:
      privileged_mount_points: '{{ (ansible_facts.mounts | rejectattr(''options'', ''search'', ''noexec|nosuid'') | rejectattr(''mount'',
        ''match'', ''/proc($|/.*$)'') | map(attribute=''mount'') | list ) }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - audit_rules_privileged_commands
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects Information on the Use of Privileged Commands - Set
      List of Mount Points Which Permits Execution of Privileged Commands'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects Information on the Use of Privileged Commands - Set List of Mount Points Which
        Permits Execution of Privileged Commands'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_privileged_commands | default(true) |
    bool)
- block:
  - name: Ensure auditd Collects Information on the Use of Privileged Commands - Search for Privileged Commands in Eligible
      Mount Points
    ansible.builtin.shell:
      cmd: find {{ item }} -xdev -perm /6000 -type f 2>/dev/null
    register: result_privileged_commands_search
    changed_when: false
    failed_when: false
    with_items: '{{ privileged_mount_points }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - audit_rules_privileged_commands
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects Information on the Use of Privileged Commands - Search
      for Privileged Commands in Eligible Mount Points'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects Information on the Use of Privileged Commands - Search for Privileged Commands
        in Eligible Mount Points'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_privileged_commands | default(true) |
    bool)
- block:
  - name: Ensure auditd Collects Information on the Use of Privileged Commands - Set List of Privileged Commands Found in
      Eligible Mount Points
    ansible.builtin.set_fact:
      privileged_commands: '{{ privileged_commands | default([]) + item.stdout_lines }}'
    loop: '{{ result_privileged_commands_search.results }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - item is not skipped
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - audit_rules_privileged_commands
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects Information on the Use of Privileged Commands - Set
      List of Privileged Commands Found in Eligible Mount Points'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects Information on the Use of Privileged Commands - Set List of Privileged Commands
        Found in Eligible Mount Points'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_privileged_commands | default(true) |
    bool)
- block:
  - name: Ensure auditd Collects Information on the Use of Privileged Commands - Privileged Commands are Present in the System
    block:
    - name: Ensure auditd Collects Information on the Use of Privileged Commands - Ensure Rules for All Privileged Commands
        in augenrules Format
      ansible.builtin.lineinfile:
        path: /etc/audit/rules.d/privileged.rules
        line: -a always,exit -F path={{ item }} -F perm=x -F auid>=1000 -F auid!=unset -F key=privileged
        regexp: ^.*path={{ item | regex_escape() }} .*$
        create: true
      with_items:
      - '{{ privileged_commands }}'
    - name: Ensure auditd Collects Information on the Use of Privileged Commands - Ensure Rules for All Privileged Commands
        in auditctl Format
      ansible.builtin.lineinfile:
        path: /etc/audit/audit.rules
        line: -a always,exit -F path={{ item }} -F perm=x -F auid>=1000 -F auid!=unset -F key=privileged
        regexp: ^.*path={{ item | regex_escape() }} .*$
        create: true
      with_items:
      - '{{ privileged_commands }}'
    - name: Ensure auditd Collects Information on the Use of Privileged Commands - Search for Duplicated Rules in Other Files
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        recurse: false
        contains: ^-a always,exit -F path={{ item }} .*$
        patterns: '*.rules'
      with_items:
      - '{{ privileged_commands }}'
      register: result_augenrules_files
    - name: Ensure auditd Collects Information on the Use of Privileged Commands - Ensure Rules for Privileged Commands are
        Defined Only in One File
      ansible.builtin.lineinfile:
        path: '{{ item.1.path }}'
        regexp: ^-a always,exit -F path={{ item.0.item }} .*$
        state: absent
      with_subelements:
      - '{{ result_augenrules_files.results }}'
      - files
      when:
      - item.1.path != '/etc/audit/rules.d/privileged.rules'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - privileged_commands is defined
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-2(4)
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.2.2
    - audit_rules_privileged_commands
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects Information on the Use of Privileged Commands - Privileged
      Commands are Present in the System'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects Information on the Use of Privileged Commands - Privileged Commands are Present
        in the System'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP
        evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_privileged_commands | default(true) |
    bool)
- block:
  - name: Ensure auditd Collects Information on the Use of Privileged Commands - kmod - Perform remediation of Audit rules
      for /usr/bin/kmod
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/bin/kmod -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/privileged.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/privileged.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:( -S |,)\w+)+)(
          -F path=/usr/bin/kmod -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/bin/kmod -F auid>=1000 -F auid!=unset -F key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/bin/kmod -F auid>=1000 -F auid!=unset
          (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F path=/usr/bin/kmod
          -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/bin/kmod -F auid>=1000 -F auid!=unset -F key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - NIST-800-53-AU-12(a)
    - NIST-800-53-AU-12.1(ii)
    - NIST-800-53-AU-12.1(iv)AU-12(c)
    - NIST-800-53-AU-3
    - NIST-800-53-AU-3.1
    - NIST-800-53-MA-4(1)(a)
    - audit_rules_privileged_commands_kmod
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects Information on the Use of Privileged Commands - kmod
      - Perform remediation of Audit rules for /usr/bin/kmod'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects Information on the Use of Privileged Commands - kmod - Perform remediation
        of Audit rules for /usr/bin/kmod'' raised an error (module or templating) and was skipped; continuing so later rules
        still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_privileged_commands_kmod | default(true)
    | bool)
- block:
  - name: Ensure auditd Collects Information on the Use of Privileged Commands - usermod - Perform remediation of Audit rules
      for /usr/sbin/usermod
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/sbin/usermod -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/privileged.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/privileged.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:( -S |,)\w+)+)(
          -F path=/usr/sbin/usermod -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/sbin/usermod -F auid>=1000 -F auid!=unset -F key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls: []
        syscall_grouping: []
    - name: Check existence of  in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F path=/usr/sbin/usermod -F auid>=1000
          -F auid!=unset (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F path=/usr/sbin/usermod
          -F auid>=1000 -F auid!=unset (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit{{ syscalls | join(',') }} -F path=/usr/sbin/usermod -F auid>=1000 -F auid!=unset -F key=privileged
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - audit_rules_privileged_commands_usermod
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Ensure auditd Collects Information on the Use of Privileged Commands - usermod
      - Perform remediation of Audit rules for /usr/sbin/usermod'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure auditd Collects Information on the Use of Privileged Commands - usermod - Perform remediation
        of Audit rules for /usr/sbin/usermod'' raised an error (module or templating) and was skipped; continuing so later
        rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_privileged_commands_usermod | default(true)
    | bool)
- block:
  - name: Set architecture for audit tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_adjtimex
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit tasks'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_time_adjtimex | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for adjtimex for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - adjtimex
        syscall_grouping:
        - adjtimex
        - settimeofday
        - stime
    - name: Check existence of adjtimex in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/audit_time_rules.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/audit_time_rules.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=audit_time_rules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - adjtimex
        syscall_grouping:
        - adjtimex
        - settimeofday
        - stime
    - name: Check existence of adjtimex in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=audit_time_rules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_adjtimex
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for adjtimex for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for adjtimex for 32bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_time_adjtimex | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for adjtimex for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - adjtimex
        syscall_grouping:
        - adjtimex
        - settimeofday
    - name: Check existence of adjtimex in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/audit_time_rules.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/audit_time_rules.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F key=audit_time_rules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - adjtimex
        syscall_grouping:
        - adjtimex
        - settimeofday
        - stime
    - name: Check existence of adjtimex in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F key=audit_time_rules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_adjtimex
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for adjtimex for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for adjtimex for 64bit platform'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_time_adjtimex | default(true) | bool)
- block:
  - name: Set architecture for audit tasks
    ansible.builtin.set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_clock_settime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit tasks'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_time_clock_settime | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for clock_settime for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - clock_settime
        syscall_grouping: []
    - name: Check existence of clock_settime in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F a0=0x0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/time-change.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/time-change.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F a0=0x0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F a0=0x0 -F key=time-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - clock_settime
        syscall_grouping: []
    - name: Check existence of clock_settime in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F a0=0x0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          a0=0x0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F a0=0x0 -F key=time-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_clock_settime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for clock_settime for 32bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for clock_settime for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_time_clock_settime | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for clock_settime for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - clock_settime
        syscall_grouping: []
    - name: Check existence of clock_settime in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F a0=0x0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/time-change.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/time-change.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( -F a0=0x0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F a0=0x0 -F key=time-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - clock_settime
        syscall_grouping: []
    - name: Check existence of clock_settime in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* -F a0=0x0 (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( -F
          a0=0x0 (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F a0=0x0 -F key=time-change
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_clock_settime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for clock_settime for 64bit platform''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for clock_settime for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_time_clock_settime | default(true) | bool)
- block:
  - name: Set architecture for audit tasks
    set_fact:
      audit_arch: b64
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ansible_architecture == "aarch64" or ansible_architecture == "ppc64" or ansible_architecture == "ppc64le" or ansible_architecture
      == "s390x" or ansible_architecture == "x86_64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_settimeofday
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Set architecture for audit tasks'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set architecture for audit tasks'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_time_settimeofday | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for settimeofday for 32bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - settimeofday
        syscall_grouping:
        - adjtimex
        - settimeofday
        - stime
    - name: Check existence of settimeofday in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/audit_time_rules.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/audit_time_rules.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=audit_time_rules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - settimeofday
        syscall_grouping:
        - adjtimex
        - settimeofday
        - stime
    - name: Check existence of settimeofday in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=audit_time_rules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_settimeofday
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for settimeofday for 32bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for settimeofday for 32bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_time_settimeofday | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for settimeofday for 64bit platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - settimeofday
        syscall_grouping:
        - adjtimex
        - settimeofday
        - stime
    - name: Check existence of settimeofday in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/audit_time_rules.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/audit_time_rules.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F key=audit_time_rules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - settimeofday
        syscall_grouping:
        - adjtimex
        - settimeofday
        - stime
    - name: Check existence of settimeofday in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b64(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b64)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b64 -S {{ syscalls | join(',') }} -F key=audit_time_rules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - audit_arch == "b64"
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_settimeofday
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for settimeofday for 64bit platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for settimeofday for 64bit platform'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_time_settimeofday | default(true) | bool)
- block:
  - name: Perform remediation of Audit rules for stime syscall for x86 platform
    block:
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - stime
        syscall_grouping:
        - adjtimex
        - settimeofday
        - stime
    - name: Check existence of stime in /etc/audit/rules.d/
      ansible.builtin.find:
        paths: /etc/audit/rules.d
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: '*.rules'
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Reset syscalls found per file
      ansible.builtin.set_fact:
        syscalls_per_file: {}
        found_paths_dict: {}
    - name: Declare syscalls found per file
      ansible.builtin.set_fact: syscalls_per_file="{{ syscalls_per_file | combine( {item.files[0].path :[item.item] + syscalls_per_file.get(item.files[0].path,
        []) } ) }}"
      loop: '{{ find_command.results | selectattr(''matched'') | list }}'
    - name: Declare files where syscalls were found
      ansible.builtin.set_fact: found_paths="{{ find_command.results | map(attribute='files') | flatten | map(attribute='path')
        | list }}"
    - name: Count occurrences of syscalls in paths
      ansible.builtin.set_fact: found_paths_dict="{{ found_paths_dict | combine({ item:1+found_paths_dict.get(item, 0) })
        }}"
      loop: '{{ find_command.results | map(attribute=''files'') | flatten | map(attribute=''path'') | list }}'
    - name: Get path with most syscalls
      ansible.builtin.set_fact: audit_file="{{ (found_paths_dict | dict2items() | sort(attribute='value') | last).key }}"
      when: found_paths | length >= 1
    - name: No file with syscall found, set path to /etc/audit/rules.d/audit_time_rules.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/rules.d/audit_time_rules.rules"
      when: found_paths | length == 0
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_per_file[audit_file] | join("|") }}))\b)((?:(
          -S |,)\w+)+)( (?:-k |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=audit_time_rules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    - name: Declare list of syscalls
      ansible.builtin.set_fact:
        syscalls:
        - stime
        syscall_grouping:
        - adjtimex
        - settimeofday
        - stime
    - name: Check existence of stime in /etc/audit/audit.rules
      ansible.builtin.find:
        paths: /etc/audit
        contains: -a always,exit -F arch=b32(( -S |,)\w+)*(( -S |,){{ item }})+(( -S |,)\w+)* (-k\s+|-F\s+key=)\S+\s*$
        patterns: audit.rules
      register: find_command
      loop: '{{ (syscall_grouping + syscalls) | unique }}'
    - name: Set path to /etc/audit/audit.rules
      ansible.builtin.set_fact: audit_file="/etc/audit/audit.rules"
    - name: Declare found syscalls
      ansible.builtin.set_fact: syscalls_found="{{ find_command.results | selectattr('matched') | map(attribute='item') |
        list }}"
    - name: Declare missing syscalls
      ansible.builtin.set_fact: missing_syscalls="{{ syscalls | difference(syscalls_found) }}"
    - name: Replace the audit rule in {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        regexp: (-a always,exit -F arch=b32)(?=.*(?:(?:-S |,)(?:{{ syscalls_found | join("|") }}))\b)((?:( -S |,)\w+)+)( (?:-k
          |-F key=)\w+)
        line: \1\2\3{{ missing_syscalls | join("\3") }}\4
        backrefs: true
        state: present
        mode: g-rwx,o-rwx
      when: syscalls_found | length > 0 and missing_syscalls | length > 0
    - name: Add the audit rule to {{ audit_file }}
      ansible.builtin.lineinfile:
        path: '{{ audit_file }}'
        line: -a always,exit -F arch=b32 -S {{ syscalls | join(',') }} -F key=audit_time_rules
        create: true
        mode: g-rwx,o-rwx
        state: present
      when: syscalls_found | length == 0
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - ( not ( ansible_architecture == "aarch64" ) and not ( ansible_architecture == "s390x" ) )
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_stime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Perform remediation of Audit rules for stime syscall for x86 platform'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Perform remediation of Audit rules for stime syscall for x86 platform'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_time_stime | default(true) | bool)
- block:
  - name: Record Attempts to Alter the localtime File - Check if watch rule for /etc/localtime already exists in /etc/audit/rules.d/
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^\s*-w\s+/etc/localtime\s+-p\s+wa(\s|$)+
      patterns: '*.rules'
    register: find_existing_watch_rules_d
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_watch_localtime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter the localtime File - Check if watch rule for /etc/localtime
      already exists in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter the localtime File - Check if watch rule for /etc/localtime already exists
        in /etc/audit/rules.d/'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_time_watch_localtime | default(true) |
    bool)
- block:
  - name: Record Attempts to Alter the localtime File - Search /etc/audit/rules.d for other rules with specified key audit_time_rules
    ansible.builtin.find:
      paths: /etc/audit/rules.d
      contains: ^.*(?:-F key=|-k\s+)audit_time_rules$
      patterns: '*.rules'
    register: find_watch_key
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_watch_localtime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter the localtime File - Search /etc/audit/rules.d for
      other rules with specified key audit_time_rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter the localtime File - Search /etc/audit/rules.d for other rules with specified
        key audit_time_rules'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_time_watch_localtime | default(true) |
    bool)
- block:
  - name: Record Attempts to Alter the localtime File - Use /etc/audit/rules.d/audit_time_rules.rules as the recipient for
      the rule
    ansible.builtin.set_fact:
      all_files:
      - /etc/audit/rules.d/audit_time_rules.rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched == 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_watch_localtime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter the localtime File - Use /etc/audit/rules.d/audit_time_rules.rules
      as the recipient for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter the localtime File - Use /etc/audit/rules.d/audit_time_rules.rules as the
        recipient for the rule'' raised an error (module or templating) and was skipped; continuing so later rules still run.
        OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_time_watch_localtime | default(true) |
    bool)
- block:
  - name: Record Attempts to Alter the localtime File - Use matched file as the recipient for the rule
    ansible.builtin.set_fact:
      all_files:
      - '{{ find_watch_key.files | map(attribute=''path'') | list | first }}'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_watch_key.matched is defined and find_watch_key.matched > 0 and find_existing_watch_rules_d.matched is defined
      and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_watch_localtime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter the localtime File - Use matched file as the recipient
      for the rule'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter the localtime File - Use matched file as the recipient for the rule'' raised
        an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting
        system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_time_watch_localtime | default(true) |
    bool)
- block:
  - name: Record Attempts to Alter the localtime File - Add watch rule for /etc/localtime in /etc/audit/rules.d/
    ansible.builtin.lineinfile:
      path: '{{ all_files[0] }}'
      line: -w /etc/localtime -p wa -k audit_time_rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_rules_d.matched is defined and find_existing_watch_rules_d.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_watch_localtime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter the localtime File - Add watch rule for /etc/localtime
      in /etc/audit/rules.d/'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter the localtime File - Add watch rule for /etc/localtime in /etc/audit/rules.d/''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_time_watch_localtime | default(true) |
    bool)
- block:
  - name: Record Attempts to Alter the localtime File - Check if watch rule for /etc/localtime already exists in /etc/audit/audit.rules
    ansible.builtin.find:
      paths: /etc/audit/
      contains: ^\s*-w\s+/etc/localtime\s+-p\s+wa(\s|$)+
      patterns: audit.rules
    register: find_existing_watch_audit_rules
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_watch_localtime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter the localtime File - Check if watch rule for /etc/localtime
      already exists in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter the localtime File - Check if watch rule for /etc/localtime already exists
        in /etc/audit/audit.rules'' raised an error (module or templating) and was skipped; continuing so later rules still
        run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_time_watch_localtime | default(true) |
    bool)
- block:
  - name: Record Attempts to Alter the localtime File - Add watch rule for /etc/localtime in /etc/audit/audit.rules
    ansible.builtin.lineinfile:
      line: -w /etc/localtime -p wa -k audit_time_rules
      state: present
      dest: /etc/audit/audit.rules
      create: true
      mode: '0600'
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    - find_existing_watch_audit_rules.matched is defined and find_existing_watch_audit_rules.matched == 0
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.1.7
    - NIST-800-53-AC-6(9)
    - NIST-800-53-AU-12(c)
    - NIST-800-53-AU-2(d)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.4.2.b
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.3
    - audit_rules_time_watch_localtime
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Record Attempts to Alter the localtime File - Add watch rule for /etc/localtime
      in /etc/audit/audit.rules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Record Attempts to Alter the localtime File - Add watch rule for /etc/localtime in /etc/audit/audit.rules''
        raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the
        resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_audit_rules_time_watch_localtime | default(true) |
    bool)
- block:
  - name: Configure auditd mail_acct Action on Low Disk Space - Configure auditd mail_acct Action on Low Disk Space
    ansible.builtin.lineinfile:
      dest: /etc/audit/auditd.conf
      regexp: ^action_mail_acct
      line: action_mail_acct = {{ var_auditd_action_mail_acct }}
      state: present
      create: true
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AU-5(2)
    - NIST-800-53-AU-5(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(1)
    - PCI-DSS-Req-10.7.a
    - auditd_data_retention_action_mail_acct
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure auditd mail_acct Action on Low Disk Space - Configure auditd mail_acct
      Action on Low Disk Space'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure auditd mail_acct Action on Low Disk Space - Configure auditd mail_acct Action on Low Disk
        Space'' raised an error (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates
        the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_auditd_data_retention_action_mail_acct | default(true)
    | bool)
- block:
  - name: Configure auditd admin_space_left Action on Low Disk Space
    ansible.builtin.lineinfile:
      dest: /etc/audit/auditd.conf
      line: admin_space_left_action = {{ var_auditd_admin_space_left_action .split('|')[0] }}
      regexp: ^\s*admin_space_left_action\s*=\s*.*$
      state: present
      create: true
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AU-5(1)
    - NIST-800-53-AU-5(2)
    - NIST-800-53-AU-5(4)
    - NIST-800-53-AU-5(b)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.7
    - PCI-DSSv4-10.5
    - PCI-DSSv4-10.5.1
    - auditd_data_retention_admin_space_left_action
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure auditd admin_space_left Action on Low Disk Space'' (OpenSCAP is
      the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure auditd admin_space_left Action on Low Disk Space'' raised an error (module or templating)
        and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_auditd_data_retention_admin_space_left_action | default(true)
    | bool)
- block:
  - name: Configure auditd Max Log File Size
    ansible.builtin.lineinfile:
      dest: /etc/audit/auditd.conf
      regexp: ^\s*max_log_file\s*=\s*.*$
      line: max_log_file = {{ var_auditd_max_log_file }}
      state: present
      create: true
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-53-AU-11
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.7
    - auditd_data_retention_max_log_file
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure auditd Max Log File Size'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure auditd Max Log File Size'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_auditd_data_retention_max_log_file | default(true)
    | bool)
- block:
  - name: Configure auditd max_log_file_action Upon Reaching Maximum Log Size
    ansible.builtin.lineinfile:
      dest: /etc/audit/auditd.conf
      line: max_log_file_action = {{ var_auditd_max_log_file_action }}
      regexp: ^\s*max_log_file_action\s*=\s*.*$
      state: present
      create: true
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-53-AU-5(1)
    - NIST-800-53-AU-5(2)
    - NIST-800-53-AU-5(4)
    - NIST-800-53-AU-5(b)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.7
    - auditd_data_retention_max_log_file_action
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure auditd max_log_file_action Upon Reaching Maximum Log Size'' (OpenSCAP
      is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure auditd max_log_file_action Upon Reaching Maximum Log Size'' raised an error (module or
        templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_auditd_data_retention_max_log_file_action | default(true)
    | bool)
- block:
  - name: Configure auditd space_left Action on Low Disk Space
    ansible.builtin.lineinfile:
      dest: /etc/audit/auditd.conf
      line: space_left_action = {{ var_auditd_space_left_action.split('|')[0] }}
      regexp: ^\s*space_left_action\s*=\s*.*$
      state: present
      create: true
    when:
    - '"audit" in ansible_facts.packages'
    - '"kernel" in ansible_facts.packages'
    tags:
    - CJIS-5.4.1.1
    - NIST-800-171-3.3.1
    - NIST-800-53-AU-5(1)
    - NIST-800-53-AU-5(2)
    - NIST-800-53-AU-5(4)
    - NIST-800-53-AU-5(b)
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-10.7
    - PCI-DSSv4-10.5
    - PCI-DSSv4-10.5.1
    - auditd_data_retention_space_left_action
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy
  rescue:
  - name: 'StigForge: continue after error in ''Configure auditd space_left Action on Low Disk Space'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Configure auditd space_left Action on Low Disk Space'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_auditd_data_retention_space_left_action | default(true)
    | bool)
- block:
  - name: Set the file_groupownership_audit_binaries_newgroup variable if represented by gid
    ansible.builtin.set_fact:
      file_groupownership_audit_binaries_newgroup: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_groupownership_audit_binaries_newgroup variable if represented
      by gid'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_groupownership_audit_binaries_newgroup variable if represented by gid'' raised an error
        (module or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system
        state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/auditctl
    ansible.builtin.stat:
      path: /sbin/auditctl
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/auditctl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/auditctl'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure group owner on /sbin/auditctl
    ansible.builtin.file:
      path: /sbin/auditctl
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/auditctl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/auditctl'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/aureport
    ansible.builtin.stat:
      path: /sbin/aureport
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/aureport'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/aureport'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure group owner on /sbin/aureport
    ansible.builtin.file:
      path: /sbin/aureport
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/aureport'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/aureport'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/ausearch
    ansible.builtin.stat:
      path: /sbin/ausearch
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/ausearch'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/ausearch'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure group owner on /sbin/ausearch
    ansible.builtin.file:
      path: /sbin/ausearch
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/ausearch'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/ausearch'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/autrace
    ansible.builtin.stat:
      path: /sbin/autrace
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/autrace'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/autrace'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure group owner on /sbin/autrace
    ansible.builtin.file:
      path: /sbin/autrace
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/autrace'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/autrace'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/auditd
    ansible.builtin.stat:
      path: /sbin/auditd
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/auditd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure group owner on /sbin/auditd
    ansible.builtin.file:
      path: /sbin/auditd
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/auditd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/audispd
    ansible.builtin.stat:
      path: /sbin/audispd
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/audispd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/audispd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure group owner on /sbin/audispd
    ansible.builtin.file:
      path: /sbin/audispd
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/audispd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/audispd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Test for existence /sbin/augenrules
    ansible.builtin.stat:
      path: /sbin/augenrules
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/augenrules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/augenrules'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Ensure group owner on /sbin/augenrules
    ansible.builtin.file:
      path: /sbin/augenrules
      follow: false
      group: '{{ file_groupownership_audit_binaries_newgroup }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_groupownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure group owner on /sbin/augenrules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure group owner on /sbin/augenrules'' raised an error (module or templating) and was skipped;
        continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_groupownership_audit_binaries | default(true)
    | bool)
- block:
  - name: Set the file_ownership_audit_binaries_newown variable if represented by uid
    ansible.builtin.set_fact:
      file_ownership_audit_binaries_newown: '0'
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Set the file_ownership_audit_binaries_newown variable if represented by uid''
      (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Set the file_ownership_audit_binaries_newown variable if represented by uid'' raised an error (module
        or templating) and was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state
        independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_binaries | default(true) | bool)
- block:
  - name: Test for existence /sbin/auditctl
    ansible.builtin.stat:
      path: /sbin/auditctl
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/auditctl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/auditctl'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_binaries | default(true) | bool)
- block:
  - name: Ensure owner on /sbin/auditctl
    ansible.builtin.file:
      path: /sbin/auditctl
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/auditctl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/auditctl'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_binaries | default(true) | bool)
- block:
  - name: Test for existence /sbin/aureport
    ansible.builtin.stat:
      path: /sbin/aureport
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/aureport'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/aureport'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_binaries | default(true) | bool)
- block:
  - name: Ensure owner on /sbin/aureport
    ansible.builtin.file:
      path: /sbin/aureport
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/aureport'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/aureport'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_binaries | default(true) | bool)
- block:
  - name: Test for existence /sbin/ausearch
    ansible.builtin.stat:
      path: /sbin/ausearch
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/ausearch'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/ausearch'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_binaries | default(true) | bool)
- block:
  - name: Ensure owner on /sbin/ausearch
    ansible.builtin.file:
      path: /sbin/ausearch
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/ausearch'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/ausearch'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_binaries | default(true) | bool)
- block:
  - name: Test for existence /sbin/autrace
    ansible.builtin.stat:
      path: /sbin/autrace
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/autrace'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/autrace'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_binaries | default(true) | bool)
- block:
  - name: Ensure owner on /sbin/autrace
    ansible.builtin.file:
      path: /sbin/autrace
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/autrace'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/autrace'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_binaries | default(true) | bool)
- block:
  - name: Test for existence /sbin/auditd
    ansible.builtin.stat:
      path: /sbin/auditd
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/auditd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_binaries | default(true) | bool)
- block:
  - name: Ensure owner on /sbin/auditd
    ansible.builtin.file:
      path: /sbin/auditd
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/auditd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_binaries | default(true) | bool)
- block:
  - name: Test for existence /sbin/audispd
    ansible.builtin.stat:
      path: /sbin/audispd
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/audispd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/audispd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_binaries | default(true) | bool)
- block:
  - name: Ensure owner on /sbin/audispd
    ansible.builtin.file:
      path: /sbin/audispd
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/audispd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/audispd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_binaries | default(true) | bool)
- block:
  - name: Test for existence /sbin/augenrules
    ansible.builtin.stat:
      path: /sbin/augenrules
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/augenrules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/augenrules'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_binaries | default(true) | bool)
- block:
  - name: Ensure owner on /sbin/augenrules
    ansible.builtin.file:
      path: /sbin/augenrules
      follow: false
      owner: '{{ file_ownership_audit_binaries_newown }}'
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_ownership_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure owner on /sbin/augenrules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure owner on /sbin/augenrules'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_ownership_audit_binaries | default(true) | bool)
- block:
  - name: Test for existence /sbin/auditctl
    ansible.builtin.stat:
      path: /sbin/auditctl
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/auditctl'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/auditctl'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_audit_binaries | default(true) |
    bool)
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/auditctl
    ansible.builtin.file:
      path: /sbin/auditctl
      mode: u-s,g-ws,o-wt
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/auditctl'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/auditctl'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_audit_binaries | default(true) |
    bool)
- block:
  - name: Test for existence /sbin/aureport
    ansible.builtin.stat:
      path: /sbin/aureport
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/aureport'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/aureport'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_audit_binaries | default(true) |
    bool)
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/aureport
    ansible.builtin.file:
      path: /sbin/aureport
      mode: u-s,g-ws,o-wt
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/aureport'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/aureport'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_audit_binaries | default(true) |
    bool)
- block:
  - name: Test for existence /sbin/ausearch
    ansible.builtin.stat:
      path: /sbin/ausearch
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/ausearch'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/ausearch'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_audit_binaries | default(true) |
    bool)
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/ausearch
    ansible.builtin.file:
      path: /sbin/ausearch
      mode: u-s,g-ws,o-wt
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/ausearch'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/ausearch'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_audit_binaries | default(true) |
    bool)
- block:
  - name: Test for existence /sbin/autrace
    ansible.builtin.stat:
      path: /sbin/autrace
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/autrace'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/autrace'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_audit_binaries | default(true) |
    bool)
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/autrace
    ansible.builtin.file:
      path: /sbin/autrace
      mode: u-s,g-ws,o-wt
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/autrace'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/autrace'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_audit_binaries | default(true) |
    bool)
- block:
  - name: Test for existence /sbin/auditd
    ansible.builtin.stat:
      path: /sbin/auditd
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/auditd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_audit_binaries | default(true) |
    bool)
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/auditd
    ansible.builtin.file:
      path: /sbin/auditd
      mode: u-s,g-ws,o-wt
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/auditd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/auditd'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_audit_binaries | default(true) |
    bool)
- block:
  - name: Test for existence /sbin/audispd
    ansible.builtin.stat:
      path: /sbin/audispd
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/audispd'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/audispd'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_audit_binaries | default(true) |
    bool)
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/audispd
    ansible.builtin.file:
      path: /sbin/audispd
      mode: u-s,g-ws,o-wt
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/audispd'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/audispd'' raised an error (module or templating) and was
        skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_audit_binaries | default(true) |
    bool)
- block:
  - name: Test for existence /sbin/augenrules
    ansible.builtin.stat:
      path: /sbin/augenrules
    register: file_exists
    when: '"kernel" in ansible_facts.packages'
    tags:
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Test for existence /sbin/augenrules'' (OpenSCAP is the score gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Test for existence /sbin/augenrules'' raised an error (module or templating) and was skipped; continuing
        so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_audit_binaries | default(true) |
    bool)
- block:
  - name: Ensure permission u-s,g-ws,o-wt on /sbin/augenrules
    ansible.builtin.file:
      path: /sbin/augenrules
      mode: u-s,g-ws,o-wt
    when:
    - '"kernel" in ansible_facts.packages'
    - file_exists.stat is defined and (file_exists is not skipped and file_exists.stat.exists)
    tags:
    - configure_strategy
    - file_permissions_audit_binaries
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
  rescue:
  - name: 'StigForge: continue after error in ''Ensure permission u-s,g-ws,o-wt on /sbin/augenrules'' (OpenSCAP is the score
      gate)'
    ansible.builtin.debug:
      msg: 'StigForge: ''Ensure permission u-s,g-ws,o-wt on /sbin/augenrules'' raised an error (module or templating) and
        was skipped; continuing so later rules still run. OpenSCAP evaluates the resulting system state independently.'
  when: (stigforge_rules_enabled | default(true) | bool) and (al2023cis_file_permissions_audit_binaries | default(true) |
    bool)
